const numeric = /^[0-9]+$/
const compareIdentifiers = (a, b) => ***REMOVED***
  const anum = numeric.test(a)
  const bnum = numeric.test(b)

  if (anum && bnum) ***REMOVED***
    a = +a
    b = +b
***REMOVED***

  return a === b ? 0
    : (anum && !bnum) ? -1
    : (bnum && !anum) ? 1
    : a < b ? -1
    : 1
}

const rcompareIdentifiers = (a, b) => compareIdentifiers(b, a)

module.exports = ***REMOVED***
  compareIdentifiers,
  rcompareIdentifiers,
}
