const Range = require('../classes/range')
const satisfies = (version, range, options) => ***REMOVED***
  try ***REMOVED***
    range = new Range(range, options)
***REMOVED*** catch (er) ***REMOVED***
    return false
***REMOVED***
  return range.test(version)
}
module.exports = satisfies
