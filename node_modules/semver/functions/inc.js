const SemVer = require('../classes/semver')

const inc = (version, release, options, identifier) => ***REMOVED***
  if (typeof (options) === 'string') ***REMOVED***
    identifier = options
    options = undefined
***REMOVED***

  try ***REMOVED***
    return new SemVer(
      version instanceof SemVer ? version.version : version,
      options
    ).inc(release, identifier).version
***REMOVED*** catch (er) ***REMOVED***
    return null
***REMOVED***
}
module.exports = inc
