const ***REMOVED*** MAX_LENGTH } = require('../internal/constants')
const ***REMOVED*** re, t } = require('../internal/re')
const SemVer = require('../classes/semver')

const parseOptions = require('../internal/parse-options')
const parse = (version, options) => ***REMOVED***
  options = parseOptions(options)

  if (version instanceof SemVer) ***REMOVED***
    return version
***REMOVED***

  if (typeof version !== 'string') ***REMOVED***
    return null
***REMOVED***

  if (version.length > MAX_LENGTH) ***REMOVED***
    return null
***REMOVED***

  const r = options.loose ? re[t.LOOSE] : re[t.FULL]
  if (!r.test(version)) ***REMOVED***
    return null
***REMOVED***

  try ***REMOVED***
    return new SemVer(version, options)
***REMOVED*** catch (er) ***REMOVED***
    return null
***REMOVED***
}

module.exports = parse
