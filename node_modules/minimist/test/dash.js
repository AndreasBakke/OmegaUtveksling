var parse = require('../');
var test = require('tape');

test('-', function (t) ***REMOVED***
    t.plan(5);
    t.deepEqual(parse([ '-n', '-' ]), ***REMOVED*** n: '-', _: [] });
    t.deepEqual(parse([ '-' ]), ***REMOVED*** _: [ '-' ] });
    t.deepEqual(parse([ '-f-' ]), ***REMOVED*** f: '-', _: [] });
    t.deepEqual(
        parse([ '-b', '-' ], ***REMOVED*** boolean: 'b' }),
        ***REMOVED*** b: true, _: [ '-' ] }
    );
    t.deepEqual(
        parse([ '-s', '-' ], ***REMOVED*** string: 's' }),
        ***REMOVED*** s: '-', _: [] }
    );
});

test('-a -- b', function (t) ***REMOVED***
    t.plan(3);
    t.deepEqual(parse([ '-a', '--', 'b' ]), ***REMOVED*** a: true, _: [ 'b' ] });
    t.deepEqual(parse([ '--a', '--', 'b' ]), ***REMOVED*** a: true, _: [ 'b' ] });
    t.deepEqual(parse([ '--a', '--', 'b' ]), ***REMOVED*** a: true, _: [ 'b' ] });
});

test('move arguments after the -- into their own `--` array', function(t) ***REMOVED***
    t.plan(1);
    t.deepEqual(
        parse([ '--name', 'John', 'before', '--', 'after' ], ***REMOVED*** '--': true }),
        ***REMOVED*** name: 'John', _: [ 'before' ], '--': [ 'after' ] });
});
