import ***REMOVED*** LoadBalancer, ChannelControlHelper, LoadBalancingConfig } from './load-balancer';
import ***REMOVED*** SubchannelAddress } from './subchannel-address';
export declare class ChildLoadBalancerHandler implements LoadBalancer ***REMOVED***
    private readonly channelControlHelper;
    private currentChild;
    private pendingChild;
    private ChildPolicyHelper;
    constructor(channelControlHelper: ChannelControlHelper);
    /**
     * Prerequisites: lbConfig !== null and lbConfig.name is registered
     * @param addressList
     * @param lbConfig
     * @param attributes
     */
    updateAddressList(addressList: SubchannelAddress[], lbConfig: LoadBalancingConfig, attributes: ***REMOVED***
        [key: string]: unknown;
  ***REMOVED***): void;
    exitIdle(): void;
    resetBackoff(): void;
    destroy(): void;
    getTypeName(): string;
}
