import ***REMOVED*** Call } from './call-stream';
import ***REMOVED*** Channel } from './channel';
import ***REMOVED*** BaseFilter, Filter, FilterFactory } from './filter';
import ***REMOVED*** Metadata } from './metadata';
export declare class CallCredentialsFilter extends BaseFilter implements Filter ***REMOVED***
    private readonly channel;
    private readonly stream;
    private serviceUrl;
    constructor(channel: Channel, stream: Call);
    sendMetadata(metadata: Promise<Metadata>): Promise<Metadata>;
}
export declare class CallCredentialsFilterFactory implements FilterFactory<CallCredentialsFilter> ***REMOVED***
    private readonly channel;
    constructor(channel: Channel);
    createFilter(callStream: Call): CallCredentialsFilter;
}
