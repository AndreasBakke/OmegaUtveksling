/// <reference types="node" />
import ***REMOVED*** Call, WriteObject } from './call-stream';
import ***REMOVED*** Channel } from './channel';
import ***REMOVED*** ChannelOptions } from './channel-options';
import ***REMOVED*** BaseFilter, Filter, FilterFactory } from './filter';
import ***REMOVED*** Metadata } from './metadata';
declare type SharedCompressionFilterConfig = ***REMOVED***
    serverSupportedEncodingHeader?: string;
};
export declare class CompressionFilter extends BaseFilter implements Filter ***REMOVED***
    private sharedFilterConfig;
    private sendCompression;
    private receiveCompression;
    private currentCompressionAlgorithm;
    constructor(channelOptions: ChannelOptions, sharedFilterConfig: SharedCompressionFilterConfig);
    sendMetadata(metadata: Promise<Metadata>): Promise<Metadata>;
    receiveMetadata(metadata: Metadata): Metadata;
    sendMessage(message: Promise<WriteObject>): Promise<WriteObject>;
    receiveMessage(message: Promise<Buffer>): Promise<Buffer>;
}
export declare class CompressionFilterFactory implements FilterFactory<CompressionFilter> ***REMOVED***
    private readonly channel;
    private readonly options;
    private sharedFilterConfig;
    constructor(channel: Channel, options: ChannelOptions);
    createFilter(callStream: Call): CompressionFilter;
}
export ***REMOVED***};
