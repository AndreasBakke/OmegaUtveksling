/// <reference types="node" />
import ***REMOVED*** SecureServerOptions } from 'http2';
export interface KeyCertPair ***REMOVED***
    private_key: Buffer;
    cert_chain: Buffer;
}
export declare abstract class ServerCredentials ***REMOVED***
    abstract _isSecure(): boolean;
    abstract _getSettings(): SecureServerOptions | null;
    static createInsecure(): ServerCredentials;
    static createSsl(rootCerts: Buffer | null, keyCertPairs: KeyCertPair[], checkClientCertificate?: boolean): ServerCredentials;
}
