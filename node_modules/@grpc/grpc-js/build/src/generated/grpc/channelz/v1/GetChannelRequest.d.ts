import type ***REMOVED*** Long } from '@grpc/proto-loader';
export interface GetChannelRequest ***REMOVED***
    /**
     * channel_id is the identifier of the specific channel to get.
     */
    'channel_id'?: (number | string | Long);
}
export interface GetChannelRequest__Output ***REMOVED***
    /**
     * channel_id is the identifier of the specific channel to get.
     */
    'channel_id': (string);
}
