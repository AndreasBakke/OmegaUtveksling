import type ***REMOVED*** Long } from '@grpc/proto-loader';
export interface Timestamp ***REMOVED***
    'seconds'?: (number | string | Long);
    'nanos'?: (number);
}
export interface Timestamp__Output ***REMOVED***
    'seconds': (string);
    'nanos': (number);
}
