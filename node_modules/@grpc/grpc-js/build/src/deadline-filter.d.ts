import ***REMOVED*** Call, StatusObject } from './call-stream';
import ***REMOVED*** Channel } from './channel';
import ***REMOVED*** BaseFilter, Filter, FilterFactory } from './filter';
import ***REMOVED*** Metadata } from './metadata';
export declare class DeadlineFilter extends BaseFilter implements Filter ***REMOVED***
    private readonly channel;
    private readonly callStream;
    private timer;
    private deadline;
    constructor(channel: Channel, callStream: Call);
    private retreiveDeadline;
    private runTimer;
    refresh(): void;
    sendMetadata(metadata: Promise<Metadata>): Promise<Metadata>;
    receiveTrailers(status: StatusObject): StatusObject;
}
export declare class DeadlineFilterFactory implements FilterFactory<DeadlineFilter> ***REMOVED***
    private readonly channel;
    constructor(channel: Channel);
    createFilter(callStream: Call): DeadlineFilter;
}
