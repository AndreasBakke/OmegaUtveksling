function memoize(fn) ***REMOVED***
  var cache = Object.create(null);
  return function (arg) ***REMOVED***
    if (cache[arg] === undefined) cache[arg] = fn(arg);
    return cache[arg];
***REMOVED***;
}

export default memoize;
