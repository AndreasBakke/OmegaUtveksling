// @flow

export default function memoize<V>(fn: string => V): string => V ***REMOVED***
  const cache = Object.create(null)

  return (arg: string) => ***REMOVED***
    if (cache[arg] === undefined) cache[arg] = fn(arg)
    return cache[arg]
***REMOVED***
}
