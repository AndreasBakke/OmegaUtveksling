'use strict'
const co = require('co')
const expect = require('expect.js')

const describe = require('mocha').describe
const it = require('mocha').it

const Pool = require('../')

describe('pool ending', () => ***REMOVED***
  it('ends without being used', (done) => ***REMOVED***
    const pool = new Pool()
    pool.end(done)
***REMOVED***)

  it('ends with a promise', () => ***REMOVED***
    return new Pool().end()
***REMOVED***)

  it(
    'ends with clients',
    co.wrap(function* () ***REMOVED***
      const pool = new Pool()
      const res = yield pool.query('SELECT $1::text as name', ['brianc'])
      expect(res.rows[0].name).to.equal('brianc')
      return pool.end()
  ***REMOVED***)
  )

  it(
    'allows client to finish',
    co.wrap(function* () ***REMOVED***
      const pool = new Pool()
      const query = pool.query('SELECT $1::text as name', ['brianc'])
      yield pool.end()
      const res = yield query
      expect(res.rows[0].name).to.equal('brianc')
  ***REMOVED***)
  )
})
