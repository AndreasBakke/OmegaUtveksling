'use strict'
const expect = require('expect.js')

const describe = require('mocha').describe
const it = require('mocha').it

const Pool = require('../')

describe('verify', () => ***REMOVED***
  it('verifies a client with a callback', (done) => ***REMOVED***
    const pool = new Pool(***REMOVED***
      verify: (client, cb) => ***REMOVED***
        cb(new Error('nope'))
    ***REMOVED***,
  ***REMOVED***)

    pool.connect((err, client) => ***REMOVED***
      expect(err).to.be.an(Error)
      expect(err.message).to.be('nope')
      pool.end()
      done()
  ***REMOVED***)
***REMOVED***)
})
