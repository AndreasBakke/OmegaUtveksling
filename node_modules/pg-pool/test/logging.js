const expect = require('expect.js')

const describe = require('mocha').describe
const it = require('mocha').it

const Pool = require('../')

describe('logging', function () ***REMOVED***
  it('logs to supplied log function if given', function () ***REMOVED***
    const messages = []
    const log = function (msg) ***REMOVED***
      messages.push(msg)
  ***REMOVED***
    const pool = new Pool(***REMOVED*** log: log })
    return pool.query('SELECT NOW()').then(function () ***REMOVED***
      expect(messages.length).to.be.greaterThan(0)
      return pool.end()
  ***REMOVED***)
***REMOVED***)
})
