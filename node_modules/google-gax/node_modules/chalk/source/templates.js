'use strict';
const TEMPLATE_REGEX = /(?:\\(u(?:[a-f\d]***REMOVED***4}|\***REMOVED***[a-f\d]***REMOVED***1,6}\})|x[a-f\d]***REMOVED***2}|.))|(?:\***REMOVED***(~)?(\w+(?:\([^)]*\))?(?:\.\w+(?:\([^)]*\))?)*)(?:[ \t]|(?=\r?\n)))|(\})|((?:.|[\r\n\f])+?)/gi;
const STYLE_REGEX = /(?:^|\.)(\w+)(?:\(([^)]*)\))?/g;
const STRING_REGEX = /^(['"])((?:\\.|(?!\1)[^\\])*)\1$/;
const ESCAPE_REGEX = /\\(u(?:[a-f\d]***REMOVED***4}|***REMOVED***[a-f\d]***REMOVED***1,6}})|x[a-f\d]***REMOVED***2}|.)|([^\\])/gi;

const ESCAPES = new Map([
	['n', '\n'],
	['r', '\r'],
	['t', '\t'],
	['b', '\b'],
	['f', '\f'],
	['v', '\v'],
	['0', '\0'],
	['\\', '\\'],
	['e', '\u001B'],
	['a', '\u0007']
]);

function unescape(c) ***REMOVED***
	const u = c[0] === 'u';
	const bracket = c[1] === '***REMOVED***';

	if ((u && !bracket && c.length === 5) || (c[0] === 'x' && c.length === 3)) ***REMOVED***
		return String.fromCharCode(parseInt(c.slice(1), 16));
	}

	if (u && bracket) ***REMOVED***
		return String.fromCodePoint(parseInt(c.slice(2, -1), 16));
	}

	return ESCAPES.get(c) || c;
}

function parseArguments(name, arguments_) ***REMOVED***
	const results = [];
	const chunks = arguments_.trim().split(/\s*,\s*/g);
	let matches;

	for (const chunk of chunks) ***REMOVED***
		const number = Number(chunk);
		if (!Number.isNaN(number)) ***REMOVED***
			results.push(number);
		} else if ((matches = chunk.match(STRING_REGEX))) ***REMOVED***
			results.push(matches[2].replace(ESCAPE_REGEX, (m, escape, character) => escape ? unescape(escape) : character));
		} else ***REMOVED***
			throw new Error(`Invalid Chalk template style argument: $***REMOVED***chunk} (in style '$***REMOVED***name}')`);
		}
	}

	return results;
}

function parseStyle(style) ***REMOVED***
	STYLE_REGEX.lastIndex = 0;

	const results = [];
	let matches;

	while ((matches = STYLE_REGEX.exec(style)) !== null) ***REMOVED***
		const name = matches[1];

		if (matches[2]) ***REMOVED***
			const args = parseArguments(name, matches[2]);
			results.push([name].concat(args));
		} else ***REMOVED***
			results.push([name]);
		}
	}

	return results;
}

function buildStyle(chalk, styles) ***REMOVED***
	const enabled = ***REMOVED***};

	for (const layer of styles) ***REMOVED***
		for (const style of layer.styles) ***REMOVED***
			enabled[style[0]] = layer.inverse ? null : style.slice(1);
		}
	}

	let current = chalk;
	for (const [styleName, styles] of Object.entries(enabled)) ***REMOVED***
		if (!Array.isArray(styles)) ***REMOVED***
			continue;
		}

		if (!(styleName in current)) ***REMOVED***
			throw new Error(`Unknown Chalk style: $***REMOVED***styleName}`);
		}

		current = styles.length > 0 ? current[styleName](...styles) : current[styleName];
	}

	return current;
}

module.exports = (chalk, temporary) => ***REMOVED***
	const styles = [];
	const chunks = [];
	let chunk = [];

	// eslint-disable-next-line max-params
	temporary.replace(TEMPLATE_REGEX, (m, escapeCharacter, inverse, style, close, character) => ***REMOVED***
		if (escapeCharacter) ***REMOVED***
			chunk.push(unescape(escapeCharacter));
		} else if (style) ***REMOVED***
			const string = chunk.join('');
			chunk = [];
			chunks.push(styles.length === 0 ? string : buildStyle(chalk, styles)(string));
			styles.push(***REMOVED***inverse, styles: parseStyle(style)});
		} else if (close) ***REMOVED***
			if (styles.length === 0) ***REMOVED***
				throw new Error('Found extraneous } in Chalk template literal');
			}

			chunks.push(buildStyle(chalk, styles)(chunk.join('')));
			chunk = [];
			styles.pop();
		} else ***REMOVED***
			chunk.push(character);
		}
	});

	chunks.push(chunk.join(''));

	if (styles.length > 0) ***REMOVED***
		const errMessage = `Chalk template literal is missing $***REMOVED***styles.length} closing bracket$***REMOVED***styles.length === 1 ? '' : 's'} (\`}\`)`;
		throw new Error(errMessage);
	}

	return chunks.join('');
};
