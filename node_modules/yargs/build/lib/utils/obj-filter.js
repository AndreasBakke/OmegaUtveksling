import ***REMOVED*** objectKeys } from '../typings/common-types.js';
export function objFilter(original = ***REMOVED***}, filter = () => true) ***REMOVED***
    const obj = ***REMOVED***};
    objectKeys(original).forEach(key => ***REMOVED***
        if (filter(key, original[key])) ***REMOVED***
            obj[key] = original[key];
      ***REMOVED***
  ***REMOVED***);
    return obj;
}
