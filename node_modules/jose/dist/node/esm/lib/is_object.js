function isObjectLike(value) ***REMOVED***
    return typeof value === 'object' && value !== null;
}
export default function isObject(input) ***REMOVED***
    if (!isObjectLike(input) || Object.prototype.toString.call(input) !== '[object Object]') ***REMOVED***
        return false;
  ***REMOVED***
    if (Object.getPrototypeOf(input) === null) ***REMOVED***
        return true;
  ***REMOVED***
    let proto = input;
    while (Object.getPrototypeOf(proto) !== null) ***REMOVED***
        proto = Object.getPrototypeOf(proto);
  ***REMOVED***
    return Object.getPrototypeOf(input) === proto;
}
