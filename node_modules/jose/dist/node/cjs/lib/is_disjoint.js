"use strict";
Object.defineProperty(exports, "__esModule", ***REMOVED*** value: true });
const isDisjoint = (...headers) => ***REMOVED***
    const sources = headers.filter(Boolean);
    if (sources.length === 0 || sources.length === 1) ***REMOVED***
        return true;
  ***REMOVED***
    let acc;
    for (const header of sources) ***REMOVED***
        const parameters = Object.keys(header);
        if (!acc || acc.size === 0) ***REMOVED***
            acc = new Set(parameters);
            continue;
      ***REMOVED***
        for (const parameter of parameters) ***REMOVED***
            if (acc.has(parameter)) ***REMOVED***
                return false;
          ***REMOVED***
            acc.add(parameter);
      ***REMOVED***
  ***REMOVED***
    return true;
};
exports.default = isDisjoint;
