const validateAlgorithms = (option, algorithms) => ***REMOVED***
    if (algorithms !== undefined &&
        (!Array.isArray(algorithms) || algorithms.some((s) => typeof s !== 'string'))) ***REMOVED***
        throw new TypeError(`"$***REMOVED***option}" option must be an array of strings`);
  ***REMOVED***
    if (!algorithms) ***REMOVED***
        return undefined;
  ***REMOVED***
    return new Set(algorithms);
};
export default validateAlgorithms;
