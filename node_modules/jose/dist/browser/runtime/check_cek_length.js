import ***REMOVED*** JWEInvalid } from '../util/errors.js';
const checkCekLength = (cek, expected) => ***REMOVED***
    const actual = cek.byteLength << 3;
    if (actual !== expected) ***REMOVED***
        throw new JWEInvalid(`Invalid Content Encryption Key length. Expected $***REMOVED***expected} bits, got $***REMOVED***actual} bits`);
  ***REMOVED***
};
export default checkCekLength;
