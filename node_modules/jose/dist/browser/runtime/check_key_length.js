export default (alg, key) => ***REMOVED***
    if (alg.startsWith('RS') || alg.startsWith('PS')) ***REMOVED***
        const ***REMOVED*** modulusLength } = key.algorithm;
        if (typeof modulusLength !== 'number' || modulusLength < 2048) ***REMOVED***
            throw new TypeError(`$***REMOVED***alg} requires key modulusLength to be 2048 bits or larger`);
      ***REMOVED***
  ***REMOVED***
};
