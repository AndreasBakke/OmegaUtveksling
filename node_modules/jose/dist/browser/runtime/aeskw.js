import bogusWebCrypto from './bogus.js';
import crypto, ***REMOVED*** isCryptoKey } from './webcrypto.js';
import ***REMOVED*** checkEncCryptoKey } from '../lib/crypto_key.js';
import invalidKeyInput from '../lib/invalid_key_input.js';
import ***REMOVED*** types } from './is_key_like.js';
function checkKeySize(key, alg) ***REMOVED***
    if (key.algorithm.length !== parseInt(alg.slice(1, 4), 10)) ***REMOVED***
        throw new TypeError(`Invalid key size for alg: $***REMOVED***alg}`);
  ***REMOVED***
}
function getCryptoKey(key, alg, usage) ***REMOVED***
    if (isCryptoKey(key)) ***REMOVED***
        checkEncCryptoKey(key, alg, usage);
        return key;
  ***REMOVED***
    if (key instanceof Uint8Array) ***REMOVED***
        return crypto.subtle.importKey('raw', key, 'AES-KW', true, [usage]);
  ***REMOVED***
    throw new TypeError(invalidKeyInput(key, ...types, 'Uint8Array'));
}
export const wrap = async (alg, key, cek) => ***REMOVED***
    const cryptoKey = await getCryptoKey(key, alg, 'wrapKey');
    checkKeySize(cryptoKey, alg);
    const cryptoKeyCek = await crypto.subtle.importKey('raw', cek, ...bogusWebCrypto);
    return new Uint8Array(await crypto.subtle.wrapKey('raw', cryptoKeyCek, cryptoKey, 'AES-KW'));
};
export const unwrap = async (alg, key, encryptedKey) => ***REMOVED***
    const cryptoKey = await getCryptoKey(key, alg, 'unwrapKey');
    checkKeySize(cryptoKey, alg);
    const cryptoKeyCek = await crypto.subtle.unwrapKey('raw', encryptedKey, cryptoKey, 'AES-KW', ...bogusWebCrypto);
    return new Uint8Array(await crypto.subtle.exportKey('raw', cryptoKeyCek));
};
