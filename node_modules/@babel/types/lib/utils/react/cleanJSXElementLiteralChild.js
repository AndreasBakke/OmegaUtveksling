"use strict";

Object.defineProperty(exports, "__esModule", ***REMOVED***
  value: true
});
exports.default = cleanJSXElementLiteralChild;
var _generated = require("../../builders/generated");
var _ = require("../..");
function cleanJSXElementLiteralChild(child, args) ***REMOVED***
  const lines = child.value.split(/\r\n|\n|\r/);
  let lastNonEmptyLine = 0;
  for (let i = 0; i < lines.length; i++) ***REMOVED***
    if (lines[i].match(/[^ \t]/)) ***REMOVED***
      lastNonEmptyLine = i;
  ***REMOVED***
***REMOVED***
  let str = "";
  for (let i = 0; i < lines.length; i++) ***REMOVED***
    const line = lines[i];
    const isFirstLine = i === 0;
    const isLastLine = i === lines.length - 1;
    const isLastNonEmptyLine = i === lastNonEmptyLine;

    let trimmedLine = line.replace(/\t/g, " ");

    if (!isFirstLine) ***REMOVED***
      trimmedLine = trimmedLine.replace(/^[ ]+/, "");
  ***REMOVED***

    if (!isLastLine) ***REMOVED***
      trimmedLine = trimmedLine.replace(/[ ]+$/, "");
  ***REMOVED***
    if (trimmedLine) ***REMOVED***
      if (!isLastNonEmptyLine) ***REMOVED***
        trimmedLine += " ";
    ***REMOVED***
      str += trimmedLine;
  ***REMOVED***
***REMOVED***
  if (str) args.push((0, _.inherits)((0, _generated.stringLiteral)(str), child));
}

//# sourceMappingURL=cleanJSXElementLiteralChild.js.map
