"use strict";

Object.defineProperty(exports, "__esModule", ***REMOVED***
  value: true
});
exports.default = traverse;
var _definitions = require("../definitions");
function traverse(node, handlers, state) ***REMOVED***
  if (typeof handlers === "function") ***REMOVED***
    handlers = ***REMOVED***
      enter: handlers
  ***REMOVED***;
***REMOVED***
  const ***REMOVED***
    enter,
    exit
***REMOVED*** = handlers;
  traverseSimpleImpl(node, enter, exit, state, []);
}
function traverseSimpleImpl(node, enter, exit, state, ancestors) ***REMOVED***
  const keys = _definitions.VISITOR_KEYS[node.type];
  if (!keys) return;
  if (enter) enter(node, ancestors, state);
  for (const key of keys) ***REMOVED***
    const subNode = node[key];
    if (Array.isArray(subNode)) ***REMOVED***
      for (let i = 0; i < subNode.length; i++) ***REMOVED***
        const child = subNode[i];
        if (!child) continue;
        ancestors.push(***REMOVED***
          node,
          key,
          index: i
      ***REMOVED***);
        traverseSimpleImpl(child, enter, exit, state, ancestors);
        ancestors.pop();
    ***REMOVED***
  ***REMOVED*** else if (subNode) ***REMOVED***
      ancestors.push(***REMOVED***
        node,
        key
    ***REMOVED***);
      traverseSimpleImpl(subNode, enter, exit, state, ancestors);
      ancestors.pop();
  ***REMOVED***
***REMOVED***
  if (exit) exit(node, ancestors, state);
}

//# sourceMappingURL=traverse.js.map
