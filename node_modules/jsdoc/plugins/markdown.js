/**
 * Translate doclet descriptions from Markdown into HTML.
 *
 * @module plugins/markdown
 */
const env = require('jsdoc/env');

const config = env.conf.markdown || ***REMOVED***};
const defaultTags = [
    'author',
    'classdesc',
    'description',
    'exceptions',
    'params',
    'properties',
    'returns',
    'see',
    'summary'
];
const hasOwnProp = Object.prototype.hasOwnProperty;
const parse = require('jsdoc/util/markdown').getParser();
let tags = [];
let excludeTags = [];

function shouldProcessString(tagName, text) ***REMOVED***
    let shouldProcess = true;

    // we only want to process `@author` and `@see` tags that contain Markdown links
    if ( (tagName === 'author' || tagName === 'see') && !text.includes('[') ) ***REMOVED***
        shouldProcess = false;
  ***REMOVED***

    return shouldProcess;
}

/**
 * Process the markdown source in a doclet. The properties that should be processed are
 * configurable, but always include "author", "classdesc", "description", "exceptions", "params",
 * "properties",  "returns", and "see".  Handled properties can be bare strings, objects, or arrays
 * of objects.
 */
function process(doclet) ***REMOVED***
    tags.forEach(tag => ***REMOVED***
        if ( !hasOwnProp.call(doclet, tag) ) ***REMOVED***
            return;
      ***REMOVED***

        if (typeof doclet[tag] === 'string' && shouldProcessString(tag, doclet[tag]) ) ***REMOVED***
            doclet[tag] = parse(doclet[tag]);
      ***REMOVED***
        else if ( Array.isArray(doclet[tag]) ) ***REMOVED***
            doclet[tag].forEach((value, index, original) => ***REMOVED***
                const inner = ***REMOVED***};

                inner[tag] = value;
                process(inner);
                original[index] = inner[tag];
          ***REMOVED***);
      ***REMOVED***
        else if (doclet[tag]) ***REMOVED***
            process(doclet[tag]);
      ***REMOVED***
  ***REMOVED***);
}

// set up the list of "tags" (properties) to process
if (config.tags) ***REMOVED***
    tags = config.tags.slice();
}
// set up the list of default tags to exclude from processing
if (config.excludeTags) ***REMOVED***
    excludeTags = config.excludeTags.slice();
}
defaultTags.forEach(tag => ***REMOVED***
    if (!excludeTags.includes(tag) && !tags.includes(tag)) ***REMOVED***
        tags.push(tag);
  ***REMOVED***
});

exports.handlers = ***REMOVED***
    /**
     * Translate Markdown syntax in a new doclet's description into HTML. Is run
     * by JSDoc 3 whenever a "newDoclet" event fires.
     */
    newDoclet(***REMOVED***doclet}) ***REMOVED***
        process(doclet);
  ***REMOVED***
};
