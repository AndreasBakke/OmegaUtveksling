<?js
    var self = this;
    var isGlobalPage;

    docs.forEach(function(doc, i) ***REMOVED***
?>

<?js
    // we only need to check this once
    if (typeof isGlobalPage === 'undefined') ***REMOVED***
        isGlobalPage = (doc.kind === 'globalobj');
  ***REMOVED***
?>
<?js if (doc.kind === 'mainpage' || (doc.kind === 'package')) ***REMOVED*** ?>
    <?js= self.partial('mainpage.tmpl', doc) ?>
<?js } else if (doc.kind === 'source') ***REMOVED*** ?>
    <?js= self.partial('source.tmpl', doc) ?>
<?js } else ***REMOVED*** ?>

<section>

<header>
    <?js if (!doc.longname || doc.kind !== 'module') ***REMOVED*** ?>
        <h2><?js if (doc.attribs) ***REMOVED*** ?><span class="attribs"><?js= doc.attribs ?></span><?js }
            if (doc.ancestors && doc.ancestors.length) ***REMOVED*** ?>
            <span class="ancestors"><?js= doc.ancestors.join('') ?></span><?js
       ***REMOVED***
        ?><?js= doc.name ?><?js
         if (doc.variation) ***REMOVED*** ?>
            <sup class="variation"><?js= doc.variation ?></sup><?js }
         if (doc.signature && !doc.hideconstructor) ***REMOVED*** ?><?js= doc.signature ?><?js } ?></h2>
        <?js if (doc.classdesc) ***REMOVED*** ?>
            <div class="class-description"><?js= doc.classdesc ?></div>
        <?js } ?>
    <?js } else if (doc.kind === 'module' && doc.modules) ***REMOVED*** ?>
        <?js doc.modules.forEach(function(module) ***REMOVED*** ?>
            <?js if (module.classdesc) ***REMOVED*** ?>
                <div class="class-description"><?js= module.classdesc ?></div>
            <?js } ?>
        <?js }) ?>
    <?js } ?>
</header>

<article>
    <div class="container-overview">
    <?js if (doc.kind === 'module' && doc.modules) ***REMOVED*** ?>
        <?js if (doc.description) ***REMOVED*** ?>
            <div class="description"><?js= doc.description ?></div>
        <?js } ?>

        <?js doc.modules.forEach(function(module) ***REMOVED*** ?>
            <?js= self.partial('method.tmpl', module) ?>
        <?js }) ?>
    <?js } else if (doc.kind === 'class' || (doc.kind === 'namespace' && doc.signature)) ***REMOVED*** ?>
        <?js= self.partial('method.tmpl', doc) ?>
    <?js } else ***REMOVED*** ?>
        <?js if (doc.description) ***REMOVED*** ?>
            <div class="description"><?js= doc.description ?></div>
        <?js } ?>

        <?js= self.partial('details.tmpl', doc) ?>

        <?js if (doc.examples && doc.examples.length) ***REMOVED*** ?>
            <h3>Example<?js= doc.examples.length > 1? 's':'' ?></h3>
            <?js= self.partial('examples.tmpl', doc.examples) ?>
        <?js } ?>
    <?js } ?>
    </div>

    <?js if (doc.augments && doc.augments.length) ***REMOVED*** ?>
        <h3 class="subsection-title">Extends</h3>

        <?js= self.partial('augments.tmpl', doc) ?>
    <?js } ?>

    <?js if (doc.requires && doc.requires.length) ***REMOVED*** ?>
        <h3 class="subsection-title">Requires</h3>

        <ul><?js doc.requires.forEach(function(r) ***REMOVED*** ?>
            <li><?js= self.linkto(r, r) ?></li>
        <?js }); ?></ul>
    <?js } ?>

    <?js
        var classes = self.find(***REMOVED***kind: 'class', memberof: doc.longname});
        if (!isGlobalPage && classes && classes.length) ***REMOVED***
    ?>
        <h3 class="subsection-title">Classes</h3>

        <dl><?js classes.forEach(function(c) ***REMOVED*** ?>
            <dt><?js= self.linkto(c.longname, c.name) ?></dt>
            <dd><?js if (c.summary) ***REMOVED*** ?><?js= c.summary ?><?js } ?></dd>
        <?js }); ?></dl>
    <?js } ?>

    <?js
        var interfaces = self.find(***REMOVED***kind: 'interface', memberof: doc.longname});
        if (!isGlobalPage && interfaces && interfaces.length) ***REMOVED***
    ?>
        <h3 class="subsection-title">Interfaces</h3>

        <dl><?js interfaces.forEach(function(i) ***REMOVED*** ?>
            <dt><?js= self.linkto(i.longname, i.name) ?></dt>
            <dd><?js if (i.summary) ***REMOVED*** ?><?js= i.summary ?><?js } ?></dd>
        <?js }); ?></dl>
    <?js } ?>

    <?js
        var mixins = self.find(***REMOVED***kind: 'mixin', memberof: doc.longname});
        if (!isGlobalPage && mixins && mixins.length) ***REMOVED***
    ?>
        <h3 class="subsection-title">Mixins</h3>

        <dl><?js mixins.forEach(function(m) ***REMOVED*** ?>
            <dt><?js= self.linkto(m.longname, m.name) ?></dt>
            <dd><?js if (m.summary) ***REMOVED*** ?><?js= m.summary ?><?js } ?></dd>
        <?js }); ?></dl>
    <?js } ?>

    <?js
        var namespaces = self.find(***REMOVED***kind: 'namespace', memberof: doc.longname});
        if (!isGlobalPage && namespaces && namespaces.length) ***REMOVED***
    ?>
        <h3 class="subsection-title">Namespaces</h3>

        <dl><?js namespaces.forEach(function(n) ***REMOVED*** ?>
            <dt><?js= self.linkto(n.longname, n.name) ?></dt>
            <dd><?js if (n.summary) ***REMOVED*** ?><?js= n.summary ?><?js } ?></dd>
        <?js }); ?></dl>
    <?js } ?>

    <?js
        var members = self.find(***REMOVED***kind: 'member', memberof: isGlobalPage ? ***REMOVED***isUndefined: true} : doc.longname});

        // symbols that are assigned to module.exports are not globals, even though they're not a memberof anything
        if (isGlobalPage && members && members.length && members.forEach) ***REMOVED***
            members = members.filter(function(m) ***REMOVED***
                return m.longname && m.longname.indexOf('module:') !== 0;
          ***REMOVED***);
      ***REMOVED***
        if (members && members.length && members.forEach) ***REMOVED***
    ?>
        <h3 class="subsection-title">Members</h3>

        <?js members.forEach(function(p) ***REMOVED*** ?>
            <?js= self.partial('members.tmpl', p) ?>
        <?js }); ?>
    <?js } ?>

    <?js
        var methods = self.find(***REMOVED***kind: 'function', memberof: isGlobalPage ? ***REMOVED***isUndefined: true} : doc.longname});
        if (methods && methods.length && methods.forEach) ***REMOVED***
    ?>
        <h3 class="subsection-title">Methods</h3>

        <?js methods.forEach(function(m) ***REMOVED*** ?>
            <?js= self.partial('method.tmpl', m) ?>
        <?js }); ?>
    <?js } ?>

    <?js
        var typedefs = self.find(***REMOVED***kind: 'typedef', memberof: isGlobalPage ? ***REMOVED***isUndefined: true} : doc.longname});
        if (typedefs && typedefs.length && typedefs.forEach) ***REMOVED***
    ?>
        <h3 class="subsection-title">Type Definitions</h3>

        <?js typedefs.forEach(function(e) ***REMOVED***
                if (e.signature) ***REMOVED***
            ?>
                <?js= self.partial('method.tmpl', e) ?>
            <?js
              ***REMOVED***
                else ***REMOVED***
            ?>
                <?js= self.partial('members.tmpl', e) ?>
            <?js
              ***REMOVED***
          ***REMOVED***); ?>
    <?js } ?>

    <?js
        var events = self.find(***REMOVED***kind: 'event', memberof: isGlobalPage ? ***REMOVED***isUndefined: true} : doc.longname});
        if (events && events.length && events.forEach) ***REMOVED***
    ?>
        <h3 class="subsection-title">Events</h3>

        <?js events.forEach(function(e) ***REMOVED*** ?>
            <?js= self.partial('method.tmpl', e) ?>
        <?js }); ?>
    <?js } ?>
</article>

</section>
<?js } ?>

<?js }); ?>
