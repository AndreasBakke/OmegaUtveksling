<?js
    var data = obj;
    var props = data.subprops || data.properties;

    /* sort subprops under their parent props (like opts.classname) */
    var parentProp = null;
    props.forEach(function(prop, i) ***REMOVED***
        if (!prop) ***REMOVED*** return; }
        if ( parentProp && prop.name && prop.name.indexOf(parentProp.name + '.') === 0 ) ***REMOVED***
            prop.name = prop.name.substr(parentProp.name.length+1);
            parentProp.subprops = parentProp.subprops || [];
            parentProp.subprops.push(prop);
            props[i] = null;
      ***REMOVED***
        else ***REMOVED***
            parentProp = prop;
      ***REMOVED***
  ***REMOVED***);

    /* determine if we need extra columns, "attributes" and "default" */
    props.hasAttributes = false;
    props.hasDefault = false;
    props.hasName = false;

    props.forEach(function(prop) ***REMOVED***
        if (!prop) ***REMOVED*** return; }

        if (prop.optional || prop.nullable) ***REMOVED***
            props.hasAttributes = true;
      ***REMOVED***

        if (prop.name) ***REMOVED***
            props.hasName = true;
      ***REMOVED***

        if (typeof prop.defaultvalue !== 'undefined' && !data.isEnum) ***REMOVED***
            props.hasDefault = true;
      ***REMOVED***
  ***REMOVED***);
?>

<table class="props">
    <thead>
    <tr>
        <?js if (props.hasName) ***REMOVED***?>
        <th>Name</th>
        <?js } ?>

        <th>Type</th>

        <?js if (props.hasAttributes) ***REMOVED***?>
        <th>Attributes</th>
        <?js } ?>

        <?js if (props.hasDefault) ***REMOVED***?>
        <th>Default</th>
        <?js } ?>

        <th class="last">Description</th>
    </tr>
    </thead>

    <tbody>
    <?js
        var self = this;
        props.forEach(function(prop) ***REMOVED***
            if (!prop) ***REMOVED*** return; }
    ?>

        <tr>
            <?js if (props.hasName) ***REMOVED***?>
                <td class="name"><code><?js= prop.name ?></code></td>
            <?js } ?>

            <td class="type">
            <?js if (prop.type && prop.type.names) ***REMOVED***?>
                <?js= self.partial('type.tmpl', prop.type.names) ?>
            <?js } ?>
            </td>

            <?js if (props.hasAttributes) ***REMOVED***?>
                <td class="attributes">
                <?js if (prop.optional) ***REMOVED*** ?>
                    &lt;optional><br>
                <?js } ?>

                <?js if (prop.nullable) ***REMOVED*** ?>
                    &lt;nullable><br>
                <?js } ?>
                </td>
            <?js } ?>

            <?js if (props.hasDefault) ***REMOVED***?>
                <td class="default">
                <?js if (typeof prop.defaultvalue !== 'undefined') ***REMOVED*** ?>
                    <?js= self.htmlsafe(prop.defaultvalue) ?>
                <?js } ?>
                </td>
            <?js } ?>

            <td class="description last"><?js= prop.description ?><?js if (prop.subprops) ***REMOVED*** ?>
                <h6>Properties</h6><?js= self.partial('properties.tmpl', prop) ?>
            <?js } ?></td>
        </tr>

    <?js }); ?>
    </tbody>
</table>
