import ***REMOVED*** ErrorFactory, deepEqual, isBrowserExtension, isMobileCordova, isReactNative, FirebaseError, querystring, getModularInstance, base64Decode, getUA, isIE, createSubscribe, querystringDecode, extractQuerystring, isEmpty, getExperimentalSetting, getDefaultEmulatorHost } from '@firebase/util';
import ***REMOVED*** SDK_VERSION, _getProvider, _registerComponent, registerVersion, getApp } from '@firebase/app';
import ***REMOVED*** Logger, LogLevel } from '@firebase/logger';
import ***REMOVED*** __rest } from 'tslib';
import ***REMOVED*** Component } from '@firebase/component';

/**
 * @license
 * Copyright 2021 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * An enum of factors that may be used for multifactor authentication.
 *
 * @public
 */
const FactorId = ***REMOVED***
    /** Phone as second factor */
    PHONE: 'phone'
};
/**
 * Enumeration of supported providers.
 *
 * @public
 */
const ProviderId = ***REMOVED***
    /** Facebook provider ID */
    FACEBOOK: 'facebook.com',
    /** GitHub provider ID */
    GITHUB: 'github.com',
    /** Google provider ID */
    GOOGLE: 'google.com',
    /** Password provider */
    PASSWORD: 'password',
    /** Phone provider */
    PHONE: 'phone',
    /** Twitter provider ID */
    TWITTER: 'twitter.com'
};
/**
 * Enumeration of supported sign-in methods.
 *
 * @public
 */
const SignInMethod = ***REMOVED***
    /** Email link sign in method */
    EMAIL_LINK: 'emailLink',
    /** Email/password sign in method */
    EMAIL_PASSWORD: 'password',
    /** Facebook sign in method */
    FACEBOOK: 'facebook.com',
    /** GitHub sign in method */
    GITHUB: 'github.com',
    /** Google sign in method */
    GOOGLE: 'google.com',
    /** Phone sign in method */
    PHONE: 'phone',
    /** Twitter sign in method */
    TWITTER: 'twitter.com'
};
/**
 * Enumeration of supported operation types.
 *
 * @public
 */
const OperationType = ***REMOVED***
    /** Operation involving linking an additional provider to an already signed-in user. */
    LINK: 'link',
    /** Operation involving using a provider to reauthenticate an already signed-in user. */
    REAUTHENTICATE: 'reauthenticate',
    /** Operation involving signing in a user. */
    SIGN_IN: 'signIn'
};
/**
 * An enumeration of the possible email action types.
 *
 * @public
 */
const ActionCodeOperation = ***REMOVED***
    /** The email link sign-in action. */
    EMAIL_SIGNIN: 'EMAIL_SIGNIN',
    /** The password reset action. */
    PASSWORD_RESET: 'PASSWORD_RESET',
    /** The email revocation action. */
    RECOVER_EMAIL: 'RECOVER_EMAIL',
    /** The revert second factor addition email action. */
    REVERT_SECOND_FACTOR_ADDITION: 'REVERT_SECOND_FACTOR_ADDITION',
    /** The revert second factor addition email action. */
    VERIFY_AND_CHANGE_EMAIL: 'VERIFY_AND_CHANGE_EMAIL',
    /** The email verification action. */
    VERIFY_EMAIL: 'VERIFY_EMAIL'
};

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function _debugErrorMap() ***REMOVED***
    return ***REMOVED***
        ["admin-restricted-operation" /* AuthErrorCode.ADMIN_ONLY_OPERATION */]: 'This operation is restricted to administrators only.',
        ["argument-error" /* AuthErrorCode.ARGUMENT_ERROR */]: '',
        ["app-not-authorized" /* AuthErrorCode.APP_NOT_AUTHORIZED */]: "This app, identified by the domain where it's hosted, is not " +
            'authorized to use Firebase Authentication with the provided API key. ' +
            'Review your key configuration in the Google API console.',
        ["app-not-installed" /* AuthErrorCode.APP_NOT_INSTALLED */]: 'The requested mobile application corresponding to the identifier (' +
            'Android package name or iOS bundle ID) provided is not installed on ' +
            'this device.',
        ["captcha-check-failed" /* AuthErrorCode.CAPTCHA_CHECK_FAILED */]: 'The reCAPTCHA response token provided is either invalid, expired, ' +
            'already used or the domain associated with it does not match the list ' +
            'of whitelisted domains.',
        ["code-expired" /* AuthErrorCode.CODE_EXPIRED */]: 'The SMS code has expired. Please re-send the verification code to try ' +
            'again.',
        ["cordova-not-ready" /* AuthErrorCode.CORDOVA_NOT_READY */]: 'Cordova framework is not ready.',
        ["cors-unsupported" /* AuthErrorCode.CORS_UNSUPPORTED */]: 'This browser is not supported.',
        ["credential-already-in-use" /* AuthErrorCode.CREDENTIAL_ALREADY_IN_USE */]: 'This credential is already associated with a different user account.',
        ["custom-token-mismatch" /* AuthErrorCode.CREDENTIAL_MISMATCH */]: 'The custom token corresponds to a different audience.',
        ["requires-recent-login" /* AuthErrorCode.CREDENTIAL_TOO_OLD_LOGIN_AGAIN */]: 'This operation is sensitive and requires recent authentication. Log in ' +
            'again before retrying this request.',
        ["dependent-sdk-initialized-before-auth" /* AuthErrorCode.DEPENDENT_SDK_INIT_BEFORE_AUTH */]: 'Another Firebase SDK was initialized and is trying to use Auth before Auth is ' +
            'initialized. Please be sure to call `initializeAuth` or `getAuth` before ' +
            'starting any other Firebase SDK.',
        ["dynamic-link-not-activated" /* AuthErrorCode.DYNAMIC_LINK_NOT_ACTIVATED */]: 'Please activate Dynamic Links in the Firebase Console and agree to the terms and ' +
            'conditions.',
        ["email-change-needs-verification" /* AuthErrorCode.EMAIL_CHANGE_NEEDS_VERIFICATION */]: 'Multi-factor users must always have a verified email.',
        ["email-already-in-use" /* AuthErrorCode.EMAIL_EXISTS */]: 'The email address is already in use by another account.',
        ["emulator-config-failed" /* AuthErrorCode.EMULATOR_CONFIG_FAILED */]: 'Auth instance has already been used to make a network call. Auth can ' +
            'no longer be configured to use the emulator. Try calling ' +
            '"connectAuthEmulator()" sooner.',
        ["expired-action-code" /* AuthErrorCode.EXPIRED_OOB_CODE */]: 'The action code has expired.',
        ["cancelled-popup-request" /* AuthErrorCode.EXPIRED_POPUP_REQUEST */]: 'This operation has been cancelled due to another conflicting popup being opened.',
        ["internal-error" /* AuthErrorCode.INTERNAL_ERROR */]: 'An internal AuthError has occurred.',
        ["invalid-app-credential" /* AuthErrorCode.INVALID_APP_CREDENTIAL */]: 'The phone verification request contains an invalid application verifier.' +
            ' The reCAPTCHA token response is either invalid or expired.',
        ["invalid-app-id" /* AuthErrorCode.INVALID_APP_ID */]: 'The mobile app identifier is not registed for the current project.',
        ["invalid-user-token" /* AuthErrorCode.INVALID_AUTH */]: "This user's credential isn't valid for this project. This can happen " +
            "if the user's token has been tampered with, or if the user isn't for " +
            'the project associated with this API key.',
        ["invalid-auth-event" /* AuthErrorCode.INVALID_AUTH_EVENT */]: 'An internal AuthError has occurred.',
        ["invalid-verification-code" /* AuthErrorCode.INVALID_CODE */]: 'The SMS verification code used to create the phone auth credential is ' +
            'invalid. Please resend the verification code sms and be sure to use the ' +
            'verification code provided by the user.',
        ["invalid-continue-uri" /* AuthErrorCode.INVALID_CONTINUE_URI */]: 'The continue URL provided in the request is invalid.',
        ["invalid-cordova-configuration" /* AuthErrorCode.INVALID_CORDOVA_CONFIGURATION */]: 'The following Cordova plugins must be installed to enable OAuth sign-in: ' +
            'cordova-plugin-buildinfo, cordova-universal-links-plugin, ' +
            'cordova-plugin-browsertab, cordova-plugin-inappbrowser and ' +
            'cordova-plugin-customurlscheme.',
        ["invalid-custom-token" /* AuthErrorCode.INVALID_CUSTOM_TOKEN */]: 'The custom token format is incorrect. Please check the documentation.',
        ["invalid-dynamic-link-domain" /* AuthErrorCode.INVALID_DYNAMIC_LINK_DOMAIN */]: 'The provided dynamic link domain is not configured or authorized for the current project.',
        ["invalid-email" /* AuthErrorCode.INVALID_EMAIL */]: 'The email address is badly formatted.',
        ["invalid-emulator-scheme" /* AuthErrorCode.INVALID_EMULATOR_SCHEME */]: 'Emulator URL must start with a valid scheme (http:// or https://).',
        ["invalid-api-key" /* AuthErrorCode.INVALID_API_KEY */]: 'Your API key is invalid, please check you have copied it correctly.',
        ["invalid-cert-hash" /* AuthErrorCode.INVALID_CERT_HASH */]: 'The SHA-1 certificate hash provided is invalid.',
        ["invalid-credential" /* AuthErrorCode.INVALID_IDP_RESPONSE */]: 'The supplied auth credential is malformed or has expired.',
        ["invalid-message-payload" /* AuthErrorCode.INVALID_MESSAGE_PAYLOAD */]: 'The email template corresponding to this action contains invalid characters in its message. ' +
            'Please fix by going to the Auth email templates section in the Firebase Console.',
        ["invalid-multi-factor-session" /* AuthErrorCode.INVALID_MFA_SESSION */]: 'The request does not contain a valid proof of first factor successful sign-in.',
        ["invalid-oauth-provider" /* AuthErrorCode.INVALID_OAUTH_PROVIDER */]: 'EmailAuthProvider is not supported for this operation. This operation ' +
            'only supports OAuth providers.',
        ["invalid-oauth-client-id" /* AuthErrorCode.INVALID_OAUTH_CLIENT_ID */]: 'The OAuth client ID provided is either invalid or does not match the ' +
            'specified API key.',
        ["unauthorized-domain" /* AuthErrorCode.INVALID_ORIGIN */]: 'This domain is not authorized for OAuth operations for your Firebase ' +
            'project. Edit the list of authorized domains from the Firebase console.',
        ["invalid-action-code" /* AuthErrorCode.INVALID_OOB_CODE */]: 'The action code is invalid. This can happen if the code is malformed, ' +
            'expired, or has already been used.',
        ["wrong-password" /* AuthErrorCode.INVALID_PASSWORD */]: 'The password is invalid or the user does not have a password.',
        ["invalid-persistence-type" /* AuthErrorCode.INVALID_PERSISTENCE */]: 'The specified persistence type is invalid. It can only be local, session or none.',
        ["invalid-phone-number" /* AuthErrorCode.INVALID_PHONE_NUMBER */]: 'The format of the phone number provided is incorrect. Please enter the ' +
            'phone number in a format that can be parsed into E.164 format. E.164 ' +
            'phone numbers are written in the format [+][country code][subscriber ' +
            'number including area code].',
        ["invalid-provider-id" /* AuthErrorCode.INVALID_PROVIDER_ID */]: 'The specified provider ID is invalid.',
        ["invalid-recipient-email" /* AuthErrorCode.INVALID_RECIPIENT_EMAIL */]: 'The email corresponding to this action failed to send as the provided ' +
            'recipient email address is invalid.',
        ["invalid-sender" /* AuthErrorCode.INVALID_SENDER */]: 'The email template corresponding to this action contains an invalid sender email or name. ' +
            'Please fix by going to the Auth email templates section in the Firebase Console.',
        ["invalid-verification-id" /* AuthErrorCode.INVALID_SESSION_INFO */]: 'The verification ID used to create the phone auth credential is invalid.',
        ["invalid-tenant-id" /* AuthErrorCode.INVALID_TENANT_ID */]: "The Auth instance's tenant ID is invalid.",
        ["login-blocked" /* AuthErrorCode.LOGIN_BLOCKED */]: 'Login blocked by user-provided method: ***REMOVED***$originalMessage}',
        ["missing-android-pkg-name" /* AuthErrorCode.MISSING_ANDROID_PACKAGE_NAME */]: 'An Android Package Name must be provided if the Android App is required to be installed.',
        ["auth-domain-config-required" /* AuthErrorCode.MISSING_AUTH_DOMAIN */]: 'Be sure to include authDomain when calling firebase.initializeApp(), ' +
            'by following the instructions in the Firebase console.',
        ["missing-app-credential" /* AuthErrorCode.MISSING_APP_CREDENTIAL */]: 'The phone verification request is missing an application verifier ' +
            'assertion. A reCAPTCHA response token needs to be provided.',
        ["missing-verification-code" /* AuthErrorCode.MISSING_CODE */]: 'The phone auth credential was created with an empty SMS verification code.',
        ["missing-continue-uri" /* AuthErrorCode.MISSING_CONTINUE_URI */]: 'A continue URL must be provided in the request.',
        ["missing-iframe-start" /* AuthErrorCode.MISSING_IFRAME_START */]: 'An internal AuthError has occurred.',
        ["missing-ios-bundle-id" /* AuthErrorCode.MISSING_IOS_BUNDLE_ID */]: 'An iOS Bundle ID must be provided if an App Store ID is provided.',
        ["missing-or-invalid-nonce" /* AuthErrorCode.MISSING_OR_INVALID_NONCE */]: 'The request does not contain a valid nonce. This can occur if the ' +
            'SHA-256 hash of the provided raw nonce does not match the hashed nonce ' +
            'in the ID token payload.',
        ["missing-multi-factor-info" /* AuthErrorCode.MISSING_MFA_INFO */]: 'No second factor identifier is provided.',
        ["missing-multi-factor-session" /* AuthErrorCode.MISSING_MFA_SESSION */]: 'The request is missing proof of first factor successful sign-in.',
        ["missing-phone-number" /* AuthErrorCode.MISSING_PHONE_NUMBER */]: 'To send verification codes, provide a phone number for the recipient.',
        ["missing-verification-id" /* AuthErrorCode.MISSING_SESSION_INFO */]: 'The phone auth credential was created with an empty verification ID.',
        ["app-deleted" /* AuthErrorCode.MODULE_DESTROYED */]: 'This instance of FirebaseApp has been deleted.',
        ["multi-factor-info-not-found" /* AuthErrorCode.MFA_INFO_NOT_FOUND */]: 'The user does not have a second factor matching the identifier provided.',
        ["multi-factor-auth-required" /* AuthErrorCode.MFA_REQUIRED */]: 'Proof of ownership of a second factor is required to complete sign-in.',
        ["account-exists-with-different-credential" /* AuthErrorCode.NEED_CONFIRMATION */]: 'An account already exists with the same email address but different ' +
            'sign-in credentials. Sign in using a provider associated with this ' +
            'email address.',
        ["network-request-failed" /* AuthErrorCode.NETWORK_REQUEST_FAILED */]: 'A network AuthError (such as timeout, interrupted connection or unreachable host) has occurred.',
        ["no-auth-event" /* AuthErrorCode.NO_AUTH_EVENT */]: 'An internal AuthError has occurred.',
        ["no-such-provider" /* AuthErrorCode.NO_SUCH_PROVIDER */]: 'User was not linked to an account with the given provider.',
        ["null-user" /* AuthErrorCode.NULL_USER */]: 'A null user object was provided as the argument for an operation which ' +
            'requires a non-null user object.',
        ["operation-not-allowed" /* AuthErrorCode.OPERATION_NOT_ALLOWED */]: 'The given sign-in provider is disabled for this Firebase project. ' +
            'Enable it in the Firebase console, under the sign-in method tab of the ' +
            'Auth section.',
        ["operation-not-supported-in-this-environment" /* AuthErrorCode.OPERATION_NOT_SUPPORTED */]: 'This operation is not supported in the environment this application is ' +
            'running on. "location.protocol" must be http, https or chrome-extension' +
            ' and web storage must be enabled.',
        ["popup-blocked" /* AuthErrorCode.POPUP_BLOCKED */]: 'Unable to establish a connection with the popup. It may have been blocked by the browser.',
        ["popup-closed-by-user" /* AuthErrorCode.POPUP_CLOSED_BY_USER */]: 'The popup has been closed by the user before finalizing the operation.',
        ["provider-already-linked" /* AuthErrorCode.PROVIDER_ALREADY_LINKED */]: 'User can only be linked to one identity for the given provider.',
        ["quota-exceeded" /* AuthErrorCode.QUOTA_EXCEEDED */]: "The project's quota for this operation has been exceeded.",
        ["redirect-cancelled-by-user" /* AuthErrorCode.REDIRECT_CANCELLED_BY_USER */]: 'The redirect operation has been cancelled by the user before finalizing.',
        ["redirect-operation-pending" /* AuthErrorCode.REDIRECT_OPERATION_PENDING */]: 'A redirect sign-in operation is already pending.',
        ["rejected-credential" /* AuthErrorCode.REJECTED_CREDENTIAL */]: 'The request contains malformed or mismatching credentials.',
        ["second-factor-already-in-use" /* AuthErrorCode.SECOND_FACTOR_ALREADY_ENROLLED */]: 'The second factor is already enrolled on this account.',
        ["maximum-second-factor-count-exceeded" /* AuthErrorCode.SECOND_FACTOR_LIMIT_EXCEEDED */]: 'The maximum allowed number of second factors on a user has been exceeded.',
        ["tenant-id-mismatch" /* AuthErrorCode.TENANT_ID_MISMATCH */]: "The provided tenant ID does not match the Auth instance's tenant ID",
        ["timeout" /* AuthErrorCode.TIMEOUT */]: 'The operation has timed out.',
        ["user-token-expired" /* AuthErrorCode.TOKEN_EXPIRED */]: "The user's credential is no longer valid. The user must sign in again.",
        ["too-many-requests" /* AuthErrorCode.TOO_MANY_ATTEMPTS_TRY_LATER */]: 'We have blocked all requests from this device due to unusual activity. ' +
            'Try again later.',
        ["unauthorized-continue-uri" /* AuthErrorCode.UNAUTHORIZED_DOMAIN */]: 'The domain of the continue URL is not whitelisted.  Please whitelist ' +
            'the domain in the Firebase console.',
        ["unsupported-first-factor" /* AuthErrorCode.UNSUPPORTED_FIRST_FACTOR */]: 'Enrolling a second factor or signing in with a multi-factor account requires sign-in with a supported first factor.',
        ["unsupported-persistence-type" /* AuthErrorCode.UNSUPPORTED_PERSISTENCE */]: 'The current environment does not support the specified persistence type.',
        ["unsupported-tenant-operation" /* AuthErrorCode.UNSUPPORTED_TENANT_OPERATION */]: 'This operation is not supported in a multi-tenant context.',
        ["unverified-email" /* AuthErrorCode.UNVERIFIED_EMAIL */]: 'The operation requires a verified email.',
        ["user-cancelled" /* AuthErrorCode.USER_CANCELLED */]: 'The user did not grant your application the permissions it requested.',
        ["user-not-found" /* AuthErrorCode.USER_DELETED */]: 'There is no user record corresponding to this identifier. The user may ' +
            'have been deleted.',
        ["user-disabled" /* AuthErrorCode.USER_DISABLED */]: 'The user account has been disabled by an administrator.',
        ["user-mismatch" /* AuthErrorCode.USER_MISMATCH */]: 'The supplied credentials do not correspond to the previously signed in user.',
        ["user-signed-out" /* AuthErrorCode.USER_SIGNED_OUT */]: '',
        ["weak-password" /* AuthErrorCode.WEAK_PASSWORD */]: 'The password must be 6 characters long or more.',
        ["web-storage-unsupported" /* AuthErrorCode.WEB_STORAGE_UNSUPPORTED */]: 'This browser is not supported or 3rd party cookies and data may be disabled.',
        ["already-initialized" /* AuthErrorCode.ALREADY_INITIALIZED */]: 'initializeAuth() has already been called with ' +
            'different options. To avoid this error, call initializeAuth() with the ' +
            'same options as when it was originally called, or call getAuth() to return the' +
            ' already initialized instance.'
  ***REMOVED***;
}
function _prodErrorMap() ***REMOVED***
    // We will include this one message in the prod error map since by the very
    // nature of this error, developers will never be able to see the message
    // using the debugErrorMap (which is installed during auth initialization).
    return ***REMOVED***
        ["dependent-sdk-initialized-before-auth" /* AuthErrorCode.DEPENDENT_SDK_INIT_BEFORE_AUTH */]: 'Another Firebase SDK was initialized and is trying to use Auth before Auth is ' +
            'initialized. Please be sure to call `initializeAuth` or `getAuth` before ' +
            'starting any other Firebase SDK.'
  ***REMOVED***;
}
/**
 * A verbose error map with detailed descriptions for most error codes.
 *
 * See discussion at ***REMOVED***@link AuthErrorMap}
 *
 * @public
 */
const debugErrorMap = _debugErrorMap;
/**
 * A minimal error map with all verbose error messages stripped.
 *
 * See discussion at ***REMOVED***@link AuthErrorMap}
 *
 * @public
 */
const prodErrorMap = _prodErrorMap;
const _DEFAULT_AUTH_ERROR_FACTORY = new ErrorFactory('auth', 'Firebase', _prodErrorMap());
/**
 * A map of potential `Auth` error codes, for easier comparison with errors
 * thrown by the SDK.
 *
 * @remarks
 * Note that you can't tree-shake individual keys
 * in the map, so by using the map you might substantially increase your
 * bundle size.
 *
 * @public
 */
const AUTH_ERROR_CODES_MAP_DO_NOT_USE_INTERNALLY = ***REMOVED***
    ADMIN_ONLY_OPERATION: 'auth/admin-restricted-operation',
    ARGUMENT_ERROR: 'auth/argument-error',
    APP_NOT_AUTHORIZED: 'auth/app-not-authorized',
    APP_NOT_INSTALLED: 'auth/app-not-installed',
    CAPTCHA_CHECK_FAILED: 'auth/captcha-check-failed',
    CODE_EXPIRED: 'auth/code-expired',
    CORDOVA_NOT_READY: 'auth/cordova-not-ready',
    CORS_UNSUPPORTED: 'auth/cors-unsupported',
    CREDENTIAL_ALREADY_IN_USE: 'auth/credential-already-in-use',
    CREDENTIAL_MISMATCH: 'auth/custom-token-mismatch',
    CREDENTIAL_TOO_OLD_LOGIN_AGAIN: 'auth/requires-recent-login',
    DEPENDENT_SDK_INIT_BEFORE_AUTH: 'auth/dependent-sdk-initialized-before-auth',
    DYNAMIC_LINK_NOT_ACTIVATED: 'auth/dynamic-link-not-activated',
    EMAIL_CHANGE_NEEDS_VERIFICATION: 'auth/email-change-needs-verification',
    EMAIL_EXISTS: 'auth/email-already-in-use',
    EMULATOR_CONFIG_FAILED: 'auth/emulator-config-failed',
    EXPIRED_OOB_CODE: 'auth/expired-action-code',
    EXPIRED_POPUP_REQUEST: 'auth/cancelled-popup-request',
    INTERNAL_ERROR: 'auth/internal-error',
    INVALID_API_KEY: 'auth/invalid-api-key',
    INVALID_APP_CREDENTIAL: 'auth/invalid-app-credential',
    INVALID_APP_ID: 'auth/invalid-app-id',
    INVALID_AUTH: 'auth/invalid-user-token',
    INVALID_AUTH_EVENT: 'auth/invalid-auth-event',
    INVALID_CERT_HASH: 'auth/invalid-cert-hash',
    INVALID_CODE: 'auth/invalid-verification-code',
    INVALID_CONTINUE_URI: 'auth/invalid-continue-uri',
    INVALID_CORDOVA_CONFIGURATION: 'auth/invalid-cordova-configuration',
    INVALID_CUSTOM_TOKEN: 'auth/invalid-custom-token',
    INVALID_DYNAMIC_LINK_DOMAIN: 'auth/invalid-dynamic-link-domain',
    INVALID_EMAIL: 'auth/invalid-email',
    INVALID_EMULATOR_SCHEME: 'auth/invalid-emulator-scheme',
    INVALID_IDP_RESPONSE: 'auth/invalid-credential',
    INVALID_MESSAGE_PAYLOAD: 'auth/invalid-message-payload',
    INVALID_MFA_SESSION: 'auth/invalid-multi-factor-session',
    INVALID_OAUTH_CLIENT_ID: 'auth/invalid-oauth-client-id',
    INVALID_OAUTH_PROVIDER: 'auth/invalid-oauth-provider',
    INVALID_OOB_CODE: 'auth/invalid-action-code',
    INVALID_ORIGIN: 'auth/unauthorized-domain',
    INVALID_PASSWORD: 'auth/wrong-password',
    INVALID_PERSISTENCE: 'auth/invalid-persistence-type',
    INVALID_PHONE_NUMBER: 'auth/invalid-phone-number',
    INVALID_PROVIDER_ID: 'auth/invalid-provider-id',
    INVALID_RECIPIENT_EMAIL: 'auth/invalid-recipient-email',
    INVALID_SENDER: 'auth/invalid-sender',
    INVALID_SESSION_INFO: 'auth/invalid-verification-id',
    INVALID_TENANT_ID: 'auth/invalid-tenant-id',
    MFA_INFO_NOT_FOUND: 'auth/multi-factor-info-not-found',
    MFA_REQUIRED: 'auth/multi-factor-auth-required',
    MISSING_ANDROID_PACKAGE_NAME: 'auth/missing-android-pkg-name',
    MISSING_APP_CREDENTIAL: 'auth/missing-app-credential',
    MISSING_AUTH_DOMAIN: 'auth/auth-domain-config-required',
    MISSING_CODE: 'auth/missing-verification-code',
    MISSING_CONTINUE_URI: 'auth/missing-continue-uri',
    MISSING_IFRAME_START: 'auth/missing-iframe-start',
    MISSING_IOS_BUNDLE_ID: 'auth/missing-ios-bundle-id',
    MISSING_OR_INVALID_NONCE: 'auth/missing-or-invalid-nonce',
    MISSING_MFA_INFO: 'auth/missing-multi-factor-info',
    MISSING_MFA_SESSION: 'auth/missing-multi-factor-session',
    MISSING_PHONE_NUMBER: 'auth/missing-phone-number',
    MISSING_SESSION_INFO: 'auth/missing-verification-id',
    MODULE_DESTROYED: 'auth/app-deleted',
    NEED_CONFIRMATION: 'auth/account-exists-with-different-credential',
    NETWORK_REQUEST_FAILED: 'auth/network-request-failed',
    NULL_USER: 'auth/null-user',
    NO_AUTH_EVENT: 'auth/no-auth-event',
    NO_SUCH_PROVIDER: 'auth/no-such-provider',
    OPERATION_NOT_ALLOWED: 'auth/operation-not-allowed',
    OPERATION_NOT_SUPPORTED: 'auth/operation-not-supported-in-this-environment',
    POPUP_BLOCKED: 'auth/popup-blocked',
    POPUP_CLOSED_BY_USER: 'auth/popup-closed-by-user',
    PROVIDER_ALREADY_LINKED: 'auth/provider-already-linked',
    QUOTA_EXCEEDED: 'auth/quota-exceeded',
    REDIRECT_CANCELLED_BY_USER: 'auth/redirect-cancelled-by-user',
    REDIRECT_OPERATION_PENDING: 'auth/redirect-operation-pending',
    REJECTED_CREDENTIAL: 'auth/rejected-credential',
    SECOND_FACTOR_ALREADY_ENROLLED: 'auth/second-factor-already-in-use',
    SECOND_FACTOR_LIMIT_EXCEEDED: 'auth/maximum-second-factor-count-exceeded',
    TENANT_ID_MISMATCH: 'auth/tenant-id-mismatch',
    TIMEOUT: 'auth/timeout',
    TOKEN_EXPIRED: 'auth/user-token-expired',
    TOO_MANY_ATTEMPTS_TRY_LATER: 'auth/too-many-requests',
    UNAUTHORIZED_DOMAIN: 'auth/unauthorized-continue-uri',
    UNSUPPORTED_FIRST_FACTOR: 'auth/unsupported-first-factor',
    UNSUPPORTED_PERSISTENCE: 'auth/unsupported-persistence-type',
    UNSUPPORTED_TENANT_OPERATION: 'auth/unsupported-tenant-operation',
    UNVERIFIED_EMAIL: 'auth/unverified-email',
    USER_CANCELLED: 'auth/user-cancelled',
    USER_DELETED: 'auth/user-not-found',
    USER_DISABLED: 'auth/user-disabled',
    USER_MISMATCH: 'auth/user-mismatch',
    USER_SIGNED_OUT: 'auth/user-signed-out',
    WEAK_PASSWORD: 'auth/weak-password',
    WEB_STORAGE_UNSUPPORTED: 'auth/web-storage-unsupported',
    ALREADY_INITIALIZED: 'auth/already-initialized'
};

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const logClient = new Logger('@firebase/auth');
function _logError(msg, ...args) ***REMOVED***
    if (logClient.logLevel <= LogLevel.ERROR) ***REMOVED***
        logClient.error(`Auth ($***REMOVED***SDK_VERSION}): $***REMOVED***msg}`, ...args);
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function _fail(authOrCode, ...rest) ***REMOVED***
    throw createErrorInternal(authOrCode, ...rest);
}
function _createError(authOrCode, ...rest) ***REMOVED***
    return createErrorInternal(authOrCode, ...rest);
}
function _errorWithCustomMessage(auth, code, message) ***REMOVED***
    const errorMap = Object.assign(Object.assign(***REMOVED***}, prodErrorMap()), ***REMOVED*** [code]: message });
    const factory = new ErrorFactory('auth', 'Firebase', errorMap);
    return factory.create(code, ***REMOVED***
        appName: auth.name
  ***REMOVED***);
}
function _assertInstanceOf(auth, object, instance) ***REMOVED***
    const constructorInstance = instance;
    if (!(object instanceof constructorInstance)) ***REMOVED***
        if (constructorInstance.name !== object.constructor.name) ***REMOVED***
            _fail(auth, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
      ***REMOVED***
        throw _errorWithCustomMessage(auth, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */, `Type of $***REMOVED***object.constructor.name} does not match expected instance.` +
            `Did you pass a reference from a different Auth SDK?`);
  ***REMOVED***
}
function createErrorInternal(authOrCode, ...rest) ***REMOVED***
    if (typeof authOrCode !== 'string') ***REMOVED***
        const code = rest[0];
        const fullParams = [...rest.slice(1)];
        if (fullParams[0]) ***REMOVED***
            fullParams[0].appName = authOrCode.name;
      ***REMOVED***
        return authOrCode._errorFactory.create(code, ...fullParams);
  ***REMOVED***
    return _DEFAULT_AUTH_ERROR_FACTORY.create(authOrCode, ...rest);
}
function _assert(assertion, authOrCode, ...rest) ***REMOVED***
    if (!assertion) ***REMOVED***
        throw createErrorInternal(authOrCode, ...rest);
  ***REMOVED***
}
/**
 * Unconditionally fails, throwing an internal error with the given message.
 *
 * @param failure type of failure encountered
 * @throws Error
 */
function debugFail(failure) ***REMOVED***
    // Log the failure in addition to throw an exception, just in case the
    // exception is swallowed.
    const message = `INTERNAL ASSERTION FAILED: ` + failure;
    _logError(message);
    // NOTE: We don't use FirebaseError here because these are internal failures
    // that cannot be handled by the user. (Also it would create a circular
    // dependency between the error and assert modules which doesn't work.)
    throw new Error(message);
}
/**
 * Fails if the given assertion condition is false, throwing an Error with the
 * given message if it did.
 *
 * @param assertion
 * @param message
 */
function debugAssert(assertion, message) ***REMOVED***
    if (!assertion) ***REMOVED***
        debugFail(message);
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const instanceCache = new Map();
function _getInstance(cls) ***REMOVED***
    debugAssert(cls instanceof Function, 'Expected a class definition');
    let instance = instanceCache.get(cls);
    if (instance) ***REMOVED***
        debugAssert(instance instanceof cls, 'Instance stored in cache mismatched with class');
        return instance;
  ***REMOVED***
    instance = new cls();
    instanceCache.set(cls, instance);
    return instance;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Initializes an ***REMOVED***@link Auth} instance with fine-grained control over
 * ***REMOVED***@link Dependencies}.
 *
 * @remarks
 *
 * This function allows more control over the ***REMOVED***@link Auth} instance than
 * ***REMOVED***@link getAuth}. `getAuth` uses platform-specific defaults to supply
 * the ***REMOVED***@link Dependencies}. In general, `getAuth` is the easiest way to
 * initialize Auth and works for most use cases. Use `initializeAuth` if you
 * need control over which persistence layer is used, or to minimize bundle
 * size if you're not using either `signInWithPopup` or `signInWithRedirect`.
 *
 * For example, if your app only uses anonymous accounts and you only want
 * accounts saved for the current session, initialize `Auth` with:
 *
 * ```js
 * const auth = initializeAuth(app, ***REMOVED***
 *   persistence: browserSessionPersistence,
 *   popupRedirectResolver: undefined,
 * });
 * ```
 *
 * @public
 */
function initializeAuth(app, deps) ***REMOVED***
    const provider = _getProvider(app, 'auth');
    if (provider.isInitialized()) ***REMOVED***
        const auth = provider.getImmediate();
        const initialOptions = provider.getOptions();
        if (deepEqual(initialOptions, deps !== null && deps !== void 0 ? deps : ***REMOVED***})) ***REMOVED***
            return auth;
      ***REMOVED***
        else ***REMOVED***
            _fail(auth, "already-initialized" /* AuthErrorCode.ALREADY_INITIALIZED */);
      ***REMOVED***
  ***REMOVED***
    const auth = provider.initialize(***REMOVED*** options: deps });
    return auth;
}
function _initializeAuthInstance(auth, deps) ***REMOVED***
    const persistence = (deps === null || deps === void 0 ? void 0 : deps.persistence) || [];
    const hierarchy = (Array.isArray(persistence) ? persistence : [persistence]).map(_getInstance);
    if (deps === null || deps === void 0 ? void 0 : deps.errorMap) ***REMOVED***
        auth._updateErrorMap(deps.errorMap);
  ***REMOVED***
    // This promise is intended to float; auth initialization happens in the
    // background, meanwhile the auth object may be used by the app.
    // eslint-disable-next-line @typescript-eslint/no-floating-promises
    auth._initializeWithPersistence(hierarchy, deps === null || deps === void 0 ? void 0 : deps.popupRedirectResolver);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function _getCurrentUrl() ***REMOVED***
    var _a;
    return (typeof self !== 'undefined' && ((_a = self.location) === null || _a === void 0 ? void 0 : _a.href)) || '';
}
function _isHttpOrHttps() ***REMOVED***
    return _getCurrentScheme() === 'http:' || _getCurrentScheme() === 'https:';
}
function _getCurrentScheme() ***REMOVED***
    var _a;
    return (typeof self !== 'undefined' && ((_a = self.location) === null || _a === void 0 ? void 0 : _a.protocol)) || null;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Determine whether the browser is working online
 */
function _isOnline() ***REMOVED***
    if (typeof navigator !== 'undefined' &&
        navigator &&
        'onLine' in navigator &&
        typeof navigator.onLine === 'boolean' &&
        // Apply only for traditional web apps and Chrome extensions.
        // This is especially true for Cordova apps which have unreliable
        // navigator.onLine behavior unless cordova-plugin-network-information is
        // installed which overwrites the native navigator.onLine value and
        // defines navigator.connection.
        (_isHttpOrHttps() || isBrowserExtension() || 'connection' in navigator)) ***REMOVED***
        return navigator.onLine;
  ***REMOVED***
    // If we can't determine the state, assume it is online.
    return true;
}
function _getUserLanguage() ***REMOVED***
    if (typeof navigator === 'undefined') ***REMOVED***
        return null;
  ***REMOVED***
    const navigatorLanguage = navigator;
    return (
    // Most reliable, but only supported in Chrome/Firefox.
    (navigatorLanguage.languages && navigatorLanguage.languages[0]) ||
        // Supported in most browsers, but returns the language of the browser
        // UI, not the language set in browser settings.
        navigatorLanguage.language ||
        // Couldn't determine language.
        null);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * A structure to help pick between a range of long and short delay durations
 * depending on the current environment. In general, the long delay is used for
 * mobile environments whereas short delays are used for desktop environments.
 */
class Delay ***REMOVED***
    constructor(shortDelay, longDelay) ***REMOVED***
        this.shortDelay = shortDelay;
        this.longDelay = longDelay;
        // Internal error when improperly initialized.
        debugAssert(longDelay > shortDelay, 'Short delay should be less than long delay!');
        this.isMobile = isMobileCordova() || isReactNative();
  ***REMOVED***
    get() ***REMOVED***
        if (!_isOnline()) ***REMOVED***
            // Pick the shorter timeout.
            return Math.min(5000 /* DelayMin.OFFLINE */, this.shortDelay);
      ***REMOVED***
        // If running in a mobile environment, return the long delay, otherwise
        // return the short delay.
        // This could be improved in the future to dynamically change based on other
        // variables instead of just reading the current environment.
        return this.isMobile ? this.longDelay : this.shortDelay;
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function _emulatorUrl(config, path) ***REMOVED***
    debugAssert(config.emulator, 'Emulator should always be set here');
    const ***REMOVED*** url } = config.emulator;
    if (!path) ***REMOVED***
        return url;
  ***REMOVED***
    return `$***REMOVED***url}$***REMOVED***path.startsWith('/') ? path.slice(1) : path}`;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class FetchProvider ***REMOVED***
    static initialize(fetchImpl, headersImpl, responseImpl) ***REMOVED***
        this.fetchImpl = fetchImpl;
        if (headersImpl) ***REMOVED***
            this.headersImpl = headersImpl;
      ***REMOVED***
        if (responseImpl) ***REMOVED***
            this.responseImpl = responseImpl;
      ***REMOVED***
  ***REMOVED***
    static fetch() ***REMOVED***
        if (this.fetchImpl) ***REMOVED***
            return this.fetchImpl;
      ***REMOVED***
        if (typeof self !== 'undefined' && 'fetch' in self) ***REMOVED***
            return self.fetch;
      ***REMOVED***
        debugFail('Could not find fetch implementation, make sure you call FetchProvider.initialize() with an appropriate polyfill');
  ***REMOVED***
    static headers() ***REMOVED***
        if (this.headersImpl) ***REMOVED***
            return this.headersImpl;
      ***REMOVED***
        if (typeof self !== 'undefined' && 'Headers' in self) ***REMOVED***
            return self.Headers;
      ***REMOVED***
        debugFail('Could not find Headers implementation, make sure you call FetchProvider.initialize() with an appropriate polyfill');
  ***REMOVED***
    static response() ***REMOVED***
        if (this.responseImpl) ***REMOVED***
            return this.responseImpl;
      ***REMOVED***
        if (typeof self !== 'undefined' && 'Response' in self) ***REMOVED***
            return self.Response;
      ***REMOVED***
        debugFail('Could not find Response implementation, make sure you call FetchProvider.initialize() with an appropriate polyfill');
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Map from errors returned by the server to errors to developer visible errors
 */
const SERVER_ERROR_MAP = ***REMOVED***
    // Custom token errors.
    ["CREDENTIAL_MISMATCH" /* ServerError.CREDENTIAL_MISMATCH */]: "custom-token-mismatch" /* AuthErrorCode.CREDENTIAL_MISMATCH */,
    // This can only happen if the SDK sends a bad request.
    ["MISSING_CUSTOM_TOKEN" /* ServerError.MISSING_CUSTOM_TOKEN */]: "internal-error" /* AuthErrorCode.INTERNAL_ERROR */,
    // Create Auth URI errors.
    ["INVALID_IDENTIFIER" /* ServerError.INVALID_IDENTIFIER */]: "invalid-email" /* AuthErrorCode.INVALID_EMAIL */,
    // This can only happen if the SDK sends a bad request.
    ["MISSING_CONTINUE_URI" /* ServerError.MISSING_CONTINUE_URI */]: "internal-error" /* AuthErrorCode.INTERNAL_ERROR */,
    // Sign in with email and password errors (some apply to sign up too).
    ["INVALID_PASSWORD" /* ServerError.INVALID_PASSWORD */]: "wrong-password" /* AuthErrorCode.INVALID_PASSWORD */,
    // This can only happen if the SDK sends a bad request.
    ["MISSING_PASSWORD" /* ServerError.MISSING_PASSWORD */]: "internal-error" /* AuthErrorCode.INTERNAL_ERROR */,
    // Sign up with email and password errors.
    ["EMAIL_EXISTS" /* ServerError.EMAIL_EXISTS */]: "email-already-in-use" /* AuthErrorCode.EMAIL_EXISTS */,
    ["PASSWORD_LOGIN_DISABLED" /* ServerError.PASSWORD_LOGIN_DISABLED */]: "operation-not-allowed" /* AuthErrorCode.OPERATION_NOT_ALLOWED */,
    // Verify assertion for sign in with credential errors:
    ["INVALID_IDP_RESPONSE" /* ServerError.INVALID_IDP_RESPONSE */]: "invalid-credential" /* AuthErrorCode.INVALID_IDP_RESPONSE */,
    ["INVALID_PENDING_TOKEN" /* ServerError.INVALID_PENDING_TOKEN */]: "invalid-credential" /* AuthErrorCode.INVALID_IDP_RESPONSE */,
    ["FEDERATED_USER_ID_ALREADY_LINKED" /* ServerError.FEDERATED_USER_ID_ALREADY_LINKED */]: "credential-already-in-use" /* AuthErrorCode.CREDENTIAL_ALREADY_IN_USE */,
    // This can only happen if the SDK sends a bad request.
    ["MISSING_REQ_TYPE" /* ServerError.MISSING_REQ_TYPE */]: "internal-error" /* AuthErrorCode.INTERNAL_ERROR */,
    // Send Password reset email errors:
    ["EMAIL_NOT_FOUND" /* ServerError.EMAIL_NOT_FOUND */]: "user-not-found" /* AuthErrorCode.USER_DELETED */,
    ["RESET_PASSWORD_EXCEED_LIMIT" /* ServerError.RESET_PASSWORD_EXCEED_LIMIT */]: "too-many-requests" /* AuthErrorCode.TOO_MANY_ATTEMPTS_TRY_LATER */,
    ["EXPIRED_OOB_CODE" /* ServerError.EXPIRED_OOB_CODE */]: "expired-action-code" /* AuthErrorCode.EXPIRED_OOB_CODE */,
    ["INVALID_OOB_CODE" /* ServerError.INVALID_OOB_CODE */]: "invalid-action-code" /* AuthErrorCode.INVALID_OOB_CODE */,
    // This can only happen if the SDK sends a bad request.
    ["MISSING_OOB_CODE" /* ServerError.MISSING_OOB_CODE */]: "internal-error" /* AuthErrorCode.INTERNAL_ERROR */,
    // Operations that require ID token in request:
    ["CREDENTIAL_TOO_OLD_LOGIN_AGAIN" /* ServerError.CREDENTIAL_TOO_OLD_LOGIN_AGAIN */]: "requires-recent-login" /* AuthErrorCode.CREDENTIAL_TOO_OLD_LOGIN_AGAIN */,
    ["INVALID_ID_TOKEN" /* ServerError.INVALID_ID_TOKEN */]: "invalid-user-token" /* AuthErrorCode.INVALID_AUTH */,
    ["TOKEN_EXPIRED" /* ServerError.TOKEN_EXPIRED */]: "user-token-expired" /* AuthErrorCode.TOKEN_EXPIRED */,
    ["USER_NOT_FOUND" /* ServerError.USER_NOT_FOUND */]: "user-token-expired" /* AuthErrorCode.TOKEN_EXPIRED */,
    // Other errors.
    ["TOO_MANY_ATTEMPTS_TRY_LATER" /* ServerError.TOO_MANY_ATTEMPTS_TRY_LATER */]: "too-many-requests" /* AuthErrorCode.TOO_MANY_ATTEMPTS_TRY_LATER */,
    // Phone Auth related errors.
    ["INVALID_CODE" /* ServerError.INVALID_CODE */]: "invalid-verification-code" /* AuthErrorCode.INVALID_CODE */,
    ["INVALID_SESSION_INFO" /* ServerError.INVALID_SESSION_INFO */]: "invalid-verification-id" /* AuthErrorCode.INVALID_SESSION_INFO */,
    ["INVALID_TEMPORARY_PROOF" /* ServerError.INVALID_TEMPORARY_PROOF */]: "invalid-credential" /* AuthErrorCode.INVALID_IDP_RESPONSE */,
    ["MISSING_SESSION_INFO" /* ServerError.MISSING_SESSION_INFO */]: "missing-verification-id" /* AuthErrorCode.MISSING_SESSION_INFO */,
    ["SESSION_EXPIRED" /* ServerError.SESSION_EXPIRED */]: "code-expired" /* AuthErrorCode.CODE_EXPIRED */,
    // Other action code errors when additional settings passed.
    // MISSING_CONTINUE_URI is getting mapped to INTERNAL_ERROR above.
    // This is OK as this error will be caught by client side validation.
    ["MISSING_ANDROID_PACKAGE_NAME" /* ServerError.MISSING_ANDROID_PACKAGE_NAME */]: "missing-android-pkg-name" /* AuthErrorCode.MISSING_ANDROID_PACKAGE_NAME */,
    ["UNAUTHORIZED_DOMAIN" /* ServerError.UNAUTHORIZED_DOMAIN */]: "unauthorized-continue-uri" /* AuthErrorCode.UNAUTHORIZED_DOMAIN */,
    // getProjectConfig errors when clientId is passed.
    ["INVALID_OAUTH_CLIENT_ID" /* ServerError.INVALID_OAUTH_CLIENT_ID */]: "invalid-oauth-client-id" /* AuthErrorCode.INVALID_OAUTH_CLIENT_ID */,
    // User actions (sign-up or deletion) disabled errors.
    ["ADMIN_ONLY_OPERATION" /* ServerError.ADMIN_ONLY_OPERATION */]: "admin-restricted-operation" /* AuthErrorCode.ADMIN_ONLY_OPERATION */,
    // Multi factor related errors.
    ["INVALID_MFA_PENDING_CREDENTIAL" /* ServerError.INVALID_MFA_PENDING_CREDENTIAL */]: "invalid-multi-factor-session" /* AuthErrorCode.INVALID_MFA_SESSION */,
    ["MFA_ENROLLMENT_NOT_FOUND" /* ServerError.MFA_ENROLLMENT_NOT_FOUND */]: "multi-factor-info-not-found" /* AuthErrorCode.MFA_INFO_NOT_FOUND */,
    ["MISSING_MFA_ENROLLMENT_ID" /* ServerError.MISSING_MFA_ENROLLMENT_ID */]: "missing-multi-factor-info" /* AuthErrorCode.MISSING_MFA_INFO */,
    ["MISSING_MFA_PENDING_CREDENTIAL" /* ServerError.MISSING_MFA_PENDING_CREDENTIAL */]: "missing-multi-factor-session" /* AuthErrorCode.MISSING_MFA_SESSION */,
    ["SECOND_FACTOR_EXISTS" /* ServerError.SECOND_FACTOR_EXISTS */]: "second-factor-already-in-use" /* AuthErrorCode.SECOND_FACTOR_ALREADY_ENROLLED */,
    ["SECOND_FACTOR_LIMIT_EXCEEDED" /* ServerError.SECOND_FACTOR_LIMIT_EXCEEDED */]: "maximum-second-factor-count-exceeded" /* AuthErrorCode.SECOND_FACTOR_LIMIT_EXCEEDED */,
    // Blocking functions related errors.
    ["BLOCKING_FUNCTION_ERROR_RESPONSE" /* ServerError.BLOCKING_FUNCTION_ERROR_RESPONSE */]: "internal-error" /* AuthErrorCode.INTERNAL_ERROR */
};

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const DEFAULT_API_TIMEOUT_MS = new Delay(30000, 60000);
function _addTidIfNecessary(auth, request) ***REMOVED***
    if (auth.tenantId && !request.tenantId) ***REMOVED***
        return Object.assign(Object.assign(***REMOVED***}, request), ***REMOVED*** tenantId: auth.tenantId });
  ***REMOVED***
    return request;
}
async function _performApiRequest(auth, method, path, request, customErrorMap = ***REMOVED***}) ***REMOVED***
    return _performFetchWithErrorHandling(auth, customErrorMap, async () => ***REMOVED***
        let body = ***REMOVED***};
        let params = ***REMOVED***};
        if (request) ***REMOVED***
            if (method === "GET" /* HttpMethod.GET */) ***REMOVED***
                params = request;
          ***REMOVED***
            else ***REMOVED***
                body = ***REMOVED***
                    body: JSON.stringify(request)
              ***REMOVED***;
          ***REMOVED***
      ***REMOVED***
        const query = querystring(Object.assign(***REMOVED*** key: auth.config.apiKey }, params)).slice(1);
        const headers = await auth._getAdditionalHeaders();
        headers["Content-Type" /* HttpHeader.CONTENT_TYPE */] = 'application/json';
        if (auth.languageCode) ***REMOVED***
            headers["X-Firebase-Locale" /* HttpHeader.X_FIREBASE_LOCALE */] = auth.languageCode;
      ***REMOVED***
        return FetchProvider.fetch()(_getFinalTarget(auth, auth.config.apiHost, path, query), Object.assign(***REMOVED*** method,
            headers, referrerPolicy: 'no-referrer' }, body));
  ***REMOVED***);
}
async function _performFetchWithErrorHandling(auth, customErrorMap, fetchFn) ***REMOVED***
    auth._canInitEmulator = false;
    const errorMap = Object.assign(Object.assign(***REMOVED***}, SERVER_ERROR_MAP), customErrorMap);
    try ***REMOVED***
        const networkTimeout = new NetworkTimeout(auth);
        const response = await Promise.race([
            fetchFn(),
            networkTimeout.promise
        ]);
        // If we've reached this point, the fetch succeeded and the networkTimeout
        // didn't throw; clear the network timeout delay so that Node won't hang
        networkTimeout.clearNetworkTimeout();
        const json = await response.json();
        if ('needConfirmation' in json) ***REMOVED***
            throw _makeTaggedError(auth, "account-exists-with-different-credential" /* AuthErrorCode.NEED_CONFIRMATION */, json);
      ***REMOVED***
        if (response.ok && !('errorMessage' in json)) ***REMOVED***
            return json;
      ***REMOVED***
        else ***REMOVED***
            const errorMessage = response.ok ? json.errorMessage : json.error.message;
            const [serverErrorCode, serverErrorMessage] = errorMessage.split(' : ');
            if (serverErrorCode === "FEDERATED_USER_ID_ALREADY_LINKED" /* ServerError.FEDERATED_USER_ID_ALREADY_LINKED */) ***REMOVED***
                throw _makeTaggedError(auth, "credential-already-in-use" /* AuthErrorCode.CREDENTIAL_ALREADY_IN_USE */, json);
          ***REMOVED***
            else if (serverErrorCode === "EMAIL_EXISTS" /* ServerError.EMAIL_EXISTS */) ***REMOVED***
                throw _makeTaggedError(auth, "email-already-in-use" /* AuthErrorCode.EMAIL_EXISTS */, json);
          ***REMOVED***
            else if (serverErrorCode === "USER_DISABLED" /* ServerError.USER_DISABLED */) ***REMOVED***
                throw _makeTaggedError(auth, "user-disabled" /* AuthErrorCode.USER_DISABLED */, json);
          ***REMOVED***
            const authError = errorMap[serverErrorCode] ||
                serverErrorCode
                    .toLowerCase()
                    .replace(/[_\s]+/g, '-');
            if (serverErrorMessage) ***REMOVED***
                throw _errorWithCustomMessage(auth, authError, serverErrorMessage);
          ***REMOVED***
            else ***REMOVED***
                _fail(auth, authError);
          ***REMOVED***
      ***REMOVED***
  ***REMOVED***
    catch (e) ***REMOVED***
        if (e instanceof FirebaseError) ***REMOVED***
            throw e;
      ***REMOVED***
        _fail(auth, "network-request-failed" /* AuthErrorCode.NETWORK_REQUEST_FAILED */);
  ***REMOVED***
}
async function _performSignInRequest(auth, method, path, request, customErrorMap = ***REMOVED***}) ***REMOVED***
    const serverResponse = (await _performApiRequest(auth, method, path, request, customErrorMap));
    if ('mfaPendingCredential' in serverResponse) ***REMOVED***
        _fail(auth, "multi-factor-auth-required" /* AuthErrorCode.MFA_REQUIRED */, ***REMOVED***
            _serverResponse: serverResponse
      ***REMOVED***);
  ***REMOVED***
    return serverResponse;
}
function _getFinalTarget(auth, host, path, query) ***REMOVED***
    const base = `$***REMOVED***host}$***REMOVED***path}?$***REMOVED***query}`;
    if (!auth.config.emulator) ***REMOVED***
        return `$***REMOVED***auth.config.apiScheme}://$***REMOVED***base}`;
  ***REMOVED***
    return _emulatorUrl(auth.config, base);
}
class NetworkTimeout ***REMOVED***
    constructor(auth) ***REMOVED***
        this.auth = auth;
        // Node timers and browser timers are fundamentally incompatible, but we
        // don't care about the value here
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        this.timer = null;
        this.promise = new Promise((_, reject) => ***REMOVED***
            this.timer = setTimeout(() => ***REMOVED***
                return reject(_createError(this.auth, "network-request-failed" /* AuthErrorCode.NETWORK_REQUEST_FAILED */));
          ***REMOVED***, DEFAULT_API_TIMEOUT_MS.get());
      ***REMOVED***);
  ***REMOVED***
    clearNetworkTimeout() ***REMOVED***
        clearTimeout(this.timer);
  ***REMOVED***
}
function _makeTaggedError(auth, code, response) ***REMOVED***
    const errorParams = ***REMOVED***
        appName: auth.name
  ***REMOVED***;
    if (response.email) ***REMOVED***
        errorParams.email = response.email;
  ***REMOVED***
    if (response.phoneNumber) ***REMOVED***
        errorParams.phoneNumber = response.phoneNumber;
  ***REMOVED***
    const error = _createError(auth, code, errorParams);
    // We know customData is defined on error because errorParams is defined
    error.customData._tokenResponse = response;
    return error;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function deleteAccount(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:delete" /* Endpoint.DELETE_ACCOUNT */, request);
}
async function deleteLinkedAccounts(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:update" /* Endpoint.SET_ACCOUNT_INFO */, request);
}
async function getAccountInfo(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:lookup" /* Endpoint.GET_ACCOUNT_INFO */, request);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function utcTimestampToDateString(utcTimestamp) ***REMOVED***
    if (!utcTimestamp) ***REMOVED***
        return undefined;
  ***REMOVED***
    try ***REMOVED***
        // Convert to date object.
        const date = new Date(Number(utcTimestamp));
        // Test date is valid.
        if (!isNaN(date.getTime())) ***REMOVED***
            // Convert to UTC date string.
            return date.toUTCString();
      ***REMOVED***
  ***REMOVED***
    catch (e) ***REMOVED***
        // Do nothing. undefined will be returned.
  ***REMOVED***
    return undefined;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Returns a JSON Web Token (JWT) used to identify the user to a Firebase service.
 *
 * @remarks
 * Returns the current token if it has not expired or if it will not expire in the next five
 * minutes. Otherwise, this will refresh the token and return a new one.
 *
 * @param user - The user.
 * @param forceRefresh - Force refresh regardless of token expiration.
 *
 * @public
 */
function getIdToken(user, forceRefresh = false) ***REMOVED***
    return getModularInstance(user).getIdToken(forceRefresh);
}
/**
 * Returns a deserialized JSON Web Token (JWT) used to identitfy the user to a Firebase service.
 *
 * @remarks
 * Returns the current token if it has not expired or if it will not expire in the next five
 * minutes. Otherwise, this will refresh the token and return a new one.
 *
 * @param user - The user.
 * @param forceRefresh - Force refresh regardless of token expiration.
 *
 * @public
 */
async function getIdTokenResult(user, forceRefresh = false) ***REMOVED***
    const userInternal = getModularInstance(user);
    const token = await userInternal.getIdToken(forceRefresh);
    const claims = _parseToken(token);
    _assert(claims && claims.exp && claims.auth_time && claims.iat, userInternal.auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
    const firebase = typeof claims.firebase === 'object' ? claims.firebase : undefined;
    const signInProvider = firebase === null || firebase === void 0 ? void 0 : firebase['sign_in_provider'];
    return ***REMOVED***
        claims,
        token,
        authTime: utcTimestampToDateString(secondsStringToMilliseconds(claims.auth_time)),
        issuedAtTime: utcTimestampToDateString(secondsStringToMilliseconds(claims.iat)),
        expirationTime: utcTimestampToDateString(secondsStringToMilliseconds(claims.exp)),
        signInProvider: signInProvider || null,
        signInSecondFactor: (firebase === null || firebase === void 0 ? void 0 : firebase['sign_in_second_factor']) || null
  ***REMOVED***;
}
function secondsStringToMilliseconds(seconds) ***REMOVED***
    return Number(seconds) * 1000;
}
function _parseToken(token) ***REMOVED***
    const [algorithm, payload, signature] = token.split('.');
    if (algorithm === undefined ||
        payload === undefined ||
        signature === undefined) ***REMOVED***
        _logError('JWT malformed, contained fewer than 3 sections');
        return null;
  ***REMOVED***
    try ***REMOVED***
        const decoded = base64Decode(payload);
        if (!decoded) ***REMOVED***
            _logError('Failed to decode base64 JWT payload');
            return null;
      ***REMOVED***
        return JSON.parse(decoded);
  ***REMOVED***
    catch (e) ***REMOVED***
        _logError('Caught error parsing JWT payload as JSON', e === null || e === void 0 ? void 0 : e.toString());
        return null;
  ***REMOVED***
}
/**
 * Extract expiresIn TTL from a token by subtracting the expiration from the issuance.
 */
function _tokenExpiresIn(token) ***REMOVED***
    const parsedToken = _parseToken(token);
    _assert(parsedToken, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
    _assert(typeof parsedToken.exp !== 'undefined', "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
    _assert(typeof parsedToken.iat !== 'undefined', "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
    return Number(parsedToken.exp) - Number(parsedToken.iat);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function _logoutIfInvalidated(user, promise, bypassAuthState = false) ***REMOVED***
    if (bypassAuthState) ***REMOVED***
        return promise;
  ***REMOVED***
    try ***REMOVED***
        return await promise;
  ***REMOVED***
    catch (e) ***REMOVED***
        if (e instanceof FirebaseError && isUserInvalidated(e)) ***REMOVED***
            if (user.auth.currentUser === user) ***REMOVED***
                await user.auth.signOut();
          ***REMOVED***
      ***REMOVED***
        throw e;
  ***REMOVED***
}
function isUserInvalidated(***REMOVED*** code }) ***REMOVED***
    return (code === `auth/$***REMOVED***"user-disabled" /* AuthErrorCode.USER_DISABLED */}` ||
        code === `auth/$***REMOVED***"user-token-expired" /* AuthErrorCode.TOKEN_EXPIRED */}`);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class ProactiveRefresh ***REMOVED***
    constructor(user) ***REMOVED***
        this.user = user;
        this.isRunning = false;
        // Node timers and browser timers return fundamentally different types.
        // We don't actually care what the value is but TS won't accept unknown and
        // we can't cast properly in both environments.
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        this.timerId = null;
        this.errorBackoff = 30000 /* Duration.RETRY_BACKOFF_MIN */;
  ***REMOVED***
    _start() ***REMOVED***
        if (this.isRunning) ***REMOVED***
            return;
      ***REMOVED***
        this.isRunning = true;
        this.schedule();
  ***REMOVED***
    _stop() ***REMOVED***
        if (!this.isRunning) ***REMOVED***
            return;
      ***REMOVED***
        this.isRunning = false;
        if (this.timerId !== null) ***REMOVED***
            clearTimeout(this.timerId);
      ***REMOVED***
  ***REMOVED***
    getInterval(wasError) ***REMOVED***
        var _a;
        if (wasError) ***REMOVED***
            const interval = this.errorBackoff;
            this.errorBackoff = Math.min(this.errorBackoff * 2, 960000 /* Duration.RETRY_BACKOFF_MAX */);
            return interval;
      ***REMOVED***
        else ***REMOVED***
            // Reset the error backoff
            this.errorBackoff = 30000 /* Duration.RETRY_BACKOFF_MIN */;
            const expTime = (_a = this.user.stsTokenManager.expirationTime) !== null && _a !== void 0 ? _a : 0;
            const interval = expTime - Date.now() - 300000 /* Duration.OFFSET */;
            return Math.max(0, interval);
      ***REMOVED***
  ***REMOVED***
    schedule(wasError = false) ***REMOVED***
        if (!this.isRunning) ***REMOVED***
            // Just in case...
            return;
      ***REMOVED***
        const interval = this.getInterval(wasError);
        this.timerId = setTimeout(async () => ***REMOVED***
            await this.iteration();
      ***REMOVED***, interval);
  ***REMOVED***
    async iteration() ***REMOVED***
        try ***REMOVED***
            await this.user.getIdToken(true);
      ***REMOVED***
        catch (e) ***REMOVED***
            // Only retry on network errors
            if ((e === null || e === void 0 ? void 0 : e.code) ===
                `auth/$***REMOVED***"network-request-failed" /* AuthErrorCode.NETWORK_REQUEST_FAILED */}`) ***REMOVED***
                this.schedule(/* wasError */ true);
          ***REMOVED***
            return;
      ***REMOVED***
        this.schedule();
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class UserMetadata ***REMOVED***
    constructor(createdAt, lastLoginAt) ***REMOVED***
        this.createdAt = createdAt;
        this.lastLoginAt = lastLoginAt;
        this._initializeTime();
  ***REMOVED***
    _initializeTime() ***REMOVED***
        this.lastSignInTime = utcTimestampToDateString(this.lastLoginAt);
        this.creationTime = utcTimestampToDateString(this.createdAt);
  ***REMOVED***
    _copy(metadata) ***REMOVED***
        this.createdAt = metadata.createdAt;
        this.lastLoginAt = metadata.lastLoginAt;
        this._initializeTime();
  ***REMOVED***
    toJSON() ***REMOVED***
        return ***REMOVED***
            createdAt: this.createdAt,
            lastLoginAt: this.lastLoginAt
      ***REMOVED***;
  ***REMOVED***
}

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function _reloadWithoutSaving(user) ***REMOVED***
    var _a;
    const auth = user.auth;
    const idToken = await user.getIdToken();
    const response = await _logoutIfInvalidated(user, getAccountInfo(auth, ***REMOVED*** idToken }));
    _assert(response === null || response === void 0 ? void 0 : response.users.length, auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
    const coreAccount = response.users[0];
    user._notifyReloadListener(coreAccount);
    const newProviderData = ((_a = coreAccount.providerUserInfo) === null || _a === void 0 ? void 0 : _a.length)
        ? extractProviderData(coreAccount.providerUserInfo)
        : [];
    const providerData = mergeProviderData(user.providerData, newProviderData);
    // Preserves the non-nonymous status of the stored user, even if no more
    // credentials (federated or email/password) are linked to the user. If
    // the user was previously anonymous, then use provider data to update.
    // On the other hand, if it was not anonymous before, it should never be
    // considered anonymous now.
    const oldIsAnonymous = user.isAnonymous;
    const newIsAnonymous = !(user.email && coreAccount.passwordHash) && !(providerData === null || providerData === void 0 ? void 0 : providerData.length);
    const isAnonymous = !oldIsAnonymous ? false : newIsAnonymous;
    const updates = ***REMOVED***
        uid: coreAccount.localId,
        displayName: coreAccount.displayName || null,
        photoURL: coreAccount.photoUrl || null,
        email: coreAccount.email || null,
        emailVerified: coreAccount.emailVerified || false,
        phoneNumber: coreAccount.phoneNumber || null,
        tenantId: coreAccount.tenantId || null,
        providerData,
        metadata: new UserMetadata(coreAccount.createdAt, coreAccount.lastLoginAt),
        isAnonymous
  ***REMOVED***;
    Object.assign(user, updates);
}
/**
 * Reloads user account data, if signed in.
 *
 * @param user - The user.
 *
 * @public
 */
async function reload(user) ***REMOVED***
    const userInternal = getModularInstance(user);
    await _reloadWithoutSaving(userInternal);
    // Even though the current user hasn't changed, update
    // current user will trigger a persistence update w/ the
    // new info.
    await userInternal.auth._persistUserIfCurrent(userInternal);
    userInternal.auth._notifyListenersIfCurrent(userInternal);
}
function mergeProviderData(original, newData) ***REMOVED***
    const deduped = original.filter(o => !newData.some(n => n.providerId === o.providerId));
    return [...deduped, ...newData];
}
function extractProviderData(providers) ***REMOVED***
    return providers.map((_a) => ***REMOVED***
        var ***REMOVED*** providerId } = _a, provider = __rest(_a, ["providerId"]);
        return ***REMOVED***
            providerId,
            uid: provider.rawId || '',
            displayName: provider.displayName || null,
            email: provider.email || null,
            phoneNumber: provider.phoneNumber || null,
            photoURL: provider.photoUrl || null
      ***REMOVED***;
  ***REMOVED***);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function requestStsToken(auth, refreshToken) ***REMOVED***
    const response = await _performFetchWithErrorHandling(auth, ***REMOVED***}, async () => ***REMOVED***
        const body = querystring(***REMOVED***
            'grant_type': 'refresh_token',
            'refresh_token': refreshToken
      ***REMOVED***).slice(1);
        const ***REMOVED*** tokenApiHost, apiKey } = auth.config;
        const url = _getFinalTarget(auth, tokenApiHost, "/v1/token" /* Endpoint.TOKEN */, `key=$***REMOVED***apiKey}`);
        const headers = await auth._getAdditionalHeaders();
        headers["Content-Type" /* HttpHeader.CONTENT_TYPE */] = 'application/x-www-form-urlencoded';
        return FetchProvider.fetch()(url, ***REMOVED***
            method: "POST" /* HttpMethod.POST */,
            headers,
            body
      ***REMOVED***);
  ***REMOVED***);
    // The response comes back in snake_case. Convert to camel:
    return ***REMOVED***
        accessToken: response.access_token,
        expiresIn: response.expires_in,
        refreshToken: response.refresh_token
  ***REMOVED***;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * We need to mark this class as internal explicitly to exclude it in the public typings, because
 * it references AuthInternal which has a circular dependency with UserInternal.
 *
 * @internal
 */
class StsTokenManager ***REMOVED***
    constructor() ***REMOVED***
        this.refreshToken = null;
        this.accessToken = null;
        this.expirationTime = null;
  ***REMOVED***
    get isExpired() ***REMOVED***
        return (!this.expirationTime ||
            Date.now() > this.expirationTime - 30000 /* Buffer.TOKEN_REFRESH */);
  ***REMOVED***
    updateFromServerResponse(response) ***REMOVED***
        _assert(response.idToken, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        _assert(typeof response.idToken !== 'undefined', "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        _assert(typeof response.refreshToken !== 'undefined', "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        const expiresIn = 'expiresIn' in response && typeof response.expiresIn !== 'undefined'
            ? Number(response.expiresIn)
            : _tokenExpiresIn(response.idToken);
        this.updateTokensAndExpiration(response.idToken, response.refreshToken, expiresIn);
  ***REMOVED***
    async getToken(auth, forceRefresh = false) ***REMOVED***
        _assert(!this.accessToken || this.refreshToken, auth, "user-token-expired" /* AuthErrorCode.TOKEN_EXPIRED */);
        if (!forceRefresh && this.accessToken && !this.isExpired) ***REMOVED***
            return this.accessToken;
      ***REMOVED***
        if (this.refreshToken) ***REMOVED***
            await this.refresh(auth, this.refreshToken);
            return this.accessToken;
      ***REMOVED***
        return null;
  ***REMOVED***
    clearRefreshToken() ***REMOVED***
        this.refreshToken = null;
  ***REMOVED***
    async refresh(auth, oldToken) ***REMOVED***
        const ***REMOVED*** accessToken, refreshToken, expiresIn } = await requestStsToken(auth, oldToken);
        this.updateTokensAndExpiration(accessToken, refreshToken, Number(expiresIn));
  ***REMOVED***
    updateTokensAndExpiration(accessToken, refreshToken, expiresInSec) ***REMOVED***
        this.refreshToken = refreshToken || null;
        this.accessToken = accessToken || null;
        this.expirationTime = Date.now() + expiresInSec * 1000;
  ***REMOVED***
    static fromJSON(appName, object) ***REMOVED***
        const ***REMOVED*** refreshToken, accessToken, expirationTime } = object;
        const manager = new StsTokenManager();
        if (refreshToken) ***REMOVED***
            _assert(typeof refreshToken === 'string', "internal-error" /* AuthErrorCode.INTERNAL_ERROR */, ***REMOVED***
                appName
          ***REMOVED***);
            manager.refreshToken = refreshToken;
      ***REMOVED***
        if (accessToken) ***REMOVED***
            _assert(typeof accessToken === 'string', "internal-error" /* AuthErrorCode.INTERNAL_ERROR */, ***REMOVED***
                appName
          ***REMOVED***);
            manager.accessToken = accessToken;
      ***REMOVED***
        if (expirationTime) ***REMOVED***
            _assert(typeof expirationTime === 'number', "internal-error" /* AuthErrorCode.INTERNAL_ERROR */, ***REMOVED***
                appName
          ***REMOVED***);
            manager.expirationTime = expirationTime;
      ***REMOVED***
        return manager;
  ***REMOVED***
    toJSON() ***REMOVED***
        return ***REMOVED***
            refreshToken: this.refreshToken,
            accessToken: this.accessToken,
            expirationTime: this.expirationTime
      ***REMOVED***;
  ***REMOVED***
    _assign(stsTokenManager) ***REMOVED***
        this.accessToken = stsTokenManager.accessToken;
        this.refreshToken = stsTokenManager.refreshToken;
        this.expirationTime = stsTokenManager.expirationTime;
  ***REMOVED***
    _clone() ***REMOVED***
        return Object.assign(new StsTokenManager(), this.toJSON());
  ***REMOVED***
    _performRefresh() ***REMOVED***
        return debugFail('not implemented');
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function assertStringOrUndefined(assertion, appName) ***REMOVED***
    _assert(typeof assertion === 'string' || typeof assertion === 'undefined', "internal-error" /* AuthErrorCode.INTERNAL_ERROR */, ***REMOVED*** appName });
}
class UserImpl ***REMOVED***
    constructor(_a) ***REMOVED***
        var ***REMOVED*** uid, auth, stsTokenManager } = _a, opt = __rest(_a, ["uid", "auth", "stsTokenManager"]);
        // For the user object, provider is always Firebase.
        this.providerId = "firebase" /* ProviderId.FIREBASE */;
        this.proactiveRefresh = new ProactiveRefresh(this);
        this.reloadUserInfo = null;
        this.reloadListener = null;
        this.uid = uid;
        this.auth = auth;
        this.stsTokenManager = stsTokenManager;
        this.accessToken = stsTokenManager.accessToken;
        this.displayName = opt.displayName || null;
        this.email = opt.email || null;
        this.emailVerified = opt.emailVerified || false;
        this.phoneNumber = opt.phoneNumber || null;
        this.photoURL = opt.photoURL || null;
        this.isAnonymous = opt.isAnonymous || false;
        this.tenantId = opt.tenantId || null;
        this.providerData = opt.providerData ? [...opt.providerData] : [];
        this.metadata = new UserMetadata(opt.createdAt || undefined, opt.lastLoginAt || undefined);
  ***REMOVED***
    async getIdToken(forceRefresh) ***REMOVED***
        const accessToken = await _logoutIfInvalidated(this, this.stsTokenManager.getToken(this.auth, forceRefresh));
        _assert(accessToken, this.auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        if (this.accessToken !== accessToken) ***REMOVED***
            this.accessToken = accessToken;
            await this.auth._persistUserIfCurrent(this);
            this.auth._notifyListenersIfCurrent(this);
      ***REMOVED***
        return accessToken;
  ***REMOVED***
    getIdTokenResult(forceRefresh) ***REMOVED***
        return getIdTokenResult(this, forceRefresh);
  ***REMOVED***
    reload() ***REMOVED***
        return reload(this);
  ***REMOVED***
    _assign(user) ***REMOVED***
        if (this === user) ***REMOVED***
            return;
      ***REMOVED***
        _assert(this.uid === user.uid, this.auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        this.displayName = user.displayName;
        this.photoURL = user.photoURL;
        this.email = user.email;
        this.emailVerified = user.emailVerified;
        this.phoneNumber = user.phoneNumber;
        this.isAnonymous = user.isAnonymous;
        this.tenantId = user.tenantId;
        this.providerData = user.providerData.map(userInfo => (Object.assign(***REMOVED***}, userInfo)));
        this.metadata._copy(user.metadata);
        this.stsTokenManager._assign(user.stsTokenManager);
  ***REMOVED***
    _clone(auth) ***REMOVED***
        return new UserImpl(Object.assign(Object.assign(***REMOVED***}, this), ***REMOVED*** auth, stsTokenManager: this.stsTokenManager._clone() }));
  ***REMOVED***
    _onReload(callback) ***REMOVED***
        // There should only ever be one listener, and that is a single instance of MultiFactorUser
        _assert(!this.reloadListener, this.auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        this.reloadListener = callback;
        if (this.reloadUserInfo) ***REMOVED***
            this._notifyReloadListener(this.reloadUserInfo);
            this.reloadUserInfo = null;
      ***REMOVED***
  ***REMOVED***
    _notifyReloadListener(userInfo) ***REMOVED***
        if (this.reloadListener) ***REMOVED***
            this.reloadListener(userInfo);
      ***REMOVED***
        else ***REMOVED***
            // If no listener is subscribed yet, save the result so it's available when they do subscribe
            this.reloadUserInfo = userInfo;
      ***REMOVED***
  ***REMOVED***
    _startProactiveRefresh() ***REMOVED***
        this.proactiveRefresh._start();
  ***REMOVED***
    _stopProactiveRefresh() ***REMOVED***
        this.proactiveRefresh._stop();
  ***REMOVED***
    async _updateTokensIfNecessary(response, reload = false) ***REMOVED***
        let tokensRefreshed = false;
        if (response.idToken &&
            response.idToken !== this.stsTokenManager.accessToken) ***REMOVED***
            this.stsTokenManager.updateFromServerResponse(response);
            tokensRefreshed = true;
      ***REMOVED***
        if (reload) ***REMOVED***
            await _reloadWithoutSaving(this);
      ***REMOVED***
        await this.auth._persistUserIfCurrent(this);
        if (tokensRefreshed) ***REMOVED***
            this.auth._notifyListenersIfCurrent(this);
      ***REMOVED***
  ***REMOVED***
    async delete() ***REMOVED***
        const idToken = await this.getIdToken();
        await _logoutIfInvalidated(this, deleteAccount(this.auth, ***REMOVED*** idToken }));
        this.stsTokenManager.clearRefreshToken();
        // TODO: Determine if cancellable-promises are necessary to use in this class so that delete()
        //       cancels pending actions...
        return this.auth.signOut();
  ***REMOVED***
    toJSON() ***REMOVED***
        return Object.assign(Object.assign(***REMOVED*** uid: this.uid, email: this.email || undefined, emailVerified: this.emailVerified, displayName: this.displayName || undefined, isAnonymous: this.isAnonymous, photoURL: this.photoURL || undefined, phoneNumber: this.phoneNumber || undefined, tenantId: this.tenantId || undefined, providerData: this.providerData.map(userInfo => (Object.assign(***REMOVED***}, userInfo))), stsTokenManager: this.stsTokenManager.toJSON(), 
            // Redirect event ID must be maintained in case there is a pending
            // redirect event.
            _redirectEventId: this._redirectEventId }, this.metadata.toJSON()), ***REMOVED*** 
            // Required for compatibility with the legacy SDK (go/firebase-auth-sdk-persistence-parsing):
            apiKey: this.auth.config.apiKey, appName: this.auth.name });
  ***REMOVED***
    get refreshToken() ***REMOVED***
        return this.stsTokenManager.refreshToken || '';
  ***REMOVED***
    static _fromJSON(auth, object) ***REMOVED***
        var _a, _b, _c, _d, _e, _f, _g, _h;
        const displayName = (_a = object.displayName) !== null && _a !== void 0 ? _a : undefined;
        const email = (_b = object.email) !== null && _b !== void 0 ? _b : undefined;
        const phoneNumber = (_c = object.phoneNumber) !== null && _c !== void 0 ? _c : undefined;
        const photoURL = (_d = object.photoURL) !== null && _d !== void 0 ? _d : undefined;
        const tenantId = (_e = object.tenantId) !== null && _e !== void 0 ? _e : undefined;
        const _redirectEventId = (_f = object._redirectEventId) !== null && _f !== void 0 ? _f : undefined;
        const createdAt = (_g = object.createdAt) !== null && _g !== void 0 ? _g : undefined;
        const lastLoginAt = (_h = object.lastLoginAt) !== null && _h !== void 0 ? _h : undefined;
        const ***REMOVED*** uid, emailVerified, isAnonymous, providerData, stsTokenManager: plainObjectTokenManager } = object;
        _assert(uid && plainObjectTokenManager, auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        const stsTokenManager = StsTokenManager.fromJSON(this.name, plainObjectTokenManager);
        _assert(typeof uid === 'string', auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        assertStringOrUndefined(displayName, auth.name);
        assertStringOrUndefined(email, auth.name);
        _assert(typeof emailVerified === 'boolean', auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        _assert(typeof isAnonymous === 'boolean', auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        assertStringOrUndefined(phoneNumber, auth.name);
        assertStringOrUndefined(photoURL, auth.name);
        assertStringOrUndefined(tenantId, auth.name);
        assertStringOrUndefined(_redirectEventId, auth.name);
        assertStringOrUndefined(createdAt, auth.name);
        assertStringOrUndefined(lastLoginAt, auth.name);
        const user = new UserImpl(***REMOVED***
            uid,
            auth,
            email,
            emailVerified,
            displayName,
            isAnonymous,
            photoURL,
            phoneNumber,
            tenantId,
            stsTokenManager,
            createdAt,
            lastLoginAt
      ***REMOVED***);
        if (providerData && Array.isArray(providerData)) ***REMOVED***
            user.providerData = providerData.map(userInfo => (Object.assign(***REMOVED***}, userInfo)));
      ***REMOVED***
        if (_redirectEventId) ***REMOVED***
            user._redirectEventId = _redirectEventId;
      ***REMOVED***
        return user;
  ***REMOVED***
    /**
     * Initialize a User from an idToken server response
     * @param auth
     * @param idTokenResponse
     */
    static async _fromIdTokenResponse(auth, idTokenResponse, isAnonymous = false) ***REMOVED***
        const stsTokenManager = new StsTokenManager();
        stsTokenManager.updateFromServerResponse(idTokenResponse);
        // Initialize the Firebase Auth user.
        const user = new UserImpl(***REMOVED***
            uid: idTokenResponse.localId,
            auth,
            stsTokenManager,
            isAnonymous
      ***REMOVED***);
        // Updates the user info and data and resolves with a user instance.
        await _reloadWithoutSaving(user);
        return user;
  ***REMOVED***
}

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class InMemoryPersistence ***REMOVED***
    constructor() ***REMOVED***
        this.type = "NONE" /* PersistenceType.NONE */;
        this.storage = ***REMOVED***};
  ***REMOVED***
    async _isAvailable() ***REMOVED***
        return true;
  ***REMOVED***
    async _set(key, value) ***REMOVED***
        this.storage[key] = value;
  ***REMOVED***
    async _get(key) ***REMOVED***
        const value = this.storage[key];
        return value === undefined ? null : value;
  ***REMOVED***
    async _remove(key) ***REMOVED***
        delete this.storage[key];
  ***REMOVED***
    _addListener(_key, _listener) ***REMOVED***
        // Listeners are not supported for in-memory storage since it cannot be shared across windows/workers
        return;
  ***REMOVED***
    _removeListener(_key, _listener) ***REMOVED***
        // Listeners are not supported for in-memory storage since it cannot be shared across windows/workers
        return;
  ***REMOVED***
}
InMemoryPersistence.type = 'NONE';
/**
 * An implementation of ***REMOVED***@link Persistence} of type 'NONE'.
 *
 * @public
 */
const inMemoryPersistence = InMemoryPersistence;

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function _persistenceKeyName(key, apiKey, appName) ***REMOVED***
    return `$***REMOVED***"firebase" /* Namespace.PERSISTENCE */}:$***REMOVED***key}:$***REMOVED***apiKey}:$***REMOVED***appName}`;
}
class PersistenceUserManager ***REMOVED***
    constructor(persistence, auth, userKey) ***REMOVED***
        this.persistence = persistence;
        this.auth = auth;
        this.userKey = userKey;
        const ***REMOVED*** config, name } = this.auth;
        this.fullUserKey = _persistenceKeyName(this.userKey, config.apiKey, name);
        this.fullPersistenceKey = _persistenceKeyName("persistence" /* KeyName.PERSISTENCE_USER */, config.apiKey, name);
        this.boundEventHandler = auth._onStorageEvent.bind(auth);
        this.persistence._addListener(this.fullUserKey, this.boundEventHandler);
  ***REMOVED***
    setCurrentUser(user) ***REMOVED***
        return this.persistence._set(this.fullUserKey, user.toJSON());
  ***REMOVED***
    async getCurrentUser() ***REMOVED***
        const blob = await this.persistence._get(this.fullUserKey);
        return blob ? UserImpl._fromJSON(this.auth, blob) : null;
  ***REMOVED***
    removeCurrentUser() ***REMOVED***
        return this.persistence._remove(this.fullUserKey);
  ***REMOVED***
    savePersistenceForRedirect() ***REMOVED***
        return this.persistence._set(this.fullPersistenceKey, this.persistence.type);
  ***REMOVED***
    async setPersistence(newPersistence) ***REMOVED***
        if (this.persistence === newPersistence) ***REMOVED***
            return;
      ***REMOVED***
        const currentUser = await this.getCurrentUser();
        await this.removeCurrentUser();
        this.persistence = newPersistence;
        if (currentUser) ***REMOVED***
            return this.setCurrentUser(currentUser);
      ***REMOVED***
  ***REMOVED***
    delete() ***REMOVED***
        this.persistence._removeListener(this.fullUserKey, this.boundEventHandler);
  ***REMOVED***
    static async create(auth, persistenceHierarchy, userKey = "authUser" /* KeyName.AUTH_USER */) ***REMOVED***
        if (!persistenceHierarchy.length) ***REMOVED***
            return new PersistenceUserManager(_getInstance(inMemoryPersistence), auth, userKey);
      ***REMOVED***
        // Eliminate any persistences that are not available
        const availablePersistences = (await Promise.all(persistenceHierarchy.map(async (persistence) => ***REMOVED***
            if (await persistence._isAvailable()) ***REMOVED***
                return persistence;
          ***REMOVED***
            return undefined;
      ***REMOVED***))).filter(persistence => persistence);
        // Fall back to the first persistence listed, or in memory if none available
        let selectedPersistence = availablePersistences[0] ||
            _getInstance(inMemoryPersistence);
        const key = _persistenceKeyName(userKey, auth.config.apiKey, auth.name);
        // Pull out the existing user, setting the chosen persistence to that
        // persistence if the user exists.
        let userToMigrate = null;
        // Note, here we check for a user in _all_ persistences, not just the
        // ones deemed available. If we can migrate a user out of a broken
        // persistence, we will (but only if that persistence supports migration).
        for (const persistence of persistenceHierarchy) ***REMOVED***
            try ***REMOVED***
                const blob = await persistence._get(key);
                if (blob) ***REMOVED***
                    const user = UserImpl._fromJSON(auth, blob); // throws for unparsable blob (wrong format)
                    if (persistence !== selectedPersistence) ***REMOVED***
                        userToMigrate = user;
                  ***REMOVED***
                    selectedPersistence = persistence;
                    break;
              ***REMOVED***
          ***REMOVED***
            catch (_a) ***REMOVED*** }
      ***REMOVED***
        // If we find the user in a persistence that does support migration, use
        // that migration path (of only persistences that support migration)
        const migrationHierarchy = availablePersistences.filter(p => p._shouldAllowMigration);
        // If the persistence does _not_ allow migration, just finish off here
        if (!selectedPersistence._shouldAllowMigration ||
            !migrationHierarchy.length) ***REMOVED***
            return new PersistenceUserManager(selectedPersistence, auth, userKey);
      ***REMOVED***
        selectedPersistence = migrationHierarchy[0];
        if (userToMigrate) ***REMOVED***
            // This normally shouldn't throw since chosenPersistence.isAvailable() is true, but if it does
            // we'll just let it bubble to surface the error.
            await selectedPersistence._set(key, userToMigrate.toJSON());
      ***REMOVED***
        // Attempt to clear the key in other persistences but ignore errors. This helps prevent issues
        // such as users getting stuck with a previous account after signing out and refreshing the tab.
        await Promise.all(persistenceHierarchy.map(async (persistence) => ***REMOVED***
            if (persistence !== selectedPersistence) ***REMOVED***
                try ***REMOVED***
                    await persistence._remove(key);
              ***REMOVED***
                catch (_a) ***REMOVED*** }
          ***REMOVED***
      ***REMOVED***));
        return new PersistenceUserManager(selectedPersistence, auth, userKey);
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Determine the browser for the purposes of reporting usage to the API
 */
function _getBrowserName(userAgent) ***REMOVED***
    const ua = userAgent.toLowerCase();
    if (ua.includes('opera/') || ua.includes('opr/') || ua.includes('opios/')) ***REMOVED***
        return "Opera" /* BrowserName.OPERA */;
  ***REMOVED***
    else if (_isIEMobile(ua)) ***REMOVED***
        // Windows phone IEMobile browser.
        return "IEMobile" /* BrowserName.IEMOBILE */;
  ***REMOVED***
    else if (ua.includes('msie') || ua.includes('trident/')) ***REMOVED***
        return "IE" /* BrowserName.IE */;
  ***REMOVED***
    else if (ua.includes('edge/')) ***REMOVED***
        return "Edge" /* BrowserName.EDGE */;
  ***REMOVED***
    else if (_isFirefox(ua)) ***REMOVED***
        return "Firefox" /* BrowserName.FIREFOX */;
  ***REMOVED***
    else if (ua.includes('silk/')) ***REMOVED***
        return "Silk" /* BrowserName.SILK */;
  ***REMOVED***
    else if (_isBlackBerry(ua)) ***REMOVED***
        // Blackberry browser.
        return "Blackberry" /* BrowserName.BLACKBERRY */;
  ***REMOVED***
    else if (_isWebOS(ua)) ***REMOVED***
        // WebOS default browser.
        return "Webos" /* BrowserName.WEBOS */;
  ***REMOVED***
    else if (_isSafari(ua)) ***REMOVED***
        return "Safari" /* BrowserName.SAFARI */;
  ***REMOVED***
    else if ((ua.includes('chrome/') || _isChromeIOS(ua)) &&
        !ua.includes('edge/')) ***REMOVED***
        return "Chrome" /* BrowserName.CHROME */;
  ***REMOVED***
    else if (_isAndroid(ua)) ***REMOVED***
        // Android stock browser.
        return "Android" /* BrowserName.ANDROID */;
  ***REMOVED***
    else ***REMOVED***
        // Most modern browsers have name/version at end of user agent string.
        const re = /([a-zA-Z\d\.]+)\/[a-zA-Z\d\.]*$/;
        const matches = userAgent.match(re);
        if ((matches === null || matches === void 0 ? void 0 : matches.length) === 2) ***REMOVED***
            return matches[1];
      ***REMOVED***
  ***REMOVED***
    return "Other" /* BrowserName.OTHER */;
}
function _isFirefox(ua = getUA()) ***REMOVED***
    return /firefox\//i.test(ua);
}
function _isSafari(userAgent = getUA()) ***REMOVED***
    const ua = userAgent.toLowerCase();
    return (ua.includes('safari/') &&
        !ua.includes('chrome/') &&
        !ua.includes('crios/') &&
        !ua.includes('android'));
}
function _isChromeIOS(ua = getUA()) ***REMOVED***
    return /crios\//i.test(ua);
}
function _isIEMobile(ua = getUA()) ***REMOVED***
    return /iemobile/i.test(ua);
}
function _isAndroid(ua = getUA()) ***REMOVED***
    return /android/i.test(ua);
}
function _isBlackBerry(ua = getUA()) ***REMOVED***
    return /blackberry/i.test(ua);
}
function _isWebOS(ua = getUA()) ***REMOVED***
    return /webos/i.test(ua);
}
function _isIOS(ua = getUA()) ***REMOVED***
    return (/iphone|ipad|ipod/i.test(ua) ||
        (/macintosh/i.test(ua) && /mobile/i.test(ua)));
}
function _isIOS7Or8(ua = getUA()) ***REMOVED***
    return (/(iPad|iPhone|iPod).*OS 7_\d/i.test(ua) ||
        /(iPad|iPhone|iPod).*OS 8_\d/i.test(ua));
}
function _isIOSStandalone(ua = getUA()) ***REMOVED***
    var _a;
    return _isIOS(ua) && !!((_a = window.navigator) === null || _a === void 0 ? void 0 : _a.standalone);
}
function _isIE10() ***REMOVED***
    return isIE() && document.documentMode === 10;
}
function _isMobileBrowser(ua = getUA()) ***REMOVED***
    // TODO: implement getBrowserName equivalent for OS.
    return (_isIOS(ua) ||
        _isAndroid(ua) ||
        _isWebOS(ua) ||
        _isBlackBerry(ua) ||
        /windows phone/i.test(ua) ||
        _isIEMobile(ua));
}
function _isIframe() ***REMOVED***
    try ***REMOVED***
        // Check that the current window is not the top window.
        // If so, return true.
        return !!(window && window !== window.top);
  ***REMOVED***
    catch (e) ***REMOVED***
        return false;
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/*
 * Determine the SDK version string
 */
function _getClientVersion(clientPlatform, frameworks = []) ***REMOVED***
    let reportedPlatform;
    switch (clientPlatform) ***REMOVED***
        case "Browser" /* ClientPlatform.BROWSER */:
            // In a browser environment, report the browser name.
            reportedPlatform = _getBrowserName(getUA());
            break;
        case "Worker" /* ClientPlatform.WORKER */:
            // Technically a worker runs from a browser but we need to differentiate a
            // worker from a browser.
            // For example: Chrome-Worker/JsCore/4.9.1/FirebaseCore-web.
            reportedPlatform = `$***REMOVED***_getBrowserName(getUA())}-$***REMOVED***clientPlatform}`;
            break;
        default:
            reportedPlatform = clientPlatform;
  ***REMOVED***
    const reportedFrameworks = frameworks.length
        ? frameworks.join(',')
        : 'FirebaseCore-web'; /* default value if no other framework is used */
    return `$***REMOVED***reportedPlatform}/$***REMOVED***"JsCore" /* ClientImplementation.CORE */}/$***REMOVED***SDK_VERSION}/$***REMOVED***reportedFrameworks}`;
}

/**
 * @license
 * Copyright 2022 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class AuthMiddlewareQueue ***REMOVED***
    constructor(auth) ***REMOVED***
        this.auth = auth;
        this.queue = [];
  ***REMOVED***
    pushCallback(callback, onAbort) ***REMOVED***
        // The callback could be sync or async. Wrap it into a
        // function that is always async.
        const wrappedCallback = (user) => new Promise((resolve, reject) => ***REMOVED***
            try ***REMOVED***
                const result = callback(user);
                // Either resolve with existing promise or wrap a non-promise
                // return value into a promise.
                resolve(result);
          ***REMOVED***
            catch (e) ***REMOVED***
                // Sync callback throws.
                reject(e);
          ***REMOVED***
      ***REMOVED***);
        // Attach the onAbort if present
        wrappedCallback.onAbort = onAbort;
        this.queue.push(wrappedCallback);
        const index = this.queue.length - 1;
        return () => ***REMOVED***
            // Unsubscribe. Replace with no-op. Do not remove from array, or it will disturb
            // indexing of other elements.
            this.queue[index] = () => Promise.resolve();
      ***REMOVED***;
  ***REMOVED***
    async runMiddleware(nextUser) ***REMOVED***
        if (this.auth.currentUser === nextUser) ***REMOVED***
            return;
      ***REMOVED***
        // While running the middleware, build a temporary stack of onAbort
        // callbacks to call if one middleware callback rejects.
        const onAbortStack = [];
        try ***REMOVED***
            for (const beforeStateCallback of this.queue) ***REMOVED***
                await beforeStateCallback(nextUser);
                // Only push the onAbort if the callback succeeds
                if (beforeStateCallback.onAbort) ***REMOVED***
                    onAbortStack.push(beforeStateCallback.onAbort);
              ***REMOVED***
          ***REMOVED***
      ***REMOVED***
        catch (e) ***REMOVED***
            // Run all onAbort, with separate try/catch to ignore any errors and
            // continue
            onAbortStack.reverse();
            for (const onAbort of onAbortStack) ***REMOVED***
                try ***REMOVED***
                    onAbort();
              ***REMOVED***
                catch (_) ***REMOVED***
                    /* swallow error */
              ***REMOVED***
          ***REMOVED***
            throw this.auth._errorFactory.create("login-blocked" /* AuthErrorCode.LOGIN_BLOCKED */, ***REMOVED***
                originalMessage: e === null || e === void 0 ? void 0 : e.message
          ***REMOVED***);
      ***REMOVED***
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class AuthImpl ***REMOVED***
    constructor(app, heartbeatServiceProvider, config) ***REMOVED***
        this.app = app;
        this.heartbeatServiceProvider = heartbeatServiceProvider;
        this.config = config;
        this.currentUser = null;
        this.emulatorConfig = null;
        this.operations = Promise.resolve();
        this.authStateSubscription = new Subscription(this);
        this.idTokenSubscription = new Subscription(this);
        this.beforeStateQueue = new AuthMiddlewareQueue(this);
        this.redirectUser = null;
        this.isProactiveRefreshEnabled = false;
        // Any network calls will set this to true and prevent subsequent emulator
        // initialization
        this._canInitEmulator = true;
        this._isInitialized = false;
        this._deleted = false;
        this._initializationPromise = null;
        this._popupRedirectResolver = null;
        this._errorFactory = _DEFAULT_AUTH_ERROR_FACTORY;
        // Tracks the last notified UID for state change listeners to prevent
        // repeated calls to the callbacks. Undefined means it's never been
        // called, whereas null means it's been called with a signed out user
        this.lastNotifiedUid = undefined;
        this.languageCode = null;
        this.tenantId = null;
        this.settings = ***REMOVED*** appVerificationDisabledForTesting: false };
        this.frameworks = [];
        this.name = app.name;
        this.clientVersion = config.sdkClientVersion;
  ***REMOVED***
    _initializeWithPersistence(persistenceHierarchy, popupRedirectResolver) ***REMOVED***
        if (popupRedirectResolver) ***REMOVED***
            this._popupRedirectResolver = _getInstance(popupRedirectResolver);
      ***REMOVED***
        // Have to check for app deletion throughout initialization (after each
        // promise resolution)
        this._initializationPromise = this.queue(async () => ***REMOVED***
            var _a, _b;
            if (this._deleted) ***REMOVED***
                return;
          ***REMOVED***
            this.persistenceManager = await PersistenceUserManager.create(this, persistenceHierarchy);
            if (this._deleted) ***REMOVED***
                return;
          ***REMOVED***
            // Initialize the resolver early if necessary (only applicable to web:
            // this will cause the iframe to load immediately in certain cases)
            if ((_a = this._popupRedirectResolver) === null || _a === void 0 ? void 0 : _a._shouldInitProactively) ***REMOVED***
                // If this fails, don't halt auth loading
                try ***REMOVED***
                    await this._popupRedirectResolver._initialize(this);
              ***REMOVED***
                catch (e) ***REMOVED***
                    /* Ignore the error */
              ***REMOVED***
          ***REMOVED***
            await this.initializeCurrentUser(popupRedirectResolver);
            this.lastNotifiedUid = ((_b = this.currentUser) === null || _b === void 0 ? void 0 : _b.uid) || null;
            if (this._deleted) ***REMOVED***
                return;
          ***REMOVED***
            this._isInitialized = true;
      ***REMOVED***);
        return this._initializationPromise;
  ***REMOVED***
    /**
     * If the persistence is changed in another window, the user manager will let us know
     */
    async _onStorageEvent() ***REMOVED***
        if (this._deleted) ***REMOVED***
            return;
      ***REMOVED***
        const user = await this.assertedPersistence.getCurrentUser();
        if (!this.currentUser && !user) ***REMOVED***
            // No change, do nothing (was signed out and remained signed out).
            return;
      ***REMOVED***
        // If the same user is to be synchronized.
        if (this.currentUser && user && this.currentUser.uid === user.uid) ***REMOVED***
            // Data update, simply copy data changes.
            this._currentUser._assign(user);
            // If tokens changed from previous user tokens, this will trigger
            // notifyAuthListeners_.
            await this.currentUser.getIdToken();
            return;
      ***REMOVED***
        // Update current Auth state. Either a new login or logout.
        // Skip blocking callbacks, they should not apply to a change in another tab.
        await this._updateCurrentUser(user, /* skipBeforeStateCallbacks */ true);
  ***REMOVED***
    async initializeCurrentUser(popupRedirectResolver) ***REMOVED***
        var _a;
        // First check to see if we have a pending redirect event.
        const previouslyStoredUser = (await this.assertedPersistence.getCurrentUser());
        let futureCurrentUser = previouslyStoredUser;
        let needsTocheckMiddleware = false;
        if (popupRedirectResolver && this.config.authDomain) ***REMOVED***
            await this.getOrInitRedirectPersistenceManager();
            const redirectUserEventId = (_a = this.redirectUser) === null || _a === void 0 ? void 0 : _a._redirectEventId;
            const storedUserEventId = futureCurrentUser === null || futureCurrentUser === void 0 ? void 0 : futureCurrentUser._redirectEventId;
            const result = await this.tryRedirectSignIn(popupRedirectResolver);
            // If the stored user (i.e. the old "currentUser") has a redirectId that
            // matches the redirect user, then we want to initially sign in with the
            // new user object from result.
            // TODO(samgho): More thoroughly test all of this
            if ((!redirectUserEventId || redirectUserEventId === storedUserEventId) &&
                (result === null || result === void 0 ? void 0 : result.user)) ***REMOVED***
                futureCurrentUser = result.user;
                needsTocheckMiddleware = true;
          ***REMOVED***
      ***REMOVED***
        // If no user in persistence, there is no current user. Set to null.
        if (!futureCurrentUser) ***REMOVED***
            return this.directlySetCurrentUser(null);
      ***REMOVED***
        if (!futureCurrentUser._redirectEventId) ***REMOVED***
            // This isn't a redirect link operation, we can reload and bail.
            // First though, ensure that we check the middleware is happy.
            if (needsTocheckMiddleware) ***REMOVED***
                try ***REMOVED***
                    await this.beforeStateQueue.runMiddleware(futureCurrentUser);
              ***REMOVED***
                catch (e) ***REMOVED***
                    futureCurrentUser = previouslyStoredUser;
                    // We know this is available since the bit is only set when the
                    // resolver is available
                    this._popupRedirectResolver._overrideRedirectResult(this, () => Promise.reject(e));
              ***REMOVED***
          ***REMOVED***
            if (futureCurrentUser) ***REMOVED***
                return this.reloadAndSetCurrentUserOrClear(futureCurrentUser);
          ***REMOVED***
            else ***REMOVED***
                return this.directlySetCurrentUser(null);
          ***REMOVED***
      ***REMOVED***
        _assert(this._popupRedirectResolver, this, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
        await this.getOrInitRedirectPersistenceManager();
        // If the redirect user's event ID matches the current user's event ID,
        // DO NOT reload the current user, otherwise they'll be cleared from storage.
        // This is important for the reauthenticateWithRedirect() flow.
        if (this.redirectUser &&
            this.redirectUser._redirectEventId === futureCurrentUser._redirectEventId) ***REMOVED***
            return this.directlySetCurrentUser(futureCurrentUser);
      ***REMOVED***
        return this.reloadAndSetCurrentUserOrClear(futureCurrentUser);
  ***REMOVED***
    async tryRedirectSignIn(redirectResolver) ***REMOVED***
        // The redirect user needs to be checked (and signed in if available)
        // during auth initialization. All of the normal sign in and link/reauth
        // flows call back into auth and push things onto the promise queue. We
        // need to await the result of the redirect sign in *inside the promise
        // queue*. This presents a problem: we run into deadlock. See:
        //    ┌> [Initialization] ─────┐
        //    ┌> [<other queue tasks>] │
        //    └─ [getRedirectResult] <─┘
        //    where [] are tasks on the queue and arrows denote awaits
        // Initialization will never complete because it's waiting on something
        // that's waiting for initialization to complete!
        //
        // Instead, this method calls getRedirectResult() (stored in
        // _completeRedirectFn) with an optional parameter that instructs all of
        // the underlying auth operations to skip anything that mutates auth state.
        let result = null;
        try ***REMOVED***
            // We know this._popupRedirectResolver is set since redirectResolver
            // is passed in. The _completeRedirectFn expects the unwrapped extern.
            result = await this._popupRedirectResolver._completeRedirectFn(this, redirectResolver, true);
      ***REMOVED***
        catch (e) ***REMOVED***
            // Swallow any errors here; the code can retrieve them in
            // getRedirectResult().
            await this._setRedirectUser(null);
      ***REMOVED***
        return result;
  ***REMOVED***
    async reloadAndSetCurrentUserOrClear(user) ***REMOVED***
        try ***REMOVED***
            await _reloadWithoutSaving(user);
      ***REMOVED***
        catch (e) ***REMOVED***
            if ((e === null || e === void 0 ? void 0 : e.code) !==
                `auth/$***REMOVED***"network-request-failed" /* AuthErrorCode.NETWORK_REQUEST_FAILED */}`) ***REMOVED***
                // Something's wrong with the user's token. Log them out and remove
                // them from storage
                return this.directlySetCurrentUser(null);
          ***REMOVED***
      ***REMOVED***
        return this.directlySetCurrentUser(user);
  ***REMOVED***
    useDeviceLanguage() ***REMOVED***
        this.languageCode = _getUserLanguage();
  ***REMOVED***
    async _delete() ***REMOVED***
        this._deleted = true;
  ***REMOVED***
    async updateCurrentUser(userExtern) ***REMOVED***
        // The public updateCurrentUser method needs to make a copy of the user,
        // and also check that the project matches
        const user = userExtern
            ? getModularInstance(userExtern)
            : null;
        if (user) ***REMOVED***
            _assert(user.auth.config.apiKey === this.config.apiKey, this, "invalid-user-token" /* AuthErrorCode.INVALID_AUTH */);
      ***REMOVED***
        return this._updateCurrentUser(user && user._clone(this));
  ***REMOVED***
    async _updateCurrentUser(user, skipBeforeStateCallbacks = false) ***REMOVED***
        if (this._deleted) ***REMOVED***
            return;
      ***REMOVED***
        if (user) ***REMOVED***
            _assert(this.tenantId === user.tenantId, this, "tenant-id-mismatch" /* AuthErrorCode.TENANT_ID_MISMATCH */);
      ***REMOVED***
        if (!skipBeforeStateCallbacks) ***REMOVED***
            await this.beforeStateQueue.runMiddleware(user);
      ***REMOVED***
        return this.queue(async () => ***REMOVED***
            await this.directlySetCurrentUser(user);
            this.notifyAuthListeners();
      ***REMOVED***);
  ***REMOVED***
    async signOut() ***REMOVED***
        // Run first, to block _setRedirectUser() if any callbacks fail.
        await this.beforeStateQueue.runMiddleware(null);
        // Clear the redirect user when signOut is called
        if (this.redirectPersistenceManager || this._popupRedirectResolver) ***REMOVED***
            await this._setRedirectUser(null);
      ***REMOVED***
        // Prevent callbacks from being called again in _updateCurrentUser, as
        // they were already called in the first line.
        return this._updateCurrentUser(null, /* skipBeforeStateCallbacks */ true);
  ***REMOVED***
    setPersistence(persistence) ***REMOVED***
        return this.queue(async () => ***REMOVED***
            await this.assertedPersistence.setPersistence(_getInstance(persistence));
      ***REMOVED***);
  ***REMOVED***
    _getPersistence() ***REMOVED***
        return this.assertedPersistence.persistence.type;
  ***REMOVED***
    _updateErrorMap(errorMap) ***REMOVED***
        this._errorFactory = new ErrorFactory('auth', 'Firebase', errorMap());
  ***REMOVED***
    onAuthStateChanged(nextOrObserver, error, completed) ***REMOVED***
        return this.registerStateListener(this.authStateSubscription, nextOrObserver, error, completed);
  ***REMOVED***
    beforeAuthStateChanged(callback, onAbort) ***REMOVED***
        return this.beforeStateQueue.pushCallback(callback, onAbort);
  ***REMOVED***
    onIdTokenChanged(nextOrObserver, error, completed) ***REMOVED***
        return this.registerStateListener(this.idTokenSubscription, nextOrObserver, error, completed);
  ***REMOVED***
    toJSON() ***REMOVED***
        var _a;
        return ***REMOVED***
            apiKey: this.config.apiKey,
            authDomain: this.config.authDomain,
            appName: this.name,
            currentUser: (_a = this._currentUser) === null || _a === void 0 ? void 0 : _a.toJSON()
      ***REMOVED***;
  ***REMOVED***
    async _setRedirectUser(user, popupRedirectResolver) ***REMOVED***
        const redirectManager = await this.getOrInitRedirectPersistenceManager(popupRedirectResolver);
        return user === null
            ? redirectManager.removeCurrentUser()
            : redirectManager.setCurrentUser(user);
  ***REMOVED***
    async getOrInitRedirectPersistenceManager(popupRedirectResolver) ***REMOVED***
        if (!this.redirectPersistenceManager) ***REMOVED***
            const resolver = (popupRedirectResolver && _getInstance(popupRedirectResolver)) ||
                this._popupRedirectResolver;
            _assert(resolver, this, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
            this.redirectPersistenceManager = await PersistenceUserManager.create(this, [_getInstance(resolver._redirectPersistence)], "redirectUser" /* KeyName.REDIRECT_USER */);
            this.redirectUser =
                await this.redirectPersistenceManager.getCurrentUser();
      ***REMOVED***
        return this.redirectPersistenceManager;
  ***REMOVED***
    async _redirectUserForId(id) ***REMOVED***
        var _a, _b;
        // Make sure we've cleared any pending persistence actions if we're not in
        // the initializer
        if (this._isInitialized) ***REMOVED***
            await this.queue(async () => ***REMOVED*** });
      ***REMOVED***
        if (((_a = this._currentUser) === null || _a === void 0 ? void 0 : _a._redirectEventId) === id) ***REMOVED***
            return this._currentUser;
      ***REMOVED***
        if (((_b = this.redirectUser) === null || _b === void 0 ? void 0 : _b._redirectEventId) === id) ***REMOVED***
            return this.redirectUser;
      ***REMOVED***
        return null;
  ***REMOVED***
    async _persistUserIfCurrent(user) ***REMOVED***
        if (user === this.currentUser) ***REMOVED***
            return this.queue(async () => this.directlySetCurrentUser(user));
      ***REMOVED***
  ***REMOVED***
    /** Notifies listeners only if the user is current */
    _notifyListenersIfCurrent(user) ***REMOVED***
        if (user === this.currentUser) ***REMOVED***
            this.notifyAuthListeners();
      ***REMOVED***
  ***REMOVED***
    _key() ***REMOVED***
        return `$***REMOVED***this.config.authDomain}:$***REMOVED***this.config.apiKey}:$***REMOVED***this.name}`;
  ***REMOVED***
    _startProactiveRefresh() ***REMOVED***
        this.isProactiveRefreshEnabled = true;
        if (this.currentUser) ***REMOVED***
            this._currentUser._startProactiveRefresh();
      ***REMOVED***
  ***REMOVED***
    _stopProactiveRefresh() ***REMOVED***
        this.isProactiveRefreshEnabled = false;
        if (this.currentUser) ***REMOVED***
            this._currentUser._stopProactiveRefresh();
      ***REMOVED***
  ***REMOVED***
    /** Returns the current user cast as the internal type */
    get _currentUser() ***REMOVED***
        return this.currentUser;
  ***REMOVED***
    notifyAuthListeners() ***REMOVED***
        var _a, _b;
        if (!this._isInitialized) ***REMOVED***
            return;
      ***REMOVED***
        this.idTokenSubscription.next(this.currentUser);
        const currentUid = (_b = (_a = this.currentUser) === null || _a === void 0 ? void 0 : _a.uid) !== null && _b !== void 0 ? _b : null;
        if (this.lastNotifiedUid !== currentUid) ***REMOVED***
            this.lastNotifiedUid = currentUid;
            this.authStateSubscription.next(this.currentUser);
      ***REMOVED***
  ***REMOVED***
    registerStateListener(subscription, nextOrObserver, error, completed) ***REMOVED***
        if (this._deleted) ***REMOVED***
            return () => ***REMOVED*** };
      ***REMOVED***
        const cb = typeof nextOrObserver === 'function'
            ? nextOrObserver
            : nextOrObserver.next.bind(nextOrObserver);
        const promise = this._isInitialized
            ? Promise.resolve()
            : this._initializationPromise;
        _assert(promise, this, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        // The callback needs to be called asynchronously per the spec.
        // eslint-disable-next-line @typescript-eslint/no-floating-promises
        promise.then(() => cb(this.currentUser));
        if (typeof nextOrObserver === 'function') ***REMOVED***
            return subscription.addObserver(nextOrObserver, error, completed);
      ***REMOVED***
        else ***REMOVED***
            return subscription.addObserver(nextOrObserver);
      ***REMOVED***
  ***REMOVED***
    /**
     * Unprotected (from race conditions) method to set the current user. This
     * should only be called from within a queued callback. This is necessary
     * because the queue shouldn't rely on another queued callback.
     */
    async directlySetCurrentUser(user) ***REMOVED***
        if (this.currentUser && this.currentUser !== user) ***REMOVED***
            this._currentUser._stopProactiveRefresh();
      ***REMOVED***
        if (user && this.isProactiveRefreshEnabled) ***REMOVED***
            user._startProactiveRefresh();
      ***REMOVED***
        this.currentUser = user;
        if (user) ***REMOVED***
            await this.assertedPersistence.setCurrentUser(user);
      ***REMOVED***
        else ***REMOVED***
            await this.assertedPersistence.removeCurrentUser();
      ***REMOVED***
  ***REMOVED***
    queue(action) ***REMOVED***
        // In case something errors, the callback still should be called in order
        // to keep the promise chain alive
        this.operations = this.operations.then(action, action);
        return this.operations;
  ***REMOVED***
    get assertedPersistence() ***REMOVED***
        _assert(this.persistenceManager, this, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        return this.persistenceManager;
  ***REMOVED***
    _logFramework(framework) ***REMOVED***
        if (!framework || this.frameworks.includes(framework)) ***REMOVED***
            return;
      ***REMOVED***
        this.frameworks.push(framework);
        // Sort alphabetically so that "FirebaseCore-web,FirebaseUI-web" and
        // "FirebaseUI-web,FirebaseCore-web" aren't viewed as different.
        this.frameworks.sort();
        this.clientVersion = _getClientVersion(this.config.clientPlatform, this._getFrameworks());
  ***REMOVED***
    _getFrameworks() ***REMOVED***
        return this.frameworks;
  ***REMOVED***
    async _getAdditionalHeaders() ***REMOVED***
        var _a;
        // Additional headers on every request
        const headers = ***REMOVED***
            ["X-Client-Version" /* HttpHeader.X_CLIENT_VERSION */]: this.clientVersion
      ***REMOVED***;
        if (this.app.options.appId) ***REMOVED***
            headers["X-Firebase-gmpid" /* HttpHeader.X_FIREBASE_GMPID */] = this.app.options.appId;
      ***REMOVED***
        // If the heartbeat service exists, add the heartbeat string
        const heartbeatsHeader = await ((_a = this.heartbeatServiceProvider
            .getImmediate(***REMOVED***
            optional: true
      ***REMOVED***)) === null || _a === void 0 ? void 0 : _a.getHeartbeatsHeader());
        if (heartbeatsHeader) ***REMOVED***
            headers["X-Firebase-Client" /* HttpHeader.X_FIREBASE_CLIENT */] = heartbeatsHeader;
      ***REMOVED***
        return headers;
  ***REMOVED***
}
/**
 * Method to be used to cast down to our private implmentation of Auth.
 * It will also handle unwrapping from the compat type if necessary
 *
 * @param auth Auth object passed in from developer
 */
function _castAuth(auth) ***REMOVED***
    return getModularInstance(auth);
}
/** Helper class to wrap subscriber logic */
class Subscription ***REMOVED***
    constructor(auth) ***REMOVED***
        this.auth = auth;
        this.observer = null;
        this.addObserver = createSubscribe(observer => (this.observer = observer));
  ***REMOVED***
    get next() ***REMOVED***
        _assert(this.observer, this.auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        return this.observer.next.bind(this.observer);
  ***REMOVED***
}

/**
 * Changes the ***REMOVED***@link Auth} instance to communicate with the Firebase Auth Emulator, instead of production
 * Firebase Auth services.
 *
 * @remarks
 * This must be called synchronously immediately following the first call to
 * ***REMOVED***@link initializeAuth}.  Do not use with production credentials as emulator
 * traffic is not encrypted.
 *
 *
 * @example
 * ```javascript
 * connectAuthEmulator(auth, 'http://127.0.0.1:9099', ***REMOVED*** disableWarnings: true });
 * ```
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param url - The URL at which the emulator is running (eg, 'http://localhost:9099').
 * @param options - Optional. `options.disableWarnings` defaults to `false`. Set it to
 * `true` to disable the warning banner attached to the DOM.
 *
 * @public
 */
function connectAuthEmulator(auth, url, options) ***REMOVED***
    const authInternal = _castAuth(auth);
    _assert(authInternal._canInitEmulator, authInternal, "emulator-config-failed" /* AuthErrorCode.EMULATOR_CONFIG_FAILED */);
    _assert(/^https?:\/\//.test(url), authInternal, "invalid-emulator-scheme" /* AuthErrorCode.INVALID_EMULATOR_SCHEME */);
    const disableWarnings = !!(options === null || options === void 0 ? void 0 : options.disableWarnings);
    const protocol = extractProtocol(url);
    const ***REMOVED*** host, port } = extractHostAndPort(url);
    const portStr = port === null ? '' : `:$***REMOVED***port}`;
    // Always replace path with "/" (even if input url had no path at all, or had a different one).
    authInternal.config.emulator = ***REMOVED*** url: `$***REMOVED***protocol}//$***REMOVED***host}$***REMOVED***portStr}/` };
    authInternal.settings.appVerificationDisabledForTesting = true;
    authInternal.emulatorConfig = Object.freeze(***REMOVED***
        host,
        port,
        protocol: protocol.replace(':', ''),
        options: Object.freeze(***REMOVED*** disableWarnings })
  ***REMOVED***);
    if (!disableWarnings) ***REMOVED***
        emitEmulatorWarning();
  ***REMOVED***
}
function extractProtocol(url) ***REMOVED***
    const protocolEnd = url.indexOf(':');
    return protocolEnd < 0 ? '' : url.substr(0, protocolEnd + 1);
}
function extractHostAndPort(url) ***REMOVED***
    const protocol = extractProtocol(url);
    const authority = /(\/\/)?([^?#/]+)/.exec(url.substr(protocol.length)); // Between // and /, ? or #.
    if (!authority) ***REMOVED***
        return ***REMOVED*** host: '', port: null };
  ***REMOVED***
    const hostAndPort = authority[2].split('@').pop() || ''; // Strip out "username:password@".
    const bracketedIPv6 = /^(\[[^\]]+\])(:|$)/.exec(hostAndPort);
    if (bracketedIPv6) ***REMOVED***
        const host = bracketedIPv6[1];
        return ***REMOVED*** host, port: parsePort(hostAndPort.substr(host.length + 1)) };
  ***REMOVED***
    else ***REMOVED***
        const [host, port] = hostAndPort.split(':');
        return ***REMOVED*** host, port: parsePort(port) };
  ***REMOVED***
}
function parsePort(portStr) ***REMOVED***
    if (!portStr) ***REMOVED***
        return null;
  ***REMOVED***
    const port = Number(portStr);
    if (isNaN(port)) ***REMOVED***
        return null;
  ***REMOVED***
    return port;
}
function emitEmulatorWarning() ***REMOVED***
    function attachBanner() ***REMOVED***
        const el = document.createElement('p');
        const sty = el.style;
        el.innerText =
            'Running in emulator mode. Do not use with production credentials.';
        sty.position = 'fixed';
        sty.width = '100%';
        sty.backgroundColor = '#ffffff';
        sty.border = '.1em solid #000000';
        sty.color = '#b50000';
        sty.bottom = '0px';
        sty.left = '0px';
        sty.margin = '0px';
        sty.zIndex = '10000';
        sty.textAlign = 'center';
        el.classList.add('firebase-emulator-warning');
        document.body.appendChild(el);
  ***REMOVED***
    if (typeof console !== 'undefined' && typeof console.info === 'function') ***REMOVED***
        console.info('WARNING: You are using the Auth Emulator,' +
            ' which is intended for local testing only.  Do not use with' +
            ' production credentials.');
  ***REMOVED***
    if (typeof window !== 'undefined' && typeof document !== 'undefined') ***REMOVED***
        if (document.readyState === 'loading') ***REMOVED***
            window.addEventListener('DOMContentLoaded', attachBanner);
      ***REMOVED***
        else ***REMOVED***
            attachBanner();
      ***REMOVED***
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Interface that represents the credentials returned by an ***REMOVED***@link AuthProvider}.
 *
 * @remarks
 * Implementations specify the details about each auth provider's credential requirements.
 *
 * @public
 */
class AuthCredential ***REMOVED***
    /** @internal */
    constructor(
    /**
     * The authentication provider ID for the credential.
     *
     * @remarks
     * For example, 'facebook.com', or 'google.com'.
     */
    providerId, 
    /**
     * The authentication sign in method for the credential.
     *
     * @remarks
     * For example, ***REMOVED***@link SignInMethod}.EMAIL_PASSWORD, or
     * ***REMOVED***@link SignInMethod}.EMAIL_LINK. This corresponds to the sign-in method
     * identifier as returned in ***REMOVED***@link fetchSignInMethodsForEmail}.
     */
    signInMethod) ***REMOVED***
        this.providerId = providerId;
        this.signInMethod = signInMethod;
  ***REMOVED***
    /**
     * Returns a JSON-serializable representation of this object.
     *
     * @returns a JSON-serializable representation of this object.
     */
    toJSON() ***REMOVED***
        return debugFail('not implemented');
  ***REMOVED***
    /** @internal */
    _getIdTokenResponse(_auth) ***REMOVED***
        return debugFail('not implemented');
  ***REMOVED***
    /** @internal */
    _linkToIdToken(_auth, _idToken) ***REMOVED***
        return debugFail('not implemented');
  ***REMOVED***
    /** @internal */
    _getReauthenticationResolver(_auth) ***REMOVED***
        return debugFail('not implemented');
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function resetPassword(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:resetPassword" /* Endpoint.RESET_PASSWORD */, _addTidIfNecessary(auth, request));
}
async function updateEmailPassword(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:update" /* Endpoint.SET_ACCOUNT_INFO */, request);
}
async function applyActionCode$1(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:update" /* Endpoint.SET_ACCOUNT_INFO */, _addTidIfNecessary(auth, request));
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function signInWithPassword(auth, request) ***REMOVED***
    return _performSignInRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:signInWithPassword" /* Endpoint.SIGN_IN_WITH_PASSWORD */, _addTidIfNecessary(auth, request));
}
async function sendOobCode(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:sendOobCode" /* Endpoint.SEND_OOB_CODE */, _addTidIfNecessary(auth, request));
}
async function sendEmailVerification$1(auth, request) ***REMOVED***
    return sendOobCode(auth, request);
}
async function sendPasswordResetEmail$1(auth, request) ***REMOVED***
    return sendOobCode(auth, request);
}
async function sendSignInLinkToEmail$1(auth, request) ***REMOVED***
    return sendOobCode(auth, request);
}
async function verifyAndChangeEmail(auth, request) ***REMOVED***
    return sendOobCode(auth, request);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function signInWithEmailLink$1(auth, request) ***REMOVED***
    return _performSignInRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:signInWithEmailLink" /* Endpoint.SIGN_IN_WITH_EMAIL_LINK */, _addTidIfNecessary(auth, request));
}
async function signInWithEmailLinkForLinking(auth, request) ***REMOVED***
    return _performSignInRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:signInWithEmailLink" /* Endpoint.SIGN_IN_WITH_EMAIL_LINK */, _addTidIfNecessary(auth, request));
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Interface that represents the credentials returned by ***REMOVED***@link EmailAuthProvider} for
 * ***REMOVED***@link ProviderId}.PASSWORD
 *
 * @remarks
 * Covers both ***REMOVED***@link SignInMethod}.EMAIL_PASSWORD and
 * ***REMOVED***@link SignInMethod}.EMAIL_LINK.
 *
 * @public
 */
class EmailAuthCredential extends AuthCredential ***REMOVED***
    /** @internal */
    constructor(
    /** @internal */
    _email, 
    /** @internal */
    _password, signInMethod, 
    /** @internal */
    _tenantId = null) ***REMOVED***
        super("password" /* ProviderId.PASSWORD */, signInMethod);
        this._email = _email;
        this._password = _password;
        this._tenantId = _tenantId;
  ***REMOVED***
    /** @internal */
    static _fromEmailAndPassword(email, password) ***REMOVED***
        return new EmailAuthCredential(email, password, "password" /* SignInMethod.EMAIL_PASSWORD */);
  ***REMOVED***
    /** @internal */
    static _fromEmailAndCode(email, oobCode, tenantId = null) ***REMOVED***
        return new EmailAuthCredential(email, oobCode, "emailLink" /* SignInMethod.EMAIL_LINK */, tenantId);
  ***REMOVED***
    /** ***REMOVED***@inheritdoc AuthCredential.toJSON} */
    toJSON() ***REMOVED***
        return ***REMOVED***
            email: this._email,
            password: this._password,
            signInMethod: this.signInMethod,
            tenantId: this._tenantId
      ***REMOVED***;
  ***REMOVED***
    /**
     * Static method to deserialize a JSON representation of an object into an ***REMOVED***@link  AuthCredential}.
     *
     * @param json - Either `object` or the stringified representation of the object. When string is
     * provided, `JSON.parse` would be called first.
     *
     * @returns If the JSON input does not represent an ***REMOVED***@link AuthCredential}, null is returned.
     */
    static fromJSON(json) ***REMOVED***
        const obj = typeof json === 'string' ? JSON.parse(json) : json;
        if ((obj === null || obj === void 0 ? void 0 : obj.email) && (obj === null || obj === void 0 ? void 0 : obj.password)) ***REMOVED***
            if (obj.signInMethod === "password" /* SignInMethod.EMAIL_PASSWORD */) ***REMOVED***
                return this._fromEmailAndPassword(obj.email, obj.password);
          ***REMOVED***
            else if (obj.signInMethod === "emailLink" /* SignInMethod.EMAIL_LINK */) ***REMOVED***
                return this._fromEmailAndCode(obj.email, obj.password, obj.tenantId);
          ***REMOVED***
      ***REMOVED***
        return null;
  ***REMOVED***
    /** @internal */
    async _getIdTokenResponse(auth) ***REMOVED***
        switch (this.signInMethod) ***REMOVED***
            case "password" /* SignInMethod.EMAIL_PASSWORD */:
                return signInWithPassword(auth, ***REMOVED***
                    returnSecureToken: true,
                    email: this._email,
                    password: this._password
              ***REMOVED***);
            case "emailLink" /* SignInMethod.EMAIL_LINK */:
                return signInWithEmailLink$1(auth, ***REMOVED***
                    email: this._email,
                    oobCode: this._password
              ***REMOVED***);
            default:
                _fail(auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
      ***REMOVED***
  ***REMOVED***
    /** @internal */
    async _linkToIdToken(auth, idToken) ***REMOVED***
        switch (this.signInMethod) ***REMOVED***
            case "password" /* SignInMethod.EMAIL_PASSWORD */:
                return updateEmailPassword(auth, ***REMOVED***
                    idToken,
                    returnSecureToken: true,
                    email: this._email,
                    password: this._password
              ***REMOVED***);
            case "emailLink" /* SignInMethod.EMAIL_LINK */:
                return signInWithEmailLinkForLinking(auth, ***REMOVED***
                    idToken,
                    email: this._email,
                    oobCode: this._password
              ***REMOVED***);
            default:
                _fail(auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
      ***REMOVED***
  ***REMOVED***
    /** @internal */
    _getReauthenticationResolver(auth) ***REMOVED***
        return this._getIdTokenResponse(auth);
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function signInWithIdp(auth, request) ***REMOVED***
    return _performSignInRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:signInWithIdp" /* Endpoint.SIGN_IN_WITH_IDP */, _addTidIfNecessary(auth, request));
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const IDP_REQUEST_URI$1 = 'http://localhost';
/**
 * Represents the OAuth credentials returned by an ***REMOVED***@link OAuthProvider}.
 *
 * @remarks
 * Implementations specify the details about each auth provider's credential requirements.
 *
 * @public
 */
class OAuthCredential extends AuthCredential ***REMOVED***
    constructor() ***REMOVED***
        super(...arguments);
        this.pendingToken = null;
  ***REMOVED***
    /** @internal */
    static _fromParams(params) ***REMOVED***
        const cred = new OAuthCredential(params.providerId, params.signInMethod);
        if (params.idToken || params.accessToken) ***REMOVED***
            // OAuth 2 and either ID token or access token.
            if (params.idToken) ***REMOVED***
                cred.idToken = params.idToken;
          ***REMOVED***
            if (params.accessToken) ***REMOVED***
                cred.accessToken = params.accessToken;
          ***REMOVED***
            // Add nonce if available and no pendingToken is present.
            if (params.nonce && !params.pendingToken) ***REMOVED***
                cred.nonce = params.nonce;
          ***REMOVED***
            if (params.pendingToken) ***REMOVED***
                cred.pendingToken = params.pendingToken;
          ***REMOVED***
      ***REMOVED***
        else if (params.oauthToken && params.oauthTokenSecret) ***REMOVED***
            // OAuth 1 and OAuth token with token secret
            cred.accessToken = params.oauthToken;
            cred.secret = params.oauthTokenSecret;
      ***REMOVED***
        else ***REMOVED***
            _fail("argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
      ***REMOVED***
        return cred;
  ***REMOVED***
    /** ***REMOVED***@inheritdoc AuthCredential.toJSON}  */
    toJSON() ***REMOVED***
        return ***REMOVED***
            idToken: this.idToken,
            accessToken: this.accessToken,
            secret: this.secret,
            nonce: this.nonce,
            pendingToken: this.pendingToken,
            providerId: this.providerId,
            signInMethod: this.signInMethod
      ***REMOVED***;
  ***REMOVED***
    /**
     * Static method to deserialize a JSON representation of an object into an
     * ***REMOVED***@link  AuthCredential}.
     *
     * @param json - Input can be either Object or the stringified representation of the object.
     * When string is provided, JSON.parse would be called first.
     *
     * @returns If the JSON input does not represent an ***REMOVED***@link  AuthCredential}, null is returned.
     */
    static fromJSON(json) ***REMOVED***
        const obj = typeof json === 'string' ? JSON.parse(json) : json;
        const ***REMOVED*** providerId, signInMethod } = obj, rest = __rest(obj, ["providerId", "signInMethod"]);
        if (!providerId || !signInMethod) ***REMOVED***
            return null;
      ***REMOVED***
        const cred = new OAuthCredential(providerId, signInMethod);
        cred.idToken = rest.idToken || undefined;
        cred.accessToken = rest.accessToken || undefined;
        cred.secret = rest.secret;
        cred.nonce = rest.nonce;
        cred.pendingToken = rest.pendingToken || null;
        return cred;
  ***REMOVED***
    /** @internal */
    _getIdTokenResponse(auth) ***REMOVED***
        const request = this.buildRequest();
        return signInWithIdp(auth, request);
  ***REMOVED***
    /** @internal */
    _linkToIdToken(auth, idToken) ***REMOVED***
        const request = this.buildRequest();
        request.idToken = idToken;
        return signInWithIdp(auth, request);
  ***REMOVED***
    /** @internal */
    _getReauthenticationResolver(auth) ***REMOVED***
        const request = this.buildRequest();
        request.autoCreate = false;
        return signInWithIdp(auth, request);
  ***REMOVED***
    buildRequest() ***REMOVED***
        const request = ***REMOVED***
            requestUri: IDP_REQUEST_URI$1,
            returnSecureToken: true
      ***REMOVED***;
        if (this.pendingToken) ***REMOVED***
            request.pendingToken = this.pendingToken;
      ***REMOVED***
        else ***REMOVED***
            const postBody = ***REMOVED***};
            if (this.idToken) ***REMOVED***
                postBody['id_token'] = this.idToken;
          ***REMOVED***
            if (this.accessToken) ***REMOVED***
                postBody['access_token'] = this.accessToken;
          ***REMOVED***
            if (this.secret) ***REMOVED***
                postBody['oauth_token_secret'] = this.secret;
          ***REMOVED***
            postBody['providerId'] = this.providerId;
            if (this.nonce && !this.pendingToken) ***REMOVED***
                postBody['nonce'] = this.nonce;
          ***REMOVED***
            request.postBody = querystring(postBody);
      ***REMOVED***
        return request;
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function sendPhoneVerificationCode(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:sendVerificationCode" /* Endpoint.SEND_VERIFICATION_CODE */, _addTidIfNecessary(auth, request));
}
async function signInWithPhoneNumber$1(auth, request) ***REMOVED***
    return _performSignInRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:signInWithPhoneNumber" /* Endpoint.SIGN_IN_WITH_PHONE_NUMBER */, _addTidIfNecessary(auth, request));
}
async function linkWithPhoneNumber$1(auth, request) ***REMOVED***
    const response = await _performSignInRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:signInWithPhoneNumber" /* Endpoint.SIGN_IN_WITH_PHONE_NUMBER */, _addTidIfNecessary(auth, request));
    if (response.temporaryProof) ***REMOVED***
        throw _makeTaggedError(auth, "account-exists-with-different-credential" /* AuthErrorCode.NEED_CONFIRMATION */, response);
  ***REMOVED***
    return response;
}
const VERIFY_PHONE_NUMBER_FOR_EXISTING_ERROR_MAP_ = ***REMOVED***
    ["USER_NOT_FOUND" /* ServerError.USER_NOT_FOUND */]: "user-not-found" /* AuthErrorCode.USER_DELETED */
};
async function verifyPhoneNumberForExisting(auth, request) ***REMOVED***
    const apiRequest = Object.assign(Object.assign(***REMOVED***}, request), ***REMOVED*** operation: 'REAUTH' });
    return _performSignInRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:signInWithPhoneNumber" /* Endpoint.SIGN_IN_WITH_PHONE_NUMBER */, _addTidIfNecessary(auth, apiRequest), VERIFY_PHONE_NUMBER_FOR_EXISTING_ERROR_MAP_);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Represents the credentials returned by ***REMOVED***@link PhoneAuthProvider}.
 *
 * @public
 */
class PhoneAuthCredential extends AuthCredential ***REMOVED***
    constructor(params) ***REMOVED***
        super("phone" /* ProviderId.PHONE */, "phone" /* SignInMethod.PHONE */);
        this.params = params;
  ***REMOVED***
    /** @internal */
    static _fromVerification(verificationId, verificationCode) ***REMOVED***
        return new PhoneAuthCredential(***REMOVED*** verificationId, verificationCode });
  ***REMOVED***
    /** @internal */
    static _fromTokenResponse(phoneNumber, temporaryProof) ***REMOVED***
        return new PhoneAuthCredential(***REMOVED*** phoneNumber, temporaryProof });
  ***REMOVED***
    /** @internal */
    _getIdTokenResponse(auth) ***REMOVED***
        return signInWithPhoneNumber$1(auth, this._makeVerificationRequest());
  ***REMOVED***
    /** @internal */
    _linkToIdToken(auth, idToken) ***REMOVED***
        return linkWithPhoneNumber$1(auth, Object.assign(***REMOVED*** idToken }, this._makeVerificationRequest()));
  ***REMOVED***
    /** @internal */
    _getReauthenticationResolver(auth) ***REMOVED***
        return verifyPhoneNumberForExisting(auth, this._makeVerificationRequest());
  ***REMOVED***
    /** @internal */
    _makeVerificationRequest() ***REMOVED***
        const ***REMOVED*** temporaryProof, phoneNumber, verificationId, verificationCode } = this.params;
        if (temporaryProof && phoneNumber) ***REMOVED***
            return ***REMOVED*** temporaryProof, phoneNumber };
      ***REMOVED***
        return ***REMOVED***
            sessionInfo: verificationId,
            code: verificationCode
      ***REMOVED***;
  ***REMOVED***
    /** ***REMOVED***@inheritdoc AuthCredential.toJSON} */
    toJSON() ***REMOVED***
        const obj = ***REMOVED***
            providerId: this.providerId
      ***REMOVED***;
        if (this.params.phoneNumber) ***REMOVED***
            obj.phoneNumber = this.params.phoneNumber;
      ***REMOVED***
        if (this.params.temporaryProof) ***REMOVED***
            obj.temporaryProof = this.params.temporaryProof;
      ***REMOVED***
        if (this.params.verificationCode) ***REMOVED***
            obj.verificationCode = this.params.verificationCode;
      ***REMOVED***
        if (this.params.verificationId) ***REMOVED***
            obj.verificationId = this.params.verificationId;
      ***REMOVED***
        return obj;
  ***REMOVED***
    /** Generates a phone credential based on a plain object or a JSON string. */
    static fromJSON(json) ***REMOVED***
        if (typeof json === 'string') ***REMOVED***
            json = JSON.parse(json);
      ***REMOVED***
        const ***REMOVED*** verificationId, verificationCode, phoneNumber, temporaryProof } = json;
        if (!verificationCode &&
            !verificationId &&
            !phoneNumber &&
            !temporaryProof) ***REMOVED***
            return null;
      ***REMOVED***
        return new PhoneAuthCredential(***REMOVED***
            verificationId,
            verificationCode,
            phoneNumber,
            temporaryProof
      ***REMOVED***);
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Maps the mode string in action code URL to Action Code Info operation.
 *
 * @param mode
 */
function parseMode(mode) ***REMOVED***
    switch (mode) ***REMOVED***
        case 'recoverEmail':
            return "RECOVER_EMAIL" /* ActionCodeOperation.RECOVER_EMAIL */;
        case 'resetPassword':
            return "PASSWORD_RESET" /* ActionCodeOperation.PASSWORD_RESET */;
        case 'signIn':
            return "EMAIL_SIGNIN" /* ActionCodeOperation.EMAIL_SIGNIN */;
        case 'verifyEmail':
            return "VERIFY_EMAIL" /* ActionCodeOperation.VERIFY_EMAIL */;
        case 'verifyAndChangeEmail':
            return "VERIFY_AND_CHANGE_EMAIL" /* ActionCodeOperation.VERIFY_AND_CHANGE_EMAIL */;
        case 'revertSecondFactorAddition':
            return "REVERT_SECOND_FACTOR_ADDITION" /* ActionCodeOperation.REVERT_SECOND_FACTOR_ADDITION */;
        default:
            return null;
  ***REMOVED***
}
/**
 * Helper to parse FDL links
 *
 * @param url
 */
function parseDeepLink(url) ***REMOVED***
    const link = querystringDecode(extractQuerystring(url))['link'];
    // Double link case (automatic redirect).
    const doubleDeepLink = link
        ? querystringDecode(extractQuerystring(link))['deep_link_id']
        : null;
    // iOS custom scheme links.
    const iOSDeepLink = querystringDecode(extractQuerystring(url))['deep_link_id'];
    const iOSDoubleDeepLink = iOSDeepLink
        ? querystringDecode(extractQuerystring(iOSDeepLink))['link']
        : null;
    return iOSDoubleDeepLink || iOSDeepLink || doubleDeepLink || link || url;
}
/**
 * A utility class to parse email action URLs such as password reset, email verification,
 * email link sign in, etc.
 *
 * @public
 */
class ActionCodeURL ***REMOVED***
    /**
     * @param actionLink - The link from which to extract the URL.
     * @returns The ***REMOVED***@link ActionCodeURL} object, or null if the link is invalid.
     *
     * @internal
     */
    constructor(actionLink) ***REMOVED***
        var _a, _b, _c, _d, _e, _f;
        const searchParams = querystringDecode(extractQuerystring(actionLink));
        const apiKey = (_a = searchParams["apiKey" /* QueryField.API_KEY */]) !== null && _a !== void 0 ? _a : null;
        const code = (_b = searchParams["oobCode" /* QueryField.CODE */]) !== null && _b !== void 0 ? _b : null;
        const operation = parseMode((_c = searchParams["mode" /* QueryField.MODE */]) !== null && _c !== void 0 ? _c : null);
        // Validate API key, code and mode.
        _assert(apiKey && code && operation, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
        this.apiKey = apiKey;
        this.operation = operation;
        this.code = code;
        this.continueUrl = (_d = searchParams["continueUrl" /* QueryField.CONTINUE_URL */]) !== null && _d !== void 0 ? _d : null;
        this.languageCode = (_e = searchParams["languageCode" /* QueryField.LANGUAGE_CODE */]) !== null && _e !== void 0 ? _e : null;
        this.tenantId = (_f = searchParams["tenantId" /* QueryField.TENANT_ID */]) !== null && _f !== void 0 ? _f : null;
  ***REMOVED***
    /**
     * Parses the email action link string and returns an ***REMOVED***@link ActionCodeURL} if the link is valid,
     * otherwise returns null.
     *
     * @param link  - The email action link string.
     * @returns The ***REMOVED***@link ActionCodeURL} object, or null if the link is invalid.
     *
     * @public
     */
    static parseLink(link) ***REMOVED***
        const actionLink = parseDeepLink(link);
        try ***REMOVED***
            return new ActionCodeURL(actionLink);
      ***REMOVED***
        catch (_a) ***REMOVED***
            return null;
      ***REMOVED***
  ***REMOVED***
}
/**
 * Parses the email action link string and returns an ***REMOVED***@link ActionCodeURL} if
 * the link is valid, otherwise returns null.
 *
 * @public
 */
function parseActionCodeURL(link) ***REMOVED***
    return ActionCodeURL.parseLink(link);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Provider for generating ***REMOVED***@link EmailAuthCredential}.
 *
 * @public
 */
class EmailAuthProvider ***REMOVED***
    constructor() ***REMOVED***
        /**
         * Always set to ***REMOVED***@link ProviderId}.PASSWORD, even for email link.
         */
        this.providerId = EmailAuthProvider.PROVIDER_ID;
  ***REMOVED***
    /**
     * Initialize an ***REMOVED***@link AuthCredential} using an email and password.
     *
     * @example
     * ```javascript
     * const authCredential = EmailAuthProvider.credential(email, password);
     * const userCredential = await signInWithCredential(auth, authCredential);
     * ```
     *
     * @example
     * ```javascript
     * const userCredential = await signInWithEmailAndPassword(auth, email, password);
     * ```
     *
     * @param email - Email address.
     * @param password - User account password.
     * @returns The auth provider credential.
     */
    static credential(email, password) ***REMOVED***
        return EmailAuthCredential._fromEmailAndPassword(email, password);
  ***REMOVED***
    /**
     * Initialize an ***REMOVED***@link AuthCredential} using an email and an email link after a sign in with
     * email link operation.
     *
     * @example
     * ```javascript
     * const authCredential = EmailAuthProvider.credentialWithLink(auth, email, emailLink);
     * const userCredential = await signInWithCredential(auth, authCredential);
     * ```
     *
     * @example
     * ```javascript
     * await sendSignInLinkToEmail(auth, email);
     * // Obtain emailLink from user.
     * const userCredential = await signInWithEmailLink(auth, email, emailLink);
     * ```
     *
     * @param auth - The ***REMOVED***@link Auth} instance used to verify the link.
     * @param email - Email address.
     * @param emailLink - Sign-in email link.
     * @returns - The auth provider credential.
     */
    static credentialWithLink(email, emailLink) ***REMOVED***
        const actionCodeUrl = ActionCodeURL.parseLink(emailLink);
        _assert(actionCodeUrl, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
        return EmailAuthCredential._fromEmailAndCode(email, actionCodeUrl.code, actionCodeUrl.tenantId);
  ***REMOVED***
}
/**
 * Always set to ***REMOVED***@link ProviderId}.PASSWORD, even for email link.
 */
EmailAuthProvider.PROVIDER_ID = "password" /* ProviderId.PASSWORD */;
/**
 * Always set to ***REMOVED***@link SignInMethod}.EMAIL_PASSWORD.
 */
EmailAuthProvider.EMAIL_PASSWORD_SIGN_IN_METHOD = "password" /* SignInMethod.EMAIL_PASSWORD */;
/**
 * Always set to ***REMOVED***@link SignInMethod}.EMAIL_LINK.
 */
EmailAuthProvider.EMAIL_LINK_SIGN_IN_METHOD = "emailLink" /* SignInMethod.EMAIL_LINK */;

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * The base class for all Federated providers (OAuth (including OIDC), SAML).
 *
 * This class is not meant to be instantiated directly.
 *
 * @public
 */
class FederatedAuthProvider ***REMOVED***
    /**
     * Constructor for generic OAuth providers.
     *
     * @param providerId - Provider for which credentials should be generated.
     */
    constructor(providerId) ***REMOVED***
        this.providerId = providerId;
        /** @internal */
        this.defaultLanguageCode = null;
        /** @internal */
        this.customParameters = ***REMOVED***};
  ***REMOVED***
    /**
     * Set the language gode.
     *
     * @param languageCode - language code
     */
    setDefaultLanguage(languageCode) ***REMOVED***
        this.defaultLanguageCode = languageCode;
  ***REMOVED***
    /**
     * Sets the OAuth custom parameters to pass in an OAuth request for popup and redirect sign-in
     * operations.
     *
     * @remarks
     * For a detailed list, check the reserved required OAuth 2.0 parameters such as `client_id`,
     * `redirect_uri`, `scope`, `response_type`, and `state` are not allowed and will be ignored.
     *
     * @param customOAuthParameters - The custom OAuth parameters to pass in the OAuth request.
     */
    setCustomParameters(customOAuthParameters) ***REMOVED***
        this.customParameters = customOAuthParameters;
        return this;
  ***REMOVED***
    /**
     * Retrieve the current list of ***REMOVED***@link CustomParameters}.
     */
    getCustomParameters() ***REMOVED***
        return this.customParameters;
  ***REMOVED***
}

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Common code to all OAuth providers. This is separate from the
 * ***REMOVED***@link OAuthProvider} so that child providers (like
 * ***REMOVED***@link GoogleAuthProvider}) don't inherit the `credential` instance method.
 * Instead, they rely on a static `credential` method.
 */
class BaseOAuthProvider extends FederatedAuthProvider ***REMOVED***
    constructor() ***REMOVED***
        super(...arguments);
        /** @internal */
        this.scopes = [];
  ***REMOVED***
    /**
     * Add an OAuth scope to the credential.
     *
     * @param scope - Provider OAuth scope to add.
     */
    addScope(scope) ***REMOVED***
        // If not already added, add scope to list.
        if (!this.scopes.includes(scope)) ***REMOVED***
            this.scopes.push(scope);
      ***REMOVED***
        return this;
  ***REMOVED***
    /**
     * Retrieve the current list of OAuth scopes.
     */
    getScopes() ***REMOVED***
        return [...this.scopes];
  ***REMOVED***
}
/**
 * Provider for generating generic ***REMOVED***@link OAuthCredential}.
 *
 * @example
 * ```javascript
 * // Sign in using a redirect.
 * const provider = new OAuthProvider('google.com');
 * // Start a sign in process for an unauthenticated user.
 * provider.addScope('profile');
 * provider.addScope('email');
 * await signInWithRedirect(auth, provider);
 * // This will trigger a full page redirect away from your app
 *
 * // After returning from the redirect when your app initializes you can obtain the result
 * const result = await getRedirectResult(auth);
 * if (result) ***REMOVED***
 *   // This is the signed-in user
 *   const user = result.user;
 *   // This gives you a OAuth Access Token for the provider.
 *   const credential = provider.credentialFromResult(auth, result);
 *   const token = credential.accessToken;
 * }
 * ```
 *
 * @example
 * ```javascript
 * // Sign in using a popup.
 * const provider = new OAuthProvider('google.com');
 * provider.addScope('profile');
 * provider.addScope('email');
 * const result = await signInWithPopup(auth, provider);
 *
 * // The signed-in user info.
 * const user = result.user;
 * // This gives you a OAuth Access Token for the provider.
 * const credential = provider.credentialFromResult(auth, result);
 * const token = credential.accessToken;
 * ```
 * @public
 */
class OAuthProvider extends BaseOAuthProvider ***REMOVED***
    /**
     * Creates an ***REMOVED***@link OAuthCredential} from a JSON string or a plain object.
     * @param json - A plain object or a JSON string
     */
    static credentialFromJSON(json) ***REMOVED***
        const obj = typeof json === 'string' ? JSON.parse(json) : json;
        _assert('providerId' in obj && 'signInMethod' in obj, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
        return OAuthCredential._fromParams(obj);
  ***REMOVED***
    /**
     * Creates a ***REMOVED***@link OAuthCredential} from a generic OAuth provider's access token or ID token.
     *
     * @remarks
     * The raw nonce is required when an ID token with a nonce field is provided. The SHA-256 hash of
     * the raw nonce must match the nonce field in the ID token.
     *
     * @example
     * ```javascript
     * // `googleUser` from the onsuccess Google Sign In callback.
     * // Initialize a generate OAuth provider with a `google.com` providerId.
     * const provider = new OAuthProvider('google.com');
     * const credential = provider.credential(***REMOVED***
     *   idToken: googleUser.getAuthResponse().id_token,
     * });
     * const result = await signInWithCredential(credential);
     * ```
     *
     * @param params - Either the options object containing the ID token, access token and raw nonce
     * or the ID token string.
     */
    credential(params) ***REMOVED***
        return this._credential(Object.assign(Object.assign(***REMOVED***}, params), ***REMOVED*** nonce: params.rawNonce }));
  ***REMOVED***
    /** An internal credential method that accepts more permissive options */
    _credential(params) ***REMOVED***
        _assert(params.idToken || params.accessToken, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
        // For OAuthCredential, sign in method is same as providerId.
        return OAuthCredential._fromParams(Object.assign(Object.assign(***REMOVED***}, params), ***REMOVED*** providerId: this.providerId, signInMethod: this.providerId }));
  ***REMOVED***
    /**
     * Used to extract the underlying ***REMOVED***@link OAuthCredential} from a ***REMOVED***@link UserCredential}.
     *
     * @param userCredential - The user credential.
     */
    static credentialFromResult(userCredential) ***REMOVED***
        return OAuthProvider.oauthCredentialFromTaggedObject(userCredential);
  ***REMOVED***
    /**
     * Used to extract the underlying ***REMOVED***@link OAuthCredential} from a ***REMOVED***@link AuthError} which was
     * thrown during a sign-in, link, or reauthenticate operation.
     *
     * @param userCredential - The user credential.
     */
    static credentialFromError(error) ***REMOVED***
        return OAuthProvider.oauthCredentialFromTaggedObject((error.customData || ***REMOVED***}));
  ***REMOVED***
    static oauthCredentialFromTaggedObject(***REMOVED*** _tokenResponse: tokenResponse }) ***REMOVED***
        if (!tokenResponse) ***REMOVED***
            return null;
      ***REMOVED***
        const ***REMOVED*** oauthIdToken, oauthAccessToken, oauthTokenSecret, pendingToken, nonce, providerId } = tokenResponse;
        if (!oauthAccessToken &&
            !oauthTokenSecret &&
            !oauthIdToken &&
            !pendingToken) ***REMOVED***
            return null;
      ***REMOVED***
        if (!providerId) ***REMOVED***
            return null;
      ***REMOVED***
        try ***REMOVED***
            return new OAuthProvider(providerId)._credential(***REMOVED***
                idToken: oauthIdToken,
                accessToken: oauthAccessToken,
                nonce,
                pendingToken
          ***REMOVED***);
      ***REMOVED***
        catch (e) ***REMOVED***
            return null;
      ***REMOVED***
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Provider for generating an ***REMOVED***@link OAuthCredential} for ***REMOVED***@link ProviderId}.FACEBOOK.
 *
 * @example
 * ```javascript
 * // Sign in using a redirect.
 * const provider = new FacebookAuthProvider();
 * // Start a sign in process for an unauthenticated user.
 * provider.addScope('user_birthday');
 * await signInWithRedirect(auth, provider);
 * // This will trigger a full page redirect away from your app
 *
 * // After returning from the redirect when your app initializes you can obtain the result
 * const result = await getRedirectResult(auth);
 * if (result) ***REMOVED***
 *   // This is the signed-in user
 *   const user = result.user;
 *   // This gives you a Facebook Access Token.
 *   const credential = FacebookAuthProvider.credentialFromResult(result);
 *   const token = credential.accessToken;
 * }
 * ```
 *
 * @example
 * ```javascript
 * // Sign in using a popup.
 * const provider = new FacebookAuthProvider();
 * provider.addScope('user_birthday');
 * const result = await signInWithPopup(auth, provider);
 *
 * // The signed-in user info.
 * const user = result.user;
 * // This gives you a Facebook Access Token.
 * const credential = FacebookAuthProvider.credentialFromResult(result);
 * const token = credential.accessToken;
 * ```
 *
 * @public
 */
class FacebookAuthProvider extends BaseOAuthProvider ***REMOVED***
    constructor() ***REMOVED***
        super("facebook.com" /* ProviderId.FACEBOOK */);
  ***REMOVED***
    /**
     * Creates a credential for Facebook.
     *
     * @example
     * ```javascript
     * // `event` from the Facebook auth.authResponseChange callback.
     * const credential = FacebookAuthProvider.credential(event.authResponse.accessToken);
     * const result = await signInWithCredential(credential);
     * ```
     *
     * @param accessToken - Facebook access token.
     */
    static credential(accessToken) ***REMOVED***
        return OAuthCredential._fromParams(***REMOVED***
            providerId: FacebookAuthProvider.PROVIDER_ID,
            signInMethod: FacebookAuthProvider.FACEBOOK_SIGN_IN_METHOD,
            accessToken
      ***REMOVED***);
  ***REMOVED***
    /**
     * Used to extract the underlying ***REMOVED***@link OAuthCredential} from a ***REMOVED***@link UserCredential}.
     *
     * @param userCredential - The user credential.
     */
    static credentialFromResult(userCredential) ***REMOVED***
        return FacebookAuthProvider.credentialFromTaggedObject(userCredential);
  ***REMOVED***
    /**
     * Used to extract the underlying ***REMOVED***@link OAuthCredential} from a ***REMOVED***@link AuthError} which was
     * thrown during a sign-in, link, or reauthenticate operation.
     *
     * @param userCredential - The user credential.
     */
    static credentialFromError(error) ***REMOVED***
        return FacebookAuthProvider.credentialFromTaggedObject((error.customData || ***REMOVED***}));
  ***REMOVED***
    static credentialFromTaggedObject(***REMOVED*** _tokenResponse: tokenResponse }) ***REMOVED***
        if (!tokenResponse || !('oauthAccessToken' in tokenResponse)) ***REMOVED***
            return null;
      ***REMOVED***
        if (!tokenResponse.oauthAccessToken) ***REMOVED***
            return null;
      ***REMOVED***
        try ***REMOVED***
            return FacebookAuthProvider.credential(tokenResponse.oauthAccessToken);
      ***REMOVED***
        catch (_a) ***REMOVED***
            return null;
      ***REMOVED***
  ***REMOVED***
}
/** Always set to ***REMOVED***@link SignInMethod}.FACEBOOK. */
FacebookAuthProvider.FACEBOOK_SIGN_IN_METHOD = "facebook.com" /* SignInMethod.FACEBOOK */;
/** Always set to ***REMOVED***@link ProviderId}.FACEBOOK. */
FacebookAuthProvider.PROVIDER_ID = "facebook.com" /* ProviderId.FACEBOOK */;

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Provider for generating an an ***REMOVED***@link OAuthCredential} for ***REMOVED***@link ProviderId}.GOOGLE.
 *
 * @example
 * ```javascript
 * // Sign in using a redirect.
 * const provider = new GoogleAuthProvider();
 * // Start a sign in process for an unauthenticated user.
 * provider.addScope('profile');
 * provider.addScope('email');
 * await signInWithRedirect(auth, provider);
 * // This will trigger a full page redirect away from your app
 *
 * // After returning from the redirect when your app initializes you can obtain the result
 * const result = await getRedirectResult(auth);
 * if (result) ***REMOVED***
 *   // This is the signed-in user
 *   const user = result.user;
 *   // This gives you a Google Access Token.
 *   const credential = GoogleAuthProvider.credentialFromResult(result);
 *   const token = credential.accessToken;
 * }
 * ```
 *
 * @example
 * ```javascript
 * // Sign in using a popup.
 * const provider = new GoogleAuthProvider();
 * provider.addScope('profile');
 * provider.addScope('email');
 * const result = await signInWithPopup(auth, provider);
 *
 * // The signed-in user info.
 * const user = result.user;
 * // This gives you a Google Access Token.
 * const credential = GoogleAuthProvider.credentialFromResult(result);
 * const token = credential.accessToken;
 * ```
 *
 * @public
 */
class GoogleAuthProvider extends BaseOAuthProvider ***REMOVED***
    constructor() ***REMOVED***
        super("google.com" /* ProviderId.GOOGLE */);
        this.addScope('profile');
  ***REMOVED***
    /**
     * Creates a credential for Google. At least one of ID token and access token is required.
     *
     * @example
     * ```javascript
     * // \`googleUser\` from the onsuccess Google Sign In callback.
     * const credential = GoogleAuthProvider.credential(googleUser.getAuthResponse().id_token);
     * const result = await signInWithCredential(credential);
     * ```
     *
     * @param idToken - Google ID token.
     * @param accessToken - Google access token.
     */
    static credential(idToken, accessToken) ***REMOVED***
        return OAuthCredential._fromParams(***REMOVED***
            providerId: GoogleAuthProvider.PROVIDER_ID,
            signInMethod: GoogleAuthProvider.GOOGLE_SIGN_IN_METHOD,
            idToken,
            accessToken
      ***REMOVED***);
  ***REMOVED***
    /**
     * Used to extract the underlying ***REMOVED***@link OAuthCredential} from a ***REMOVED***@link UserCredential}.
     *
     * @param userCredential - The user credential.
     */
    static credentialFromResult(userCredential) ***REMOVED***
        return GoogleAuthProvider.credentialFromTaggedObject(userCredential);
  ***REMOVED***
    /**
     * Used to extract the underlying ***REMOVED***@link OAuthCredential} from a ***REMOVED***@link AuthError} which was
     * thrown during a sign-in, link, or reauthenticate operation.
     *
     * @param userCredential - The user credential.
     */
    static credentialFromError(error) ***REMOVED***
        return GoogleAuthProvider.credentialFromTaggedObject((error.customData || ***REMOVED***}));
  ***REMOVED***
    static credentialFromTaggedObject(***REMOVED*** _tokenResponse: tokenResponse }) ***REMOVED***
        if (!tokenResponse) ***REMOVED***
            return null;
      ***REMOVED***
        const ***REMOVED*** oauthIdToken, oauthAccessToken } = tokenResponse;
        if (!oauthIdToken && !oauthAccessToken) ***REMOVED***
            // This could be an oauth 1 credential or a phone credential
            return null;
      ***REMOVED***
        try ***REMOVED***
            return GoogleAuthProvider.credential(oauthIdToken, oauthAccessToken);
      ***REMOVED***
        catch (_a) ***REMOVED***
            return null;
      ***REMOVED***
  ***REMOVED***
}
/** Always set to ***REMOVED***@link SignInMethod}.GOOGLE. */
GoogleAuthProvider.GOOGLE_SIGN_IN_METHOD = "google.com" /* SignInMethod.GOOGLE */;
/** Always set to ***REMOVED***@link ProviderId}.GOOGLE. */
GoogleAuthProvider.PROVIDER_ID = "google.com" /* ProviderId.GOOGLE */;

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Provider for generating an ***REMOVED***@link OAuthCredential} for ***REMOVED***@link ProviderId}.GITHUB.
 *
 * @remarks
 * GitHub requires an OAuth 2.0 redirect, so you can either handle the redirect directly, or use
 * the ***REMOVED***@link signInWithPopup} handler:
 *
 * @example
 * ```javascript
 * // Sign in using a redirect.
 * const provider = new GithubAuthProvider();
 * // Start a sign in process for an unauthenticated user.
 * provider.addScope('repo');
 * await signInWithRedirect(auth, provider);
 * // This will trigger a full page redirect away from your app
 *
 * // After returning from the redirect when your app initializes you can obtain the result
 * const result = await getRedirectResult(auth);
 * if (result) ***REMOVED***
 *   // This is the signed-in user
 *   const user = result.user;
 *   // This gives you a Github Access Token.
 *   const credential = GithubAuthProvider.credentialFromResult(result);
 *   const token = credential.accessToken;
 * }
 * ```
 *
 * @example
 * ```javascript
 * // Sign in using a popup.
 * const provider = new GithubAuthProvider();
 * provider.addScope('repo');
 * const result = await signInWithPopup(auth, provider);
 *
 * // The signed-in user info.
 * const user = result.user;
 * // This gives you a Github Access Token.
 * const credential = GithubAuthProvider.credentialFromResult(result);
 * const token = credential.accessToken;
 * ```
 * @public
 */
class GithubAuthProvider extends BaseOAuthProvider ***REMOVED***
    constructor() ***REMOVED***
        super("github.com" /* ProviderId.GITHUB */);
  ***REMOVED***
    /**
     * Creates a credential for Github.
     *
     * @param accessToken - Github access token.
     */
    static credential(accessToken) ***REMOVED***
        return OAuthCredential._fromParams(***REMOVED***
            providerId: GithubAuthProvider.PROVIDER_ID,
            signInMethod: GithubAuthProvider.GITHUB_SIGN_IN_METHOD,
            accessToken
      ***REMOVED***);
  ***REMOVED***
    /**
     * Used to extract the underlying ***REMOVED***@link OAuthCredential} from a ***REMOVED***@link UserCredential}.
     *
     * @param userCredential - The user credential.
     */
    static credentialFromResult(userCredential) ***REMOVED***
        return GithubAuthProvider.credentialFromTaggedObject(userCredential);
  ***REMOVED***
    /**
     * Used to extract the underlying ***REMOVED***@link OAuthCredential} from a ***REMOVED***@link AuthError} which was
     * thrown during a sign-in, link, or reauthenticate operation.
     *
     * @param userCredential - The user credential.
     */
    static credentialFromError(error) ***REMOVED***
        return GithubAuthProvider.credentialFromTaggedObject((error.customData || ***REMOVED***}));
  ***REMOVED***
    static credentialFromTaggedObject(***REMOVED*** _tokenResponse: tokenResponse }) ***REMOVED***
        if (!tokenResponse || !('oauthAccessToken' in tokenResponse)) ***REMOVED***
            return null;
      ***REMOVED***
        if (!tokenResponse.oauthAccessToken) ***REMOVED***
            return null;
      ***REMOVED***
        try ***REMOVED***
            return GithubAuthProvider.credential(tokenResponse.oauthAccessToken);
      ***REMOVED***
        catch (_a) ***REMOVED***
            return null;
      ***REMOVED***
  ***REMOVED***
}
/** Always set to ***REMOVED***@link SignInMethod}.GITHUB. */
GithubAuthProvider.GITHUB_SIGN_IN_METHOD = "github.com" /* SignInMethod.GITHUB */;
/** Always set to ***REMOVED***@link ProviderId}.GITHUB. */
GithubAuthProvider.PROVIDER_ID = "github.com" /* ProviderId.GITHUB */;

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const IDP_REQUEST_URI = 'http://localhost';
/**
 * @public
 */
class SAMLAuthCredential extends AuthCredential ***REMOVED***
    /** @internal */
    constructor(providerId, pendingToken) ***REMOVED***
        super(providerId, providerId);
        this.pendingToken = pendingToken;
  ***REMOVED***
    /** @internal */
    _getIdTokenResponse(auth) ***REMOVED***
        const request = this.buildRequest();
        return signInWithIdp(auth, request);
  ***REMOVED***
    /** @internal */
    _linkToIdToken(auth, idToken) ***REMOVED***
        const request = this.buildRequest();
        request.idToken = idToken;
        return signInWithIdp(auth, request);
  ***REMOVED***
    /** @internal */
    _getReauthenticationResolver(auth) ***REMOVED***
        const request = this.buildRequest();
        request.autoCreate = false;
        return signInWithIdp(auth, request);
  ***REMOVED***
    /** ***REMOVED***@inheritdoc AuthCredential.toJSON}  */
    toJSON() ***REMOVED***
        return ***REMOVED***
            signInMethod: this.signInMethod,
            providerId: this.providerId,
            pendingToken: this.pendingToken
      ***REMOVED***;
  ***REMOVED***
    /**
     * Static method to deserialize a JSON representation of an object into an
     * ***REMOVED***@link  AuthCredential}.
     *
     * @param json - Input can be either Object or the stringified representation of the object.
     * When string is provided, JSON.parse would be called first.
     *
     * @returns If the JSON input does not represent an ***REMOVED***@link  AuthCredential}, null is returned.
     */
    static fromJSON(json) ***REMOVED***
        const obj = typeof json === 'string' ? JSON.parse(json) : json;
        const ***REMOVED*** providerId, signInMethod, pendingToken } = obj;
        if (!providerId ||
            !signInMethod ||
            !pendingToken ||
            providerId !== signInMethod) ***REMOVED***
            return null;
      ***REMOVED***
        return new SAMLAuthCredential(providerId, pendingToken);
  ***REMOVED***
    /**
     * Helper static method to avoid exposing the constructor to end users.
     *
     * @internal
     */
    static _create(providerId, pendingToken) ***REMOVED***
        return new SAMLAuthCredential(providerId, pendingToken);
  ***REMOVED***
    buildRequest() ***REMOVED***
        return ***REMOVED***
            requestUri: IDP_REQUEST_URI,
            returnSecureToken: true,
            pendingToken: this.pendingToken
      ***REMOVED***;
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const SAML_PROVIDER_PREFIX = 'saml.';
/**
 * An ***REMOVED***@link AuthProvider} for SAML.
 *
 * @public
 */
class SAMLAuthProvider extends FederatedAuthProvider ***REMOVED***
    /**
     * Constructor. The providerId must start with "saml."
     * @param providerId - SAML provider ID.
     */
    constructor(providerId) ***REMOVED***
        _assert(providerId.startsWith(SAML_PROVIDER_PREFIX), "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
        super(providerId);
  ***REMOVED***
    /**
     * Generates an ***REMOVED***@link AuthCredential} from a ***REMOVED***@link UserCredential} after a
     * successful SAML flow completes.
     *
     * @remarks
     *
     * For example, to get an ***REMOVED***@link AuthCredential}, you could write the
     * following code:
     *
     * ```js
     * const userCredential = await signInWithPopup(auth, samlProvider);
     * const credential = SAMLAuthProvider.credentialFromResult(userCredential);
     * ```
     *
     * @param userCredential - The user credential.
     */
    static credentialFromResult(userCredential) ***REMOVED***
        return SAMLAuthProvider.samlCredentialFromTaggedObject(userCredential);
  ***REMOVED***
    /**
     * Used to extract the underlying ***REMOVED***@link OAuthCredential} from a ***REMOVED***@link AuthError} which was
     * thrown during a sign-in, link, or reauthenticate operation.
     *
     * @param userCredential - The user credential.
     */
    static credentialFromError(error) ***REMOVED***
        return SAMLAuthProvider.samlCredentialFromTaggedObject((error.customData || ***REMOVED***}));
  ***REMOVED***
    /**
     * Creates an ***REMOVED***@link AuthCredential} from a JSON string or a plain object.
     * @param json - A plain object or a JSON string
     */
    static credentialFromJSON(json) ***REMOVED***
        const credential = SAMLAuthCredential.fromJSON(json);
        _assert(credential, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
        return credential;
  ***REMOVED***
    static samlCredentialFromTaggedObject(***REMOVED*** _tokenResponse: tokenResponse }) ***REMOVED***
        if (!tokenResponse) ***REMOVED***
            return null;
      ***REMOVED***
        const ***REMOVED*** pendingToken, providerId } = tokenResponse;
        if (!pendingToken || !providerId) ***REMOVED***
            return null;
      ***REMOVED***
        try ***REMOVED***
            return SAMLAuthCredential._create(providerId, pendingToken);
      ***REMOVED***
        catch (e) ***REMOVED***
            return null;
      ***REMOVED***
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Provider for generating an ***REMOVED***@link OAuthCredential} for ***REMOVED***@link ProviderId}.TWITTER.
 *
 * @example
 * ```javascript
 * // Sign in using a redirect.
 * const provider = new TwitterAuthProvider();
 * // Start a sign in process for an unauthenticated user.
 * await signInWithRedirect(auth, provider);
 * // This will trigger a full page redirect away from your app
 *
 * // After returning from the redirect when your app initializes you can obtain the result
 * const result = await getRedirectResult(auth);
 * if (result) ***REMOVED***
 *   // This is the signed-in user
 *   const user = result.user;
 *   // This gives you a Twitter Access Token and Secret.
 *   const credential = TwitterAuthProvider.credentialFromResult(result);
 *   const token = credential.accessToken;
 *   const secret = credential.secret;
 * }
 * ```
 *
 * @example
 * ```javascript
 * // Sign in using a popup.
 * const provider = new TwitterAuthProvider();
 * const result = await signInWithPopup(auth, provider);
 *
 * // The signed-in user info.
 * const user = result.user;
 * // This gives you a Twitter Access Token and Secret.
 * const credential = TwitterAuthProvider.credentialFromResult(result);
 * const token = credential.accessToken;
 * const secret = credential.secret;
 * ```
 *
 * @public
 */
class TwitterAuthProvider extends BaseOAuthProvider ***REMOVED***
    constructor() ***REMOVED***
        super("twitter.com" /* ProviderId.TWITTER */);
  ***REMOVED***
    /**
     * Creates a credential for Twitter.
     *
     * @param token - Twitter access token.
     * @param secret - Twitter secret.
     */
    static credential(token, secret) ***REMOVED***
        return OAuthCredential._fromParams(***REMOVED***
            providerId: TwitterAuthProvider.PROVIDER_ID,
            signInMethod: TwitterAuthProvider.TWITTER_SIGN_IN_METHOD,
            oauthToken: token,
            oauthTokenSecret: secret
      ***REMOVED***);
  ***REMOVED***
    /**
     * Used to extract the underlying ***REMOVED***@link OAuthCredential} from a ***REMOVED***@link UserCredential}.
     *
     * @param userCredential - The user credential.
     */
    static credentialFromResult(userCredential) ***REMOVED***
        return TwitterAuthProvider.credentialFromTaggedObject(userCredential);
  ***REMOVED***
    /**
     * Used to extract the underlying ***REMOVED***@link OAuthCredential} from a ***REMOVED***@link AuthError} which was
     * thrown during a sign-in, link, or reauthenticate operation.
     *
     * @param userCredential - The user credential.
     */
    static credentialFromError(error) ***REMOVED***
        return TwitterAuthProvider.credentialFromTaggedObject((error.customData || ***REMOVED***}));
  ***REMOVED***
    static credentialFromTaggedObject(***REMOVED*** _tokenResponse: tokenResponse }) ***REMOVED***
        if (!tokenResponse) ***REMOVED***
            return null;
      ***REMOVED***
        const ***REMOVED*** oauthAccessToken, oauthTokenSecret } = tokenResponse;
        if (!oauthAccessToken || !oauthTokenSecret) ***REMOVED***
            return null;
      ***REMOVED***
        try ***REMOVED***
            return TwitterAuthProvider.credential(oauthAccessToken, oauthTokenSecret);
      ***REMOVED***
        catch (_a) ***REMOVED***
            return null;
      ***REMOVED***
  ***REMOVED***
}
/** Always set to ***REMOVED***@link SignInMethod}.TWITTER. */
TwitterAuthProvider.TWITTER_SIGN_IN_METHOD = "twitter.com" /* SignInMethod.TWITTER */;
/** Always set to ***REMOVED***@link ProviderId}.TWITTER. */
TwitterAuthProvider.PROVIDER_ID = "twitter.com" /* ProviderId.TWITTER */;

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function signUp(auth, request) ***REMOVED***
    return _performSignInRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:signUp" /* Endpoint.SIGN_UP */, _addTidIfNecessary(auth, request));
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class UserCredentialImpl ***REMOVED***
    constructor(params) ***REMOVED***
        this.user = params.user;
        this.providerId = params.providerId;
        this._tokenResponse = params._tokenResponse;
        this.operationType = params.operationType;
  ***REMOVED***
    static async _fromIdTokenResponse(auth, operationType, idTokenResponse, isAnonymous = false) ***REMOVED***
        const user = await UserImpl._fromIdTokenResponse(auth, idTokenResponse, isAnonymous);
        const providerId = providerIdForResponse(idTokenResponse);
        const userCred = new UserCredentialImpl(***REMOVED***
            user,
            providerId,
            _tokenResponse: idTokenResponse,
            operationType
      ***REMOVED***);
        return userCred;
  ***REMOVED***
    static async _forOperation(user, operationType, response) ***REMOVED***
        await user._updateTokensIfNecessary(response, /* reload */ true);
        const providerId = providerIdForResponse(response);
        return new UserCredentialImpl(***REMOVED***
            user,
            providerId,
            _tokenResponse: response,
            operationType
      ***REMOVED***);
  ***REMOVED***
}
function providerIdForResponse(response) ***REMOVED***
    if (response.providerId) ***REMOVED***
        return response.providerId;
  ***REMOVED***
    if ('phoneNumber' in response) ***REMOVED***
        return "phone" /* ProviderId.PHONE */;
  ***REMOVED***
    return null;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Asynchronously signs in as an anonymous user.
 *
 * @remarks
 * If there is already an anonymous user signed in, that user will be returned; otherwise, a
 * new anonymous user identity will be created and returned.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 *
 * @public
 */
async function signInAnonymously(auth) ***REMOVED***
    var _a;
    const authInternal = _castAuth(auth);
    await authInternal._initializationPromise;
    if ((_a = authInternal.currentUser) === null || _a === void 0 ? void 0 : _a.isAnonymous) ***REMOVED***
        // If an anonymous user is already signed in, no need to sign them in again.
        return new UserCredentialImpl(***REMOVED***
            user: authInternal.currentUser,
            providerId: null,
            operationType: "signIn" /* OperationType.SIGN_IN */
      ***REMOVED***);
  ***REMOVED***
    const response = await signUp(authInternal, ***REMOVED***
        returnSecureToken: true
  ***REMOVED***);
    const userCredential = await UserCredentialImpl._fromIdTokenResponse(authInternal, "signIn" /* OperationType.SIGN_IN */, response, true);
    await authInternal._updateCurrentUser(userCredential.user);
    return userCredential;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class MultiFactorError extends FirebaseError ***REMOVED***
    constructor(auth, error, operationType, user) ***REMOVED***
        var _a;
        super(error.code, error.message);
        this.operationType = operationType;
        this.user = user;
        // https://github.com/Microsoft/TypeScript-wiki/blob/master/Breaking-Changes.md#extending-built-ins-like-error-array-and-map-may-no-longer-work
        Object.setPrototypeOf(this, MultiFactorError.prototype);
        this.customData = ***REMOVED***
            appName: auth.name,
            tenantId: (_a = auth.tenantId) !== null && _a !== void 0 ? _a : undefined,
            _serverResponse: error.customData._serverResponse,
            operationType
      ***REMOVED***;
  ***REMOVED***
    static _fromErrorAndOperation(auth, error, operationType, user) ***REMOVED***
        return new MultiFactorError(auth, error, operationType, user);
  ***REMOVED***
}
function _processCredentialSavingMfaContextIfNecessary(auth, operationType, credential, user) ***REMOVED***
    const idTokenProvider = operationType === "reauthenticate" /* OperationType.REAUTHENTICATE */
        ? credential._getReauthenticationResolver(auth)
        : credential._getIdTokenResponse(auth);
    return idTokenProvider.catch(error => ***REMOVED***
        if (error.code === `auth/$***REMOVED***"multi-factor-auth-required" /* AuthErrorCode.MFA_REQUIRED */}`) ***REMOVED***
            throw MultiFactorError._fromErrorAndOperation(auth, error, operationType, user);
      ***REMOVED***
        throw error;
  ***REMOVED***);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Takes a set of UserInfo provider data and converts it to a set of names
 */
function providerDataAsNames(providerData) ***REMOVED***
    return new Set(providerData
        .map((***REMOVED*** providerId }) => providerId)
        .filter(pid => !!pid));
}

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Unlinks a provider from a user account.
 *
 * @param user - The user.
 * @param providerId - The provider to unlink.
 *
 * @public
 */
async function unlink(user, providerId) ***REMOVED***
    const userInternal = getModularInstance(user);
    await _assertLinkedStatus(true, userInternal, providerId);
    const ***REMOVED*** providerUserInfo } = await deleteLinkedAccounts(userInternal.auth, ***REMOVED***
        idToken: await userInternal.getIdToken(),
        deleteProvider: [providerId]
  ***REMOVED***);
    const providersLeft = providerDataAsNames(providerUserInfo || []);
    userInternal.providerData = userInternal.providerData.filter(pd => providersLeft.has(pd.providerId));
    if (!providersLeft.has("phone" /* ProviderId.PHONE */)) ***REMOVED***
        userInternal.phoneNumber = null;
  ***REMOVED***
    await userInternal.auth._persistUserIfCurrent(userInternal);
    return userInternal;
}
async function _link$1(user, credential, bypassAuthState = false) ***REMOVED***
    const response = await _logoutIfInvalidated(user, credential._linkToIdToken(user.auth, await user.getIdToken()), bypassAuthState);
    return UserCredentialImpl._forOperation(user, "link" /* OperationType.LINK */, response);
}
async function _assertLinkedStatus(expected, user, provider) ***REMOVED***
    await _reloadWithoutSaving(user);
    const providerIds = providerDataAsNames(user.providerData);
    const code = expected === false
        ? "provider-already-linked" /* AuthErrorCode.PROVIDER_ALREADY_LINKED */
        : "no-such-provider" /* AuthErrorCode.NO_SUCH_PROVIDER */;
    _assert(providerIds.has(provider) === expected, user.auth, code);
}

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function _reauthenticate(user, credential, bypassAuthState = false) ***REMOVED***
    const ***REMOVED*** auth } = user;
    const operationType = "reauthenticate" /* OperationType.REAUTHENTICATE */;
    try ***REMOVED***
        const response = await _logoutIfInvalidated(user, _processCredentialSavingMfaContextIfNecessary(auth, operationType, credential, user), bypassAuthState);
        _assert(response.idToken, auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        const parsed = _parseToken(response.idToken);
        _assert(parsed, auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        const ***REMOVED*** sub: localId } = parsed;
        _assert(user.uid === localId, auth, "user-mismatch" /* AuthErrorCode.USER_MISMATCH */);
        return UserCredentialImpl._forOperation(user, operationType, response);
  ***REMOVED***
    catch (e) ***REMOVED***
        // Convert user deleted error into user mismatch
        if ((e === null || e === void 0 ? void 0 : e.code) === `auth/$***REMOVED***"user-not-found" /* AuthErrorCode.USER_DELETED */}`) ***REMOVED***
            _fail(auth, "user-mismatch" /* AuthErrorCode.USER_MISMATCH */);
      ***REMOVED***
        throw e;
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function _signInWithCredential(auth, credential, bypassAuthState = false) ***REMOVED***
    const operationType = "signIn" /* OperationType.SIGN_IN */;
    const response = await _processCredentialSavingMfaContextIfNecessary(auth, operationType, credential);
    const userCredential = await UserCredentialImpl._fromIdTokenResponse(auth, operationType, response);
    if (!bypassAuthState) ***REMOVED***
        await auth._updateCurrentUser(userCredential.user);
  ***REMOVED***
    return userCredential;
}
/**
 * Asynchronously signs in with the given credentials.
 *
 * @remarks
 * An ***REMOVED***@link AuthProvider} can be used to generate the credential.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param credential - The auth credential.
 *
 * @public
 */
async function signInWithCredential(auth, credential) ***REMOVED***
    return _signInWithCredential(_castAuth(auth), credential);
}
/**
 * Links the user account with the given credentials.
 *
 * @remarks
 * An ***REMOVED***@link AuthProvider} can be used to generate the credential.
 *
 * @param user - The user.
 * @param credential - The auth credential.
 *
 * @public
 */
async function linkWithCredential(user, credential) ***REMOVED***
    const userInternal = getModularInstance(user);
    await _assertLinkedStatus(false, userInternal, credential.providerId);
    return _link$1(userInternal, credential);
}
/**
 * Re-authenticates a user using a fresh credential.
 *
 * @remarks
 * Use before operations such as ***REMOVED***@link updatePassword} that require tokens from recent sign-in
 * attempts. This method can be used to recover from a `CREDENTIAL_TOO_OLD_LOGIN_AGAIN` error.
 *
 * @param user - The user.
 * @param credential - The auth credential.
 *
 * @public
 */
async function reauthenticateWithCredential(user, credential) ***REMOVED***
    return _reauthenticate(getModularInstance(user), credential);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function signInWithCustomToken$1(auth, request) ***REMOVED***
    return _performSignInRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:signInWithCustomToken" /* Endpoint.SIGN_IN_WITH_CUSTOM_TOKEN */, _addTidIfNecessary(auth, request));
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Asynchronously signs in using a custom token.
 *
 * @remarks
 * Custom tokens are used to integrate Firebase Auth with existing auth systems, and must
 * be generated by an auth backend using the
 * ***REMOVED***@link https://firebase.google.com/docs/reference/admin/node/admin.auth.Auth#createcustomtoken | createCustomToken}
 * method in the ***REMOVED***@link https://firebase.google.com/docs/auth/admin | Admin SDK} .
 *
 * Fails with an error if the token is invalid, expired, or not accepted by the Firebase Auth service.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param customToken - The custom token to sign in with.
 *
 * @public
 */
async function signInWithCustomToken(auth, customToken) ***REMOVED***
    const authInternal = _castAuth(auth);
    const response = await signInWithCustomToken$1(authInternal, ***REMOVED***
        token: customToken,
        returnSecureToken: true
  ***REMOVED***);
    const cred = await UserCredentialImpl._fromIdTokenResponse(authInternal, "signIn" /* OperationType.SIGN_IN */, response);
    await authInternal._updateCurrentUser(cred.user);
    return cred;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class MultiFactorInfoImpl ***REMOVED***
    constructor(factorId, response) ***REMOVED***
        this.factorId = factorId;
        this.uid = response.mfaEnrollmentId;
        this.enrollmentTime = new Date(response.enrolledAt).toUTCString();
        this.displayName = response.displayName;
  ***REMOVED***
    static _fromServerResponse(auth, enrollment) ***REMOVED***
        if ('phoneInfo' in enrollment) ***REMOVED***
            return PhoneMultiFactorInfoImpl._fromServerResponse(auth, enrollment);
      ***REMOVED***
        return _fail(auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
  ***REMOVED***
}
class PhoneMultiFactorInfoImpl extends MultiFactorInfoImpl ***REMOVED***
    constructor(response) ***REMOVED***
        super("phone" /* FactorId.PHONE */, response);
        this.phoneNumber = response.phoneInfo;
  ***REMOVED***
    static _fromServerResponse(_auth, enrollment) ***REMOVED***
        return new PhoneMultiFactorInfoImpl(enrollment);
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function _setActionCodeSettingsOnRequest(auth, request, actionCodeSettings) ***REMOVED***
    var _a;
    _assert(((_a = actionCodeSettings.url) === null || _a === void 0 ? void 0 : _a.length) > 0, auth, "invalid-continue-uri" /* AuthErrorCode.INVALID_CONTINUE_URI */);
    _assert(typeof actionCodeSettings.dynamicLinkDomain === 'undefined' ||
        actionCodeSettings.dynamicLinkDomain.length > 0, auth, "invalid-dynamic-link-domain" /* AuthErrorCode.INVALID_DYNAMIC_LINK_DOMAIN */);
    request.continueUrl = actionCodeSettings.url;
    request.dynamicLinkDomain = actionCodeSettings.dynamicLinkDomain;
    request.canHandleCodeInApp = actionCodeSettings.handleCodeInApp;
    if (actionCodeSettings.iOS) ***REMOVED***
        _assert(actionCodeSettings.iOS.bundleId.length > 0, auth, "missing-ios-bundle-id" /* AuthErrorCode.MISSING_IOS_BUNDLE_ID */);
        request.iOSBundleId = actionCodeSettings.iOS.bundleId;
  ***REMOVED***
    if (actionCodeSettings.android) ***REMOVED***
        _assert(actionCodeSettings.android.packageName.length > 0, auth, "missing-android-pkg-name" /* AuthErrorCode.MISSING_ANDROID_PACKAGE_NAME */);
        request.androidInstallApp = actionCodeSettings.android.installApp;
        request.androidMinimumVersionCode =
            actionCodeSettings.android.minimumVersion;
        request.androidPackageName = actionCodeSettings.android.packageName;
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Sends a password reset email to the given email address.
 *
 * @remarks
 * To complete the password reset, call ***REMOVED***@link confirmPasswordReset} with the code supplied in
 * the email sent to the user, along with the new password specified by the user.
 *
 * @example
 * ```javascript
 * const actionCodeSettings = ***REMOVED***
 *   url: 'https://www.example.com/?email=user@example.com',
 *   iOS: ***REMOVED***
 *      bundleId: 'com.example.ios'
 * ***REMOVED***,
 *   android: ***REMOVED***
 *     packageName: 'com.example.android',
 *     installApp: true,
 *     minimumVersion: '12'
 * ***REMOVED***,
 *   handleCodeInApp: true
 * };
 * await sendPasswordResetEmail(auth, 'user@example.com', actionCodeSettings);
 * // Obtain code from user.
 * await confirmPasswordReset('user@example.com', code);
 * ```
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param email - The user's email address.
 * @param actionCodeSettings - The ***REMOVED***@link ActionCodeSettings}.
 *
 * @public
 */
async function sendPasswordResetEmail(auth, email, actionCodeSettings) ***REMOVED***
    const authModular = getModularInstance(auth);
    const request = ***REMOVED***
        requestType: "PASSWORD_RESET" /* ActionCodeOperation.PASSWORD_RESET */,
        email
  ***REMOVED***;
    if (actionCodeSettings) ***REMOVED***
        _setActionCodeSettingsOnRequest(authModular, request, actionCodeSettings);
  ***REMOVED***
    await sendPasswordResetEmail$1(authModular, request);
}
/**
 * Completes the password reset process, given a confirmation code and new password.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param oobCode - A confirmation code sent to the user.
 * @param newPassword - The new password.
 *
 * @public
 */
async function confirmPasswordReset(auth, oobCode, newPassword) ***REMOVED***
    await resetPassword(getModularInstance(auth), ***REMOVED***
        oobCode,
        newPassword
  ***REMOVED***);
    // Do not return the email.
}
/**
 * Applies a verification code sent to the user by email or other out-of-band mechanism.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param oobCode - A verification code sent to the user.
 *
 * @public
 */
async function applyActionCode(auth, oobCode) ***REMOVED***
    await applyActionCode$1(getModularInstance(auth), ***REMOVED*** oobCode });
}
/**
 * Checks a verification code sent to the user by email or other out-of-band mechanism.
 *
 * @returns metadata about the code.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param oobCode - A verification code sent to the user.
 *
 * @public
 */
async function checkActionCode(auth, oobCode) ***REMOVED***
    const authModular = getModularInstance(auth);
    const response = await resetPassword(authModular, ***REMOVED*** oobCode });
    // Email could be empty only if the request type is EMAIL_SIGNIN or
    // VERIFY_AND_CHANGE_EMAIL.
    // New email should not be empty if the request type is
    // VERIFY_AND_CHANGE_EMAIL.
    // Multi-factor info could not be empty if the request type is
    // REVERT_SECOND_FACTOR_ADDITION.
    const operation = response.requestType;
    _assert(operation, authModular, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
    switch (operation) ***REMOVED***
        case "EMAIL_SIGNIN" /* ActionCodeOperation.EMAIL_SIGNIN */:
            break;
        case "VERIFY_AND_CHANGE_EMAIL" /* ActionCodeOperation.VERIFY_AND_CHANGE_EMAIL */:
            _assert(response.newEmail, authModular, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
            break;
        case "REVERT_SECOND_FACTOR_ADDITION" /* ActionCodeOperation.REVERT_SECOND_FACTOR_ADDITION */:
            _assert(response.mfaInfo, authModular, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        // fall through
        default:
            _assert(response.email, authModular, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
  ***REMOVED***
    // The multi-factor info for revert second factor addition
    let multiFactorInfo = null;
    if (response.mfaInfo) ***REMOVED***
        multiFactorInfo = MultiFactorInfoImpl._fromServerResponse(_castAuth(authModular), response.mfaInfo);
  ***REMOVED***
    return ***REMOVED***
        data: ***REMOVED***
            email: (response.requestType === "VERIFY_AND_CHANGE_EMAIL" /* ActionCodeOperation.VERIFY_AND_CHANGE_EMAIL */
                ? response.newEmail
                : response.email) || null,
            previousEmail: (response.requestType === "VERIFY_AND_CHANGE_EMAIL" /* ActionCodeOperation.VERIFY_AND_CHANGE_EMAIL */
                ? response.email
                : response.newEmail) || null,
            multiFactorInfo
      ***REMOVED***,
        operation
  ***REMOVED***;
}
/**
 * Checks a password reset code sent to the user by email or other out-of-band mechanism.
 *
 * @returns the user's email address if valid.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param code - A verification code sent to the user.
 *
 * @public
 */
async function verifyPasswordResetCode(auth, code) ***REMOVED***
    const ***REMOVED*** data } = await checkActionCode(getModularInstance(auth), code);
    // Email should always be present since a code was sent to it
    return data.email;
}
/**
 * Creates a new user account associated with the specified email address and password.
 *
 * @remarks
 * On successful creation of the user account, this user will also be signed in to your application.
 *
 * User account creation can fail if the account already exists or the password is invalid.
 *
 * Note: The email address acts as a unique identifier for the user and enables an email-based
 * password reset. This function will create a new user account and set the initial user password.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param email - The user's email address.
 * @param password - The user's chosen password.
 *
 * @public
 */
async function createUserWithEmailAndPassword(auth, email, password) ***REMOVED***
    const authInternal = _castAuth(auth);
    const response = await signUp(authInternal, ***REMOVED***
        returnSecureToken: true,
        email,
        password
  ***REMOVED***);
    const userCredential = await UserCredentialImpl._fromIdTokenResponse(authInternal, "signIn" /* OperationType.SIGN_IN */, response);
    await authInternal._updateCurrentUser(userCredential.user);
    return userCredential;
}
/**
 * Asynchronously signs in using an email and password.
 *
 * @remarks
 * Fails with an error if the email address and password do not match.
 *
 * Note: The user's password is NOT the password used to access the user's email account. The
 * email address serves as a unique identifier for the user, and the password is used to access
 * the user's account in your Firebase project. See also: ***REMOVED***@link createUserWithEmailAndPassword}.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param email - The users email address.
 * @param password - The users password.
 *
 * @public
 */
function signInWithEmailAndPassword(auth, email, password) ***REMOVED***
    return signInWithCredential(getModularInstance(auth), EmailAuthProvider.credential(email, password));
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Sends a sign-in email link to the user with the specified email.
 *
 * @remarks
 * The sign-in operation has to always be completed in the app unlike other out of band email
 * actions (password reset and email verifications). This is because, at the end of the flow,
 * the user is expected to be signed in and their Auth state persisted within the app.
 *
 * To complete sign in with the email link, call ***REMOVED***@link signInWithEmailLink} with the email
 * address and the email link supplied in the email sent to the user.
 *
 * @example
 * ```javascript
 * const actionCodeSettings = ***REMOVED***
 *   url: 'https://www.example.com/?email=user@example.com',
 *   iOS: ***REMOVED***
 *      bundleId: 'com.example.ios'
 * ***REMOVED***,
 *   android: ***REMOVED***
 *     packageName: 'com.example.android',
 *     installApp: true,
 *     minimumVersion: '12'
 * ***REMOVED***,
 *   handleCodeInApp: true
 * };
 * await sendSignInLinkToEmail(auth, 'user@example.com', actionCodeSettings);
 * // Obtain emailLink from the user.
 * if(isSignInWithEmailLink(auth, emailLink)) ***REMOVED***
 *   await signInWithEmailLink(auth, 'user@example.com', emailLink);
 * }
 * ```
 *
 * @param authInternal - The ***REMOVED***@link Auth} instance.
 * @param email - The user's email address.
 * @param actionCodeSettings - The ***REMOVED***@link ActionCodeSettings}.
 *
 * @public
 */
async function sendSignInLinkToEmail(auth, email, actionCodeSettings) ***REMOVED***
    const authModular = getModularInstance(auth);
    const request = ***REMOVED***
        requestType: "EMAIL_SIGNIN" /* ActionCodeOperation.EMAIL_SIGNIN */,
        email
  ***REMOVED***;
    _assert(actionCodeSettings.handleCodeInApp, authModular, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
    if (actionCodeSettings) ***REMOVED***
        _setActionCodeSettingsOnRequest(authModular, request, actionCodeSettings);
  ***REMOVED***
    await sendSignInLinkToEmail$1(authModular, request);
}
/**
 * Checks if an incoming link is a sign-in with email link suitable for ***REMOVED***@link signInWithEmailLink}.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param emailLink - The link sent to the user's email address.
 *
 * @public
 */
function isSignInWithEmailLink(auth, emailLink) ***REMOVED***
    const actionCodeUrl = ActionCodeURL.parseLink(emailLink);
    return (actionCodeUrl === null || actionCodeUrl === void 0 ? void 0 : actionCodeUrl.operation) === "EMAIL_SIGNIN" /* ActionCodeOperation.EMAIL_SIGNIN */;
}
/**
 * Asynchronously signs in using an email and sign-in email link.
 *
 * @remarks
 * If no link is passed, the link is inferred from the current URL.
 *
 * Fails with an error if the email address is invalid or OTP in email link expires.
 *
 * Note: Confirm the link is a sign-in email link before calling this method firebase.auth.Auth.isSignInWithEmailLink.
 *
 * @example
 * ```javascript
 * const actionCodeSettings = ***REMOVED***
 *   url: 'https://www.example.com/?email=user@example.com',
 *   iOS: ***REMOVED***
 *      bundleId: 'com.example.ios'
 * ***REMOVED***,
 *   android: ***REMOVED***
 *     packageName: 'com.example.android',
 *     installApp: true,
 *     minimumVersion: '12'
 * ***REMOVED***,
 *   handleCodeInApp: true
 * };
 * await sendSignInLinkToEmail(auth, 'user@example.com', actionCodeSettings);
 * // Obtain emailLink from the user.
 * if(isSignInWithEmailLink(auth, emailLink)) ***REMOVED***
 *   await signInWithEmailLink(auth, 'user@example.com', emailLink);
 * }
 * ```
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param email - The user's email address.
 * @param emailLink - The link sent to the user's email address.
 *
 * @public
 */
async function signInWithEmailLink(auth, email, emailLink) ***REMOVED***
    const authModular = getModularInstance(auth);
    const credential = EmailAuthProvider.credentialWithLink(email, emailLink || _getCurrentUrl());
    // Check if the tenant ID in the email link matches the tenant ID on Auth
    // instance.
    _assert(credential._tenantId === (authModular.tenantId || null), authModular, "tenant-id-mismatch" /* AuthErrorCode.TENANT_ID_MISMATCH */);
    return signInWithCredential(authModular, credential);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function createAuthUri(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:createAuthUri" /* Endpoint.CREATE_AUTH_URI */, _addTidIfNecessary(auth, request));
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Gets the list of possible sign in methods for the given email address.
 *
 * @remarks
 * This is useful to differentiate methods of sign-in for the same provider, eg.
 * ***REMOVED***@link EmailAuthProvider} which has 2 methods of sign-in,
 * ***REMOVED***@link SignInMethod}.EMAIL_PASSWORD and
 * ***REMOVED***@link SignInMethod}.EMAIL_LINK.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param email - The user's email address.
 *
 * @public
 */
async function fetchSignInMethodsForEmail(auth, email) ***REMOVED***
    // createAuthUri returns an error if continue URI is not http or https.
    // For environments like Cordova, Chrome extensions, native frameworks, file
    // systems, etc, use http://localhost as continue URL.
    const continueUri = _isHttpOrHttps() ? _getCurrentUrl() : 'http://localhost';
    const request = ***REMOVED***
        identifier: email,
        continueUri
  ***REMOVED***;
    const ***REMOVED*** signinMethods } = await createAuthUri(getModularInstance(auth), request);
    return signinMethods || [];
}
/**
 * Sends a verification email to a user.
 *
 * @remarks
 * The verification process is completed by calling ***REMOVED***@link applyActionCode}.
 *
 * @example
 * ```javascript
 * const actionCodeSettings = ***REMOVED***
 *   url: 'https://www.example.com/?email=user@example.com',
 *   iOS: ***REMOVED***
 *      bundleId: 'com.example.ios'
 * ***REMOVED***,
 *   android: ***REMOVED***
 *     packageName: 'com.example.android',
 *     installApp: true,
 *     minimumVersion: '12'
 * ***REMOVED***,
 *   handleCodeInApp: true
 * };
 * await sendEmailVerification(user, actionCodeSettings);
 * // Obtain code from the user.
 * await applyActionCode(auth, code);
 * ```
 *
 * @param user - The user.
 * @param actionCodeSettings - The ***REMOVED***@link ActionCodeSettings}.
 *
 * @public
 */
async function sendEmailVerification(user, actionCodeSettings) ***REMOVED***
    const userInternal = getModularInstance(user);
    const idToken = await user.getIdToken();
    const request = ***REMOVED***
        requestType: "VERIFY_EMAIL" /* ActionCodeOperation.VERIFY_EMAIL */,
        idToken
  ***REMOVED***;
    if (actionCodeSettings) ***REMOVED***
        _setActionCodeSettingsOnRequest(userInternal.auth, request, actionCodeSettings);
  ***REMOVED***
    const ***REMOVED*** email } = await sendEmailVerification$1(userInternal.auth, request);
    if (email !== user.email) ***REMOVED***
        await user.reload();
  ***REMOVED***
}
/**
 * Sends a verification email to a new email address.
 *
 * @remarks
 * The user's email will be updated to the new one after being verified.
 *
 * If you have a custom email action handler, you can complete the verification process by calling
 * ***REMOVED***@link applyActionCode}.
 *
 * @example
 * ```javascript
 * const actionCodeSettings = ***REMOVED***
 *   url: 'https://www.example.com/?email=user@example.com',
 *   iOS: ***REMOVED***
 *      bundleId: 'com.example.ios'
 * ***REMOVED***,
 *   android: ***REMOVED***
 *     packageName: 'com.example.android',
 *     installApp: true,
 *     minimumVersion: '12'
 * ***REMOVED***,
 *   handleCodeInApp: true
 * };
 * await verifyBeforeUpdateEmail(user, 'newemail@example.com', actionCodeSettings);
 * // Obtain code from the user.
 * await applyActionCode(auth, code);
 * ```
 *
 * @param user - The user.
 * @param newEmail - The new email address to be verified before update.
 * @param actionCodeSettings - The ***REMOVED***@link ActionCodeSettings}.
 *
 * @public
 */
async function verifyBeforeUpdateEmail(user, newEmail, actionCodeSettings) ***REMOVED***
    const userInternal = getModularInstance(user);
    const idToken = await user.getIdToken();
    const request = ***REMOVED***
        requestType: "VERIFY_AND_CHANGE_EMAIL" /* ActionCodeOperation.VERIFY_AND_CHANGE_EMAIL */,
        idToken,
        newEmail
  ***REMOVED***;
    if (actionCodeSettings) ***REMOVED***
        _setActionCodeSettingsOnRequest(userInternal.auth, request, actionCodeSettings);
  ***REMOVED***
    const ***REMOVED*** email } = await verifyAndChangeEmail(userInternal.auth, request);
    if (email !== user.email) ***REMOVED***
        // If the local copy of the email on user is outdated, reload the
        // user.
        await user.reload();
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function updateProfile$1(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v1/accounts:update" /* Endpoint.SET_ACCOUNT_INFO */, request);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Updates a user's profile data.
 *
 * @param user - The user.
 * @param profile - The profile's `displayName` and `photoURL` to update.
 *
 * @public
 */
async function updateProfile(user, ***REMOVED*** displayName, photoURL: photoUrl }) ***REMOVED***
    if (displayName === undefined && photoUrl === undefined) ***REMOVED***
        return;
  ***REMOVED***
    const userInternal = getModularInstance(user);
    const idToken = await userInternal.getIdToken();
    const profileRequest = ***REMOVED***
        idToken,
        displayName,
        photoUrl,
        returnSecureToken: true
  ***REMOVED***;
    const response = await _logoutIfInvalidated(userInternal, updateProfile$1(userInternal.auth, profileRequest));
    userInternal.displayName = response.displayName || null;
    userInternal.photoURL = response.photoUrl || null;
    // Update the password provider as well
    const passwordProvider = userInternal.providerData.find((***REMOVED*** providerId }) => providerId === "password" /* ProviderId.PASSWORD */);
    if (passwordProvider) ***REMOVED***
        passwordProvider.displayName = userInternal.displayName;
        passwordProvider.photoURL = userInternal.photoURL;
  ***REMOVED***
    await userInternal._updateTokensIfNecessary(response);
}
/**
 * Updates the user's email address.
 *
 * @remarks
 * An email will be sent to the original email address (if it was set) that allows to revoke the
 * email address change, in order to protect them from account hijacking.
 *
 * Important: this is a security sensitive operation that requires the user to have recently signed
 * in. If this requirement isn't met, ask the user to authenticate again and then call
 * ***REMOVED***@link reauthenticateWithCredential}.
 *
 * @param user - The user.
 * @param newEmail - The new email address.
 *
 * @public
 */
function updateEmail(user, newEmail) ***REMOVED***
    return updateEmailOrPassword(getModularInstance(user), newEmail, null);
}
/**
 * Updates the user's password.
 *
 * @remarks
 * Important: this is a security sensitive operation that requires the user to have recently signed
 * in. If this requirement isn't met, ask the user to authenticate again and then call
 * ***REMOVED***@link reauthenticateWithCredential}.
 *
 * @param user - The user.
 * @param newPassword - The new password.
 *
 * @public
 */
function updatePassword(user, newPassword) ***REMOVED***
    return updateEmailOrPassword(getModularInstance(user), null, newPassword);
}
async function updateEmailOrPassword(user, email, password) ***REMOVED***
    const ***REMOVED*** auth } = user;
    const idToken = await user.getIdToken();
    const request = ***REMOVED***
        idToken,
        returnSecureToken: true
  ***REMOVED***;
    if (email) ***REMOVED***
        request.email = email;
  ***REMOVED***
    if (password) ***REMOVED***
        request.password = password;
  ***REMOVED***
    const response = await _logoutIfInvalidated(user, updateEmailPassword(auth, request));
    await user._updateTokensIfNecessary(response, /* reload */ true);
}

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Parse the `AdditionalUserInfo` from the ID token response.
 *
 */
function _fromIdTokenResponse(idTokenResponse) ***REMOVED***
    var _a, _b;
    if (!idTokenResponse) ***REMOVED***
        return null;
  ***REMOVED***
    const ***REMOVED*** providerId } = idTokenResponse;
    const profile = idTokenResponse.rawUserInfo
        ? JSON.parse(idTokenResponse.rawUserInfo)
        : ***REMOVED***};
    const isNewUser = idTokenResponse.isNewUser ||
        idTokenResponse.kind === "identitytoolkit#SignupNewUserResponse" /* IdTokenResponseKind.SignupNewUser */;
    if (!providerId && (idTokenResponse === null || idTokenResponse === void 0 ? void 0 : idTokenResponse.idToken)) ***REMOVED***
        const signInProvider = (_b = (_a = _parseToken(idTokenResponse.idToken)) === null || _a === void 0 ? void 0 : _a.firebase) === null || _b === void 0 ? void 0 : _b['sign_in_provider'];
        if (signInProvider) ***REMOVED***
            const filteredProviderId = signInProvider !== "anonymous" /* ProviderId.ANONYMOUS */ &&
                signInProvider !== "custom" /* ProviderId.CUSTOM */
                ? signInProvider
                : null;
            // Uses generic class in accordance with the legacy SDK.
            return new GenericAdditionalUserInfo(isNewUser, filteredProviderId);
      ***REMOVED***
  ***REMOVED***
    if (!providerId) ***REMOVED***
        return null;
  ***REMOVED***
    switch (providerId) ***REMOVED***
        case "facebook.com" /* ProviderId.FACEBOOK */:
            return new FacebookAdditionalUserInfo(isNewUser, profile);
        case "github.com" /* ProviderId.GITHUB */:
            return new GithubAdditionalUserInfo(isNewUser, profile);
        case "google.com" /* ProviderId.GOOGLE */:
            return new GoogleAdditionalUserInfo(isNewUser, profile);
        case "twitter.com" /* ProviderId.TWITTER */:
            return new TwitterAdditionalUserInfo(isNewUser, profile, idTokenResponse.screenName || null);
        case "custom" /* ProviderId.CUSTOM */:
        case "anonymous" /* ProviderId.ANONYMOUS */:
            return new GenericAdditionalUserInfo(isNewUser, null);
        default:
            return new GenericAdditionalUserInfo(isNewUser, providerId, profile);
  ***REMOVED***
}
class GenericAdditionalUserInfo ***REMOVED***
    constructor(isNewUser, providerId, profile = ***REMOVED***}) ***REMOVED***
        this.isNewUser = isNewUser;
        this.providerId = providerId;
        this.profile = profile;
  ***REMOVED***
}
class FederatedAdditionalUserInfoWithUsername extends GenericAdditionalUserInfo ***REMOVED***
    constructor(isNewUser, providerId, profile, username) ***REMOVED***
        super(isNewUser, providerId, profile);
        this.username = username;
  ***REMOVED***
}
class FacebookAdditionalUserInfo extends GenericAdditionalUserInfo ***REMOVED***
    constructor(isNewUser, profile) ***REMOVED***
        super(isNewUser, "facebook.com" /* ProviderId.FACEBOOK */, profile);
  ***REMOVED***
}
class GithubAdditionalUserInfo extends FederatedAdditionalUserInfoWithUsername ***REMOVED***
    constructor(isNewUser, profile) ***REMOVED***
        super(isNewUser, "github.com" /* ProviderId.GITHUB */, profile, typeof (profile === null || profile === void 0 ? void 0 : profile.login) === 'string' ? profile === null || profile === void 0 ? void 0 : profile.login : null);
  ***REMOVED***
}
class GoogleAdditionalUserInfo extends GenericAdditionalUserInfo ***REMOVED***
    constructor(isNewUser, profile) ***REMOVED***
        super(isNewUser, "google.com" /* ProviderId.GOOGLE */, profile);
  ***REMOVED***
}
class TwitterAdditionalUserInfo extends FederatedAdditionalUserInfoWithUsername ***REMOVED***
    constructor(isNewUser, profile, screenName) ***REMOVED***
        super(isNewUser, "twitter.com" /* ProviderId.TWITTER */, profile, screenName);
  ***REMOVED***
}
/**
 * Extracts provider specific ***REMOVED***@link AdditionalUserInfo} for the given credential.
 *
 * @param userCredential - The user credential.
 *
 * @public
 */
function getAdditionalUserInfo(userCredential) ***REMOVED***
    const ***REMOVED*** user, _tokenResponse } = userCredential;
    if (user.isAnonymous && !_tokenResponse) ***REMOVED***
        // Handle the special case where signInAnonymously() gets called twice.
        // No network call is made so there's nothing to actually fill this in
        return ***REMOVED***
            providerId: null,
            isNewUser: false,
            profile: null
      ***REMOVED***;
  ***REMOVED***
    return _fromIdTokenResponse(_tokenResponse);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// Non-optional auth methods.
/**
 * Changes the type of persistence on the ***REMOVED***@link Auth} instance for the currently saved
 * `Auth` session and applies this type of persistence for future sign-in requests, including
 * sign-in with redirect requests.
 *
 * @remarks
 * This makes it easy for a user signing in to specify whether their session should be
 * remembered or not. It also makes it easier to never persist the `Auth` state for applications
 * that are shared by other users or have sensitive data.
 *
 * @example
 * ```javascript
 * setPersistence(auth, browserSessionPersistence);
 * ```
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param persistence - The ***REMOVED***@link Persistence} to use.
 * @returns A `Promise` that resolves once the persistence change has completed
 *
 * @public
 */
function setPersistence(auth, persistence) ***REMOVED***
    return getModularInstance(auth).setPersistence(persistence);
}
/**
 * Adds an observer for changes to the signed-in user's ID token.
 *
 * @remarks
 * This includes sign-in, sign-out, and token refresh events.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param nextOrObserver - callback triggered on change.
 * @param error - Deprecated. This callback is never triggered. Errors
 * on signing in/out can be caught in promises returned from
 * sign-in/sign-out functions.
 * @param completed - Deprecated. This callback is never triggered.
 *
 * @public
 */
function onIdTokenChanged(auth, nextOrObserver, error, completed) ***REMOVED***
    return getModularInstance(auth).onIdTokenChanged(nextOrObserver, error, completed);
}
/**
 * Adds a blocking callback that runs before an auth state change
 * sets a new user.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param callback - callback triggered before new user value is set.
 *   If this throws, it blocks the user from being set.
 * @param onAbort - callback triggered if a later `beforeAuthStateChanged()`
 *   callback throws, allowing you to undo any side effects.
 */
function beforeAuthStateChanged(auth, callback, onAbort) ***REMOVED***
    return getModularInstance(auth).beforeAuthStateChanged(callback, onAbort);
}
/**
 * Adds an observer for changes to the user's sign-in state.
 *
 * @remarks
 * To keep the old behavior, see ***REMOVED***@link onIdTokenChanged}.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param nextOrObserver - callback triggered on change.
 * @param error - Deprecated. This callback is never triggered. Errors
 * on signing in/out can be caught in promises returned from
 * sign-in/sign-out functions.
 * @param completed - Deprecated. This callback is never triggered.
 *
 * @public
 */
function onAuthStateChanged(auth, nextOrObserver, error, completed) ***REMOVED***
    return getModularInstance(auth).onAuthStateChanged(nextOrObserver, error, completed);
}
/**
 * Sets the current language to the default device/browser preference.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 *
 * @public
 */
function useDeviceLanguage(auth) ***REMOVED***
    getModularInstance(auth).useDeviceLanguage();
}
/**
 * Asynchronously sets the provided user as ***REMOVED***@link Auth.currentUser} on the
 * ***REMOVED***@link Auth} instance.
 *
 * @remarks
 * A new instance copy of the user provided will be made and set as currentUser.
 *
 * This will trigger ***REMOVED***@link onAuthStateChanged} and ***REMOVED***@link onIdTokenChanged} listeners
 * like other sign in methods.
 *
 * The operation fails with an error if the user to be updated belongs to a different Firebase
 * project.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param user - The new ***REMOVED***@link User}.
 *
 * @public
 */
function updateCurrentUser(auth, user) ***REMOVED***
    return getModularInstance(auth).updateCurrentUser(user);
}
/**
 * Signs out the current user.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 *
 * @public
 */
function signOut(auth) ***REMOVED***
    return getModularInstance(auth).signOut();
}
/**
 * Deletes and signs out the user.
 *
 * @remarks
 * Important: this is a security-sensitive operation that requires the user to have recently
 * signed in. If this requirement isn't met, ask the user to authenticate again and then call
 * ***REMOVED***@link reauthenticateWithCredential}.
 *
 * @param user - The user.
 *
 * @public
 */
async function deleteUser(user) ***REMOVED***
    return getModularInstance(user).delete();
}

class MultiFactorSessionImpl ***REMOVED***
    constructor(type, credential, auth) ***REMOVED***
        this.type = type;
        this.credential = credential;
        this.auth = auth;
  ***REMOVED***
    static _fromIdtoken(idToken, auth) ***REMOVED***
        return new MultiFactorSessionImpl("enroll" /* MultiFactorSessionType.ENROLL */, idToken, auth);
  ***REMOVED***
    static _fromMfaPendingCredential(mfaPendingCredential) ***REMOVED***
        return new MultiFactorSessionImpl("signin" /* MultiFactorSessionType.SIGN_IN */, mfaPendingCredential);
  ***REMOVED***
    toJSON() ***REMOVED***
        const key = this.type === "enroll" /* MultiFactorSessionType.ENROLL */
            ? 'idToken'
            : 'pendingCredential';
        return ***REMOVED***
            multiFactorSession: ***REMOVED***
                [key]: this.credential
          ***REMOVED***
      ***REMOVED***;
  ***REMOVED***
    static fromJSON(obj) ***REMOVED***
        var _a, _b;
        if (obj === null || obj === void 0 ? void 0 : obj.multiFactorSession) ***REMOVED***
            if ((_a = obj.multiFactorSession) === null || _a === void 0 ? void 0 : _a.pendingCredential) ***REMOVED***
                return MultiFactorSessionImpl._fromMfaPendingCredential(obj.multiFactorSession.pendingCredential);
          ***REMOVED***
            else if ((_b = obj.multiFactorSession) === null || _b === void 0 ? void 0 : _b.idToken) ***REMOVED***
                return MultiFactorSessionImpl._fromIdtoken(obj.multiFactorSession.idToken);
          ***REMOVED***
      ***REMOVED***
        return null;
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class MultiFactorResolverImpl ***REMOVED***
    constructor(session, hints, signInResolver) ***REMOVED***
        this.session = session;
        this.hints = hints;
        this.signInResolver = signInResolver;
  ***REMOVED***
    /** @internal */
    static _fromError(authExtern, error) ***REMOVED***
        const auth = _castAuth(authExtern);
        const serverResponse = error.customData._serverResponse;
        const hints = (serverResponse.mfaInfo || []).map(enrollment => MultiFactorInfoImpl._fromServerResponse(auth, enrollment));
        _assert(serverResponse.mfaPendingCredential, auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        const session = MultiFactorSessionImpl._fromMfaPendingCredential(serverResponse.mfaPendingCredential);
        return new MultiFactorResolverImpl(session, hints, async (assertion) => ***REMOVED***
            const mfaResponse = await assertion._process(auth, session);
            // Clear out the unneeded fields from the old login response
            delete serverResponse.mfaInfo;
            delete serverResponse.mfaPendingCredential;
            // Use in the new token & refresh token in the old response
            const idTokenResponse = Object.assign(Object.assign(***REMOVED***}, serverResponse), ***REMOVED*** idToken: mfaResponse.idToken, refreshToken: mfaResponse.refreshToken });
            // TODO: we should collapse this switch statement into UserCredentialImpl._forOperation and have it support the SIGN_IN case
            switch (error.operationType) ***REMOVED***
                case "signIn" /* OperationType.SIGN_IN */:
                    const userCredential = await UserCredentialImpl._fromIdTokenResponse(auth, error.operationType, idTokenResponse);
                    await auth._updateCurrentUser(userCredential.user);
                    return userCredential;
                case "reauthenticate" /* OperationType.REAUTHENTICATE */:
                    _assert(error.user, auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
                    return UserCredentialImpl._forOperation(error.user, error.operationType, idTokenResponse);
                default:
                    _fail(auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
          ***REMOVED***
      ***REMOVED***);
  ***REMOVED***
    async resolveSignIn(assertionExtern) ***REMOVED***
        const assertion = assertionExtern;
        return this.signInResolver(assertion);
  ***REMOVED***
}
/**
 * Provides a ***REMOVED***@link MultiFactorResolver} suitable for completion of a
 * multi-factor flow.
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param error - The ***REMOVED***@link MultiFactorError} raised during a sign-in, or
 * reauthentication operation.
 *
 * @public
 */
function getMultiFactorResolver(auth, error) ***REMOVED***
    var _a;
    const authModular = getModularInstance(auth);
    const errorInternal = error;
    _assert(error.customData.operationType, authModular, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
    _assert((_a = errorInternal.customData._serverResponse) === null || _a === void 0 ? void 0 : _a.mfaPendingCredential, authModular, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
    return MultiFactorResolverImpl._fromError(authModular, errorInternal);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function startEnrollPhoneMfa(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v2/accounts/mfaEnrollment:start" /* Endpoint.START_MFA_ENROLLMENT */, _addTidIfNecessary(auth, request));
}
function finalizeEnrollPhoneMfa(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v2/accounts/mfaEnrollment:finalize" /* Endpoint.FINALIZE_MFA_ENROLLMENT */, _addTidIfNecessary(auth, request));
}
function withdrawMfa(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v2/accounts/mfaEnrollment:withdraw" /* Endpoint.WITHDRAW_MFA */, _addTidIfNecessary(auth, request));
}

class MultiFactorUserImpl ***REMOVED***
    constructor(user) ***REMOVED***
        this.user = user;
        this.enrolledFactors = [];
        user._onReload(userInfo => ***REMOVED***
            if (userInfo.mfaInfo) ***REMOVED***
                this.enrolledFactors = userInfo.mfaInfo.map(enrollment => MultiFactorInfoImpl._fromServerResponse(user.auth, enrollment));
          ***REMOVED***
      ***REMOVED***);
  ***REMOVED***
    static _fromUser(user) ***REMOVED***
        return new MultiFactorUserImpl(user);
  ***REMOVED***
    async getSession() ***REMOVED***
        return MultiFactorSessionImpl._fromIdtoken(await this.user.getIdToken(), this.user.auth);
  ***REMOVED***
    async enroll(assertionExtern, displayName) ***REMOVED***
        const assertion = assertionExtern;
        const session = (await this.getSession());
        const finalizeMfaResponse = await _logoutIfInvalidated(this.user, assertion._process(this.user.auth, session, displayName));
        // New tokens will be issued after enrollment of the new second factors.
        // They need to be updated on the user.
        await this.user._updateTokensIfNecessary(finalizeMfaResponse);
        // The user needs to be reloaded to get the new multi-factor information
        // from server. USER_RELOADED event will be triggered and `enrolledFactors`
        // will be updated.
        return this.user.reload();
  ***REMOVED***
    async unenroll(infoOrUid) ***REMOVED***
        const mfaEnrollmentId = typeof infoOrUid === 'string' ? infoOrUid : infoOrUid.uid;
        const idToken = await this.user.getIdToken();
        const idTokenResponse = await _logoutIfInvalidated(this.user, withdrawMfa(this.user.auth, ***REMOVED***
            idToken,
            mfaEnrollmentId
      ***REMOVED***));
        // Remove the second factor from the user's list.
        this.enrolledFactors = this.enrolledFactors.filter((***REMOVED*** uid }) => uid !== mfaEnrollmentId);
        // Depending on whether the backend decided to revoke the user's session,
        // the tokenResponse may be empty. If the tokens were not updated (and they
        // are now invalid), reloading the user will discover this and invalidate
        // the user's state accordingly.
        await this.user._updateTokensIfNecessary(idTokenResponse);
        try ***REMOVED***
            await this.user.reload();
      ***REMOVED***
        catch (e) ***REMOVED***
            if ((e === null || e === void 0 ? void 0 : e.code) !== `auth/$***REMOVED***"user-token-expired" /* AuthErrorCode.TOKEN_EXPIRED */}`) ***REMOVED***
                throw e;
          ***REMOVED***
      ***REMOVED***
  ***REMOVED***
}
const multiFactorUserCache = new WeakMap();
/**
 * The ***REMOVED***@link MultiFactorUser} corresponding to the user.
 *
 * @remarks
 * This is used to access all multi-factor properties and operations related to the user.
 *
 * @param user - The user.
 *
 * @public
 */
function multiFactor(user) ***REMOVED***
    const userModular = getModularInstance(user);
    if (!multiFactorUserCache.has(userModular)) ***REMOVED***
        multiFactorUserCache.set(userModular, MultiFactorUserImpl._fromUser(userModular));
  ***REMOVED***
    return multiFactorUserCache.get(userModular);
}

const STORAGE_AVAILABLE_KEY = '__sak';

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// There are two different browser persistence types: local and session.
// Both have the same implementation but use a different underlying storage
// object.
class BrowserPersistenceClass ***REMOVED***
    constructor(storageRetriever, type) ***REMOVED***
        this.storageRetriever = storageRetriever;
        this.type = type;
  ***REMOVED***
    _isAvailable() ***REMOVED***
        try ***REMOVED***
            if (!this.storage) ***REMOVED***
                return Promise.resolve(false);
          ***REMOVED***
            this.storage.setItem(STORAGE_AVAILABLE_KEY, '1');
            this.storage.removeItem(STORAGE_AVAILABLE_KEY);
            return Promise.resolve(true);
      ***REMOVED***
        catch (_a) ***REMOVED***
            return Promise.resolve(false);
      ***REMOVED***
  ***REMOVED***
    _set(key, value) ***REMOVED***
        this.storage.setItem(key, JSON.stringify(value));
        return Promise.resolve();
  ***REMOVED***
    _get(key) ***REMOVED***
        const json = this.storage.getItem(key);
        return Promise.resolve(json ? JSON.parse(json) : null);
  ***REMOVED***
    _remove(key) ***REMOVED***
        this.storage.removeItem(key);
        return Promise.resolve();
  ***REMOVED***
    get storage() ***REMOVED***
        return this.storageRetriever();
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function _iframeCannotSyncWebStorage() ***REMOVED***
    const ua = getUA();
    return _isSafari(ua) || _isIOS(ua);
}
// The polling period in case events are not supported
const _POLLING_INTERVAL_MS$1 = 1000;
// The IE 10 localStorage cross tab synchronization delay in milliseconds
const IE10_LOCAL_STORAGE_SYNC_DELAY = 10;
class BrowserLocalPersistence extends BrowserPersistenceClass ***REMOVED***
    constructor() ***REMOVED***
        super(() => window.localStorage, "LOCAL" /* PersistenceType.LOCAL */);
        this.boundEventHandler = (event, poll) => this.onStorageEvent(event, poll);
        this.listeners = ***REMOVED***};
        this.localCache = ***REMOVED***};
        // setTimeout return value is platform specific
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        this.pollTimer = null;
        // Safari or iOS browser and embedded in an iframe.
        this.safariLocalStorageNotSynced = _iframeCannotSyncWebStorage() && _isIframe();
        // Whether to use polling instead of depending on window events
        this.fallbackToPolling = _isMobileBrowser();
        this._shouldAllowMigration = true;
  ***REMOVED***
    forAllChangedKeys(cb) ***REMOVED***
        // Check all keys with listeners on them.
        for (const key of Object.keys(this.listeners)) ***REMOVED***
            // Get value from localStorage.
            const newValue = this.storage.getItem(key);
            const oldValue = this.localCache[key];
            // If local map value does not match, trigger listener with storage event.
            // Differentiate this simulated event from the real storage event.
            if (newValue !== oldValue) ***REMOVED***
                cb(key, oldValue, newValue);
          ***REMOVED***
      ***REMOVED***
  ***REMOVED***
    onStorageEvent(event, poll = false) ***REMOVED***
        // Key would be null in some situations, like when localStorage is cleared
        if (!event.key) ***REMOVED***
            this.forAllChangedKeys((key, _oldValue, newValue) => ***REMOVED***
                this.notifyListeners(key, newValue);
          ***REMOVED***);
            return;
      ***REMOVED***
        const key = event.key;
        // Check the mechanism how this event was detected.
        // The first event will dictate the mechanism to be used.
        if (poll) ***REMOVED***
            // Environment detects storage changes via polling.
            // Remove storage event listener to prevent possible event duplication.
            this.detachListener();
      ***REMOVED***
        else ***REMOVED***
            // Environment detects storage changes via storage event listener.
            // Remove polling listener to prevent possible event duplication.
            this.stopPolling();
      ***REMOVED***
        // Safari embedded iframe. Storage event will trigger with the delta
        // changes but no changes will be applied to the iframe localStorage.
        if (this.safariLocalStorageNotSynced) ***REMOVED***
            // Get current iframe page value.
            const storedValue = this.storage.getItem(key);
            // Value not synchronized, synchronize manually.
            if (event.newValue !== storedValue) ***REMOVED***
                if (event.newValue !== null) ***REMOVED***
                    // Value changed from current value.
                    this.storage.setItem(key, event.newValue);
              ***REMOVED***
                else ***REMOVED***
                    // Current value deleted.
                    this.storage.removeItem(key);
              ***REMOVED***
          ***REMOVED***
            else if (this.localCache[key] === event.newValue && !poll) ***REMOVED***
                // Already detected and processed, do not trigger listeners again.
                return;
          ***REMOVED***
      ***REMOVED***
        const triggerListeners = () => ***REMOVED***
            // Keep local map up to date in case storage event is triggered before
            // poll.
            const storedValue = this.storage.getItem(key);
            if (!poll && this.localCache[key] === storedValue) ***REMOVED***
                // Real storage event which has already been detected, do nothing.
                // This seems to trigger in some IE browsers for some reason.
                return;
          ***REMOVED***
            this.notifyListeners(key, storedValue);
      ***REMOVED***;
        const storedValue = this.storage.getItem(key);
        if (_isIE10() &&
            storedValue !== event.newValue &&
            event.newValue !== event.oldValue) ***REMOVED***
            // IE 10 has this weird bug where a storage event would trigger with the
            // correct key, oldValue and newValue but localStorage.getItem(key) does
            // not yield the updated value until a few milliseconds. This ensures
            // this recovers from that situation.
            setTimeout(triggerListeners, IE10_LOCAL_STORAGE_SYNC_DELAY);
      ***REMOVED***
        else ***REMOVED***
            triggerListeners();
      ***REMOVED***
  ***REMOVED***
    notifyListeners(key, value) ***REMOVED***
        this.localCache[key] = value;
        const listeners = this.listeners[key];
        if (listeners) ***REMOVED***
            for (const listener of Array.from(listeners)) ***REMOVED***
                listener(value ? JSON.parse(value) : value);
          ***REMOVED***
      ***REMOVED***
  ***REMOVED***
    startPolling() ***REMOVED***
        this.stopPolling();
        this.pollTimer = setInterval(() => ***REMOVED***
            this.forAllChangedKeys((key, oldValue, newValue) => ***REMOVED***
                this.onStorageEvent(new StorageEvent('storage', ***REMOVED***
                    key,
                    oldValue,
                    newValue
              ***REMOVED***), 
                /* poll */ true);
          ***REMOVED***);
      ***REMOVED***, _POLLING_INTERVAL_MS$1);
  ***REMOVED***
    stopPolling() ***REMOVED***
        if (this.pollTimer) ***REMOVED***
            clearInterval(this.pollTimer);
            this.pollTimer = null;
      ***REMOVED***
  ***REMOVED***
    attachListener() ***REMOVED***
        window.addEventListener('storage', this.boundEventHandler);
  ***REMOVED***
    detachListener() ***REMOVED***
        window.removeEventListener('storage', this.boundEventHandler);
  ***REMOVED***
    _addListener(key, listener) ***REMOVED***
        if (Object.keys(this.listeners).length === 0) ***REMOVED***
            // Whether browser can detect storage event when it had already been pushed to the background.
            // This may happen in some mobile browsers. A localStorage change in the foreground window
            // will not be detected in the background window via the storage event.
            // This was detected in iOS 7.x mobile browsers
            if (this.fallbackToPolling) ***REMOVED***
                this.startPolling();
          ***REMOVED***
            else ***REMOVED***
                this.attachListener();
          ***REMOVED***
      ***REMOVED***
        if (!this.listeners[key]) ***REMOVED***
            this.listeners[key] = new Set();
            // Populate the cache to avoid spuriously triggering on first poll.
            this.localCache[key] = this.storage.getItem(key);
      ***REMOVED***
        this.listeners[key].add(listener);
  ***REMOVED***
    _removeListener(key, listener) ***REMOVED***
        if (this.listeners[key]) ***REMOVED***
            this.listeners[key].delete(listener);
            if (this.listeners[key].size === 0) ***REMOVED***
                delete this.listeners[key];
          ***REMOVED***
      ***REMOVED***
        if (Object.keys(this.listeners).length === 0) ***REMOVED***
            this.detachListener();
            this.stopPolling();
      ***REMOVED***
  ***REMOVED***
    // Update local cache on base operations:
    async _set(key, value) ***REMOVED***
        await super._set(key, value);
        this.localCache[key] = JSON.stringify(value);
  ***REMOVED***
    async _get(key) ***REMOVED***
        const value = await super._get(key);
        this.localCache[key] = JSON.stringify(value);
        return value;
  ***REMOVED***
    async _remove(key) ***REMOVED***
        await super._remove(key);
        delete this.localCache[key];
  ***REMOVED***
}
BrowserLocalPersistence.type = 'LOCAL';
/**
 * An implementation of ***REMOVED***@link Persistence} of type `LOCAL` using `localStorage`
 * for the underlying storage.
 *
 * @public
 */
const browserLocalPersistence = BrowserLocalPersistence;

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class BrowserSessionPersistence extends BrowserPersistenceClass ***REMOVED***
    constructor() ***REMOVED***
        super(() => window.sessionStorage, "SESSION" /* PersistenceType.SESSION */);
  ***REMOVED***
    _addListener(_key, _listener) ***REMOVED***
        // Listeners are not supported for session storage since it cannot be shared across windows
        return;
  ***REMOVED***
    _removeListener(_key, _listener) ***REMOVED***
        // Listeners are not supported for session storage since it cannot be shared across windows
        return;
  ***REMOVED***
}
BrowserSessionPersistence.type = 'SESSION';
/**
 * An implementation of ***REMOVED***@link Persistence} of `SESSION` using `sessionStorage`
 * for the underlying storage.
 *
 * @public
 */
const browserSessionPersistence = BrowserSessionPersistence;

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Shim for Promise.allSettled, note the slightly different format of `fulfilled` vs `status`.
 *
 * @param promises - Array of promises to wait on.
 */
function _allSettled(promises) ***REMOVED***
    return Promise.all(promises.map(async (promise) => ***REMOVED***
        try ***REMOVED***
            const value = await promise;
            return ***REMOVED***
                fulfilled: true,
                value
          ***REMOVED***;
      ***REMOVED***
        catch (reason) ***REMOVED***
            return ***REMOVED***
                fulfilled: false,
                reason
          ***REMOVED***;
      ***REMOVED***
  ***REMOVED***));
}

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Interface class for receiving messages.
 *
 */
class Receiver ***REMOVED***
    constructor(eventTarget) ***REMOVED***
        this.eventTarget = eventTarget;
        this.handlersMap = ***REMOVED***};
        this.boundEventHandler = this.handleEvent.bind(this);
  ***REMOVED***
    /**
     * Obtain an instance of a Receiver for a given event target, if none exists it will be created.
     *
     * @param eventTarget - An event target (such as window or self) through which the underlying
     * messages will be received.
     */
    static _getInstance(eventTarget) ***REMOVED***
        // The results are stored in an array since objects can't be keys for other
        // objects. In addition, setting a unique property on an event target as a
        // hash map key may not be allowed due to CORS restrictions.
        const existingInstance = this.receivers.find(receiver => receiver.isListeningto(eventTarget));
        if (existingInstance) ***REMOVED***
            return existingInstance;
      ***REMOVED***
        const newInstance = new Receiver(eventTarget);
        this.receivers.push(newInstance);
        return newInstance;
  ***REMOVED***
    isListeningto(eventTarget) ***REMOVED***
        return this.eventTarget === eventTarget;
  ***REMOVED***
    /**
     * Fans out a MessageEvent to the appropriate listeners.
     *
     * @remarks
     * Sends an ***REMOVED***@link Status.ACK} upon receipt and a ***REMOVED***@link Status.DONE} once all handlers have
     * finished processing.
     *
     * @param event - The MessageEvent.
     *
     */
    async handleEvent(event) ***REMOVED***
        const messageEvent = event;
        const ***REMOVED*** eventId, eventType, data } = messageEvent.data;
        const handlers = this.handlersMap[eventType];
        if (!(handlers === null || handlers === void 0 ? void 0 : handlers.size)) ***REMOVED***
            return;
      ***REMOVED***
        messageEvent.ports[0].postMessage(***REMOVED***
            status: "ack" /* _Status.ACK */,
            eventId,
            eventType
      ***REMOVED***);
        const promises = Array.from(handlers).map(async (handler) => handler(messageEvent.origin, data));
        const response = await _allSettled(promises);
        messageEvent.ports[0].postMessage(***REMOVED***
            status: "done" /* _Status.DONE */,
            eventId,
            eventType,
            response
      ***REMOVED***);
  ***REMOVED***
    /**
     * Subscribe an event handler for a particular event.
     *
     * @param eventType - Event name to subscribe to.
     * @param eventHandler - The event handler which should receive the events.
     *
     */
    _subscribe(eventType, eventHandler) ***REMOVED***
        if (Object.keys(this.handlersMap).length === 0) ***REMOVED***
            this.eventTarget.addEventListener('message', this.boundEventHandler);
      ***REMOVED***
        if (!this.handlersMap[eventType]) ***REMOVED***
            this.handlersMap[eventType] = new Set();
      ***REMOVED***
        this.handlersMap[eventType].add(eventHandler);
  ***REMOVED***
    /**
     * Unsubscribe an event handler from a particular event.
     *
     * @param eventType - Event name to unsubscribe from.
     * @param eventHandler - Optinoal event handler, if none provided, unsubscribe all handlers on this event.
     *
     */
    _unsubscribe(eventType, eventHandler) ***REMOVED***
        if (this.handlersMap[eventType] && eventHandler) ***REMOVED***
            this.handlersMap[eventType].delete(eventHandler);
      ***REMOVED***
        if (!eventHandler || this.handlersMap[eventType].size === 0) ***REMOVED***
            delete this.handlersMap[eventType];
      ***REMOVED***
        if (Object.keys(this.handlersMap).length === 0) ***REMOVED***
            this.eventTarget.removeEventListener('message', this.boundEventHandler);
      ***REMOVED***
  ***REMOVED***
}
Receiver.receivers = [];

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function _generateEventId(prefix = '', digits = 10) ***REMOVED***
    let random = '';
    for (let i = 0; i < digits; i++) ***REMOVED***
        random += Math.floor(Math.random() * 10);
  ***REMOVED***
    return prefix + random;
}

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Interface for sending messages and waiting for a completion response.
 *
 */
class Sender ***REMOVED***
    constructor(target) ***REMOVED***
        this.target = target;
        this.handlers = new Set();
  ***REMOVED***
    /**
     * Unsubscribe the handler and remove it from our tracking Set.
     *
     * @param handler - The handler to unsubscribe.
     */
    removeMessageHandler(handler) ***REMOVED***
        if (handler.messageChannel) ***REMOVED***
            handler.messageChannel.port1.removeEventListener('message', handler.onMessage);
            handler.messageChannel.port1.close();
      ***REMOVED***
        this.handlers.delete(handler);
  ***REMOVED***
    /**
     * Send a message to the Receiver located at ***REMOVED***@link target}.
     *
     * @remarks
     * We'll first wait a bit for an ACK , if we get one we will wait significantly longer until the
     * receiver has had a chance to fully process the event.
     *
     * @param eventType - Type of event to send.
     * @param data - The payload of the event.
     * @param timeout - Timeout for waiting on an ACK from the receiver.
     *
     * @returns An array of settled promises from all the handlers that were listening on the receiver.
     */
    async _send(eventType, data, timeout = 50 /* _TimeoutDuration.ACK */) ***REMOVED***
        const messageChannel = typeof MessageChannel !== 'undefined' ? new MessageChannel() : null;
        if (!messageChannel) ***REMOVED***
            throw new Error("connection_unavailable" /* _MessageError.CONNECTION_UNAVAILABLE */);
      ***REMOVED***
        // Node timers and browser timers return fundamentally different types.
        // We don't actually care what the value is but TS won't accept unknown and
        // we can't cast properly in both environments.
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        let completionTimer;
        let handler;
        return new Promise((resolve, reject) => ***REMOVED***
            const eventId = _generateEventId('', 20);
            messageChannel.port1.start();
            const ackTimer = setTimeout(() => ***REMOVED***
                reject(new Error("unsupported_event" /* _MessageError.UNSUPPORTED_EVENT */));
          ***REMOVED***, timeout);
            handler = ***REMOVED***
                messageChannel,
                onMessage(event) ***REMOVED***
                    const messageEvent = event;
                    if (messageEvent.data.eventId !== eventId) ***REMOVED***
                        return;
                  ***REMOVED***
                    switch (messageEvent.data.status) ***REMOVED***
                        case "ack" /* _Status.ACK */:
                            // The receiver should ACK first.
                            clearTimeout(ackTimer);
                            completionTimer = setTimeout(() => ***REMOVED***
                                reject(new Error("timeout" /* _MessageError.TIMEOUT */));
                          ***REMOVED***, 3000 /* _TimeoutDuration.COMPLETION */);
                            break;
                        case "done" /* _Status.DONE */:
                            // Once the receiver's handlers are finished we will get the results.
                            clearTimeout(completionTimer);
                            resolve(messageEvent.data.response);
                            break;
                        default:
                            clearTimeout(ackTimer);
                            clearTimeout(completionTimer);
                            reject(new Error("invalid_response" /* _MessageError.INVALID_RESPONSE */));
                            break;
                  ***REMOVED***
              ***REMOVED***
          ***REMOVED***;
            this.handlers.add(handler);
            messageChannel.port1.addEventListener('message', handler.onMessage);
            this.target.postMessage(***REMOVED***
                eventType,
                eventId,
                data
          ***REMOVED***, [messageChannel.port2]);
      ***REMOVED***).finally(() => ***REMOVED***
            if (handler) ***REMOVED***
                this.removeMessageHandler(handler);
          ***REMOVED***
      ***REMOVED***);
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Lazy accessor for window, since the compat layer won't tree shake this out,
 * we need to make sure not to mess with window unless we have to
 */
function _window() ***REMOVED***
    return window;
}
function _setWindowLocation(url) ***REMOVED***
    _window().location.href = url;
}

/**
 * @license
 * Copyright 2020 Google LLC.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function _isWorker() ***REMOVED***
    return (typeof _window()['WorkerGlobalScope'] !== 'undefined' &&
        typeof _window()['importScripts'] === 'function');
}
async function _getActiveServiceWorker() ***REMOVED***
    if (!(navigator === null || navigator === void 0 ? void 0 : navigator.serviceWorker)) ***REMOVED***
        return null;
  ***REMOVED***
    try ***REMOVED***
        const registration = await navigator.serviceWorker.ready;
        return registration.active;
  ***REMOVED***
    catch (_a) ***REMOVED***
        return null;
  ***REMOVED***
}
function _getServiceWorkerController() ***REMOVED***
    var _a;
    return ((_a = navigator === null || navigator === void 0 ? void 0 : navigator.serviceWorker) === null || _a === void 0 ? void 0 : _a.controller) || null;
}
function _getWorkerGlobalScope() ***REMOVED***
    return _isWorker() ? self : null;
}

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const DB_NAME = 'firebaseLocalStorageDb';
const DB_VERSION = 1;
const DB_OBJECTSTORE_NAME = 'firebaseLocalStorage';
const DB_DATA_KEYPATH = 'fbase_key';
/**
 * Promise wrapper for IDBRequest
 *
 * Unfortunately we can't cleanly extend Promise<T> since promises are not callable in ES6
 *
 */
class DBPromise ***REMOVED***
    constructor(request) ***REMOVED***
        this.request = request;
  ***REMOVED***
    toPromise() ***REMOVED***
        return new Promise((resolve, reject) => ***REMOVED***
            this.request.addEventListener('success', () => ***REMOVED***
                resolve(this.request.result);
          ***REMOVED***);
            this.request.addEventListener('error', () => ***REMOVED***
                reject(this.request.error);
          ***REMOVED***);
      ***REMOVED***);
  ***REMOVED***
}
function getObjectStore(db, isReadWrite) ***REMOVED***
    return db
        .transaction([DB_OBJECTSTORE_NAME], isReadWrite ? 'readwrite' : 'readonly')
        .objectStore(DB_OBJECTSTORE_NAME);
}
function _deleteDatabase() ***REMOVED***
    const request = indexedDB.deleteDatabase(DB_NAME);
    return new DBPromise(request).toPromise();
}
function _openDatabase() ***REMOVED***
    const request = indexedDB.open(DB_NAME, DB_VERSION);
    return new Promise((resolve, reject) => ***REMOVED***
        request.addEventListener('error', () => ***REMOVED***
            reject(request.error);
      ***REMOVED***);
        request.addEventListener('upgradeneeded', () => ***REMOVED***
            const db = request.result;
            try ***REMOVED***
                db.createObjectStore(DB_OBJECTSTORE_NAME, ***REMOVED*** keyPath: DB_DATA_KEYPATH });
          ***REMOVED***
            catch (e) ***REMOVED***
                reject(e);
          ***REMOVED***
      ***REMOVED***);
        request.addEventListener('success', async () => ***REMOVED***
            const db = request.result;
            // Strange bug that occurs in Firefox when multiple tabs are opened at the
            // same time. The only way to recover seems to be deleting the database
            // and re-initializing it.
            // https://github.com/firebase/firebase-js-sdk/issues/634
            if (!db.objectStoreNames.contains(DB_OBJECTSTORE_NAME)) ***REMOVED***
                // Need to close the database or else you get a `blocked` event
                db.close();
                await _deleteDatabase();
                resolve(await _openDatabase());
          ***REMOVED***
            else ***REMOVED***
                resolve(db);
          ***REMOVED***
      ***REMOVED***);
  ***REMOVED***);
}
async function _putObject(db, key, value) ***REMOVED***
    const request = getObjectStore(db, true).put(***REMOVED***
        [DB_DATA_KEYPATH]: key,
        value
  ***REMOVED***);
    return new DBPromise(request).toPromise();
}
async function getObject(db, key) ***REMOVED***
    const request = getObjectStore(db, false).get(key);
    const data = await new DBPromise(request).toPromise();
    return data === undefined ? null : data.value;
}
function _deleteObject(db, key) ***REMOVED***
    const request = getObjectStore(db, true).delete(key);
    return new DBPromise(request).toPromise();
}
const _POLLING_INTERVAL_MS = 800;
const _TRANSACTION_RETRY_COUNT = 3;
class IndexedDBLocalPersistence ***REMOVED***
    constructor() ***REMOVED***
        this.type = "LOCAL" /* PersistenceType.LOCAL */;
        this._shouldAllowMigration = true;
        this.listeners = ***REMOVED***};
        this.localCache = ***REMOVED***};
        // setTimeout return value is platform specific
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        this.pollTimer = null;
        this.pendingWrites = 0;
        this.receiver = null;
        this.sender = null;
        this.serviceWorkerReceiverAvailable = false;
        this.activeServiceWorker = null;
        // Fire & forget the service worker registration as it may never resolve
        this._workerInitializationPromise =
            this.initializeServiceWorkerMessaging().then(() => ***REMOVED*** }, () => ***REMOVED*** });
  ***REMOVED***
    async _openDb() ***REMOVED***
        if (this.db) ***REMOVED***
            return this.db;
      ***REMOVED***
        this.db = await _openDatabase();
        return this.db;
  ***REMOVED***
    async _withRetries(op) ***REMOVED***
        let numAttempts = 0;
        while (true) ***REMOVED***
            try ***REMOVED***
                const db = await this._openDb();
                return await op(db);
          ***REMOVED***
            catch (e) ***REMOVED***
                if (numAttempts++ > _TRANSACTION_RETRY_COUNT) ***REMOVED***
                    throw e;
              ***REMOVED***
                if (this.db) ***REMOVED***
                    this.db.close();
                    this.db = undefined;
              ***REMOVED***
                // TODO: consider adding exponential backoff
          ***REMOVED***
      ***REMOVED***
  ***REMOVED***
    /**
     * IndexedDB events do not propagate from the main window to the worker context.  We rely on a
     * postMessage interface to send these events to the worker ourselves.
     */
    async initializeServiceWorkerMessaging() ***REMOVED***
        return _isWorker() ? this.initializeReceiver() : this.initializeSender();
  ***REMOVED***
    /**
     * As the worker we should listen to events from the main window.
     */
    async initializeReceiver() ***REMOVED***
        this.receiver = Receiver._getInstance(_getWorkerGlobalScope());
        // Refresh from persistence if we receive a KeyChanged message.
        this.receiver._subscribe("keyChanged" /* _EventType.KEY_CHANGED */, async (_origin, data) => ***REMOVED***
            const keys = await this._poll();
            return ***REMOVED***
                keyProcessed: keys.includes(data.key)
          ***REMOVED***;
      ***REMOVED***);
        // Let the sender know that we are listening so they give us more timeout.
        this.receiver._subscribe("ping" /* _EventType.PING */, async (_origin, _data) => ***REMOVED***
            return ["keyChanged" /* _EventType.KEY_CHANGED */];
      ***REMOVED***);
  ***REMOVED***
    /**
     * As the main window, we should let the worker know when keys change (set and remove).
     *
     * @remarks
     * ***REMOVED***@link https://developer.mozilla.org/en-US/docs/Web/API/ServiceWorkerContainer/ready | ServiceWorkerContainer.ready}
     * may not resolve.
     */
    async initializeSender() ***REMOVED***
        var _a, _b;
        // Check to see if there's an active service worker.
        this.activeServiceWorker = await _getActiveServiceWorker();
        if (!this.activeServiceWorker) ***REMOVED***
            return;
      ***REMOVED***
        this.sender = new Sender(this.activeServiceWorker);
        // Ping the service worker to check what events they can handle.
        const results = await this.sender._send("ping" /* _EventType.PING */, ***REMOVED***}, 800 /* _TimeoutDuration.LONG_ACK */);
        if (!results) ***REMOVED***
            return;
      ***REMOVED***
        if (((_a = results[0]) === null || _a === void 0 ? void 0 : _a.fulfilled) &&
            ((_b = results[0]) === null || _b === void 0 ? void 0 : _b.value.includes("keyChanged" /* _EventType.KEY_CHANGED */))) ***REMOVED***
            this.serviceWorkerReceiverAvailable = true;
      ***REMOVED***
  ***REMOVED***
    /**
     * Let the worker know about a changed key, the exact key doesn't technically matter since the
     * worker will just trigger a full sync anyway.
     *
     * @remarks
     * For now, we only support one service worker per page.
     *
     * @param key - Storage key which changed.
     */
    async notifyServiceWorker(key) ***REMOVED***
        if (!this.sender ||
            !this.activeServiceWorker ||
            _getServiceWorkerController() !== this.activeServiceWorker) ***REMOVED***
            return;
      ***REMOVED***
        try ***REMOVED***
            await this.sender._send("keyChanged" /* _EventType.KEY_CHANGED */, ***REMOVED*** key }, 
            // Use long timeout if receiver has previously responded to a ping from us.
            this.serviceWorkerReceiverAvailable
                ? 800 /* _TimeoutDuration.LONG_ACK */
                : 50 /* _TimeoutDuration.ACK */);
      ***REMOVED***
        catch (_a) ***REMOVED***
            // This is a best effort approach. Ignore errors.
      ***REMOVED***
  ***REMOVED***
    async _isAvailable() ***REMOVED***
        try ***REMOVED***
            if (!indexedDB) ***REMOVED***
                return false;
          ***REMOVED***
            const db = await _openDatabase();
            await _putObject(db, STORAGE_AVAILABLE_KEY, '1');
            await _deleteObject(db, STORAGE_AVAILABLE_KEY);
            return true;
      ***REMOVED***
        catch (_a) ***REMOVED*** }
        return false;
  ***REMOVED***
    async _withPendingWrite(write) ***REMOVED***
        this.pendingWrites++;
        try ***REMOVED***
            await write();
      ***REMOVED***
        finally ***REMOVED***
            this.pendingWrites--;
      ***REMOVED***
  ***REMOVED***
    async _set(key, value) ***REMOVED***
        return this._withPendingWrite(async () => ***REMOVED***
            await this._withRetries((db) => _putObject(db, key, value));
            this.localCache[key] = value;
            return this.notifyServiceWorker(key);
      ***REMOVED***);
  ***REMOVED***
    async _get(key) ***REMOVED***
        const obj = (await this._withRetries((db) => getObject(db, key)));
        this.localCache[key] = obj;
        return obj;
  ***REMOVED***
    async _remove(key) ***REMOVED***
        return this._withPendingWrite(async () => ***REMOVED***
            await this._withRetries((db) => _deleteObject(db, key));
            delete this.localCache[key];
            return this.notifyServiceWorker(key);
      ***REMOVED***);
  ***REMOVED***
    async _poll() ***REMOVED***
        // TODO: check if we need to fallback if getAll is not supported
        const result = await this._withRetries((db) => ***REMOVED***
            const getAllRequest = getObjectStore(db, false).getAll();
            return new DBPromise(getAllRequest).toPromise();
      ***REMOVED***);
        if (!result) ***REMOVED***
            return [];
      ***REMOVED***
        // If we have pending writes in progress abort, we'll get picked up on the next poll
        if (this.pendingWrites !== 0) ***REMOVED***
            return [];
      ***REMOVED***
        const keys = [];
        const keysInResult = new Set();
        for (const ***REMOVED*** fbase_key: key, value } of result) ***REMOVED***
            keysInResult.add(key);
            if (JSON.stringify(this.localCache[key]) !== JSON.stringify(value)) ***REMOVED***
                this.notifyListeners(key, value);
                keys.push(key);
          ***REMOVED***
      ***REMOVED***
        for (const localKey of Object.keys(this.localCache)) ***REMOVED***
            if (this.localCache[localKey] && !keysInResult.has(localKey)) ***REMOVED***
                // Deleted
                this.notifyListeners(localKey, null);
                keys.push(localKey);
          ***REMOVED***
      ***REMOVED***
        return keys;
  ***REMOVED***
    notifyListeners(key, newValue) ***REMOVED***
        this.localCache[key] = newValue;
        const listeners = this.listeners[key];
        if (listeners) ***REMOVED***
            for (const listener of Array.from(listeners)) ***REMOVED***
                listener(newValue);
          ***REMOVED***
      ***REMOVED***
  ***REMOVED***
    startPolling() ***REMOVED***
        this.stopPolling();
        this.pollTimer = setInterval(async () => this._poll(), _POLLING_INTERVAL_MS);
  ***REMOVED***
    stopPolling() ***REMOVED***
        if (this.pollTimer) ***REMOVED***
            clearInterval(this.pollTimer);
            this.pollTimer = null;
      ***REMOVED***
  ***REMOVED***
    _addListener(key, listener) ***REMOVED***
        if (Object.keys(this.listeners).length === 0) ***REMOVED***
            this.startPolling();
      ***REMOVED***
        if (!this.listeners[key]) ***REMOVED***
            this.listeners[key] = new Set();
            // Populate the cache to avoid spuriously triggering on first poll.
            void this._get(key); // This can happen in the background async and we can return immediately.
      ***REMOVED***
        this.listeners[key].add(listener);
  ***REMOVED***
    _removeListener(key, listener) ***REMOVED***
        if (this.listeners[key]) ***REMOVED***
            this.listeners[key].delete(listener);
            if (this.listeners[key].size === 0) ***REMOVED***
                delete this.listeners[key];
          ***REMOVED***
      ***REMOVED***
        if (Object.keys(this.listeners).length === 0) ***REMOVED***
            this.stopPolling();
      ***REMOVED***
  ***REMOVED***
}
IndexedDBLocalPersistence.type = 'LOCAL';
/**
 * An implementation of ***REMOVED***@link Persistence} of type `LOCAL` using `indexedDB`
 * for the underlying storage.
 *
 * @public
 */
const indexedDBLocalPersistence = IndexedDBLocalPersistence;

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function startSignInPhoneMfa(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v2/accounts/mfaSignIn:start" /* Endpoint.START_MFA_SIGN_IN */, _addTidIfNecessary(auth, request));
}
function finalizeSignInPhoneMfa(auth, request) ***REMOVED***
    return _performApiRequest(auth, "POST" /* HttpMethod.POST */, "/v2/accounts/mfaSignIn:finalize" /* Endpoint.FINALIZE_MFA_SIGN_IN */, _addTidIfNecessary(auth, request));
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function getRecaptchaParams(auth) ***REMOVED***
    return ((await _performApiRequest(auth, "GET" /* HttpMethod.GET */, "/v1/recaptchaParams" /* Endpoint.GET_RECAPTCHA_PARAM */)).recaptchaSiteKey || '');
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function getScriptParentElement() ***REMOVED***
    var _a, _b;
    return (_b = (_a = document.getElementsByTagName('head')) === null || _a === void 0 ? void 0 : _a[0]) !== null && _b !== void 0 ? _b : document;
}
function _loadJS(url) ***REMOVED***
    // TODO: consider adding timeout support & cancellation
    return new Promise((resolve, reject) => ***REMOVED***
        const el = document.createElement('script');
        el.setAttribute('src', url);
        el.onload = resolve;
        el.onerror = e => ***REMOVED***
            const error = _createError("internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
            error.customData = e;
            reject(error);
      ***REMOVED***;
        el.type = 'text/javascript';
        el.charset = 'UTF-8';
        getScriptParentElement().appendChild(el);
  ***REMOVED***);
}
function _generateCallbackName(prefix) ***REMOVED***
    return `__$***REMOVED***prefix}$***REMOVED***Math.floor(Math.random() * 1000000)}`;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const _SOLVE_TIME_MS = 500;
const _EXPIRATION_TIME_MS = 60000;
const _WIDGET_ID_START = 1000000000000;
class MockReCaptcha ***REMOVED***
    constructor(auth) ***REMOVED***
        this.auth = auth;
        this.counter = _WIDGET_ID_START;
        this._widgets = new Map();
  ***REMOVED***
    render(container, parameters) ***REMOVED***
        const id = this.counter;
        this._widgets.set(id, new MockWidget(container, this.auth.name, parameters || ***REMOVED***}));
        this.counter++;
        return id;
  ***REMOVED***
    reset(optWidgetId) ***REMOVED***
        var _a;
        const id = optWidgetId || _WIDGET_ID_START;
        void ((_a = this._widgets.get(id)) === null || _a === void 0 ? void 0 : _a.delete());
        this._widgets.delete(id);
  ***REMOVED***
    getResponse(optWidgetId) ***REMOVED***
        var _a;
        const id = optWidgetId || _WIDGET_ID_START;
        return ((_a = this._widgets.get(id)) === null || _a === void 0 ? void 0 : _a.getResponse()) || '';
  ***REMOVED***
    async execute(optWidgetId) ***REMOVED***
        var _a;
        const id = optWidgetId || _WIDGET_ID_START;
        void ((_a = this._widgets.get(id)) === null || _a === void 0 ? void 0 : _a.execute());
        return '';
  ***REMOVED***
}
class MockWidget ***REMOVED***
    constructor(containerOrId, appName, params) ***REMOVED***
        this.params = params;
        this.timerId = null;
        this.deleted = false;
        this.responseToken = null;
        this.clickHandler = () => ***REMOVED***
            this.execute();
      ***REMOVED***;
        const container = typeof containerOrId === 'string'
            ? document.getElementById(containerOrId)
            : containerOrId;
        _assert(container, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */, ***REMOVED*** appName });
        this.container = container;
        this.isVisible = this.params.size !== 'invisible';
        if (this.isVisible) ***REMOVED***
            this.execute();
      ***REMOVED***
        else ***REMOVED***
            this.container.addEventListener('click', this.clickHandler);
      ***REMOVED***
  ***REMOVED***
    getResponse() ***REMOVED***
        this.checkIfDeleted();
        return this.responseToken;
  ***REMOVED***
    delete() ***REMOVED***
        this.checkIfDeleted();
        this.deleted = true;
        if (this.timerId) ***REMOVED***
            clearTimeout(this.timerId);
            this.timerId = null;
      ***REMOVED***
        this.container.removeEventListener('click', this.clickHandler);
  ***REMOVED***
    execute() ***REMOVED***
        this.checkIfDeleted();
        if (this.timerId) ***REMOVED***
            return;
      ***REMOVED***
        this.timerId = window.setTimeout(() => ***REMOVED***
            this.responseToken = generateRandomAlphaNumericString(50);
            const ***REMOVED*** callback, 'expired-callback': expiredCallback } = this.params;
            if (callback) ***REMOVED***
                try ***REMOVED***
                    callback(this.responseToken);
              ***REMOVED***
                catch (e) ***REMOVED*** }
          ***REMOVED***
            this.timerId = window.setTimeout(() => ***REMOVED***
                this.timerId = null;
                this.responseToken = null;
                if (expiredCallback) ***REMOVED***
                    try ***REMOVED***
                        expiredCallback();
                  ***REMOVED***
                    catch (e) ***REMOVED*** }
              ***REMOVED***
                if (this.isVisible) ***REMOVED***
                    this.execute();
              ***REMOVED***
          ***REMOVED***, _EXPIRATION_TIME_MS);
      ***REMOVED***, _SOLVE_TIME_MS);
  ***REMOVED***
    checkIfDeleted() ***REMOVED***
        if (this.deleted) ***REMOVED***
            throw new Error('reCAPTCHA mock was already deleted!');
      ***REMOVED***
  ***REMOVED***
}
function generateRandomAlphaNumericString(len) ***REMOVED***
    const chars = [];
    const allowedChars = '1234567890abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ';
    for (let i = 0; i < len; i++) ***REMOVED***
        chars.push(allowedChars.charAt(Math.floor(Math.random() * allowedChars.length)));
  ***REMOVED***
    return chars.join('');
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ReCaptcha will load using the same callback, so the callback function needs
// to be kept around
const _JSLOAD_CALLBACK = _generateCallbackName('rcb');
const NETWORK_TIMEOUT_DELAY = new Delay(30000, 60000);
const RECAPTCHA_BASE = 'https://www.google.com/recaptcha/api.js?';
/**
 * Loader for the GReCaptcha library. There should only ever be one of this.
 */
class ReCaptchaLoaderImpl ***REMOVED***
    constructor() ***REMOVED***
        var _a;
        this.hostLanguage = '';
        this.counter = 0;
        /**
         * Check for `render()` method. `window.grecaptcha` will exist if the Enterprise
         * version of the ReCAPTCHA script was loaded by someone else (e.g. App Check) but
         * `window.grecaptcha.render()` will not. Another load will add it.
         */
        this.librarySeparatelyLoaded = !!((_a = _window().grecaptcha) === null || _a === void 0 ? void 0 : _a.render);
  ***REMOVED***
    load(auth, hl = '') ***REMOVED***
        _assert(isHostLanguageValid(hl), auth, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
        if (this.shouldResolveImmediately(hl)) ***REMOVED***
            return Promise.resolve(_window().grecaptcha);
      ***REMOVED***
        return new Promise((resolve, reject) => ***REMOVED***
            const networkTimeout = _window().setTimeout(() => ***REMOVED***
                reject(_createError(auth, "network-request-failed" /* AuthErrorCode.NETWORK_REQUEST_FAILED */));
          ***REMOVED***, NETWORK_TIMEOUT_DELAY.get());
            _window()[_JSLOAD_CALLBACK] = () => ***REMOVED***
                _window().clearTimeout(networkTimeout);
                delete _window()[_JSLOAD_CALLBACK];
                const recaptcha = _window().grecaptcha;
                if (!recaptcha) ***REMOVED***
                    reject(_createError(auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */));
                    return;
              ***REMOVED***
                // Wrap the greptcha render function so that we know if the developer has
                // called it separately
                const render = recaptcha.render;
                recaptcha.render = (container, params) => ***REMOVED***
                    const widgetId = render(container, params);
                    this.counter++;
                    return widgetId;
              ***REMOVED***;
                this.hostLanguage = hl;
                resolve(recaptcha);
          ***REMOVED***;
            const url = `$***REMOVED***RECAPTCHA_BASE}?$***REMOVED***querystring(***REMOVED***
                onload: _JSLOAD_CALLBACK,
                render: 'explicit',
                hl
          ***REMOVED***)}`;
            _loadJS(url).catch(() => ***REMOVED***
                clearTimeout(networkTimeout);
                reject(_createError(auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */));
          ***REMOVED***);
      ***REMOVED***);
  ***REMOVED***
    clearedOneInstance() ***REMOVED***
        this.counter--;
  ***REMOVED***
    shouldResolveImmediately(hl) ***REMOVED***
        var _a;
        // We can resolve immediately if:
        //   • grecaptcha is already defined AND (
        //     1. the requested language codes are the same OR
        //     2. there exists already a ReCaptcha on the page
        //     3. the library was already loaded by the app
        // In cases (2) and (3), we _can't_ reload as it would break the recaptchas
        // that are already in the page
        return (!!((_a = _window().grecaptcha) === null || _a === void 0 ? void 0 : _a.render) &&
            (hl === this.hostLanguage ||
                this.counter > 0 ||
                this.librarySeparatelyLoaded));
  ***REMOVED***
}
function isHostLanguageValid(hl) ***REMOVED***
    return hl.length <= 6 && /^\s*[a-zA-Z0-9\-]*\s*$/.test(hl);
}
class MockReCaptchaLoaderImpl ***REMOVED***
    async load(auth) ***REMOVED***
        return new MockReCaptcha(auth);
  ***REMOVED***
    clearedOneInstance() ***REMOVED*** }
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const RECAPTCHA_VERIFIER_TYPE = 'recaptcha';
const DEFAULT_PARAMS = ***REMOVED***
    theme: 'light',
    type: 'image'
};
/**
 * An ***REMOVED***@link https://www.google.com/recaptcha/ | reCAPTCHA}-based application verifier.
 *
 * @public
 */
class RecaptchaVerifier ***REMOVED***
    /**
     *
     * @param containerOrId - The reCAPTCHA container parameter.
     *
     * @remarks
     * This has different meaning depending on whether the reCAPTCHA is hidden or visible. For a
     * visible reCAPTCHA the container must be empty. If a string is used, it has to correspond to
     * an element ID. The corresponding element must also must be in the DOM at the time of
     * initialization.
     *
     * @param parameters - The optional reCAPTCHA parameters.
     *
     * @remarks
     * Check the reCAPTCHA docs for a comprehensive list. All parameters are accepted except for
     * the sitekey. Firebase Auth backend provisions a reCAPTCHA for each project and will
     * configure this upon rendering. For an invisible reCAPTCHA, a size key must have the value
     * 'invisible'.
     *
     * @param authExtern - The corresponding Firebase ***REMOVED***@link Auth} instance.
     */
    constructor(containerOrId, parameters = Object.assign(***REMOVED***}, DEFAULT_PARAMS), authExtern) ***REMOVED***
        this.parameters = parameters;
        /**
         * The application verifier type.
         *
         * @remarks
         * For a reCAPTCHA verifier, this is 'recaptcha'.
         */
        this.type = RECAPTCHA_VERIFIER_TYPE;
        this.destroyed = false;
        this.widgetId = null;
        this.tokenChangeListeners = new Set();
        this.renderPromise = null;
        this.recaptcha = null;
        this.auth = _castAuth(authExtern);
        this.isInvisible = this.parameters.size === 'invisible';
        _assert(typeof document !== 'undefined', this.auth, "operation-not-supported-in-this-environment" /* AuthErrorCode.OPERATION_NOT_SUPPORTED */);
        const container = typeof containerOrId === 'string'
            ? document.getElementById(containerOrId)
            : containerOrId;
        _assert(container, this.auth, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
        this.container = container;
        this.parameters.callback = this.makeTokenCallback(this.parameters.callback);
        this._recaptchaLoader = this.auth.settings.appVerificationDisabledForTesting
            ? new MockReCaptchaLoaderImpl()
            : new ReCaptchaLoaderImpl();
        this.validateStartingState();
        // TODO: Figure out if sdk version is needed
  ***REMOVED***
    /**
     * Waits for the user to solve the reCAPTCHA and resolves with the reCAPTCHA token.
     *
     * @returns A Promise for the reCAPTCHA token.
     */
    async verify() ***REMOVED***
        this.assertNotDestroyed();
        const id = await this.render();
        const recaptcha = this.getAssertedRecaptcha();
        const response = recaptcha.getResponse(id);
        if (response) ***REMOVED***
            return response;
      ***REMOVED***
        return new Promise(resolve => ***REMOVED***
            const tokenChange = (token) => ***REMOVED***
                if (!token) ***REMOVED***
                    return; // Ignore token expirations.
              ***REMOVED***
                this.tokenChangeListeners.delete(tokenChange);
                resolve(token);
          ***REMOVED***;
            this.tokenChangeListeners.add(tokenChange);
            if (this.isInvisible) ***REMOVED***
                recaptcha.execute(id);
          ***REMOVED***
      ***REMOVED***);
  ***REMOVED***
    /**
     * Renders the reCAPTCHA widget on the page.
     *
     * @returns A Promise that resolves with the reCAPTCHA widget ID.
     */
    render() ***REMOVED***
        try ***REMOVED***
            this.assertNotDestroyed();
      ***REMOVED***
        catch (e) ***REMOVED***
            // This method returns a promise. Since it's not async (we want to return the
            // _same_ promise if rendering is still occurring), the API surface should
            // reject with the error rather than just throw
            return Promise.reject(e);
      ***REMOVED***
        if (this.renderPromise) ***REMOVED***
            return this.renderPromise;
      ***REMOVED***
        this.renderPromise = this.makeRenderPromise().catch(e => ***REMOVED***
            this.renderPromise = null;
            throw e;
      ***REMOVED***);
        return this.renderPromise;
  ***REMOVED***
    /** @internal */
    _reset() ***REMOVED***
        this.assertNotDestroyed();
        if (this.widgetId !== null) ***REMOVED***
            this.getAssertedRecaptcha().reset(this.widgetId);
      ***REMOVED***
  ***REMOVED***
    /**
     * Clears the reCAPTCHA widget from the page and destroys the instance.
     */
    clear() ***REMOVED***
        this.assertNotDestroyed();
        this.destroyed = true;
        this._recaptchaLoader.clearedOneInstance();
        if (!this.isInvisible) ***REMOVED***
            this.container.childNodes.forEach(node => ***REMOVED***
                this.container.removeChild(node);
          ***REMOVED***);
      ***REMOVED***
  ***REMOVED***
    validateStartingState() ***REMOVED***
        _assert(!this.parameters.sitekey, this.auth, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
        _assert(this.isInvisible || !this.container.hasChildNodes(), this.auth, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
        _assert(typeof document !== 'undefined', this.auth, "operation-not-supported-in-this-environment" /* AuthErrorCode.OPERATION_NOT_SUPPORTED */);
  ***REMOVED***
    makeTokenCallback(existing) ***REMOVED***
        return token => ***REMOVED***
            this.tokenChangeListeners.forEach(listener => listener(token));
            if (typeof existing === 'function') ***REMOVED***
                existing(token);
          ***REMOVED***
            else if (typeof existing === 'string') ***REMOVED***
                const globalFunc = _window()[existing];
                if (typeof globalFunc === 'function') ***REMOVED***
                    globalFunc(token);
              ***REMOVED***
          ***REMOVED***
      ***REMOVED***;
  ***REMOVED***
    assertNotDestroyed() ***REMOVED***
        _assert(!this.destroyed, this.auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
  ***REMOVED***
    async makeRenderPromise() ***REMOVED***
        await this.init();
        if (!this.widgetId) ***REMOVED***
            let container = this.container;
            if (!this.isInvisible) ***REMOVED***
                const guaranteedEmpty = document.createElement('div');
                container.appendChild(guaranteedEmpty);
                container = guaranteedEmpty;
          ***REMOVED***
            this.widgetId = this.getAssertedRecaptcha().render(container, this.parameters);
      ***REMOVED***
        return this.widgetId;
  ***REMOVED***
    async init() ***REMOVED***
        _assert(_isHttpOrHttps() && !_isWorker(), this.auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        await domReady();
        this.recaptcha = await this._recaptchaLoader.load(this.auth, this.auth.languageCode || undefined);
        const siteKey = await getRecaptchaParams(this.auth);
        _assert(siteKey, this.auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        this.parameters.sitekey = siteKey;
  ***REMOVED***
    getAssertedRecaptcha() ***REMOVED***
        _assert(this.recaptcha, this.auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        return this.recaptcha;
  ***REMOVED***
}
function domReady() ***REMOVED***
    let resolver = null;
    return new Promise(resolve => ***REMOVED***
        if (document.readyState === 'complete') ***REMOVED***
            resolve();
            return;
      ***REMOVED***
        // Document not ready, wait for load before resolving.
        // Save resolver, so we can remove listener in case it was externally
        // cancelled.
        resolver = () => resolve();
        window.addEventListener('load', resolver);
  ***REMOVED***).catch(e => ***REMOVED***
        if (resolver) ***REMOVED***
            window.removeEventListener('load', resolver);
      ***REMOVED***
        throw e;
  ***REMOVED***);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class ConfirmationResultImpl ***REMOVED***
    constructor(verificationId, onConfirmation) ***REMOVED***
        this.verificationId = verificationId;
        this.onConfirmation = onConfirmation;
  ***REMOVED***
    confirm(verificationCode) ***REMOVED***
        const authCredential = PhoneAuthCredential._fromVerification(this.verificationId, verificationCode);
        return this.onConfirmation(authCredential);
  ***REMOVED***
}
/**
 * Asynchronously signs in using a phone number.
 *
 * @remarks
 * This method sends a code via SMS to the given
 * phone number, and returns a ***REMOVED***@link ConfirmationResult}. After the user
 * provides the code sent to their phone, call ***REMOVED***@link ConfirmationResult.confirm}
 * with the code to sign the user in.
 *
 * For abuse prevention, this method also requires a ***REMOVED***@link ApplicationVerifier}.
 * This SDK includes a reCAPTCHA-based implementation, ***REMOVED***@link RecaptchaVerifier}.
 * This function can work on other platforms that do not support the
 * ***REMOVED***@link RecaptchaVerifier} (like React Native), but you need to use a
 * third-party ***REMOVED***@link ApplicationVerifier} implementation.
 *
 * @example
 * ```javascript
 * // 'recaptcha-container' is the ID of an element in the DOM.
 * const applicationVerifier = new firebase.auth.RecaptchaVerifier('recaptcha-container');
 * const confirmationResult = await signInWithPhoneNumber(auth, phoneNumber, applicationVerifier);
 * // Obtain a verificationCode from the user.
 * const credential = await confirmationResult.confirm(verificationCode);
 * ```
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param phoneNumber - The user's phone number in E.164 format (e.g. +16505550101).
 * @param appVerifier - The ***REMOVED***@link ApplicationVerifier}.
 *
 * @public
 */
async function signInWithPhoneNumber(auth, phoneNumber, appVerifier) ***REMOVED***
    const authInternal = _castAuth(auth);
    const verificationId = await _verifyPhoneNumber(authInternal, phoneNumber, getModularInstance(appVerifier));
    return new ConfirmationResultImpl(verificationId, cred => signInWithCredential(authInternal, cred));
}
/**
 * Links the user account with the given phone number.
 *
 * @param user - The user.
 * @param phoneNumber - The user's phone number in E.164 format (e.g. +16505550101).
 * @param appVerifier - The ***REMOVED***@link ApplicationVerifier}.
 *
 * @public
 */
async function linkWithPhoneNumber(user, phoneNumber, appVerifier) ***REMOVED***
    const userInternal = getModularInstance(user);
    await _assertLinkedStatus(false, userInternal, "phone" /* ProviderId.PHONE */);
    const verificationId = await _verifyPhoneNumber(userInternal.auth, phoneNumber, getModularInstance(appVerifier));
    return new ConfirmationResultImpl(verificationId, cred => linkWithCredential(userInternal, cred));
}
/**
 * Re-authenticates a user using a fresh phone credential.
 *
 * @remarks Use before operations such as ***REMOVED***@link updatePassword} that require tokens from recent sign-in attempts.
 *
 * @param user - The user.
 * @param phoneNumber - The user's phone number in E.164 format (e.g. +16505550101).
 * @param appVerifier - The ***REMOVED***@link ApplicationVerifier}.
 *
 * @public
 */
async function reauthenticateWithPhoneNumber(user, phoneNumber, appVerifier) ***REMOVED***
    const userInternal = getModularInstance(user);
    const verificationId = await _verifyPhoneNumber(userInternal.auth, phoneNumber, getModularInstance(appVerifier));
    return new ConfirmationResultImpl(verificationId, cred => reauthenticateWithCredential(userInternal, cred));
}
/**
 * Returns a verification ID to be used in conjunction with the SMS code that is sent.
 *
 */
async function _verifyPhoneNumber(auth, options, verifier) ***REMOVED***
    var _a;
    const recaptchaToken = await verifier.verify();
    try ***REMOVED***
        _assert(typeof recaptchaToken === 'string', auth, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
        _assert(verifier.type === RECAPTCHA_VERIFIER_TYPE, auth, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
        let phoneInfoOptions;
        if (typeof options === 'string') ***REMOVED***
            phoneInfoOptions = ***REMOVED***
                phoneNumber: options
          ***REMOVED***;
      ***REMOVED***
        else ***REMOVED***
            phoneInfoOptions = options;
      ***REMOVED***
        if ('session' in phoneInfoOptions) ***REMOVED***
            const session = phoneInfoOptions.session;
            if ('phoneNumber' in phoneInfoOptions) ***REMOVED***
                _assert(session.type === "enroll" /* MultiFactorSessionType.ENROLL */, auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
                const response = await startEnrollPhoneMfa(auth, ***REMOVED***
                    idToken: session.credential,
                    phoneEnrollmentInfo: ***REMOVED***
                        phoneNumber: phoneInfoOptions.phoneNumber,
                        recaptchaToken
                  ***REMOVED***
              ***REMOVED***);
                return response.phoneSessionInfo.sessionInfo;
          ***REMOVED***
            else ***REMOVED***
                _assert(session.type === "signin" /* MultiFactorSessionType.SIGN_IN */, auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
                const mfaEnrollmentId = ((_a = phoneInfoOptions.multiFactorHint) === null || _a === void 0 ? void 0 : _a.uid) ||
                    phoneInfoOptions.multiFactorUid;
                _assert(mfaEnrollmentId, auth, "missing-multi-factor-info" /* AuthErrorCode.MISSING_MFA_INFO */);
                const response = await startSignInPhoneMfa(auth, ***REMOVED***
                    mfaPendingCredential: session.credential,
                    mfaEnrollmentId,
                    phoneSignInInfo: ***REMOVED***
                        recaptchaToken
                  ***REMOVED***
              ***REMOVED***);
                return response.phoneResponseInfo.sessionInfo;
          ***REMOVED***
      ***REMOVED***
        else ***REMOVED***
            const ***REMOVED*** sessionInfo } = await sendPhoneVerificationCode(auth, ***REMOVED***
                phoneNumber: phoneInfoOptions.phoneNumber,
                recaptchaToken
          ***REMOVED***);
            return sessionInfo;
      ***REMOVED***
  ***REMOVED***
    finally ***REMOVED***
        verifier._reset();
  ***REMOVED***
}
/**
 * Updates the user's phone number.
 *
 * @example
 * ```
 * // 'recaptcha-container' is the ID of an element in the DOM.
 * const applicationVerifier = new RecaptchaVerifier('recaptcha-container');
 * const provider = new PhoneAuthProvider(auth);
 * const verificationId = await provider.verifyPhoneNumber('+16505550101', applicationVerifier);
 * // Obtain the verificationCode from the user.
 * const phoneCredential = PhoneAuthProvider.credential(verificationId, verificationCode);
 * await updatePhoneNumber(user, phoneCredential);
 * ```
 *
 * @param user - The user.
 * @param credential - A credential authenticating the new phone number.
 *
 * @public
 */
async function updatePhoneNumber(user, credential) ***REMOVED***
    await _link$1(getModularInstance(user), credential);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Provider for generating an ***REMOVED***@link PhoneAuthCredential}.
 *
 * @example
 * ```javascript
 * // 'recaptcha-container' is the ID of an element in the DOM.
 * const applicationVerifier = new RecaptchaVerifier('recaptcha-container');
 * const provider = new PhoneAuthProvider(auth);
 * const verificationId = await provider.verifyPhoneNumber('+16505550101', applicationVerifier);
 * // Obtain the verificationCode from the user.
 * const phoneCredential = PhoneAuthProvider.credential(verificationId, verificationCode);
 * const userCredential = await signInWithCredential(auth, phoneCredential);
 * ```
 *
 * @public
 */
class PhoneAuthProvider ***REMOVED***
    /**
     * @param auth - The Firebase ***REMOVED***@link Auth} instance in which sign-ins should occur.
     *
     */
    constructor(auth) ***REMOVED***
        /** Always set to ***REMOVED***@link ProviderId}.PHONE. */
        this.providerId = PhoneAuthProvider.PROVIDER_ID;
        this.auth = _castAuth(auth);
  ***REMOVED***
    /**
     *
     * Starts a phone number authentication flow by sending a verification code to the given phone
     * number.
     *
     * @example
     * ```javascript
     * const provider = new PhoneAuthProvider(auth);
     * const verificationId = await provider.verifyPhoneNumber(phoneNumber, applicationVerifier);
     * // Obtain verificationCode from the user.
     * const authCredential = PhoneAuthProvider.credential(verificationId, verificationCode);
     * const userCredential = await signInWithCredential(auth, authCredential);
     * ```
     *
     * @example
     * An alternative flow is provided using the `signInWithPhoneNumber` method.
     * ```javascript
     * const confirmationResult = signInWithPhoneNumber(auth, phoneNumber, applicationVerifier);
     * // Obtain verificationCode from the user.
     * const userCredential = confirmationResult.confirm(verificationCode);
     * ```
     *
     * @param phoneInfoOptions - The user's ***REMOVED***@link PhoneInfoOptions}. The phone number should be in
     * E.164 format (e.g. +16505550101).
     * @param applicationVerifier - For abuse prevention, this method also requires a
     * ***REMOVED***@link ApplicationVerifier}. This SDK includes a reCAPTCHA-based implementation,
     * ***REMOVED***@link RecaptchaVerifier}.
     *
     * @returns A Promise for a verification ID that can be passed to
     * ***REMOVED***@link PhoneAuthProvider.credential} to identify this flow..
     */
    verifyPhoneNumber(phoneOptions, applicationVerifier) ***REMOVED***
        return _verifyPhoneNumber(this.auth, phoneOptions, getModularInstance(applicationVerifier));
  ***REMOVED***
    /**
     * Creates a phone auth credential, given the verification ID from
     * ***REMOVED***@link PhoneAuthProvider.verifyPhoneNumber} and the code that was sent to the user's
     * mobile device.
     *
     * @example
     * ```javascript
     * const provider = new PhoneAuthProvider(auth);
     * const verificationId = provider.verifyPhoneNumber(phoneNumber, applicationVerifier);
     * // Obtain verificationCode from the user.
     * const authCredential = PhoneAuthProvider.credential(verificationId, verificationCode);
     * const userCredential = signInWithCredential(auth, authCredential);
     * ```
     *
     * @example
     * An alternative flow is provided using the `signInWithPhoneNumber` method.
     * ```javascript
     * const confirmationResult = await signInWithPhoneNumber(auth, phoneNumber, applicationVerifier);
     * // Obtain verificationCode from the user.
     * const userCredential = await confirmationResult.confirm(verificationCode);
     * ```
     *
     * @param verificationId - The verification ID returned from ***REMOVED***@link PhoneAuthProvider.verifyPhoneNumber}.
     * @param verificationCode - The verification code sent to the user's mobile device.
     *
     * @returns The auth provider credential.
     */
    static credential(verificationId, verificationCode) ***REMOVED***
        return PhoneAuthCredential._fromVerification(verificationId, verificationCode);
  ***REMOVED***
    /**
     * Generates an ***REMOVED***@link AuthCredential} from a ***REMOVED***@link UserCredential}.
     * @param userCredential - The user credential.
     */
    static credentialFromResult(userCredential) ***REMOVED***
        const credential = userCredential;
        return PhoneAuthProvider.credentialFromTaggedObject(credential);
  ***REMOVED***
    /**
     * Returns an ***REMOVED***@link AuthCredential} when passed an error.
     *
     * @remarks
     *
     * This method works for errors like
     * `auth/account-exists-with-different-credentials`. This is useful for
     * recovering when attempting to set a user's phone number but the number
     * in question is already tied to another account. For example, the following
     * code tries to update the current user's phone number, and if that
     * fails, links the user with the account associated with that number:
     *
     * ```js
     * const provider = new PhoneAuthProvider(auth);
     * const verificationId = await provider.verifyPhoneNumber(number, verifier);
     * try ***REMOVED***
     *   const code = ''; // Prompt the user for the verification code
     *   await updatePhoneNumber(
     *       auth.currentUser,
     *       PhoneAuthProvider.credential(verificationId, code));
     * } catch (e) ***REMOVED***
     *   if ((e as FirebaseError)?.code === 'auth/account-exists-with-different-credential') ***REMOVED***
     *     const cred = PhoneAuthProvider.credentialFromError(e);
     *     await linkWithCredential(auth.currentUser, cred);
     * ***REMOVED***
     * }
     *
     * // At this point, auth.currentUser.phoneNumber === number.
     * ```
     *
     * @param error - The error to generate a credential from.
     */
    static credentialFromError(error) ***REMOVED***
        return PhoneAuthProvider.credentialFromTaggedObject((error.customData || ***REMOVED***}));
  ***REMOVED***
    static credentialFromTaggedObject(***REMOVED*** _tokenResponse: tokenResponse }) ***REMOVED***
        if (!tokenResponse) ***REMOVED***
            return null;
      ***REMOVED***
        const ***REMOVED*** phoneNumber, temporaryProof } = tokenResponse;
        if (phoneNumber && temporaryProof) ***REMOVED***
            return PhoneAuthCredential._fromTokenResponse(phoneNumber, temporaryProof);
      ***REMOVED***
        return null;
  ***REMOVED***
}
/** Always set to ***REMOVED***@link ProviderId}.PHONE. */
PhoneAuthProvider.PROVIDER_ID = "phone" /* ProviderId.PHONE */;
/** Always set to ***REMOVED***@link SignInMethod}.PHONE. */
PhoneAuthProvider.PHONE_SIGN_IN_METHOD = "phone" /* SignInMethod.PHONE */;

/**
 * @license
 * Copyright 2021 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Chooses a popup/redirect resolver to use. This prefers the override (which
 * is directly passed in), and falls back to the property set on the auth
 * object. If neither are available, this function errors w/ an argument error.
 */
function _withDefaultResolver(auth, resolverOverride) ***REMOVED***
    if (resolverOverride) ***REMOVED***
        return _getInstance(resolverOverride);
  ***REMOVED***
    _assert(auth._popupRedirectResolver, auth, "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */);
    return auth._popupRedirectResolver;
}

/**
 * @license
 * Copyright 2019 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class IdpCredential extends AuthCredential ***REMOVED***
    constructor(params) ***REMOVED***
        super("custom" /* ProviderId.CUSTOM */, "custom" /* ProviderId.CUSTOM */);
        this.params = params;
  ***REMOVED***
    _getIdTokenResponse(auth) ***REMOVED***
        return signInWithIdp(auth, this._buildIdpRequest());
  ***REMOVED***
    _linkToIdToken(auth, idToken) ***REMOVED***
        return signInWithIdp(auth, this._buildIdpRequest(idToken));
  ***REMOVED***
    _getReauthenticationResolver(auth) ***REMOVED***
        return signInWithIdp(auth, this._buildIdpRequest());
  ***REMOVED***
    _buildIdpRequest(idToken) ***REMOVED***
        const request = ***REMOVED***
            requestUri: this.params.requestUri,
            sessionId: this.params.sessionId,
            postBody: this.params.postBody,
            tenantId: this.params.tenantId,
            pendingToken: this.params.pendingToken,
            returnSecureToken: true,
            returnIdpCredential: true
      ***REMOVED***;
        if (idToken) ***REMOVED***
            request.idToken = idToken;
      ***REMOVED***
        return request;
  ***REMOVED***
}
function _signIn(params) ***REMOVED***
    return _signInWithCredential(params.auth, new IdpCredential(params), params.bypassAuthState);
}
function _reauth(params) ***REMOVED***
    const ***REMOVED*** auth, user } = params;
    _assert(user, auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
    return _reauthenticate(user, new IdpCredential(params), params.bypassAuthState);
}
async function _link(params) ***REMOVED***
    const ***REMOVED*** auth, user } = params;
    _assert(user, auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
    return _link$1(user, new IdpCredential(params), params.bypassAuthState);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Popup event manager. Handles the popup's entire lifecycle; listens to auth
 * events
 */
class AbstractPopupRedirectOperation ***REMOVED***
    constructor(auth, filter, resolver, user, bypassAuthState = false) ***REMOVED***
        this.auth = auth;
        this.resolver = resolver;
        this.user = user;
        this.bypassAuthState = bypassAuthState;
        this.pendingPromise = null;
        this.eventManager = null;
        this.filter = Array.isArray(filter) ? filter : [filter];
  ***REMOVED***
    execute() ***REMOVED***
        return new Promise(async (resolve, reject) => ***REMOVED***
            this.pendingPromise = ***REMOVED*** resolve, reject };
            try ***REMOVED***
                this.eventManager = await this.resolver._initialize(this.auth);
                await this.onExecution();
                this.eventManager.registerConsumer(this);
          ***REMOVED***
            catch (e) ***REMOVED***
                this.reject(e);
          ***REMOVED***
      ***REMOVED***);
  ***REMOVED***
    async onAuthEvent(event) ***REMOVED***
        const ***REMOVED*** urlResponse, sessionId, postBody, tenantId, error, type } = event;
        if (error) ***REMOVED***
            this.reject(error);
            return;
      ***REMOVED***
        const params = ***REMOVED***
            auth: this.auth,
            requestUri: urlResponse,
            sessionId: sessionId,
            tenantId: tenantId || undefined,
            postBody: postBody || undefined,
            user: this.user,
            bypassAuthState: this.bypassAuthState
      ***REMOVED***;
        try ***REMOVED***
            this.resolve(await this.getIdpTask(type)(params));
      ***REMOVED***
        catch (e) ***REMOVED***
            this.reject(e);
      ***REMOVED***
  ***REMOVED***
    onError(error) ***REMOVED***
        this.reject(error);
  ***REMOVED***
    getIdpTask(type) ***REMOVED***
        switch (type) ***REMOVED***
            case "signInViaPopup" /* AuthEventType.SIGN_IN_VIA_POPUP */:
            case "signInViaRedirect" /* AuthEventType.SIGN_IN_VIA_REDIRECT */:
                return _signIn;
            case "linkViaPopup" /* AuthEventType.LINK_VIA_POPUP */:
            case "linkViaRedirect" /* AuthEventType.LINK_VIA_REDIRECT */:
                return _link;
            case "reauthViaPopup" /* AuthEventType.REAUTH_VIA_POPUP */:
            case "reauthViaRedirect" /* AuthEventType.REAUTH_VIA_REDIRECT */:
                return _reauth;
            default:
                _fail(this.auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
      ***REMOVED***
  ***REMOVED***
    resolve(cred) ***REMOVED***
        debugAssert(this.pendingPromise, 'Pending promise was never set');
        this.pendingPromise.resolve(cred);
        this.unregisterAndCleanUp();
  ***REMOVED***
    reject(error) ***REMOVED***
        debugAssert(this.pendingPromise, 'Pending promise was never set');
        this.pendingPromise.reject(error);
        this.unregisterAndCleanUp();
  ***REMOVED***
    unregisterAndCleanUp() ***REMOVED***
        if (this.eventManager) ***REMOVED***
            this.eventManager.unregisterConsumer(this);
      ***REMOVED***
        this.pendingPromise = null;
        this.cleanUp();
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const _POLL_WINDOW_CLOSE_TIMEOUT = new Delay(2000, 10000);
/**
 * Authenticates a Firebase client using a popup-based OAuth authentication flow.
 *
 * @remarks
 * If succeeds, returns the signed in user along with the provider's credential. If sign in was
 * unsuccessful, returns an error object containing additional information about the error.
 *
 * @example
 * ```javascript
 * // Sign in using a popup.
 * const provider = new FacebookAuthProvider();
 * const result = await signInWithPopup(auth, provider);
 *
 * // The signed-in user info.
 * const user = result.user;
 * // This gives you a Facebook Access Token.
 * const credential = provider.credentialFromResult(auth, result);
 * const token = credential.accessToken;
 * ```
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param provider - The provider to authenticate. The provider has to be an ***REMOVED***@link OAuthProvider}.
 * Non-OAuth providers like ***REMOVED***@link EmailAuthProvider} will throw an error.
 * @param resolver - An instance of ***REMOVED***@link PopupRedirectResolver}, optional
 * if already supplied to ***REMOVED***@link initializeAuth} or provided by ***REMOVED***@link getAuth}.
 *
 *
 * @public
 */
async function signInWithPopup(auth, provider, resolver) ***REMOVED***
    const authInternal = _castAuth(auth);
    _assertInstanceOf(auth, provider, FederatedAuthProvider);
    const resolverInternal = _withDefaultResolver(authInternal, resolver);
    const action = new PopupOperation(authInternal, "signInViaPopup" /* AuthEventType.SIGN_IN_VIA_POPUP */, provider, resolverInternal);
    return action.executeNotNull();
}
/**
 * Reauthenticates the current user with the specified ***REMOVED***@link OAuthProvider} using a pop-up based
 * OAuth flow.
 *
 * @remarks
 * If the reauthentication is successful, the returned result will contain the user and the
 * provider's credential.
 *
 * @example
 * ```javascript
 * // Sign in using a popup.
 * const provider = new FacebookAuthProvider();
 * const result = await signInWithPopup(auth, provider);
 * // Reauthenticate using a popup.
 * await reauthenticateWithPopup(result.user, provider);
 * ```
 *
 * @param user - The user.
 * @param provider - The provider to authenticate. The provider has to be an ***REMOVED***@link OAuthProvider}.
 * Non-OAuth providers like ***REMOVED***@link EmailAuthProvider} will throw an error.
 * @param resolver - An instance of ***REMOVED***@link PopupRedirectResolver}, optional
 * if already supplied to ***REMOVED***@link initializeAuth} or provided by ***REMOVED***@link getAuth}.
 *
 * @public
 */
async function reauthenticateWithPopup(user, provider, resolver) ***REMOVED***
    const userInternal = getModularInstance(user);
    _assertInstanceOf(userInternal.auth, provider, FederatedAuthProvider);
    const resolverInternal = _withDefaultResolver(userInternal.auth, resolver);
    const action = new PopupOperation(userInternal.auth, "reauthViaPopup" /* AuthEventType.REAUTH_VIA_POPUP */, provider, resolverInternal, userInternal);
    return action.executeNotNull();
}
/**
 * Links the authenticated provider to the user account using a pop-up based OAuth flow.
 *
 * @remarks
 * If the linking is successful, the returned result will contain the user and the provider's credential.
 *
 *
 * @example
 * ```javascript
 * // Sign in using some other provider.
 * const result = await signInWithEmailAndPassword(auth, email, password);
 * // Link using a popup.
 * const provider = new FacebookAuthProvider();
 * await linkWithPopup(result.user, provider);
 * ```
 *
 * @param user - The user.
 * @param provider - The provider to authenticate. The provider has to be an ***REMOVED***@link OAuthProvider}.
 * Non-OAuth providers like ***REMOVED***@link EmailAuthProvider} will throw an error.
 * @param resolver - An instance of ***REMOVED***@link PopupRedirectResolver}, optional
 * if already supplied to ***REMOVED***@link initializeAuth} or provided by ***REMOVED***@link getAuth}.
 *
 * @public
 */
async function linkWithPopup(user, provider, resolver) ***REMOVED***
    const userInternal = getModularInstance(user);
    _assertInstanceOf(userInternal.auth, provider, FederatedAuthProvider);
    const resolverInternal = _withDefaultResolver(userInternal.auth, resolver);
    const action = new PopupOperation(userInternal.auth, "linkViaPopup" /* AuthEventType.LINK_VIA_POPUP */, provider, resolverInternal, userInternal);
    return action.executeNotNull();
}
/**
 * Popup event manager. Handles the popup's entire lifecycle; listens to auth
 * events
 *
 */
class PopupOperation extends AbstractPopupRedirectOperation ***REMOVED***
    constructor(auth, filter, provider, resolver, user) ***REMOVED***
        super(auth, filter, resolver, user);
        this.provider = provider;
        this.authWindow = null;
        this.pollId = null;
        if (PopupOperation.currentPopupAction) ***REMOVED***
            PopupOperation.currentPopupAction.cancel();
      ***REMOVED***
        PopupOperation.currentPopupAction = this;
  ***REMOVED***
    async executeNotNull() ***REMOVED***
        const result = await this.execute();
        _assert(result, this.auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
        return result;
  ***REMOVED***
    async onExecution() ***REMOVED***
        debugAssert(this.filter.length === 1, 'Popup operations only handle one event');
        const eventId = _generateEventId();
        this.authWindow = await this.resolver._openPopup(this.auth, this.provider, this.filter[0], // There's always one, see constructor
        eventId);
        this.authWindow.associatedEvent = eventId;
        // Check for web storage support and origin validation _after_ the popup is
        // loaded. These operations are slow (~1 second or so) Rather than
        // waiting on them before opening the window, optimistically open the popup
        // and check for storage support at the same time. If storage support is
        // not available, this will cause the whole thing to reject properly. It
        // will also close the popup, but since the promise has already rejected,
        // the popup closed by user poll will reject into the void.
        this.resolver._originValidation(this.auth).catch(e => ***REMOVED***
            this.reject(e);
      ***REMOVED***);
        this.resolver._isIframeWebStorageSupported(this.auth, isSupported => ***REMOVED***
            if (!isSupported) ***REMOVED***
                this.reject(_createError(this.auth, "web-storage-unsupported" /* AuthErrorCode.WEB_STORAGE_UNSUPPORTED */));
          ***REMOVED***
      ***REMOVED***);
        // Handle user closure. Notice this does *not* use await
        this.pollUserCancellation();
  ***REMOVED***
    get eventId() ***REMOVED***
        var _a;
        return ((_a = this.authWindow) === null || _a === void 0 ? void 0 : _a.associatedEvent) || null;
  ***REMOVED***
    cancel() ***REMOVED***
        this.reject(_createError(this.auth, "cancelled-popup-request" /* AuthErrorCode.EXPIRED_POPUP_REQUEST */));
  ***REMOVED***
    cleanUp() ***REMOVED***
        if (this.authWindow) ***REMOVED***
            this.authWindow.close();
      ***REMOVED***
        if (this.pollId) ***REMOVED***
            window.clearTimeout(this.pollId);
      ***REMOVED***
        this.authWindow = null;
        this.pollId = null;
        PopupOperation.currentPopupAction = null;
  ***REMOVED***
    pollUserCancellation() ***REMOVED***
        const poll = () => ***REMOVED***
            var _a, _b;
            if ((_b = (_a = this.authWindow) === null || _a === void 0 ? void 0 : _a.window) === null || _b === void 0 ? void 0 : _b.closed) ***REMOVED***
                // Make sure that there is sufficient time for whatever action to
                // complete. The window could have closed but the sign in network
                // call could still be in flight.
                this.pollId = window.setTimeout(() => ***REMOVED***
                    this.pollId = null;
                    this.reject(_createError(this.auth, "popup-closed-by-user" /* AuthErrorCode.POPUP_CLOSED_BY_USER */));
              ***REMOVED***, 2000 /* _Timeout.AUTH_EVENT */);
                return;
          ***REMOVED***
            this.pollId = window.setTimeout(poll, _POLL_WINDOW_CLOSE_TIMEOUT.get());
      ***REMOVED***;
        poll();
  ***REMOVED***
}
// Only one popup is ever shown at once. The lifecycle of the current popup
// can be managed / cancelled by the constructor.
PopupOperation.currentPopupAction = null;

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const PENDING_REDIRECT_KEY = 'pendingRedirect';
// We only get one redirect outcome for any one auth, so just store it
// in here.
const redirectOutcomeMap = new Map();
class RedirectAction extends AbstractPopupRedirectOperation ***REMOVED***
    constructor(auth, resolver, bypassAuthState = false) ***REMOVED***
        super(auth, [
            "signInViaRedirect" /* AuthEventType.SIGN_IN_VIA_REDIRECT */,
            "linkViaRedirect" /* AuthEventType.LINK_VIA_REDIRECT */,
            "reauthViaRedirect" /* AuthEventType.REAUTH_VIA_REDIRECT */,
            "unknown" /* AuthEventType.UNKNOWN */
        ], resolver, undefined, bypassAuthState);
        this.eventId = null;
  ***REMOVED***
    /**
     * Override the execute function; if we already have a redirect result, then
     * just return it.
     */
    async execute() ***REMOVED***
        let readyOutcome = redirectOutcomeMap.get(this.auth._key());
        if (!readyOutcome) ***REMOVED***
            try ***REMOVED***
                const hasPendingRedirect = await _getAndClearPendingRedirectStatus(this.resolver, this.auth);
                const result = hasPendingRedirect ? await super.execute() : null;
                readyOutcome = () => Promise.resolve(result);
          ***REMOVED***
            catch (e) ***REMOVED***
                readyOutcome = () => Promise.reject(e);
          ***REMOVED***
            redirectOutcomeMap.set(this.auth._key(), readyOutcome);
      ***REMOVED***
        // If we're not bypassing auth state, the ready outcome should be set to
        // null.
        if (!this.bypassAuthState) ***REMOVED***
            redirectOutcomeMap.set(this.auth._key(), () => Promise.resolve(null));
      ***REMOVED***
        return readyOutcome();
  ***REMOVED***
    async onAuthEvent(event) ***REMOVED***
        if (event.type === "signInViaRedirect" /* AuthEventType.SIGN_IN_VIA_REDIRECT */) ***REMOVED***
            return super.onAuthEvent(event);
      ***REMOVED***
        else if (event.type === "unknown" /* AuthEventType.UNKNOWN */) ***REMOVED***
            // This is a sentinel value indicating there's no pending redirect
            this.resolve(null);
            return;
      ***REMOVED***
        if (event.eventId) ***REMOVED***
            const user = await this.auth._redirectUserForId(event.eventId);
            if (user) ***REMOVED***
                this.user = user;
                return super.onAuthEvent(event);
          ***REMOVED***
            else ***REMOVED***
                this.resolve(null);
          ***REMOVED***
      ***REMOVED***
  ***REMOVED***
    async onExecution() ***REMOVED*** }
    cleanUp() ***REMOVED*** }
}
async function _getAndClearPendingRedirectStatus(resolver, auth) ***REMOVED***
    const key = pendingRedirectKey(auth);
    const persistence = resolverPersistence(resolver);
    if (!(await persistence._isAvailable())) ***REMOVED***
        return false;
  ***REMOVED***
    const hasPendingRedirect = (await persistence._get(key)) === 'true';
    await persistence._remove(key);
    return hasPendingRedirect;
}
async function _setPendingRedirectStatus(resolver, auth) ***REMOVED***
    return resolverPersistence(resolver)._set(pendingRedirectKey(auth), 'true');
}
function _clearRedirectOutcomes() ***REMOVED***
    redirectOutcomeMap.clear();
}
function _overrideRedirectResult(auth, result) ***REMOVED***
    redirectOutcomeMap.set(auth._key(), result);
}
function resolverPersistence(resolver) ***REMOVED***
    return _getInstance(resolver._redirectPersistence);
}
function pendingRedirectKey(auth) ***REMOVED***
    return _persistenceKeyName(PENDING_REDIRECT_KEY, auth.config.apiKey, auth.name);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * Authenticates a Firebase client using a full-page redirect flow.
 *
 * @remarks
 * To handle the results and errors for this operation, refer to ***REMOVED***@link getRedirectResult}.
 * Follow the [best practices](https://firebase.google.com/docs/auth/web/redirect-best-practices) when using ***REMOVED***@link signInWithRedirect}.
 *
 * @example
 * ```javascript
 * // Sign in using a redirect.
 * const provider = new FacebookAuthProvider();
 * // You can add additional scopes to the provider:
 * provider.addScope('user_birthday');
 * // Start a sign in process for an unauthenticated user.
 * await signInWithRedirect(auth, provider);
 * // This will trigger a full page redirect away from your app
 *
 * // After returning from the redirect when your app initializes you can obtain the result
 * const result = await getRedirectResult(auth);
 * if (result) ***REMOVED***
 *   // This is the signed-in user
 *   const user = result.user;
 *   // This gives you a Facebook Access Token.
 *   const credential = provider.credentialFromResult(auth, result);
 *   const token = credential.accessToken;
 * }
 * // As this API can be used for sign-in, linking and reauthentication,
 * // check the operationType to determine what triggered this redirect
 * // operation.
 * const operationType = result.operationType;
 * ```
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param provider - The provider to authenticate. The provider has to be an ***REMOVED***@link OAuthProvider}.
 * Non-OAuth providers like ***REMOVED***@link EmailAuthProvider} will throw an error.
 * @param resolver - An instance of ***REMOVED***@link PopupRedirectResolver}, optional
 * if already supplied to ***REMOVED***@link initializeAuth} or provided by ***REMOVED***@link getAuth}.
 *
 * @public
 */
function signInWithRedirect(auth, provider, resolver) ***REMOVED***
    return _signInWithRedirect(auth, provider, resolver);
}
async function _signInWithRedirect(auth, provider, resolver) ***REMOVED***
    const authInternal = _castAuth(auth);
    _assertInstanceOf(auth, provider, FederatedAuthProvider);
    // Wait for auth initialization to complete, this will process pending redirects and clear the
    // PENDING_REDIRECT_KEY in persistence. This should be completed before starting a new
    // redirect and creating a PENDING_REDIRECT_KEY entry.
    await authInternal._initializationPromise;
    const resolverInternal = _withDefaultResolver(authInternal, resolver);
    await _setPendingRedirectStatus(resolverInternal, authInternal);
    return resolverInternal._openRedirect(authInternal, provider, "signInViaRedirect" /* AuthEventType.SIGN_IN_VIA_REDIRECT */);
}
/**
 * Reauthenticates the current user with the specified ***REMOVED***@link OAuthProvider} using a full-page redirect flow.
 * @remarks
 * To handle the results and errors for this operation, refer to ***REMOVED***@link getRedirectResult}.
 * Follow the [best practices](https://firebase.google.com/docs/auth/web/redirect-best-practices) when using ***REMOVED***@link reauthenticateWithRedirect}.
 *
 * @example
 * ```javascript
 * // Sign in using a redirect.
 * const provider = new FacebookAuthProvider();
 * const result = await signInWithRedirect(auth, provider);
 * // This will trigger a full page redirect away from your app
 *
 * // After returning from the redirect when your app initializes you can obtain the result
 * const result = await getRedirectResult(auth);
 * // Reauthenticate using a redirect.
 * await reauthenticateWithRedirect(result.user, provider);
 * // This will again trigger a full page redirect away from your app
 *
 * // After returning from the redirect when your app initializes you can obtain the result
 * const result = await getRedirectResult(auth);
 * ```
 *
 * @param user - The user.
 * @param provider - The provider to authenticate. The provider has to be an ***REMOVED***@link OAuthProvider}.
 * Non-OAuth providers like ***REMOVED***@link EmailAuthProvider} will throw an error.
 * @param resolver - An instance of ***REMOVED***@link PopupRedirectResolver}, optional
 * if already supplied to ***REMOVED***@link initializeAuth} or provided by ***REMOVED***@link getAuth}.
 *
 * @public
 */
function reauthenticateWithRedirect(user, provider, resolver) ***REMOVED***
    return _reauthenticateWithRedirect(user, provider, resolver);
}
async function _reauthenticateWithRedirect(user, provider, resolver) ***REMOVED***
    const userInternal = getModularInstance(user);
    _assertInstanceOf(userInternal.auth, provider, FederatedAuthProvider);
    // Wait for auth initialization to complete, this will process pending redirects and clear the
    // PENDING_REDIRECT_KEY in persistence. This should be completed before starting a new
    // redirect and creating a PENDING_REDIRECT_KEY entry.
    await userInternal.auth._initializationPromise;
    // Allow the resolver to error before persisting the redirect user
    const resolverInternal = _withDefaultResolver(userInternal.auth, resolver);
    await _setPendingRedirectStatus(resolverInternal, userInternal.auth);
    const eventId = await prepareUserForRedirect(userInternal);
    return resolverInternal._openRedirect(userInternal.auth, provider, "reauthViaRedirect" /* AuthEventType.REAUTH_VIA_REDIRECT */, eventId);
}
/**
 * Links the ***REMOVED***@link OAuthProvider} to the user account using a full-page redirect flow.
 * @remarks
 * To handle the results and errors for this operation, refer to ***REMOVED***@link getRedirectResult}.
 * Follow the [best practices](https://firebase.google.com/docs/auth/web/redirect-best-practices) when using ***REMOVED***@link linkWithRedirect}.
 *
 * @example
 * ```javascript
 * // Sign in using some other provider.
 * const result = await signInWithEmailAndPassword(auth, email, password);
 * // Link using a redirect.
 * const provider = new FacebookAuthProvider();
 * await linkWithRedirect(result.user, provider);
 * // This will trigger a full page redirect away from your app
 *
 * // After returning from the redirect when your app initializes you can obtain the result
 * const result = await getRedirectResult(auth);
 * ```
 *
 * @param user - The user.
 * @param provider - The provider to authenticate. The provider has to be an ***REMOVED***@link OAuthProvider}.
 * Non-OAuth providers like ***REMOVED***@link EmailAuthProvider} will throw an error.
 * @param resolver - An instance of ***REMOVED***@link PopupRedirectResolver}, optional
 * if already supplied to ***REMOVED***@link initializeAuth} or provided by ***REMOVED***@link getAuth}.
 *
 *
 * @public
 */
function linkWithRedirect(user, provider, resolver) ***REMOVED***
    return _linkWithRedirect(user, provider, resolver);
}
async function _linkWithRedirect(user, provider, resolver) ***REMOVED***
    const userInternal = getModularInstance(user);
    _assertInstanceOf(userInternal.auth, provider, FederatedAuthProvider);
    // Wait for auth initialization to complete, this will process pending redirects and clear the
    // PENDING_REDIRECT_KEY in persistence. This should be completed before starting a new
    // redirect and creating a PENDING_REDIRECT_KEY entry.
    await userInternal.auth._initializationPromise;
    // Allow the resolver to error before persisting the redirect user
    const resolverInternal = _withDefaultResolver(userInternal.auth, resolver);
    await _assertLinkedStatus(false, userInternal, provider.providerId);
    await _setPendingRedirectStatus(resolverInternal, userInternal.auth);
    const eventId = await prepareUserForRedirect(userInternal);
    return resolverInternal._openRedirect(userInternal.auth, provider, "linkViaRedirect" /* AuthEventType.LINK_VIA_REDIRECT */, eventId);
}
/**
 * Returns a ***REMOVED***@link UserCredential} from the redirect-based sign-in flow.
 *
 * @remarks
 * If sign-in succeeded, returns the signed in user. If sign-in was unsuccessful, fails with an
 * error. If no redirect operation was called, returns `null`.
 *
 * @example
 * ```javascript
 * // Sign in using a redirect.
 * const provider = new FacebookAuthProvider();
 * // You can add additional scopes to the provider:
 * provider.addScope('user_birthday');
 * // Start a sign in process for an unauthenticated user.
 * await signInWithRedirect(auth, provider);
 * // This will trigger a full page redirect away from your app
 *
 * // After returning from the redirect when your app initializes you can obtain the result
 * const result = await getRedirectResult(auth);
 * if (result) ***REMOVED***
 *   // This is the signed-in user
 *   const user = result.user;
 *   // This gives you a Facebook Access Token.
 *   const credential = provider.credentialFromResult(auth, result);
 *   const token = credential.accessToken;
 * }
 * // As this API can be used for sign-in, linking and reauthentication,
 * // check the operationType to determine what triggered this redirect
 * // operation.
 * const operationType = result.operationType;
 * ```
 *
 * @param auth - The ***REMOVED***@link Auth} instance.
 * @param resolver - An instance of ***REMOVED***@link PopupRedirectResolver}, optional
 * if already supplied to ***REMOVED***@link initializeAuth} or provided by ***REMOVED***@link getAuth}.
 *
 * @public
 */
async function getRedirectResult(auth, resolver) ***REMOVED***
    await _castAuth(auth)._initializationPromise;
    return _getRedirectResult(auth, resolver, false);
}
async function _getRedirectResult(auth, resolverExtern, bypassAuthState = false) ***REMOVED***
    const authInternal = _castAuth(auth);
    const resolver = _withDefaultResolver(authInternal, resolverExtern);
    const action = new RedirectAction(authInternal, resolver, bypassAuthState);
    const result = await action.execute();
    if (result && !bypassAuthState) ***REMOVED***
        delete result.user._redirectEventId;
        await authInternal._persistUserIfCurrent(result.user);
        await authInternal._setRedirectUser(null, resolverExtern);
  ***REMOVED***
    return result;
}
async function prepareUserForRedirect(user) ***REMOVED***
    const eventId = _generateEventId(`$***REMOVED***user.uid}:::`);
    user._redirectEventId = eventId;
    await user.auth._setRedirectUser(user);
    await user.auth._persistUserIfCurrent(user);
    return eventId;
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// The amount of time to store the UIDs of seen events; this is
// set to 10 min by default
const EVENT_DUPLICATION_CACHE_DURATION_MS = 10 * 60 * 1000;
class AuthEventManager ***REMOVED***
    constructor(auth) ***REMOVED***
        this.auth = auth;
        this.cachedEventUids = new Set();
        this.consumers = new Set();
        this.queuedRedirectEvent = null;
        this.hasHandledPotentialRedirect = false;
        this.lastProcessedEventTime = Date.now();
  ***REMOVED***
    registerConsumer(authEventConsumer) ***REMOVED***
        this.consumers.add(authEventConsumer);
        if (this.queuedRedirectEvent &&
            this.isEventForConsumer(this.queuedRedirectEvent, authEventConsumer)) ***REMOVED***
            this.sendToConsumer(this.queuedRedirectEvent, authEventConsumer);
            this.saveEventToCache(this.queuedRedirectEvent);
            this.queuedRedirectEvent = null;
      ***REMOVED***
  ***REMOVED***
    unregisterConsumer(authEventConsumer) ***REMOVED***
        this.consumers.delete(authEventConsumer);
  ***REMOVED***
    onEvent(event) ***REMOVED***
        // Check if the event has already been handled
        if (this.hasEventBeenHandled(event)) ***REMOVED***
            return false;
      ***REMOVED***
        let handled = false;
        this.consumers.forEach(consumer => ***REMOVED***
            if (this.isEventForConsumer(event, consumer)) ***REMOVED***
                handled = true;
                this.sendToConsumer(event, consumer);
                this.saveEventToCache(event);
          ***REMOVED***
      ***REMOVED***);
        if (this.hasHandledPotentialRedirect || !isRedirectEvent(event)) ***REMOVED***
            // If we've already seen a redirect before, or this is a popup event,
            // bail now
            return handled;
      ***REMOVED***
        this.hasHandledPotentialRedirect = true;
        // If the redirect wasn't handled, hang on to it
        if (!handled) ***REMOVED***
            this.queuedRedirectEvent = event;
            handled = true;
      ***REMOVED***
        return handled;
  ***REMOVED***
    sendToConsumer(event, consumer) ***REMOVED***
        var _a;
        if (event.error && !isNullRedirectEvent(event)) ***REMOVED***
            const code = ((_a = event.error.code) === null || _a === void 0 ? void 0 : _a.split('auth/')[1]) ||
                "internal-error" /* AuthErrorCode.INTERNAL_ERROR */;
            consumer.onError(_createError(this.auth, code));
      ***REMOVED***
        else ***REMOVED***
            consumer.onAuthEvent(event);
      ***REMOVED***
  ***REMOVED***
    isEventForConsumer(event, consumer) ***REMOVED***
        const eventIdMatches = consumer.eventId === null ||
            (!!event.eventId && event.eventId === consumer.eventId);
        return consumer.filter.includes(event.type) && eventIdMatches;
  ***REMOVED***
    hasEventBeenHandled(event) ***REMOVED***
        if (Date.now() - this.lastProcessedEventTime >=
            EVENT_DUPLICATION_CACHE_DURATION_MS) ***REMOVED***
            this.cachedEventUids.clear();
      ***REMOVED***
        return this.cachedEventUids.has(eventUid(event));
  ***REMOVED***
    saveEventToCache(event) ***REMOVED***
        this.cachedEventUids.add(eventUid(event));
        this.lastProcessedEventTime = Date.now();
  ***REMOVED***
}
function eventUid(e) ***REMOVED***
    return [e.type, e.eventId, e.sessionId, e.tenantId].filter(v => v).join('-');
}
function isNullRedirectEvent(***REMOVED*** type, error }) ***REMOVED***
    return (type === "unknown" /* AuthEventType.UNKNOWN */ &&
        (error === null || error === void 0 ? void 0 : error.code) === `auth/$***REMOVED***"no-auth-event" /* AuthErrorCode.NO_AUTH_EVENT */}`);
}
function isRedirectEvent(event) ***REMOVED***
    switch (event.type) ***REMOVED***
        case "signInViaRedirect" /* AuthEventType.SIGN_IN_VIA_REDIRECT */:
        case "linkViaRedirect" /* AuthEventType.LINK_VIA_REDIRECT */:
        case "reauthViaRedirect" /* AuthEventType.REAUTH_VIA_REDIRECT */:
            return true;
        case "unknown" /* AuthEventType.UNKNOWN */:
            return isNullRedirectEvent(event);
        default:
            return false;
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
async function _getProjectConfig(auth, request = ***REMOVED***}) ***REMOVED***
    return _performApiRequest(auth, "GET" /* HttpMethod.GET */, "/v1/projects" /* Endpoint.GET_PROJECT_CONFIG */, request);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const IP_ADDRESS_REGEX = /^\d***REMOVED***1,3}\.\d***REMOVED***1,3}\.\d***REMOVED***1,3}\.\d***REMOVED***1,3}$/;
const HTTP_REGEX = /^https?/;
async function _validateOrigin(auth) ***REMOVED***
    // Skip origin validation if we are in an emulated environment
    if (auth.config.emulator) ***REMOVED***
        return;
  ***REMOVED***
    const ***REMOVED*** authorizedDomains } = await _getProjectConfig(auth);
    for (const domain of authorizedDomains) ***REMOVED***
        try ***REMOVED***
            if (matchDomain(domain)) ***REMOVED***
                return;
          ***REMOVED***
      ***REMOVED***
        catch (_a) ***REMOVED***
            // Do nothing if there's a URL error; just continue searching
      ***REMOVED***
  ***REMOVED***
    // In the old SDK, this error also provides helpful messages.
    _fail(auth, "unauthorized-domain" /* AuthErrorCode.INVALID_ORIGIN */);
}
function matchDomain(expected) ***REMOVED***
    const currentUrl = _getCurrentUrl();
    const ***REMOVED*** protocol, hostname } = new URL(currentUrl);
    if (expected.startsWith('chrome-extension://')) ***REMOVED***
        const ceUrl = new URL(expected);
        if (ceUrl.hostname === '' && hostname === '') ***REMOVED***
            // For some reason we're not parsing chrome URLs properly
            return (protocol === 'chrome-extension:' &&
                expected.replace('chrome-extension://', '') ===
                    currentUrl.replace('chrome-extension://', ''));
      ***REMOVED***
        return protocol === 'chrome-extension:' && ceUrl.hostname === hostname;
  ***REMOVED***
    if (!HTTP_REGEX.test(protocol)) ***REMOVED***
        return false;
  ***REMOVED***
    if (IP_ADDRESS_REGEX.test(expected)) ***REMOVED***
        // The domain has to be exactly equal to the pattern, as an IP domain will
        // only contain the IP, no extra character.
        return hostname === expected;
  ***REMOVED***
    // Dots in pattern should be escaped.
    const escapedDomainPattern = expected.replace(/\./g, '\\.');
    // Non ip address domains.
    // domain.com = *.domain.com OR domain.com
    const re = new RegExp('^(.+\\.' + escapedDomainPattern + '|' + escapedDomainPattern + ')$', 'i');
    return re.test(hostname);
}

/**
 * @license
 * Copyright 2020 Google LLC.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const NETWORK_TIMEOUT = new Delay(30000, 60000);
/**
 * Reset unlaoded GApi modules. If gapi.load fails due to a network error,
 * it will stop working after a retrial. This is a hack to fix this issue.
 */
function resetUnloadedGapiModules() ***REMOVED***
    // Clear last failed gapi.load state to force next gapi.load to first
    // load the failed gapi.iframes module.
    // Get gapix.beacon context.
    const beacon = _window().___jsl;
    // Get current hint.
    if (beacon === null || beacon === void 0 ? void 0 : beacon.H) ***REMOVED***
        // Get gapi hint.
        for (const hint of Object.keys(beacon.H)) ***REMOVED***
            // Requested modules.
            beacon.H[hint].r = beacon.H[hint].r || [];
            // Loaded modules.
            beacon.H[hint].L = beacon.H[hint].L || [];
            // Set requested modules to a copy of the loaded modules.
            beacon.H[hint].r = [...beacon.H[hint].L];
            // Clear pending callbacks.
            if (beacon.CP) ***REMOVED***
                for (let i = 0; i < beacon.CP.length; i++) ***REMOVED***
                    // Remove all failed pending callbacks.
                    beacon.CP[i] = null;
              ***REMOVED***
          ***REMOVED***
      ***REMOVED***
  ***REMOVED***
}
function loadGapi(auth) ***REMOVED***
    return new Promise((resolve, reject) => ***REMOVED***
        var _a, _b, _c;
        // Function to run when gapi.load is ready.
        function loadGapiIframe() ***REMOVED***
            // The developer may have tried to previously run gapi.load and failed.
            // Run this to fix that.
            resetUnloadedGapiModules();
            gapi.load('gapi.iframes', ***REMOVED***
                callback: () => ***REMOVED***
                    resolve(gapi.iframes.getContext());
              ***REMOVED***,
                ontimeout: () => ***REMOVED***
                    // The above reset may be sufficient, but having this reset after
                    // failure ensures that if the developer calls gapi.load after the
                    // connection is re-established and before another attempt to embed
                    // the iframe, it would work and would not be broken because of our
                    // failed attempt.
                    // Timeout when gapi.iframes.Iframe not loaded.
                    resetUnloadedGapiModules();
                    reject(_createError(auth, "network-request-failed" /* AuthErrorCode.NETWORK_REQUEST_FAILED */));
              ***REMOVED***,
                timeout: NETWORK_TIMEOUT.get()
          ***REMOVED***);
      ***REMOVED***
        if ((_b = (_a = _window().gapi) === null || _a === void 0 ? void 0 : _a.iframes) === null || _b === void 0 ? void 0 : _b.Iframe) ***REMOVED***
            // If gapi.iframes.Iframe available, resolve.
            resolve(gapi.iframes.getContext());
      ***REMOVED***
        else if (!!((_c = _window().gapi) === null || _c === void 0 ? void 0 : _c.load)) ***REMOVED***
            // Gapi loader ready, load gapi.iframes.
            loadGapiIframe();
      ***REMOVED***
        else ***REMOVED***
            // Create a new iframe callback when this is called so as not to overwrite
            // any previous defined callback. This happens if this method is called
            // multiple times in parallel and could result in the later callback
            // overwriting the previous one. This would end up with a iframe
            // timeout.
            const cbName = _generateCallbackName('iframefcb');
            // GApi loader not available, dynamically load platform.js.
            _window()[cbName] = () => ***REMOVED***
                // GApi loader should be ready.
                if (!!gapi.load) ***REMOVED***
                    loadGapiIframe();
              ***REMOVED***
                else ***REMOVED***
                    // Gapi loader failed, throw error.
                    reject(_createError(auth, "network-request-failed" /* AuthErrorCode.NETWORK_REQUEST_FAILED */));
              ***REMOVED***
          ***REMOVED***;
            // Load GApi loader.
            return _loadJS(`https://apis.google.com/js/api.js?onload=$***REMOVED***cbName}`)
                .catch(e => reject(e));
      ***REMOVED***
  ***REMOVED***).catch(error => ***REMOVED***
        // Reset cached promise to allow for retrial.
        cachedGApiLoader = null;
        throw error;
  ***REMOVED***);
}
let cachedGApiLoader = null;
function _loadGapi(auth) ***REMOVED***
    cachedGApiLoader = cachedGApiLoader || loadGapi(auth);
    return cachedGApiLoader;
}

/**
 * @license
 * Copyright 2020 Google LLC.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const PING_TIMEOUT = new Delay(5000, 15000);
const IFRAME_PATH = '__/auth/iframe';
const EMULATED_IFRAME_PATH = 'emulator/auth/iframe';
const IFRAME_ATTRIBUTES = ***REMOVED***
    style: ***REMOVED***
        position: 'absolute',
        top: '-100px',
        width: '1px',
        height: '1px'
  ***REMOVED***,
    'aria-hidden': 'true',
    tabindex: '-1'
};
// Map from apiHost to endpoint ID for passing into iframe. In current SDK, apiHost can be set to
// anything (not from a list of endpoints with IDs as in legacy), so this is the closest we can get.
const EID_FROM_APIHOST = new Map([
    ["identitytoolkit.googleapis.com" /* DefaultConfig.API_HOST */, 'p'],
    ['staging-identitytoolkit.sandbox.googleapis.com', 's'],
    ['test-identitytoolkit.sandbox.googleapis.com', 't'] // test
]);
function getIframeUrl(auth) ***REMOVED***
    const config = auth.config;
    _assert(config.authDomain, auth, "auth-domain-config-required" /* AuthErrorCode.MISSING_AUTH_DOMAIN */);
    const url = config.emulator
        ? _emulatorUrl(config, EMULATED_IFRAME_PATH)
        : `https://$***REMOVED***auth.config.authDomain}/$***REMOVED***IFRAME_PATH}`;
    const params = ***REMOVED***
        apiKey: config.apiKey,
        appName: auth.name,
        v: SDK_VERSION
  ***REMOVED***;
    const eid = EID_FROM_APIHOST.get(auth.config.apiHost);
    if (eid) ***REMOVED***
        params.eid = eid;
  ***REMOVED***
    const frameworks = auth._getFrameworks();
    if (frameworks.length) ***REMOVED***
        params.fw = frameworks.join(',');
  ***REMOVED***
    return `$***REMOVED***url}?$***REMOVED***querystring(params).slice(1)}`;
}
async function _openIframe(auth) ***REMOVED***
    const context = await _loadGapi(auth);
    const gapi = _window().gapi;
    _assert(gapi, auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
    return context.open(***REMOVED***
        where: document.body,
        url: getIframeUrl(auth),
        messageHandlersFilter: gapi.iframes.CROSS_ORIGIN_IFRAMES_FILTER,
        attributes: IFRAME_ATTRIBUTES,
        dontclear: true
  ***REMOVED***, (iframe) => new Promise(async (resolve, reject) => ***REMOVED***
        await iframe.restyle(***REMOVED***
            // Prevent iframe from closing on mouse out.
            setHideOnLeave: false
      ***REMOVED***);
        const networkError = _createError(auth, "network-request-failed" /* AuthErrorCode.NETWORK_REQUEST_FAILED */);
        // Confirm iframe is correctly loaded.
        // To fallback on failure, set a timeout.
        const networkErrorTimer = _window().setTimeout(() => ***REMOVED***
            reject(networkError);
      ***REMOVED***, PING_TIMEOUT.get());
        // Clear timer and resolve pending iframe ready promise.
        function clearTimerAndResolve() ***REMOVED***
            _window().clearTimeout(networkErrorTimer);
            resolve(iframe);
      ***REMOVED***
        // This returns an IThenable. However the reject part does not call
        // when the iframe is not loaded.
        iframe.ping(clearTimerAndResolve).then(clearTimerAndResolve, () => ***REMOVED***
            reject(networkError);
      ***REMOVED***);
  ***REMOVED***));
}

/**
 * @license
 * Copyright 2020 Google LLC.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const BASE_POPUP_OPTIONS = ***REMOVED***
    location: 'yes',
    resizable: 'yes',
    statusbar: 'yes',
    toolbar: 'no'
};
const DEFAULT_WIDTH = 500;
const DEFAULT_HEIGHT = 600;
const TARGET_BLANK = '_blank';
const FIREFOX_EMPTY_URL = 'http://localhost';
class AuthPopup ***REMOVED***
    constructor(window) ***REMOVED***
        this.window = window;
        this.associatedEvent = null;
  ***REMOVED***
    close() ***REMOVED***
        if (this.window) ***REMOVED***
            try ***REMOVED***
                this.window.close();
          ***REMOVED***
            catch (e) ***REMOVED*** }
      ***REMOVED***
  ***REMOVED***
}
function _open(auth, url, name, width = DEFAULT_WIDTH, height = DEFAULT_HEIGHT) ***REMOVED***
    const top = Math.max((window.screen.availHeight - height) / 2, 0).toString();
    const left = Math.max((window.screen.availWidth - width) / 2, 0).toString();
    let target = '';
    const options = Object.assign(Object.assign(***REMOVED***}, BASE_POPUP_OPTIONS), ***REMOVED*** width: width.toString(), height: height.toString(), top,
        left });
    // Chrome iOS 7 and 8 is returning an undefined popup win when target is
    // specified, even though the popup is not necessarily blocked.
    const ua = getUA().toLowerCase();
    if (name) ***REMOVED***
        target = _isChromeIOS(ua) ? TARGET_BLANK : name;
  ***REMOVED***
    if (_isFirefox(ua)) ***REMOVED***
        // Firefox complains when invalid URLs are popped out. Hacky way to bypass.
        url = url || FIREFOX_EMPTY_URL;
        // Firefox disables by default scrolling on popup windows, which can create
        // issues when the user has many Google accounts, for instance.
        options.scrollbars = 'yes';
  ***REMOVED***
    const optionsString = Object.entries(options).reduce((accum, [key, value]) => `$***REMOVED***accum}$***REMOVED***key}=$***REMOVED***value},`, '');
    if (_isIOSStandalone(ua) && target !== '_self') ***REMOVED***
        openAsNewWindowIOS(url || '', target);
        return new AuthPopup(null);
  ***REMOVED***
    // about:blank getting sanitized causing browsers like IE/Edge to display
    // brief error message before redirecting to handler.
    const newWin = window.open(url || '', target, optionsString);
    _assert(newWin, auth, "popup-blocked" /* AuthErrorCode.POPUP_BLOCKED */);
    // Flaky on IE edge, encapsulate with a try and catch.
    try ***REMOVED***
        newWin.focus();
  ***REMOVED***
    catch (e) ***REMOVED*** }
    return new AuthPopup(newWin);
}
function openAsNewWindowIOS(url, target) ***REMOVED***
    const el = document.createElement('a');
    el.href = url;
    el.target = target;
    const click = document.createEvent('MouseEvent');
    click.initMouseEvent('click', true, true, window, 1, 0, 0, 0, 0, false, false, false, false, 1, null);
    el.dispatchEvent(click);
}

/**
 * @license
 * Copyright 2021 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * URL for Authentication widget which will initiate the OAuth handshake
 *
 * @internal
 */
const WIDGET_PATH = '__/auth/handler';
/**
 * URL for emulated environment
 *
 * @internal
 */
const EMULATOR_WIDGET_PATH = 'emulator/auth/handler';
function _getRedirectUrl(auth, provider, authType, redirectUrl, eventId, additionalParams) ***REMOVED***
    _assert(auth.config.authDomain, auth, "auth-domain-config-required" /* AuthErrorCode.MISSING_AUTH_DOMAIN */);
    _assert(auth.config.apiKey, auth, "invalid-api-key" /* AuthErrorCode.INVALID_API_KEY */);
    const params = ***REMOVED***
        apiKey: auth.config.apiKey,
        appName: auth.name,
        authType,
        redirectUrl,
        v: SDK_VERSION,
        eventId
  ***REMOVED***;
    if (provider instanceof FederatedAuthProvider) ***REMOVED***
        provider.setDefaultLanguage(auth.languageCode);
        params.providerId = provider.providerId || '';
        if (!isEmpty(provider.getCustomParameters())) ***REMOVED***
            params.customParameters = JSON.stringify(provider.getCustomParameters());
      ***REMOVED***
        // TODO set additionalParams from the provider as well?
        for (const [key, value] of Object.entries(additionalParams || ***REMOVED***})) ***REMOVED***
            params[key] = value;
      ***REMOVED***
  ***REMOVED***
    if (provider instanceof BaseOAuthProvider) ***REMOVED***
        const scopes = provider.getScopes().filter(scope => scope !== '');
        if (scopes.length > 0) ***REMOVED***
            params.scopes = scopes.join(',');
      ***REMOVED***
  ***REMOVED***
    if (auth.tenantId) ***REMOVED***
        params.tid = auth.tenantId;
  ***REMOVED***
    // TODO: maybe set eid as endipointId
    // TODO: maybe set fw as Frameworks.join(",")
    const paramsDict = params;
    for (const key of Object.keys(paramsDict)) ***REMOVED***
        if (paramsDict[key] === undefined) ***REMOVED***
            delete paramsDict[key];
      ***REMOVED***
  ***REMOVED***
    return `$***REMOVED***getHandlerBase(auth)}?$***REMOVED***querystring(paramsDict).slice(1)}`;
}
function getHandlerBase(***REMOVED*** config }) ***REMOVED***
    if (!config.emulator) ***REMOVED***
        return `https://$***REMOVED***config.authDomain}/$***REMOVED***WIDGET_PATH}`;
  ***REMOVED***
    return _emulatorUrl(config, EMULATOR_WIDGET_PATH);
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
/**
 * The special web storage event
 *
 */
const WEB_STORAGE_SUPPORT_KEY = 'webStorageSupport';
class BrowserPopupRedirectResolver ***REMOVED***
    constructor() ***REMOVED***
        this.eventManagers = ***REMOVED***};
        this.iframes = ***REMOVED***};
        this.originValidationPromises = ***REMOVED***};
        this._redirectPersistence = browserSessionPersistence;
        this._completeRedirectFn = _getRedirectResult;
        this._overrideRedirectResult = _overrideRedirectResult;
  ***REMOVED***
    // Wrapping in async even though we don't await anywhere in order
    // to make sure errors are raised as promise rejections
    async _openPopup(auth, provider, authType, eventId) ***REMOVED***
        var _a;
        debugAssert((_a = this.eventManagers[auth._key()]) === null || _a === void 0 ? void 0 : _a.manager, '_initialize() not called before _openPopup()');
        const url = _getRedirectUrl(auth, provider, authType, _getCurrentUrl(), eventId);
        return _open(auth, url, _generateEventId());
  ***REMOVED***
    async _openRedirect(auth, provider, authType, eventId) ***REMOVED***
        await this._originValidation(auth);
        _setWindowLocation(_getRedirectUrl(auth, provider, authType, _getCurrentUrl(), eventId));
        return new Promise(() => ***REMOVED*** });
  ***REMOVED***
    _initialize(auth) ***REMOVED***
        const key = auth._key();
        if (this.eventManagers[key]) ***REMOVED***
            const ***REMOVED*** manager, promise } = this.eventManagers[key];
            if (manager) ***REMOVED***
                return Promise.resolve(manager);
          ***REMOVED***
            else ***REMOVED***
                debugAssert(promise, 'If manager is not set, promise should be');
                return promise;
          ***REMOVED***
      ***REMOVED***
        const promise = this.initAndGetManager(auth);
        this.eventManagers[key] = ***REMOVED*** promise };
        // If the promise is rejected, the key should be removed so that the
        // operation can be retried later.
        promise.catch(() => ***REMOVED***
            delete this.eventManagers[key];
      ***REMOVED***);
        return promise;
  ***REMOVED***
    async initAndGetManager(auth) ***REMOVED***
        const iframe = await _openIframe(auth);
        const manager = new AuthEventManager(auth);
        iframe.register('authEvent', (iframeEvent) => ***REMOVED***
            _assert(iframeEvent === null || iframeEvent === void 0 ? void 0 : iframeEvent.authEvent, auth, "invalid-auth-event" /* AuthErrorCode.INVALID_AUTH_EVENT */);
            // TODO: Consider splitting redirect and popup events earlier on
            const handled = manager.onEvent(iframeEvent.authEvent);
            return ***REMOVED*** status: handled ? "ACK" /* GapiOutcome.ACK */ : "ERROR" /* GapiOutcome.ERROR */ };
      ***REMOVED***, gapi.iframes.CROSS_ORIGIN_IFRAMES_FILTER);
        this.eventManagers[auth._key()] = ***REMOVED*** manager };
        this.iframes[auth._key()] = iframe;
        return manager;
  ***REMOVED***
    _isIframeWebStorageSupported(auth, cb) ***REMOVED***
        const iframe = this.iframes[auth._key()];
        iframe.send(WEB_STORAGE_SUPPORT_KEY, ***REMOVED*** type: WEB_STORAGE_SUPPORT_KEY }, result => ***REMOVED***
            var _a;
            const isSupported = (_a = result === null || result === void 0 ? void 0 : result[0]) === null || _a === void 0 ? void 0 : _a[WEB_STORAGE_SUPPORT_KEY];
            if (isSupported !== undefined) ***REMOVED***
                cb(!!isSupported);
          ***REMOVED***
            _fail(auth, "internal-error" /* AuthErrorCode.INTERNAL_ERROR */);
      ***REMOVED***, gapi.iframes.CROSS_ORIGIN_IFRAMES_FILTER);
  ***REMOVED***
    _originValidation(auth) ***REMOVED***
        const key = auth._key();
        if (!this.originValidationPromises[key]) ***REMOVED***
            this.originValidationPromises[key] = _validateOrigin(auth);
      ***REMOVED***
        return this.originValidationPromises[key];
  ***REMOVED***
    get _shouldInitProactively() ***REMOVED***
        // Mobile browsers and Safari need to optimistically initialize
        return _isMobileBrowser() || _isSafari() || _isIOS();
  ***REMOVED***
}
/**
 * An implementation of ***REMOVED***@link PopupRedirectResolver} suitable for browser
 * based applications.
 *
 * @public
 */
const browserPopupRedirectResolver = BrowserPopupRedirectResolver;

class MultiFactorAssertionImpl ***REMOVED***
    constructor(factorId) ***REMOVED***
        this.factorId = factorId;
  ***REMOVED***
    _process(auth, session, displayName) ***REMOVED***
        switch (session.type) ***REMOVED***
            case "enroll" /* MultiFactorSessionType.ENROLL */:
                return this._finalizeEnroll(auth, session.credential, displayName);
            case "signin" /* MultiFactorSessionType.SIGN_IN */:
                return this._finalizeSignIn(auth, session.credential);
            default:
                return debugFail('unexpected MultiFactorSessionType');
      ***REMOVED***
  ***REMOVED***
}

/**
 * ***REMOVED***@inheritdoc PhoneMultiFactorAssertion}
 *
 * @public
 */
class PhoneMultiFactorAssertionImpl extends MultiFactorAssertionImpl ***REMOVED***
    constructor(credential) ***REMOVED***
        super("phone" /* FactorId.PHONE */);
        this.credential = credential;
  ***REMOVED***
    /** @internal */
    static _fromCredential(credential) ***REMOVED***
        return new PhoneMultiFactorAssertionImpl(credential);
  ***REMOVED***
    /** @internal */
    _finalizeEnroll(auth, idToken, displayName) ***REMOVED***
        return finalizeEnrollPhoneMfa(auth, ***REMOVED***
            idToken,
            displayName,
            phoneVerificationInfo: this.credential._makeVerificationRequest()
      ***REMOVED***);
  ***REMOVED***
    /** @internal */
    _finalizeSignIn(auth, mfaPendingCredential) ***REMOVED***
        return finalizeSignInPhoneMfa(auth, ***REMOVED***
            mfaPendingCredential,
            phoneVerificationInfo: this.credential._makeVerificationRequest()
      ***REMOVED***);
  ***REMOVED***
}
/**
 * Provider for generating a ***REMOVED***@link PhoneMultiFactorAssertion}.
 *
 * @public
 */
class PhoneMultiFactorGenerator ***REMOVED***
    constructor() ***REMOVED*** }
    /**
     * Provides a ***REMOVED***@link PhoneMultiFactorAssertion} to confirm ownership of the phone second factor.
     *
     * @param phoneAuthCredential - A credential provided by ***REMOVED***@link PhoneAuthProvider.credential}.
     * @returns A ***REMOVED***@link PhoneMultiFactorAssertion} which can be used with
     * ***REMOVED***@link MultiFactorResolver.resolveSignIn}
     */
    static assertion(credential) ***REMOVED***
        return PhoneMultiFactorAssertionImpl._fromCredential(credential);
  ***REMOVED***
}
/**
 * The identifier of the phone second factor: `phone`.
 */
PhoneMultiFactorGenerator.FACTOR_ID = 'phone';

var name = "@firebase/auth";
var version = "0.21.1";

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
class AuthInterop ***REMOVED***
    constructor(auth) ***REMOVED***
        this.auth = auth;
        this.internalListeners = new Map();
  ***REMOVED***
    getUid() ***REMOVED***
        var _a;
        this.assertAuthConfigured();
        return ((_a = this.auth.currentUser) === null || _a === void 0 ? void 0 : _a.uid) || null;
  ***REMOVED***
    async getToken(forceRefresh) ***REMOVED***
        this.assertAuthConfigured();
        await this.auth._initializationPromise;
        if (!this.auth.currentUser) ***REMOVED***
            return null;
      ***REMOVED***
        const accessToken = await this.auth.currentUser.getIdToken(forceRefresh);
        return ***REMOVED*** accessToken };
  ***REMOVED***
    addAuthTokenListener(listener) ***REMOVED***
        this.assertAuthConfigured();
        if (this.internalListeners.has(listener)) ***REMOVED***
            return;
      ***REMOVED***
        const unsubscribe = this.auth.onIdTokenChanged(user => ***REMOVED***
            listener((user === null || user === void 0 ? void 0 : user.stsTokenManager.accessToken) || null);
      ***REMOVED***);
        this.internalListeners.set(listener, unsubscribe);
        this.updateProactiveRefresh();
  ***REMOVED***
    removeAuthTokenListener(listener) ***REMOVED***
        this.assertAuthConfigured();
        const unsubscribe = this.internalListeners.get(listener);
        if (!unsubscribe) ***REMOVED***
            return;
      ***REMOVED***
        this.internalListeners.delete(listener);
        unsubscribe();
        this.updateProactiveRefresh();
  ***REMOVED***
    assertAuthConfigured() ***REMOVED***
        _assert(this.auth._initializationPromise, "dependent-sdk-initialized-before-auth" /* AuthErrorCode.DEPENDENT_SDK_INIT_BEFORE_AUTH */);
  ***REMOVED***
    updateProactiveRefresh() ***REMOVED***
        if (this.internalListeners.size > 0) ***REMOVED***
            this.auth._startProactiveRefresh();
      ***REMOVED***
        else ***REMOVED***
            this.auth._stopProactiveRefresh();
      ***REMOVED***
  ***REMOVED***
}

/**
 * @license
 * Copyright 2020 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
function getVersionForPlatform(clientPlatform) ***REMOVED***
    switch (clientPlatform) ***REMOVED***
        case "Node" /* ClientPlatform.NODE */:
            return 'node';
        case "ReactNative" /* ClientPlatform.REACT_NATIVE */:
            return 'rn';
        case "Worker" /* ClientPlatform.WORKER */:
            return 'webworker';
        case "Cordova" /* ClientPlatform.CORDOVA */:
            return 'cordova';
        default:
            return undefined;
  ***REMOVED***
}
/** @internal */
function registerAuth(clientPlatform) ***REMOVED***
    _registerComponent(new Component("auth" /* _ComponentName.AUTH */, (container, ***REMOVED*** options: deps }) => ***REMOVED***
        const app = container.getProvider('app').getImmediate();
        const heartbeatServiceProvider = container.getProvider('heartbeat');
        const ***REMOVED*** apiKey, authDomain } = app.options;
        return ((app, heartbeatServiceProvider) => ***REMOVED***
            _assert(apiKey && !apiKey.includes(':'), "invalid-api-key" /* AuthErrorCode.INVALID_API_KEY */, ***REMOVED*** appName: app.name });
            // Auth domain is optional if IdP sign in isn't being used
            _assert(!(authDomain === null || authDomain === void 0 ? void 0 : authDomain.includes(':')), "argument-error" /* AuthErrorCode.ARGUMENT_ERROR */, ***REMOVED***
                appName: app.name
          ***REMOVED***);
            const config = ***REMOVED***
                apiKey,
                authDomain,
                clientPlatform,
                apiHost: "identitytoolkit.googleapis.com" /* DefaultConfig.API_HOST */,
                tokenApiHost: "securetoken.googleapis.com" /* DefaultConfig.TOKEN_API_HOST */,
                apiScheme: "https" /* DefaultConfig.API_SCHEME */,
                sdkClientVersion: _getClientVersion(clientPlatform)
          ***REMOVED***;
            const authInstance = new AuthImpl(app, heartbeatServiceProvider, config);
            _initializeAuthInstance(authInstance, deps);
            return authInstance;
      ***REMOVED***)(app, heartbeatServiceProvider);
  ***REMOVED***, "PUBLIC" /* ComponentType.PUBLIC */)
        /**
         * Auth can only be initialized by explicitly calling getAuth() or initializeAuth()
         * For why we do this, See go/firebase-next-auth-init
         */
        .setInstantiationMode("EXPLICIT" /* InstantiationMode.EXPLICIT */)
        /**
         * Because all firebase products that depend on auth depend on auth-internal directly,
         * we need to initialize auth-internal after auth is initialized to make it available to other firebase products.
         */
        .setInstanceCreatedCallback((container, _instanceIdentifier, _instance) => ***REMOVED***
        const authInternalProvider = container.getProvider("auth-internal" /* _ComponentName.AUTH_INTERNAL */);
        authInternalProvider.initialize();
  ***REMOVED***));
    _registerComponent(new Component("auth-internal" /* _ComponentName.AUTH_INTERNAL */, container => ***REMOVED***
        const auth = _castAuth(container.getProvider("auth" /* _ComponentName.AUTH */).getImmediate());
        return (auth => new AuthInterop(auth))(auth);
  ***REMOVED***, "PRIVATE" /* ComponentType.PRIVATE */).setInstantiationMode("EXPLICIT" /* InstantiationMode.EXPLICIT */));
    registerVersion(name, version, getVersionForPlatform(clientPlatform));
    // BUILD_TARGET will be replaced by values like esm5, esm2017, cjs5, etc during the compilation
    registerVersion(name, version, 'esm2017');
}

/**
 * @license
 * Copyright 2021 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
const DEFAULT_ID_TOKEN_MAX_AGE = 5 * 60;
const authIdTokenMaxAge = getExperimentalSetting('authIdTokenMaxAge') || DEFAULT_ID_TOKEN_MAX_AGE;
let lastPostedIdToken = null;
const mintCookieFactory = (url) => async (user) => ***REMOVED***
    const idTokenResult = user && (await user.getIdTokenResult());
    const idTokenAge = idTokenResult &&
        (new Date().getTime() - Date.parse(idTokenResult.issuedAtTime)) / 1000;
    if (idTokenAge && idTokenAge > authIdTokenMaxAge) ***REMOVED***
        return;
  ***REMOVED***
    // Specifically trip null => undefined when logged out, to delete any existing cookie
    const idToken = idTokenResult === null || idTokenResult === void 0 ? void 0 : idTokenResult.token;
    if (lastPostedIdToken === idToken) ***REMOVED***
        return;
  ***REMOVED***
    lastPostedIdToken = idToken;
    await fetch(url, ***REMOVED***
        method: idToken ? 'POST' : 'DELETE',
        headers: idToken
            ? ***REMOVED***
                'Authorization': `Bearer $***REMOVED***idToken}`
          ***REMOVED***
            : ***REMOVED***}
  ***REMOVED***);
};
/**
 * Returns the Auth instance associated with the provided ***REMOVED***@link @firebase/app#FirebaseApp}.
 * If no instance exists, initializes an Auth instance with platform-specific default dependencies.
 *
 * @param app - The Firebase App.
 *
 * @public
 */
function getAuth(app = getApp()) ***REMOVED***
    const provider = _getProvider(app, 'auth');
    if (provider.isInitialized()) ***REMOVED***
        return provider.getImmediate();
  ***REMOVED***
    const auth = initializeAuth(app, ***REMOVED***
        popupRedirectResolver: browserPopupRedirectResolver,
        persistence: [
            indexedDBLocalPersistence,
            browserLocalPersistence,
            browserSessionPersistence
        ]
  ***REMOVED***);
    const authTokenSyncUrl = getExperimentalSetting('authTokenSyncURL');
    if (authTokenSyncUrl) ***REMOVED***
        const mintCookie = mintCookieFactory(authTokenSyncUrl);
        beforeAuthStateChanged(auth, mintCookie, () => mintCookie(auth.currentUser));
        onIdTokenChanged(auth, user => mintCookie(user));
  ***REMOVED***
    const authEmulatorHost = getDefaultEmulatorHost('auth');
    if (authEmulatorHost) ***REMOVED***
        connectAuthEmulator(auth, `http://$***REMOVED***authEmulatorHost}`);
  ***REMOVED***
    return auth;
}
registerAuth("Browser" /* ClientPlatform.BROWSER */);

export ***REMOVED*** signInWithCustomToken as $, ActionCodeOperation as A, debugErrorMap as B, prodErrorMap as C, AUTH_ERROR_CODES_MAP_DO_NOT_USE_INTERNALLY as D, initializeAuth as E, FactorId as F, connectAuthEmulator as G, AuthCredential as H, EmailAuthCredential as I, OAuthCredential as J, PhoneAuthCredential as K, inMemoryPersistence as L, EmailAuthProvider as M, FacebookAuthProvider as N, OperationType as O, PhoneAuthProvider as P, GoogleAuthProvider as Q, RecaptchaVerifier as R, SignInMethod as S, GithubAuthProvider as T, OAuthProvider as U, SAMLAuthProvider as V, TwitterAuthProvider as W, signInAnonymously as X, signInWithCredential as Y, linkWithCredential as Z, reauthenticateWithCredential as _, browserSessionPersistence as a, sendPasswordResetEmail as a0, confirmPasswordReset as a1, applyActionCode as a2, checkActionCode as a3, verifyPasswordResetCode as a4, createUserWithEmailAndPassword as a5, signInWithEmailAndPassword as a6, sendSignInLinkToEmail as a7, isSignInWithEmailLink as a8, signInWithEmailLink as a9, _persistenceKeyName as aA, _getRedirectResult as aB, _overrideRedirectResult as aC, _clearRedirectOutcomes as aD, _castAuth as aE, UserImpl as aF, AuthImpl as aG, _getClientVersion as aH, _generateEventId as aI, AuthPopup as aJ, FetchProvider as aK, SAMLAuthCredential as aL, fetchSignInMethodsForEmail as aa, sendEmailVerification as ab, verifyBeforeUpdateEmail as ac, ActionCodeURL as ad, parseActionCodeURL as ae, updateProfile as af, updateEmail as ag, updatePassword as ah, getIdToken as ai, getIdTokenResult as aj, unlink as ak, getAdditionalUserInfo as al, reload as am, getMultiFactorResolver as an, multiFactor as ao, debugAssert as ap, _isIOS as aq, _isAndroid as ar, _fail as as, _getRedirectUrl as at, _getProjectConfig as au, _isIOS7Or8 as av, _createError as aw, _assert as ax, AuthEventManager as ay, _getInstance as az, browserLocalPersistence as b, signInWithPopup as c, linkWithPopup as d, reauthenticateWithPopup as e, signInWithRedirect as f, linkWithRedirect as g, reauthenticateWithRedirect as h, indexedDBLocalPersistence as i, getRedirectResult as j, browserPopupRedirectResolver as k, linkWithPhoneNumber as l, PhoneMultiFactorGenerator as m, getAuth as n, ProviderId as o, setPersistence as p, onIdTokenChanged as q, reauthenticateWithPhoneNumber as r, signInWithPhoneNumber as s, beforeAuthStateChanged as t, updatePhoneNumber as u, onAuthStateChanged as v, useDeviceLanguage as w, updateCurrentUser as x, signOut as y, deleteUser as z };
//# sourceMappingURL=index-e5b3cc81.js.map
