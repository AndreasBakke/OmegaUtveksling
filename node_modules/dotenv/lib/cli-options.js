const re = /^dotenv_config_(encoding|path|debug|override)=(.+)$/

module.exports = function optionMatcher (args) ***REMOVED***
  return args.reduce(function (acc, cur) ***REMOVED***
    const matches = cur.match(re)
    if (matches) ***REMOVED***
      acc[matches[1]] = matches[2]
  ***REMOVED***
    return acc
***REMOVED***, ***REMOVED***})
}
