import net from 'net';
import http from 'http';
import https from 'https';
import ***REMOVED*** Duplex } from 'stream';
import ***REMOVED*** EventEmitter } from 'events';
import createDebug from 'debug';
import promisify from './promisify';

const debug = createDebug('agent-base');

function isAgent(v: any): v is createAgent.AgentLike ***REMOVED***
	return Boolean(v) && typeof v.addRequest === 'function';
}

function isSecureEndpoint(): boolean ***REMOVED***
	const ***REMOVED*** stack } = new Error();
	if (typeof stack !== 'string') return false;
	return stack.split('\n').some(l => l.indexOf('(https.js:') !== -1  || l.indexOf('node:https:') !== -1);
}

function createAgent(opts?: createAgent.AgentOptions): createAgent.Agent;
function createAgent(
	callback: createAgent.AgentCallback,
	opts?: createAgent.AgentOptions
): createAgent.Agent;
function createAgent(
	callback?: createAgent.AgentCallback | createAgent.AgentOptions,
	opts?: createAgent.AgentOptions
) ***REMOVED***
	return new createAgent.Agent(callback, opts);
}

namespace createAgent ***REMOVED***
	export interface ClientRequest extends http.ClientRequest ***REMOVED***
		_last?: boolean;
		_hadError?: boolean;
		method: string;
	}

	export interface AgentRequestOptions ***REMOVED***
		host?: string;
		path?: string;
		// `port` on `http.RequestOptions` can be a string or undefined,
		// but `net.TcpNetConnectOpts` expects only a number
		port: number;
	}

	export interface HttpRequestOptions
		extends AgentRequestOptions,
			Omit<http.RequestOptions, keyof AgentRequestOptions> ***REMOVED***
		secureEndpoint: false;
	}

	export interface HttpsRequestOptions
		extends AgentRequestOptions,
			Omit<https.RequestOptions, keyof AgentRequestOptions> ***REMOVED***
		secureEndpoint: true;
	}

	export type RequestOptions = HttpRequestOptions | HttpsRequestOptions;

	export type AgentLike = Pick<createAgent.Agent, 'addRequest'> | http.Agent;

	export type AgentCallbackReturn = Duplex | AgentLike;

	export type AgentCallbackCallback = (
		err?: Error | null,
		socket?: createAgent.AgentCallbackReturn
	) => void;

	export type AgentCallbackPromise = (
		req: createAgent.ClientRequest,
		opts: createAgent.RequestOptions
	) =>
		| createAgent.AgentCallbackReturn
		| Promise<createAgent.AgentCallbackReturn>;

	export type AgentCallback = typeof Agent.prototype.callback;

	export type AgentOptions = ***REMOVED***
		timeout?: number;
	};

	/**
	 * Base `http.Agent` implementation.
	 * No pooling/keep-alive is implemented by default.
	 *
	 * @param ***REMOVED***Function} callback
	 * @api public
	 */
	export class Agent extends EventEmitter ***REMOVED***
		public timeout: number | null;
		public maxFreeSockets: number;
		public maxTotalSockets: number;
		public maxSockets: number;
		public sockets: ***REMOVED***
			[key: string]: net.Socket[];
		};
		public freeSockets: ***REMOVED***
			[key: string]: net.Socket[];
		};
		public requests: ***REMOVED***
			[key: string]: http.IncomingMessage[];
		};
		public options: https.AgentOptions;
		private promisifiedCallback?: createAgent.AgentCallbackPromise;
		private explicitDefaultPort?: number;
		private explicitProtocol?: string;

		constructor(
			callback?: createAgent.AgentCallback | createAgent.AgentOptions,
			_opts?: createAgent.AgentOptions
		) ***REMOVED***
			super();

			let opts = _opts;
			if (typeof callback === 'function') ***REMOVED***
				this.callback = callback;
			} else if (callback) ***REMOVED***
				opts = callback;
			}

			// Timeout for the socket to be returned from the callback
			this.timeout = null;
			if (opts && typeof opts.timeout === 'number') ***REMOVED***
				this.timeout = opts.timeout;
			}

			// These aren't actually used by `agent-base`, but are required
			// for the TypeScript definition files in `@types/node` :/
			this.maxFreeSockets = 1;
			this.maxSockets = 1;
			this.maxTotalSockets = Infinity;
			this.sockets = ***REMOVED***};
			this.freeSockets = ***REMOVED***};
			this.requests = ***REMOVED***};
			this.options = ***REMOVED***};
		}

		get defaultPort(): number ***REMOVED***
			if (typeof this.explicitDefaultPort === 'number') ***REMOVED***
				return this.explicitDefaultPort;
			}
			return isSecureEndpoint() ? 443 : 80;
		}

		set defaultPort(v: number) ***REMOVED***
			this.explicitDefaultPort = v;
		}

		get protocol(): string ***REMOVED***
			if (typeof this.explicitProtocol === 'string') ***REMOVED***
				return this.explicitProtocol;
			}
			return isSecureEndpoint() ? 'https:' : 'http:';
		}

		set protocol(v: string) ***REMOVED***
			this.explicitProtocol = v;
		}

		callback(
			req: createAgent.ClientRequest,
			opts: createAgent.RequestOptions,
			fn: createAgent.AgentCallbackCallback
		): void;
		callback(
			req: createAgent.ClientRequest,
			opts: createAgent.RequestOptions
		):
			| createAgent.AgentCallbackReturn
			| Promise<createAgent.AgentCallbackReturn>;
		callback(
			req: createAgent.ClientRequest,
			opts: createAgent.AgentOptions,
			fn?: createAgent.AgentCallbackCallback
		):
			| createAgent.AgentCallbackReturn
			| Promise<createAgent.AgentCallbackReturn>
			| void ***REMOVED***
			throw new Error(
				'"agent-base" has no default implementation, you must subclass and override `callback()`'
			);
		}

		/**
		 * Called by node-core's "_http_client.js" module when creating
		 * a new HTTP request with this Agent instance.
		 *
		 * @api public
		 */
		addRequest(req: ClientRequest, _opts: RequestOptions): void ***REMOVED***
			const opts: RequestOptions = ***REMOVED*** ..._opts };

			if (typeof opts.secureEndpoint !== 'boolean') ***REMOVED***
				opts.secureEndpoint = isSecureEndpoint();
			}

			if (opts.host == null) ***REMOVED***
				opts.host = 'localhost';
			}

			if (opts.port == null) ***REMOVED***
				opts.port = opts.secureEndpoint ? 443 : 80;
			}

			if (opts.protocol == null) ***REMOVED***
				opts.protocol = opts.secureEndpoint ? 'https:' : 'http:';
			}

			if (opts.host && opts.path) ***REMOVED***
				// If both a `host` and `path` are specified then it's most
				// likely the result of a `url.parse()` call... we need to
				// remove the `path` portion so that `net.connect()` doesn't
				// attempt to open that as a unix socket file.
				delete opts.path;
			}

			delete opts.agent;
			delete opts.hostname;
			delete opts._defaultAgent;
			delete opts.defaultPort;
			delete opts.createConnection;

			// Hint to use "Connection: close"
			// XXX: non-documented `http` module API :(
			req._last = true;
			req.shouldKeepAlive = false;

			let timedOut = false;
			let timeoutId: ReturnType<typeof setTimeout> | null = null;
			const timeoutMs = opts.timeout || this.timeout;

			const onerror = (err: NodeJS.ErrnoException) => ***REMOVED***
				if (req._hadError) return;
				req.emit('error', err);
				// For Safety. Some additional errors might fire later on
				// and we need to make sure we don't double-fire the error event.
				req._hadError = true;
			};

			const ontimeout = () => ***REMOVED***
				timeoutId = null;
				timedOut = true;
				const err: NodeJS.ErrnoException = new Error(
					`A "socket" was not created for HTTP request before $***REMOVED***timeoutMs}ms`
				);
				err.code = 'ETIMEOUT';
				onerror(err);
			};

			const callbackError = (err: NodeJS.ErrnoException) => ***REMOVED***
				if (timedOut) return;
				if (timeoutId !== null) ***REMOVED***
					clearTimeout(timeoutId);
					timeoutId = null;
				}
				onerror(err);
			};

			const onsocket = (socket: AgentCallbackReturn) => ***REMOVED***
				if (timedOut) return;
				if (timeoutId != null) ***REMOVED***
					clearTimeout(timeoutId);
					timeoutId = null;
				}

				if (isAgent(socket)) ***REMOVED***
					// `socket` is actually an `http.Agent` instance, so
					// relinquish responsibility for this `req` to the Agent
					// from here on
					debug(
						'Callback returned another Agent instance %o',
						socket.constructor.name
					);
					(socket as createAgent.Agent).addRequest(req, opts);
					return;
				}

				if (socket) ***REMOVED***
					socket.once('free', () => ***REMOVED***
						this.freeSocket(socket as net.Socket, opts);
					});
					req.onSocket(socket as net.Socket);
					return;
				}

				const err = new Error(
					`no Duplex stream was returned to agent-base for \`$***REMOVED***req.method} $***REMOVED***req.path}\``
				);
				onerror(err);
			};

			if (typeof this.callback !== 'function') ***REMOVED***
				onerror(new Error('`callback` is not defined'));
				return;
			}

			if (!this.promisifiedCallback) ***REMOVED***
				if (this.callback.length >= 3) ***REMOVED***
					debug('Converting legacy callback function to promise');
					this.promisifiedCallback = promisify(this.callback);
				} else ***REMOVED***
					this.promisifiedCallback = this.callback;
				}
			}

			if (typeof timeoutMs === 'number' && timeoutMs > 0) ***REMOVED***
				timeoutId = setTimeout(ontimeout, timeoutMs);
			}

			if ('port' in opts && typeof opts.port !== 'number') ***REMOVED***
				opts.port = Number(opts.port);
			}

			try ***REMOVED***
				debug(
					'Resolving socket for %o request: %o',
					opts.protocol,
					`$***REMOVED***req.method} $***REMOVED***req.path}`
				);
				Promise.resolve(this.promisifiedCallback(req, opts)).then(
					onsocket,
					callbackError
				);
			} catch (err) ***REMOVED***
				Promise.reject(err).catch(callbackError);
			}
		}

		freeSocket(socket: net.Socket, opts: AgentOptions) ***REMOVED***
			debug('Freeing socket %o %o', socket.constructor.name, opts);
			socket.destroy();
		}

		destroy() ***REMOVED***
			debug('Destroying agent %o', this.constructor.name);
		}
	}

	// So that `instanceof` works correctly
	createAgent.prototype = createAgent.Agent.prototype;
}

export = createAgent;
