import ***REMOVED*** Metadata, ServiceObject } from './nodejs-common';
import ***REMOVED*** Storage } from './storage';
export interface StopCallback ***REMOVED***
    (err: Error | null, apiResponse?: Metadata): void;
}
/**
 * Create a channel object to interact with a Cloud Storage channel.
 *
 * See ***REMOVED***@link https://cloud.google.com/storage/docs/object-change-notification| Object Change Notification}
 *
 * @class
 *
 * @param ***REMOVED***string} id The ID of the channel.
 * @param ***REMOVED***string} resourceId The resource ID of the channel.
 *
 * @example
 * ```
 * const ***REMOVED***Storage} = require('@google-cloud/storage');
 * const storage = new Storage();
 * const channel = storage.channel('id', 'resource-id');
 * ```
 */
declare class Channel extends ServiceObject ***REMOVED***
    constructor(storage: Storage, id: string, resourceId: string);
    stop(): Promise<Metadata>;
    stop(callback: StopCallback): void;
}
/**
 * Reference to the ***REMOVED***@link Channel} class.
 * @name module:@google-cloud/storage.Channel
 * @see Channel
 */
export ***REMOVED*** Channel };
