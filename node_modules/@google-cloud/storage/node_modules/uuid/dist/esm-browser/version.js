import validate from './validate.js';

function version(uuid) ***REMOVED***
  if (!validate(uuid)) ***REMOVED***
    throw TypeError('Invalid UUID');
***REMOVED***

  return parseInt(uuid.substr(14, 1), 16);
}

export default version;