"use strict";
/*!
 * Copyright 2018 Google Inc. All Rights Reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
Object.defineProperty(exports, "__esModule", ***REMOVED*** value: true });
exports.Timestamp = void 0;
const validate_1 = require("./validate");
/*!
 * Number of nanoseconds in a millisecond.
 *
 * @type ***REMOVED***number}
 */
const MS_TO_NANOS = 1000000;
/*!
 * The minimum legal value for the "seconds" property of a Timestamp object.
 *
 * This value corresponds to 0001-01-01T00:00:00Z.
 *
 * @type ***REMOVED***number}
 */
const MIN_SECONDS = -62135596800;
/*!
 * The maximum legal value for the "seconds" property of a Timestamp object.
 *
 * This value corresponds to 9999-12-31T23:59:59.999999999Z.
 *
 * @type ***REMOVED***number}
 */
const MAX_SECONDS = 253402300799;
/**
 * A Timestamp represents a point in time independent of any time zone or
 * calendar, represented as seconds and fractions of seconds at nanosecond
 * resolution in UTC Epoch time. It is encoded using the Proleptic Gregorian
 * Calendar which extends the Gregorian calendar backwards to year one. It is
 * encoded assuming all minutes are 60 seconds long, i.e. leap seconds are
 * "smeared" so that no leap second table is needed for interpretation. Range is
 * from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59.999999999Z.
 *
 * @see https://github.com/google/protobuf/blob/master/src/google/protobuf/timestamp.proto
 */
class Timestamp ***REMOVED***
    /**
     * Creates a new timestamp.
     *
     * @example
     * ```
     * let documentRef = firestore.doc('col/doc');
     *
     * documentRef.set(***REMOVED*** startTime:new Firestore.Timestamp(42, 0) });
     *
     * ```
     * @param ***REMOVED***number} seconds The number of seconds of UTC time since Unix epoch
     * 1970-01-01T00:00:00Z. Must be from 0001-01-01T00:00:00Z to
     * 9999-12-31T23:59:59Z inclusive.
     * @param ***REMOVED***number} nanoseconds The non-negative fractions of a second at
     * nanosecond resolution. Negative second values with fractions must still
     * have non-negative nanoseconds values that count forward in time. Must be
     * from 0 to 999,999,999 inclusive.
     */
    constructor(seconds, nanoseconds) ***REMOVED***
        (0, validate_1.validateInteger)('seconds', seconds, ***REMOVED***
            minValue: MIN_SECONDS,
            maxValue: MAX_SECONDS,
      ***REMOVED***);
        (0, validate_1.validateInteger)('nanoseconds', nanoseconds, ***REMOVED***
            minValue: 0,
            maxValue: 999999999,
      ***REMOVED***);
        this._seconds = seconds;
        this._nanoseconds = nanoseconds;
  ***REMOVED***
    /**
     * Creates a new timestamp with the current date, with millisecond precision.
     *
     * @example
     * ```
     * let documentRef = firestore.doc('col/doc');
     *
     * documentRef.set(***REMOVED*** updateTime:Firestore.Timestamp.now() });
     *
     * ```
     * @return ***REMOVED***Timestamp} A new `Timestamp` representing the current date.
     */
    static now() ***REMOVED***
        return Timestamp.fromMillis(Date.now());
  ***REMOVED***
    /**
     * Creates a new timestamp from the given date.
     *
     * @example
     * ```
     * let documentRef = firestore.doc('col/doc');
     *
     * let date = Date.parse('01 Jan 2000 00:00:00 GMT');
     * documentRef.set(***REMOVED*** startTime:Firestore.Timestamp.fromDate(date) });
     *
     * ```
     * @param ***REMOVED***Date} date The date to initialize the `Timestamp` from.
     * @return ***REMOVED***Timestamp} A new `Timestamp` representing the same point in time
     * as the given date.
     */
    static fromDate(date) ***REMOVED***
        return Timestamp.fromMillis(date.getTime());
  ***REMOVED***
    /**
     * Creates a new timestamp from the given number of milliseconds.
     *
     * @example
     * ```
     * let documentRef = firestore.doc('col/doc');
     *
     * documentRef.set(***REMOVED*** startTime:Firestore.Timestamp.fromMillis(42) });
     *
     * ```
     * @param ***REMOVED***number} milliseconds Number of milliseconds since Unix epoch
     * 1970-01-01T00:00:00Z.
     * @return ***REMOVED***Timestamp}  A new `Timestamp` representing the same point in time
     * as the given number of milliseconds.
     */
    static fromMillis(milliseconds) ***REMOVED***
        const seconds = Math.floor(milliseconds / 1000);
        const nanos = Math.floor((milliseconds - seconds * 1000) * MS_TO_NANOS);
        return new Timestamp(seconds, nanos);
  ***REMOVED***
    /**
     * Generates a `Timestamp` object from a Timestamp proto.
     *
     * @private
     * @internal
     * @param ***REMOVED***Object} timestamp The `Timestamp` Protobuf object.
     */
    static fromProto(timestamp) ***REMOVED***
        return new Timestamp(Number(timestamp.seconds || 0), timestamp.nanos || 0);
  ***REMOVED***
    /**
     * The number of seconds of UTC time since Unix epoch 1970-01-01T00:00:00Z.
     *
     * @example
     * ```
     * let documentRef = firestore.doc('col/doc');
     *
     * documentRef.get().then(snap => ***REMOVED***
     *   let updated = snap.updateTime;
     *   console.log(`Updated at $***REMOVED***updated.seconds}s $***REMOVED***updated.nanoseconds}ns`);
     * });
     *
     * ```
     * @type ***REMOVED***number}
     */
    get seconds() ***REMOVED***
        return this._seconds;
  ***REMOVED***
    /**
     * The non-negative fractions of a second at nanosecond resolution.
     *
     * @example
     * ```
     * let documentRef = firestore.doc('col/doc');
     *
     * documentRef.get().then(snap => ***REMOVED***
     *   let updated = snap.updateTime;
     *   console.log(`Updated at $***REMOVED***updated.seconds}s $***REMOVED***updated.nanoseconds}ns`);
     * });
     *
     * ```
     * @type ***REMOVED***number}
     */
    get nanoseconds() ***REMOVED***
        return this._nanoseconds;
  ***REMOVED***
    /**
     * Returns a new `Date` corresponding to this timestamp. This may lose
     * precision.
     *
     * @example
     * ```
     * let documentRef = firestore.doc('col/doc');
     *
     * documentRef.get().then(snap => ***REMOVED***
     *   console.log(`Document updated at: $***REMOVED***snap.updateTime.toDate()}`);
     * });
     *
     * ```
     * @return ***REMOVED***Date} JavaScript `Date` object representing the same point in time
     * as this `Timestamp`, with millisecond precision.
     */
    toDate() ***REMOVED***
        return new Date(this._seconds * 1000 + Math.round(this._nanoseconds / MS_TO_NANOS));
  ***REMOVED***
    /**
     * Returns the number of milliseconds since Unix epoch 1970-01-01T00:00:00Z.
     *
     * @example
     * ```
     * let documentRef = firestore.doc('col/doc');
     *
     * documentRef.get().then(snap => ***REMOVED***
     *   let startTime = snap.get('startTime');
     *   let endTime = snap.get('endTime');
     *   console.log(`Duration: $***REMOVED***endTime - startTime}`);
     * });
     *
     * ```
     * @return ***REMOVED***number} The point in time corresponding to this timestamp,
     * represented as the number of milliseconds since Unix epoch
     * 1970-01-01T00:00:00Z.
     */
    toMillis() ***REMOVED***
        return this._seconds * 1000 + Math.floor(this._nanoseconds / MS_TO_NANOS);
  ***REMOVED***
    /**
     * Returns 'true' if this `Timestamp` is equal to the provided one.
     *
     * @example
     * ```
     * let documentRef = firestore.doc('col/doc');
     *
     * documentRef.get().then(snap => ***REMOVED***
     *   if (snap.createTime.isEqual(snap.updateTime)) ***REMOVED***
     *     console.log('Document is in its initial state.');
     * ***REMOVED***
     * });
     *
     * ```
     * @param ***REMOVED***any} other The `Timestamp` to compare against.
     * @return ***REMOVED***boolean} 'true' if this `Timestamp` is equal to the provided one.
     */
    isEqual(other) ***REMOVED***
        return (this === other ||
            (other instanceof Timestamp &&
                this._seconds === other.seconds &&
                this._nanoseconds === other.nanoseconds));
  ***REMOVED***
    /**
     * Generates the Protobuf `Timestamp` object for this timestamp.
     *
     * @private
     * @internal
     * @returns ***REMOVED***Object} The `Timestamp` Protobuf object.
     */
    toProto() ***REMOVED***
        const timestamp = ***REMOVED***};
        if (this.seconds) ***REMOVED***
            timestamp.seconds = this.seconds.toString();
      ***REMOVED***
        if (this.nanoseconds) ***REMOVED***
            timestamp.nanos = this.nanoseconds;
      ***REMOVED***
        return ***REMOVED*** timestampValue: timestamp };
  ***REMOVED***
    /**
     * Converts this object to a primitive `string`, which allows `Timestamp` objects to be compared
     * using the `>`, `<=`, `>=` and `>` operators.
     *
     * @return ***REMOVED***string} a string encoding of this object.
     */
    valueOf() ***REMOVED***
        // This method returns a string of the form <seconds>.<nanoseconds> where <seconds> is
        // translated to have a non-negative value and both <seconds> and <nanoseconds> are left-padded
        // with zeroes to be a consistent length. Strings with this format then have a lexicographical
        // ordering that matches the expected ordering. The <seconds> translation is done to avoid
        // having a leading negative sign (i.e. a leading '-' character) in its string representation,
        // which would affect its lexicographical ordering.
        const adjustedSeconds = this.seconds - MIN_SECONDS;
        // Note: Up to 12 decimal digits are required to represent all valid 'seconds' values.
        const formattedSeconds = String(adjustedSeconds).padStart(12, '0');
        const formattedNanoseconds = String(this.nanoseconds).padStart(9, '0');
        return formattedSeconds + '.' + formattedNanoseconds;
  ***REMOVED***
}
exports.Timestamp = Timestamp;
//# sourceMappingURL=timestamp.js.map