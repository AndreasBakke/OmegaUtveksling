import ***REMOVED*** SPACE, IDENT } from '../tokenTypes'

export default tokenStream => ***REMOVED***
  const values = [tokenStream.expect(IDENT)]

  while (tokenStream.hasTokens()) ***REMOVED***
    tokenStream.expect(SPACE)
    values.push(tokenStream.expect(IDENT))
***REMOVED***

  return ***REMOVED***
    fontVariant: values,
***REMOVED***
}
