import ***REMOVED*** parseShadow } from './util'

export default tokenStream => ***REMOVED***
  const ***REMOVED*** offset, radius, color } = parseShadow(tokenStream)
  return ***REMOVED***
    shadowOffset: offset,
    shadowRadius: radius,
    shadowColor: color,
    shadowOpacity: 1,
***REMOVED***
}
