import transformCss from '..'

it('transforms font variant as an array', () => ***REMOVED***
  expect(transformCss([['font-variant', 'tabular-nums']])).toEqual(***REMOVED***
    fontVariant: ['tabular-nums'],
***REMOVED***)
})

it('transforms multiple font variant as an array', () => ***REMOVED***
  expect(
    transformCss([['font-variant', 'tabular-nums oldstyle-nums']])
  ).toEqual(***REMOVED***
    fontVariant: ['tabular-nums', 'oldstyle-nums'],
***REMOVED***)
})
