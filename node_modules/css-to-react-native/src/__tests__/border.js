import transformCss from '..'

it('transforms border none', () => ***REMOVED***
  expect(transformCss([['border', 'none']])).toEqual(***REMOVED***
    borderWidth: 0,
    borderColor: 'black',
    borderStyle: 'solid',
***REMOVED***)
})

it('transforms border shorthand', () => ***REMOVED***
  expect(transformCss([['border', '2px dashed #f00']])).toEqual(***REMOVED***
    borderWidth: 2,
    borderColor: '#f00',
    borderStyle: 'dashed',
***REMOVED***)
})

it('transforms border shorthand in other order', () => ***REMOVED***
  expect(transformCss([['border', '#f00 2px dashed']])).toEqual(***REMOVED***
    borderWidth: 2,
    borderColor: '#f00',
    borderStyle: 'dashed',
***REMOVED***)
})

it('transforms border shorthand missing color', () => ***REMOVED***
  expect(transformCss([['border', '2px dashed']])).toEqual(***REMOVED***
    borderWidth: 2,
    borderColor: 'black',
    borderStyle: 'dashed',
***REMOVED***)
})

it('transforms border shorthand missing style', () => ***REMOVED***
  expect(transformCss([['border', '2px #f00']])).toEqual(***REMOVED***
    borderWidth: 2,
    borderColor: '#f00',
    borderStyle: 'solid',
***REMOVED***)
})

it('transforms border shorthand missing width', () => ***REMOVED***
  expect(transformCss([['border', '#f00 dashed']])).toEqual(***REMOVED***
    borderWidth: 1,
    borderColor: '#f00',
    borderStyle: 'dashed',
***REMOVED***)
})

it('transforms border shorthand missing color & width', () => ***REMOVED***
  expect(transformCss([['border', 'dashed']])).toEqual(***REMOVED***
    borderWidth: 1,
    borderColor: 'black',
    borderStyle: 'dashed',
***REMOVED***)
})

it('transforms border shorthand missing style & width', () => ***REMOVED***
  expect(transformCss([['border', '#f00']])).toEqual(***REMOVED***
    borderWidth: 1,
    borderColor: '#f00',
    borderStyle: 'solid',
***REMOVED***)
})

it('transforms border shorthand missing color & style', () => ***REMOVED***
  expect(transformCss([['border', '2px']])).toEqual(***REMOVED***
    borderWidth: 2,
    borderColor: 'black',
    borderStyle: 'solid',
***REMOVED***)
})

it('transforms border for unsupported units', () => ***REMOVED***
  expect(transformCss([['border', '3em solid black']])).toEqual(***REMOVED***
    borderWidth: '3em',
    borderColor: 'black',
    borderStyle: 'solid',
***REMOVED***)
})

it('does not transform border with percentage width', () => ***REMOVED***
  expect(() => transformCss([['border', '3% solid black']])).toThrow()
})
