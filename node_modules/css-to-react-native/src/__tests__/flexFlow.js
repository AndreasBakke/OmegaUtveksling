import transformCss from '..'

it('transforms flexFlow shorthand with two values', () => ***REMOVED***
  expect(transformCss([['flex-flow', 'column wrap']])).toEqual(***REMOVED***
    flexDirection: 'column',
    flexWrap: 'wrap',
***REMOVED***)
})

it('transforms flexFlow shorthand missing flexDirection', () => ***REMOVED***
  expect(transformCss([['flex-flow', 'wrap']])).toEqual(***REMOVED***
    flexDirection: 'row',
    flexWrap: 'wrap',
***REMOVED***)
})

it('transforms flexFlow shorthand missing flexWrap', () => ***REMOVED***
  expect(transformCss([['flex-flow', 'column']])).toEqual(***REMOVED***
    flexDirection: 'column',
    flexWrap: 'nowrap',
***REMOVED***)
})
