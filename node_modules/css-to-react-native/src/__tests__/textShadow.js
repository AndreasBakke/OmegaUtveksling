import transformCss from '..'

it('textShadow with all values', () => ***REMOVED***
  expect(transformCss([['text-shadow', '10px 20px 30px red']])).toEqual(***REMOVED***
    textShadowOffset: ***REMOVED*** width: 10, height: 20 },
    textShadowRadius: 30,
    textShadowColor: 'red',
***REMOVED***)
})

it('textShadow omitting blur', () => ***REMOVED***
  expect(transformCss([['text-shadow', '10px 20px red']])).toEqual(***REMOVED***
    textShadowOffset: ***REMOVED*** width: 10, height: 20 },
    textShadowRadius: 0,
    textShadowColor: 'red',
***REMOVED***)
})

it('textShadow omitting color', () => ***REMOVED***
  expect(transformCss([['text-shadow', '10px 20px']])).toEqual(***REMOVED***
    textShadowOffset: ***REMOVED*** width: 10, height: 20 },
    textShadowRadius: 0,
    textShadowColor: 'black',
***REMOVED***)
})

it('textShadow enforces offset-x and offset-y', () => ***REMOVED***
  expect(() => transformCss([['text-shadow', 'red']])).toThrow()
  expect(() => transformCss([['text-shadow', '10px red']])).toThrow()
})
