import transformCss from '..'

it('transforms font', () => ***REMOVED***
  expect(
    transformCss([['font', 'bold italic small-caps 16px/18px "Helvetica"']])
  ).toEqual(***REMOVED***
    fontFamily: 'Helvetica',
    fontSize: 16,
    fontWeight: 'bold',
    fontStyle: 'italic',
    fontVariant: ['small-caps'],
    lineHeight: 18,
***REMOVED***)
})

it('transforms font missing font-variant', () => ***REMOVED***
  expect(transformCss([['font', 'bold italic 16px/18px "Helvetica"']])).toEqual(
    ***REMOVED***
      fontFamily: 'Helvetica',
      fontSize: 16,
      fontWeight: 'bold',
      fontStyle: 'italic',
      fontVariant: [],
      lineHeight: 18,
  ***REMOVED***
  )
})

it('transforms font missing font-style', () => ***REMOVED***
  expect(
    transformCss([['font', 'bold small-caps 16px/18px "Helvetica"']])
  ).toEqual(***REMOVED***
    fontFamily: 'Helvetica',
    fontSize: 16,
    fontWeight: 'bold',
    fontStyle: 'normal',
    fontVariant: ['small-caps'],
    lineHeight: 18,
***REMOVED***)
})

it('transforms font missing font-weight', () => ***REMOVED***
  expect(
    transformCss([['font', 'italic small-caps 16px/18px "Helvetica"']])
  ).toEqual(***REMOVED***
    fontFamily: 'Helvetica',
    fontSize: 16,
    fontWeight: 'normal',
    fontStyle: 'italic',
    fontVariant: ['small-caps'],
    lineHeight: 18,
***REMOVED***)
})

it('transforms font with font-weight normal', () => ***REMOVED***
  expect(transformCss([['font', 'normal 16px/18px "Helvetica"']])).toEqual(***REMOVED***
    fontFamily: 'Helvetica',
    fontSize: 16,
    fontWeight: 'normal',
    fontStyle: 'normal',
    fontVariant: [],
    lineHeight: 18,
***REMOVED***)
})

it('transforms font with font-weight and font-style normal', () => ***REMOVED***
  expect(
    transformCss([['font', 'normal normal 16px/18px "Helvetica"']])
  ).toEqual(***REMOVED***
    fontFamily: 'Helvetica',
    fontSize: 16,
    fontWeight: 'normal',
    fontStyle: 'normal',
    fontVariant: [],
    lineHeight: 18,
***REMOVED***)
})

it('transforms font with no font-weight, font-style, and font-variant', () => ***REMOVED***
  expect(transformCss([['font', '16px/18px "Helvetica"']])).toEqual(***REMOVED***
    fontFamily: 'Helvetica',
    fontSize: 16,
    fontWeight: 'normal',
    fontStyle: 'normal',
    fontVariant: [],
    lineHeight: 18,
***REMOVED***)
})

it('omits line height if not specified', () => ***REMOVED***
  expect(transformCss([['font', '16px "Helvetica"']])).toEqual(***REMOVED***
    fontFamily: 'Helvetica',
    fontSize: 16,
    fontWeight: 'normal',
    fontStyle: 'normal',
    fontVariant: [],
***REMOVED***)
})

it('does not allow line height as multiple', () => ***REMOVED***
  expect(() => ***REMOVED***
    transformCss([['font', '16px/1.5 "Helvetica"']])
***REMOVED***).toThrow()
})

it('transforms font without quotes', () => ***REMOVED***
  expect(
    transformCss([['font', 'bold italic small-caps 16px/18px Helvetica Neue']])
  ).toEqual(***REMOVED***
    fontFamily: 'Helvetica Neue',
    fontSize: 16,
    fontWeight: 'bold',
    fontStyle: 'italic',
    fontVariant: ['small-caps'],
    lineHeight: 18,
***REMOVED***)
})
