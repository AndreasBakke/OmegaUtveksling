import transformCss from '..'

it('transforms box-shadow into shadow- properties', () => ***REMOVED***
  expect(transformCss([['box-shadow', '10px 20px 30px red']])).toEqual(***REMOVED***
    shadowOffset: ***REMOVED*** width: 10, height: 20 },
    shadowRadius: 30,
    shadowColor: 'red',
    shadowOpacity: 1,
***REMOVED***)
})

it('transforms box-shadow without blur-radius', () => ***REMOVED***
  expect(transformCss([['box-shadow', '10px 20px red']])).toEqual(***REMOVED***
    shadowOffset: ***REMOVED*** width: 10, height: 20 },
    shadowRadius: 0,
    shadowColor: 'red',
    shadowOpacity: 1,
***REMOVED***)
})

it('transforms box-shadow without color', () => ***REMOVED***
  expect(transformCss([['box-shadow', '10px 20px']])).toEqual(***REMOVED***
    shadowOffset: ***REMOVED*** width: 10, height: 20 },
    shadowRadius: 0,
    shadowColor: 'black',
    shadowOpacity: 1,
***REMOVED***)
})

it('transforms box-shadow with rgb color', () => ***REMOVED***
  expect(
    transformCss([['box-shadow', '10px 20px rgb(100, 100, 100)']])
  ).toEqual(***REMOVED***
    shadowOffset: ***REMOVED*** width: 10, height: 20 },
    shadowRadius: 0,
    shadowColor: 'rgb(100, 100, 100)',
    shadowOpacity: 1,
***REMOVED***)
})

it('transforms box-shadow with rgba color', () => ***REMOVED***
  expect(
    transformCss([['box-shadow', '10px 20px rgba(100, 100, 100, 0.5)']])
  ).toEqual(***REMOVED***
    shadowOffset: ***REMOVED*** width: 10, height: 20 },
    shadowRadius: 0,
    shadowColor: 'rgba(100, 100, 100, 0.5)',
    shadowOpacity: 1,
***REMOVED***)
})

it('transforms box-shadow with hsl color', () => ***REMOVED***
  expect(
    transformCss([['box-shadow', '10px 20px hsl(120, 100%, 50%)']])
  ).toEqual(***REMOVED***
    shadowOffset: ***REMOVED*** width: 10, height: 20 },
    shadowRadius: 0,
    shadowColor: 'hsl(120, 100%, 50%)',
    shadowOpacity: 1,
***REMOVED***)
})

it('transforms box-shadow with hsla color', () => ***REMOVED***
  expect(
    transformCss([['box-shadow', '10px 20px hsla(120, 100%, 50%, 0.7)']])
  ).toEqual(***REMOVED***
    shadowOffset: ***REMOVED*** width: 10, height: 20 },
    shadowRadius: 0,
    shadowColor: 'hsla(120, 100%, 50%, 0.7)',
    shadowOpacity: 1,
***REMOVED***)
})

it('transforms box-shadow and throws if multiple colors are used', () => ***REMOVED***
  expect(() =>
    transformCss([['box-shadow', '0 0 0 red yellow green blue']])
  ).toThrow()
})

it('transforms box-shadow enforces offset to be present', () => ***REMOVED***
  expect(() => transformCss([['box-shadow', 'red']])).toThrow()
  expect(() => transformCss([['box-shadow', '10px red']])).toThrow()
})
