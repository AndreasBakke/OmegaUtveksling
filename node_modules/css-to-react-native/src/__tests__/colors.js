import transformCss from '..'

it('transforms hex colors', () => ***REMOVED***
  expect(transformCss([['color', '#f00']])).toEqual(***REMOVED*** color: '#f00' })
})

it('transforms rgb colors', () => ***REMOVED***
  expect(transformCss([['color', 'rgb(255, 0, 0)']])).toEqual(***REMOVED***
    color: 'rgb(255, 0, 0)',
***REMOVED***)
})

it('transforms transparent color', () => ***REMOVED***
  expect(transformCss([['color', 'transparent']])).toEqual(***REMOVED***
    color: 'transparent',
***REMOVED***)
})

it('transforms border shorthand with transparent color', () => ***REMOVED***
  expect(transformCss([['border', '2px dashed transparent']])).toEqual(***REMOVED***
    borderColor: 'transparent',
    borderStyle: 'dashed',
    borderWidth: 2,
***REMOVED***)
})

it('transforms background shorthand with transparent color', () => ***REMOVED***
  expect(transformCss([['background', 'transparent']])).toEqual(***REMOVED***
    backgroundColor: 'transparent',
***REMOVED***)
})
