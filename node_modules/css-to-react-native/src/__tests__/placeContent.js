import transformCss from '..'

it('transforms place content', () => ***REMOVED***
  expect(transformCss([['place-content', 'center center']])).toEqual(***REMOVED***
    alignContent: 'center',
    justifyContent: 'center',
***REMOVED***)
})

it('transforms place content with one value', () => ***REMOVED***
  expect(transformCss([['place-content', 'center']])).toEqual(***REMOVED***
    alignContent: 'center',
    justifyContent: 'stretch',
***REMOVED***)
})

it('does not allow justify content without align content', () => ***REMOVED***
  expect(() => transformCss([['place-content', 'space-evenly']])).toThrow()
})
