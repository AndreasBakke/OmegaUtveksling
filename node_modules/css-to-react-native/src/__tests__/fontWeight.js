import transformCss from '..'

it('transforms font weights as strings', () => ***REMOVED***
  expect(transformCss([['font-weight', '400']])).toEqual(***REMOVED*** fontWeight: '400' })
  expect(transformCss([['font-weight', 'bold']])).toEqual(***REMOVED***
    fontWeight: 'bold',
***REMOVED***)
})
