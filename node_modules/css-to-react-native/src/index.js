/* eslint-disable no-param-reassign */
import parse from 'postcss-value-parser'
import camelizeStyleName from 'camelize'
import transforms from './transforms/index'
import devPropertiesWithoutUnitsRegExp from './devPropertiesWithoutUnitsRegExp'
import TokenStream from './TokenStream'

// Note if this is wrong, you'll need to change tokenTypes.js too
const numberOrLengthRe = /^([+-]?(?:\d*\.)?\d+(?:e[+-]?\d+)?)(?:px)?$/i
const numberOnlyRe = /^[+-]?(?:\d*\.\d*|[1-9]\d*)(?:e[+-]?\d+)?$/i
const boolRe = /^true|false$/i
const nullRe = /^null$/i
const undefinedRe = /^undefined$/i

// Undocumented export
export const transformRawValue = (propName, value) => ***REMOVED***
  if (process.env.NODE_ENV !== 'production') ***REMOVED***
    const needsUnit = !devPropertiesWithoutUnitsRegExp.test(propName)
    const isNumberWithoutUnit = numberOnlyRe.test(value)
    if (needsUnit && isNumberWithoutUnit) ***REMOVED***
      // eslint-disable-next-line no-console
      console.warn(`Expected style "$***REMOVED***propName}: $***REMOVED***value}" to contain units`)
  ***REMOVED***
    if (!needsUnit && value !== '0' && !isNumberWithoutUnit) ***REMOVED***
      // eslint-disable-next-line no-console
      console.warn(`Expected style "$***REMOVED***propName}: $***REMOVED***value}" to be unitless`)
  ***REMOVED***
***REMOVED***

  const numberMatch = value.match(numberOrLengthRe)
  if (numberMatch !== null) return Number(numberMatch[1])

  const boolMatch = value.match(boolRe)
  if (boolMatch !== null) return boolMatch[0].toLowerCase() === 'true'

  const nullMatch = value.match(nullRe)
  if (nullMatch !== null) return null

  const undefinedMatch = value.match(undefinedRe)
  if (undefinedMatch !== null) return undefined

  return value
}

const baseTransformShorthandValue = (propName, value) => ***REMOVED***
  const ast = parse(value)
  const tokenStream = new TokenStream(ast.nodes)
  return transforms[propName](tokenStream)
}

const transformShorthandValue =
  process.env.NODE_ENV === 'production'
    ? baseTransformShorthandValue
    : (propName, value) => ***REMOVED***
        try ***REMOVED***
          return baseTransformShorthandValue(propName, value)
      ***REMOVED*** catch (e) ***REMOVED***
          throw new Error(`Failed to parse declaration "$***REMOVED***propName}: $***REMOVED***value}"`)
      ***REMOVED***
    ***REMOVED***

export const getStylesForProperty = (propName, inputValue, allowShorthand) => ***REMOVED***
  const isRawValue = allowShorthand === false || !(propName in transforms)
  const value = inputValue.trim()

  const propValues = isRawValue
    ? ***REMOVED*** [propName]: transformRawValue(propName, value) }
    : transformShorthandValue(propName, value)

  return propValues
}

export const getPropertyName = propName => ***REMOVED***
  const isCustomProp = /^--\w+/.test(propName)
  if (isCustomProp) ***REMOVED***
    return propName
***REMOVED***
  return camelizeStyleName(propName)
}

export default (rules, shorthandBlacklist = []) =>
  rules.reduce((accum, rule) => ***REMOVED***
    const propertyName = getPropertyName(rule[0])
    const value = rule[1]
    const allowShorthand = shorthandBlacklist.indexOf(propertyName) === -1
    return Object.assign(
      accum,
      getStylesForProperty(propertyName, value, allowShorthand)
    )
***REMOVED***, ***REMOVED***})
