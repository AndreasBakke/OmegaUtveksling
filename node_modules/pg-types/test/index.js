
var test = require('tape')
var printf = require('pff')
var getTypeParser = require('../').getTypeParser
var types = require('./types')

test('types', function (t) ***REMOVED***
  Object.keys(types).forEach(function (typeName) ***REMOVED***
    var type = types[typeName]
    t.test(typeName, function (t) ***REMOVED***
      var parser = getTypeParser(type.id, type.format)
      type.tests.forEach(function (tests) ***REMOVED***
        var input = tests[0]
        var expected = tests[1]
        var result = parser(input)
        if (typeof expected === 'function') ***REMOVED***
          return expected(t, result)
      ***REMOVED***
        t.equal(result, expected)
    ***REMOVED***)
      t.end()
  ***REMOVED***)
***REMOVED***)
})
