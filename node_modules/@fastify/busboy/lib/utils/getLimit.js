'use strict'

module.exports = function getLimit (limits, name, defaultLimit) ***REMOVED***
  if (
    !limits ||
    limits[name] === undefined ||
    limits[name] === null
  ) ***REMOVED*** return defaultLimit }

  if (
    typeof limits[name] !== 'number' ||
    isNaN(limits[name])
  ) ***REMOVED*** throw new TypeError('Limit ' + name + ' is not a valid number') }

  return limits[name]
}
