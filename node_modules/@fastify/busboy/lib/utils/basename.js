'use strict'

module.exports = function basename (path) ***REMOVED***
  if (typeof path !== 'string') ***REMOVED*** return '' }
  for (var i = path.length - 1; i >= 0; --i) ***REMOVED*** // eslint-disable-line no-var
    switch (path.charCodeAt(i)) ***REMOVED***
      case 0x2F: // '/'
      case 0x5C: // '\'
        path = path.slice(i + 1)
        return (path === '..' || path === '.' ? '' : path)
  ***REMOVED***
***REMOVED***
  return (path === '..' || path === '.' ? '' : path)
}
