declare module 'retry-request' ***REMOVED***
  // eslint-disable-next-line node/no-unpublished-import
  import * as request from 'request';

  namespace retryRequest ***REMOVED***
    function getNextRetryDelay(retryNumber: number): void;
    interface Options ***REMOVED***
      objectMode?: boolean;
      request?: typeof request;
      retries?: number;
      noResponseRetries?: number;
      currentRetryAttempt?: number;
      maxRetryDelay?: number;
      retryDelayMultiplier?: number;
      totalTimeout?: number;
      shouldRetryFn?: (response: request.RequestResponse) => boolean;
  ***REMOVED***
***REMOVED***

  function retryRequest(
    requestOpts: request.Options,
    opts: retryRequest.Options,
    callback?: request.RequestCallback
  ): ***REMOVED***abort: () => void};
  function retryRequest(
    requestOpts: request.Options,
    callback?: request.RequestCallback
  ): ***REMOVED***abort: () => void};

  export = retryRequest;
}
