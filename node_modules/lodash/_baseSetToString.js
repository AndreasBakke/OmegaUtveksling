var constant = require('./constant'),
    defineProperty = require('./_defineProperty'),
    identity = require('./identity');

/**
 * The base implementation of `setToString` without support for hot loop shorting.
 *
 * @private
 * @param ***REMOVED***Function} func The function to modify.
 * @param ***REMOVED***Function} string The `toString` result.
 * @returns ***REMOVED***Function} Returns `func`.
 */
var baseSetToString = !defineProperty ? identity : function(func, string) ***REMOVED***
  return defineProperty(func, 'toString', ***REMOVED***
    'configurable': true,
    'enumerable': false,
    'value': constant(string),
    'writable': true
***REMOVED***);
};

module.exports = baseSetToString;
