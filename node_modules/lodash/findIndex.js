var baseFindIndex = require('./_baseFindIndex'),
    baseIteratee = require('./_baseIteratee'),
    toInteger = require('./toInteger');

/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeMax = Math.max;

/**
 * This method is like `_.find` except that it returns the index of the first
 * element `predicate` returns truthy for instead of the element itself.
 *
 * @static
 * @memberOf _
 * @since 1.1.0
 * @category Array
 * @param ***REMOVED***Array} array The array to inspect.
 * @param ***REMOVED***Function} [predicate=_.identity] The function invoked per iteration.
 * @param ***REMOVED***number} [fromIndex=0] The index to search from.
 * @returns ***REMOVED***number} Returns the index of the found element, else `-1`.
 * @example
 *
 * var users = [
 *   ***REMOVED*** 'user': 'barney',  'active': false },
 *   ***REMOVED*** 'user': 'fred',    'active': false },
 *   ***REMOVED*** 'user': 'pebbles', 'active': true }
 * ];
 *
 * _.findIndex(users, function(o) ***REMOVED*** return o.user == 'barney'; });
 * // => 0
 *
 * // The `_.matches` iteratee shorthand.
 * _.findIndex(users, ***REMOVED*** 'user': 'fred', 'active': false });
 * // => 1
 *
 * // The `_.matchesProperty` iteratee shorthand.
 * _.findIndex(users, ['active', false]);
 * // => 0
 *
 * // The `_.property` iteratee shorthand.
 * _.findIndex(users, 'active');
 * // => 2
 */
function findIndex(array, predicate, fromIndex) ***REMOVED***
  var length = array == null ? 0 : array.length;
  if (!length) ***REMOVED***
    return -1;
***REMOVED***
  var index = fromIndex == null ? 0 : toInteger(fromIndex);
  if (index < 0) ***REMOVED***
    index = nativeMax(length + index, 0);
***REMOVED***
  return baseFindIndex(array, baseIteratee(predicate, 3), index);
}

module.exports = findIndex;
