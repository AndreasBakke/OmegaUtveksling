var createMathOperation = require('./_createMathOperation');

/**
 * Multiply two numbers.
 *
 * @static
 * @memberOf _
 * @since 4.7.0
 * @category Math
 * @param ***REMOVED***number} multiplier The first number in a multiplication.
 * @param ***REMOVED***number} multiplicand The second number in a multiplication.
 * @returns ***REMOVED***number} Returns the product.
 * @example
 *
 * _.multiply(6, 4);
 * // => 24
 */
var multiply = createMathOperation(function(multiplier, multiplicand) ***REMOVED***
  return multiplier * multiplicand;
}, 1);

module.exports = multiply;
