var castPath = require('./_castPath'),
    toKey = require('./_toKey');

/**
 * The base implementation of `_.get` without support for default values.
 *
 * @private
 * @param ***REMOVED***Object} object The object to query.
 * @param ***REMOVED***Array|string} path The path of the property to get.
 * @returns ***REMOVED****} Returns the resolved value.
 */
function baseGet(object, path) ***REMOVED***
  path = castPath(path, object);

  var index = 0,
      length = path.length;

  while (object != null && index < length) ***REMOVED***
    object = object[toKey(path[index++])];
***REMOVED***
  return (index && index == length) ? object : undefined;
}

module.exports = baseGet;
