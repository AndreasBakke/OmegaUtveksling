/**
 * Gets the number of `placeholder` occurrences in `array`.
 *
 * @private
 * @param ***REMOVED***Array} array The array to inspect.
 * @param ***REMOVED****} placeholder The placeholder to search for.
 * @returns ***REMOVED***number} Returns the placeholder count.
 */
function countHolders(array, placeholder) ***REMOVED***
  var length = array.length,
      result = 0;

  while (length--) ***REMOVED***
    if (array[length] === placeholder) ***REMOVED***
      ++result;
  ***REMOVED***
***REMOVED***
  return result;
}

module.exports = countHolders;
