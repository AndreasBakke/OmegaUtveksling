/** Used to match wrap detail comments. */
var reWrapDetails = /\***REMOVED***\n\/\* \[wrapped with (.+)\] \*/,
    reSplitDetails = /,? & /;

/**
 * Extracts wrapper details from the `source` body comment.
 *
 * @private
 * @param ***REMOVED***string} source The source to inspect.
 * @returns ***REMOVED***Array} Returns the wrapper details.
 */
function getWrapDetails(source) ***REMOVED***
  var match = source.match(reWrapDetails);
  return match ? match[1].split(reSplitDetails) : [];
}

module.exports = getWrapDetails;
