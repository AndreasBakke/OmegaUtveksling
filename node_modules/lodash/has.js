var baseHas = require('./_baseHas'),
    hasPath = require('./_hasPath');

/**
 * Checks if `path` is a direct property of `object`.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Object
 * @param ***REMOVED***Object} object The object to query.
 * @param ***REMOVED***Array|string} path The path to check.
 * @returns ***REMOVED***boolean} Returns `true` if `path` exists, else `false`.
 * @example
 *
 * var object = ***REMOVED*** 'a': ***REMOVED*** 'b': 2 } };
 * var other = _.create(***REMOVED*** 'a': _.create(***REMOVED*** 'b': 2 }) });
 *
 * _.has(object, 'a');
 * // => true
 *
 * _.has(object, 'a.b');
 * // => true
 *
 * _.has(object, ['a', 'b']);
 * // => true
 *
 * _.has(other, 'a');
 * // => false
 */
function has(object, path) ***REMOVED***
  return object != null && hasPath(object, path, baseHas);
}

module.exports = has;
