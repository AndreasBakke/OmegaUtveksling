var baseConvert = require('./_baseConvert');

/**
 * Converts `lodash` to an immutable auto-curried iteratee-first data-last
 * version with conversion `options` applied.
 *
 * @param ***REMOVED***Function} lodash The lodash function to convert.
 * @param ***REMOVED***Object} [options] The options object. See `baseConvert` for more details.
 * @returns ***REMOVED***Function} Returns the converted `lodash`.
 */
function browserConvert(lodash, options) ***REMOVED***
  return baseConvert(lodash, lodash, options);
}

if (typeof _ == 'function' && typeof _.runInContext == 'function') ***REMOVED***
  _ = browserConvert(_.runInContext());
}
module.exports = browserConvert;
