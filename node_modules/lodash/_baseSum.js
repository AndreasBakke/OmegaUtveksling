/**
 * The base implementation of `_.sum` and `_.sumBy` without support for
 * iteratee shorthands.
 *
 * @private
 * @param ***REMOVED***Array} array The array to iterate over.
 * @param ***REMOVED***Function} iteratee The function invoked per iteration.
 * @returns ***REMOVED***number} Returns the sum.
 */
function baseSum(array, iteratee) ***REMOVED***
  var result,
      index = -1,
      length = array.length;

  while (++index < length) ***REMOVED***
    var current = iteratee(array[index]);
    if (current !== undefined) ***REMOVED***
      result = result === undefined ? current : (result + current);
  ***REMOVED***
***REMOVED***
  return result;
}

module.exports = baseSum;
