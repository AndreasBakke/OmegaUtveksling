var baseEach = require('./_baseEach');

/**
 * The base implementation of `_.filter` without support for iteratee shorthands.
 *
 * @private
 * @param ***REMOVED***Array|Object} collection The collection to iterate over.
 * @param ***REMOVED***Function} predicate The function invoked per iteration.
 * @returns ***REMOVED***Array} Returns the new filtered array.
 */
function baseFilter(collection, predicate) ***REMOVED***
  var result = [];
  baseEach(collection, function(value, index, collection) ***REMOVED***
    if (predicate(value, index, collection)) ***REMOVED***
      result.push(value);
  ***REMOVED***
***REMOVED***);
  return result;
}

module.exports = baseFilter;
