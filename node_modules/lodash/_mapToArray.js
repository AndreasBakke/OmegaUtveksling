/**
 * Converts `map` to its key-value pairs.
 *
 * @private
 * @param ***REMOVED***Object} map The map to convert.
 * @returns ***REMOVED***Array} Returns the key-value pairs.
 */
function mapToArray(map) ***REMOVED***
  var index = -1,
      result = Array(map.size);

  map.forEach(function(value, key) ***REMOVED***
    result[++index] = [key, value];
***REMOVED***);
  return result;
}

module.exports = mapToArray;
