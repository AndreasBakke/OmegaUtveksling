var chain = require('./chain');

/**
 * Creates a `lodash` wrapper instance with explicit method chain sequences enabled.
 *
 * @name chain
 * @memberOf _
 * @since 0.1.0
 * @category Seq
 * @returns ***REMOVED***Object} Returns the new `lodash` wrapper instance.
 * @example
 *
 * var users = [
 *   ***REMOVED*** 'user': 'barney', 'age': 36 },
 *   ***REMOVED*** 'user': 'fred',   'age': 40 }
 * ];
 *
 * // A sequence without explicit chaining.
 * _(users).head();
 * // => ***REMOVED*** 'user': 'barney', 'age': 36 }
 *
 * // A sequence with explicit chaining.
 * _(users)
 *   .chain()
 *   .head()
 *   .pick('user')
 *   .value();
 * // => ***REMOVED*** 'user': 'barney' }
 */
function wrapperChain() ***REMOVED***
  return chain(this);
}

module.exports = wrapperChain;
