/**
 * A specialized version of `_.reduce` for arrays without support for
 * iteratee shorthands.
 *
 * @private
 * @param ***REMOVED***Array} [array] The array to iterate over.
 * @param ***REMOVED***Function} iteratee The function invoked per iteration.
 * @param ***REMOVED****} [accumulator] The initial value.
 * @param ***REMOVED***boolean} [initAccum] Specify using the first element of `array` as
 *  the initial value.
 * @returns ***REMOVED****} Returns the accumulated value.
 */
function arrayReduce(array, iteratee, accumulator, initAccum) ***REMOVED***
  var index = -1,
      length = array == null ? 0 : array.length;

  if (initAccum && length) ***REMOVED***
    accumulator = array[++index];
***REMOVED***
  while (++index < length) ***REMOVED***
    accumulator = iteratee(accumulator, array[index], index, array);
***REMOVED***
  return accumulator;
}

module.exports = arrayReduce;
