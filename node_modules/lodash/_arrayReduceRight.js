/**
 * A specialized version of `_.reduceRight` for arrays without support for
 * iteratee shorthands.
 *
 * @private
 * @param ***REMOVED***Array} [array] The array to iterate over.
 * @param ***REMOVED***Function} iteratee The function invoked per iteration.
 * @param ***REMOVED****} [accumulator] The initial value.
 * @param ***REMOVED***boolean} [initAccum] Specify using the last element of `array` as
 *  the initial value.
 * @returns ***REMOVED****} Returns the accumulated value.
 */
function arrayReduceRight(array, iteratee, accumulator, initAccum) ***REMOVED***
  var length = array == null ? 0 : array.length;
  if (initAccum && length) ***REMOVED***
    accumulator = array[--length];
***REMOVED***
  while (length--) ***REMOVED***
    accumulator = iteratee(accumulator, array[length], length, array);
***REMOVED***
  return accumulator;
}

module.exports = arrayReduceRight;
