/**
 * The base implementation of methods like `_.findKey` and `_.findLastKey`,
 * without support for iteratee shorthands, which iterates over `collection`
 * using `eachFunc`.
 *
 * @private
 * @param ***REMOVED***Array|Object} collection The collection to inspect.
 * @param ***REMOVED***Function} predicate The function invoked per iteration.
 * @param ***REMOVED***Function} eachFunc The function to iterate over `collection`.
 * @returns ***REMOVED****} Returns the found element or its key, else `undefined`.
 */
function baseFindKey(collection, predicate, eachFunc) ***REMOVED***
  var result;
  eachFunc(collection, function(value, key, collection) ***REMOVED***
    if (predicate(value, key, collection)) ***REMOVED***
      result = key;
      return false;
  ***REMOVED***
***REMOVED***);
  return result;
}

module.exports = baseFindKey;
