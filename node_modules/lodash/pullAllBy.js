var baseIteratee = require('./_baseIteratee'),
    basePullAll = require('./_basePullAll');

/**
 * This method is like `_.pullAll` except that it accepts `iteratee` which is
 * invoked for each element of `array` and `values` to generate the criterion
 * by which they're compared. The iteratee is invoked with one argument: (value).
 *
 * **Note:** Unlike `_.differenceBy`, this method mutates `array`.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Array
 * @param ***REMOVED***Array} array The array to modify.
 * @param ***REMOVED***Array} values The values to remove.
 * @param ***REMOVED***Function} [iteratee=_.identity] The iteratee invoked per element.
 * @returns ***REMOVED***Array} Returns `array`.
 * @example
 *
 * var array = [***REMOVED*** 'x': 1 }, ***REMOVED*** 'x': 2 }, ***REMOVED*** 'x': 3 }, ***REMOVED*** 'x': 1 }];
 *
 * _.pullAllBy(array, [***REMOVED*** 'x': 1 }, ***REMOVED*** 'x': 3 }], 'x');
 * console.log(array);
 * // => [***REMOVED*** 'x': 2 }]
 */
function pullAllBy(array, values, iteratee) ***REMOVED***
  return (array && array.length && values && values.length)
    ? basePullAll(array, values, baseIteratee(iteratee, 2))
    : array;
}

module.exports = pullAllBy;
