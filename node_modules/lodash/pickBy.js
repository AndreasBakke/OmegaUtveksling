var arrayMap = require('./_arrayMap'),
    baseIteratee = require('./_baseIteratee'),
    basePickBy = require('./_basePickBy'),
    getAllKeysIn = require('./_getAllKeysIn');

/**
 * Creates an object composed of the `object` properties `predicate` returns
 * truthy for. The predicate is invoked with two arguments: (value, key).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Object
 * @param ***REMOVED***Object} object The source object.
 * @param ***REMOVED***Function} [predicate=_.identity] The function invoked per property.
 * @returns ***REMOVED***Object} Returns the new object.
 * @example
 *
 * var object = ***REMOVED*** 'a': 1, 'b': '2', 'c': 3 };
 *
 * _.pickBy(object, _.isNumber);
 * // => ***REMOVED*** 'a': 1, 'c': 3 }
 */
function pickBy(object, predicate) ***REMOVED***
  if (object == null) ***REMOVED***
    return ***REMOVED***};
***REMOVED***
  var props = arrayMap(getAllKeysIn(object), function(prop) ***REMOVED***
    return [prop];
***REMOVED***);
  predicate = baseIteratee(predicate);
  return basePickBy(object, props, function(value, path) ***REMOVED***
    return predicate(value, path[0]);
***REMOVED***);
}

module.exports = pickBy;
