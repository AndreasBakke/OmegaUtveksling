var baseUnset = require('./_baseUnset'),
    isIndex = require('./_isIndex');

/** Used for built-in method references. */
var arrayProto = Array.prototype;

/** Built-in value references. */
var splice = arrayProto.splice;

/**
 * The base implementation of `_.pullAt` without support for individual
 * indexes or capturing the removed elements.
 *
 * @private
 * @param ***REMOVED***Array} array The array to modify.
 * @param ***REMOVED***number[]} indexes The indexes of elements to remove.
 * @returns ***REMOVED***Array} Returns `array`.
 */
function basePullAt(array, indexes) ***REMOVED***
  var length = array ? indexes.length : 0,
      lastIndex = length - 1;

  while (length--) ***REMOVED***
    var index = indexes[length];
    if (length == lastIndex || index !== previous) ***REMOVED***
      var previous = index;
      if (isIndex(index)) ***REMOVED***
        splice.call(array, index, 1);
    ***REMOVED*** else ***REMOVED***
        baseUnset(array, index);
    ***REMOVED***
  ***REMOVED***
***REMOVED***
  return array;
}

module.exports = basePullAt;
