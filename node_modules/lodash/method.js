var baseInvoke = require('./_baseInvoke'),
    baseRest = require('./_baseRest');

/**
 * Creates a function that invokes the method at `path` of a given object.
 * Any additional arguments are provided to the invoked method.
 *
 * @static
 * @memberOf _
 * @since 3.7.0
 * @category Util
 * @param ***REMOVED***Array|string} path The path of the method to invoke.
 * @param ***REMOVED***...*} [args] The arguments to invoke the method with.
 * @returns ***REMOVED***Function} Returns the new invoker function.
 * @example
 *
 * var objects = [
 *   ***REMOVED*** 'a': ***REMOVED*** 'b': _.constant(2) } },
 *   ***REMOVED*** 'a': ***REMOVED*** 'b': _.constant(1) } }
 * ];
 *
 * _.map(objects, _.method('a.b'));
 * // => [2, 1]
 *
 * _.map(objects, _.method(['a', 'b']));
 * // => [2, 1]
 */
var method = baseRest(function(path, args) ***REMOVED***
  return function(object) ***REMOVED***
    return baseInvoke(object, path, args);
***REMOVED***;
});

module.exports = method;
