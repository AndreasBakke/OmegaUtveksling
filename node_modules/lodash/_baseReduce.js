/**
 * The base implementation of `_.reduce` and `_.reduceRight`, without support
 * for iteratee shorthands, which iterates over `collection` using `eachFunc`.
 *
 * @private
 * @param ***REMOVED***Array|Object} collection The collection to iterate over.
 * @param ***REMOVED***Function} iteratee The function invoked per iteration.
 * @param ***REMOVED****} accumulator The initial value.
 * @param ***REMOVED***boolean} initAccum Specify using the first or last element of
 *  `collection` as the initial value.
 * @param ***REMOVED***Function} eachFunc The function to iterate over `collection`.
 * @returns ***REMOVED****} Returns the accumulated value.
 */
function baseReduce(collection, iteratee, accumulator, initAccum, eachFunc) ***REMOVED***
  eachFunc(collection, function(value, index, collection) ***REMOVED***
    accumulator = initAccum
      ? (initAccum = false, value)
      : iteratee(accumulator, value, index, collection);
***REMOVED***);
  return accumulator;
}

module.exports = baseReduce;
