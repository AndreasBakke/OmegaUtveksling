var Stack = require('./_Stack'),
    baseIsEqual = require('./_baseIsEqual');

/** Used to compose bitmasks for value comparisons. */
var COMPARE_PARTIAL_FLAG = 1,
    COMPARE_UNORDERED_FLAG = 2;

/**
 * The base implementation of `_.isMatch` without support for iteratee shorthands.
 *
 * @private
 * @param ***REMOVED***Object} object The object to inspect.
 * @param ***REMOVED***Object} source The object of property values to match.
 * @param ***REMOVED***Array} matchData The property names, values, and compare flags to match.
 * @param ***REMOVED***Function} [customizer] The function to customize comparisons.
 * @returns ***REMOVED***boolean} Returns `true` if `object` is a match, else `false`.
 */
function baseIsMatch(object, source, matchData, customizer) ***REMOVED***
  var index = matchData.length,
      length = index,
      noCustomizer = !customizer;

  if (object == null) ***REMOVED***
    return !length;
***REMOVED***
  object = Object(object);
  while (index--) ***REMOVED***
    var data = matchData[index];
    if ((noCustomizer && data[2])
          ? data[1] !== object[data[0]]
          : !(data[0] in object)
        ) ***REMOVED***
      return false;
  ***REMOVED***
***REMOVED***
  while (++index < length) ***REMOVED***
    data = matchData[index];
    var key = data[0],
        objValue = object[key],
        srcValue = data[1];

    if (noCustomizer && data[2]) ***REMOVED***
      if (objValue === undefined && !(key in object)) ***REMOVED***
        return false;
    ***REMOVED***
  ***REMOVED*** else ***REMOVED***
      var stack = new Stack;
      if (customizer) ***REMOVED***
        var result = customizer(objValue, srcValue, key, object, source, stack);
    ***REMOVED***
      if (!(result === undefined
            ? baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG | COMPARE_UNORDERED_FLAG, customizer, stack)
            : result
          )) ***REMOVED***
        return false;
    ***REMOVED***
  ***REMOVED***
***REMOVED***
  return true;
}

module.exports = baseIsMatch;
