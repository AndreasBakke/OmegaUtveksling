/** Used to match a single whitespace character. */
var reWhitespace = /\s/;

/**
 * Used by `_.trim` and `_.trimEnd` to get the index of the last non-whitespace
 * character of `string`.
 *
 * @private
 * @param ***REMOVED***string} string The string to inspect.
 * @returns ***REMOVED***number} Returns the index of the last non-whitespace character.
 */
function trimmedEndIndex(string) ***REMOVED***
  var index = string.length;

  while (index-- && reWhitespace.test(string.charAt(index))) ***REMOVED***}
  return index;
}

module.exports = trimmedEndIndex;
