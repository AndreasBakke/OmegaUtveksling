/**
 * The base implementation of `_.slice` without an iteratee call guard.
 *
 * @private
 * @param ***REMOVED***Array} array The array to slice.
 * @param ***REMOVED***number} [start=0] The start position.
 * @param ***REMOVED***number} [end=array.length] The end position.
 * @returns ***REMOVED***Array} Returns the slice of `array`.
 */
function baseSlice(array, start, end) ***REMOVED***
  var index = -1,
      length = array.length;

  if (start < 0) ***REMOVED***
    start = -start > length ? 0 : (length + start);
***REMOVED***
  end = end > length ? length : end;
  if (end < 0) ***REMOVED***
    end += length;
***REMOVED***
  length = start > end ? 0 : ((end - start) >>> 0);
  start >>>= 0;

  var result = Array(length);
  while (++index < length) ***REMOVED***
    result[index] = array[index + start];
***REMOVED***
  return result;
}

module.exports = baseSlice;
