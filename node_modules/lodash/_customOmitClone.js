var isPlainObject = require('./isPlainObject');

/**
 * Used by `_.omit` to customize its `_.cloneDeep` use to only clone plain
 * objects.
 *
 * @private
 * @param ***REMOVED****} value The value to inspect.
 * @param ***REMOVED***string} key The key of the property to inspect.
 * @returns ***REMOVED****} Returns the uncloned value or `undefined` to defer cloning to `_.cloneDeep`.
 */
function customOmitClone(value) ***REMOVED***
  return isPlainObject(value) ? undefined : value;
}

module.exports = customOmitClone;
