/** Error message constants. */
var FUNC_ERROR_TEXT = 'Expected a function';

/**
 * The base implementation of `_.delay` and `_.defer` which accepts `args`
 * to provide to `func`.
 *
 * @private
 * @param ***REMOVED***Function} func The function to delay.
 * @param ***REMOVED***number} wait The number of milliseconds to delay invocation.
 * @param ***REMOVED***Array} args The arguments to provide to `func`.
 * @returns ***REMOVED***number|Object} Returns the timer id or timeout object.
 */
function baseDelay(func, wait, args) ***REMOVED***
  if (typeof func != 'function') ***REMOVED***
    throw new TypeError(FUNC_ERROR_TEXT);
***REMOVED***
  return setTimeout(function() ***REMOVED*** func.apply(undefined, args); }, wait);
}

module.exports = baseDelay;
