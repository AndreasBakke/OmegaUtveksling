var baseSlice = require('./_baseSlice');

/**
 * Casts `array` to a slice if it's needed.
 *
 * @private
 * @param ***REMOVED***Array} array The array to inspect.
 * @param ***REMOVED***number} start The start position.
 * @param ***REMOVED***number} [end=array.length] The end position.
 * @returns ***REMOVED***Array} Returns the cast slice.
 */
function castSlice(array, start, end) ***REMOVED***
  var length = array.length;
  end = end === undefined ? length : end;
  return (!start && end >= length) ? array : baseSlice(array, start, end);
}

module.exports = castSlice;
