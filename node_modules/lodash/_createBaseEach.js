var isArrayLike = require('./isArrayLike');

/**
 * Creates a `baseEach` or `baseEachRight` function.
 *
 * @private
 * @param ***REMOVED***Function} eachFunc The function to iterate over a collection.
 * @param ***REMOVED***boolean} [fromRight] Specify iterating from right to left.
 * @returns ***REMOVED***Function} Returns the new base function.
 */
function createBaseEach(eachFunc, fromRight) ***REMOVED***
  return function(collection, iteratee) ***REMOVED***
    if (collection == null) ***REMOVED***
      return collection;
  ***REMOVED***
    if (!isArrayLike(collection)) ***REMOVED***
      return eachFunc(collection, iteratee);
  ***REMOVED***
    var length = collection.length,
        index = fromRight ? length : -1,
        iterable = Object(collection);

    while ((fromRight ? index-- : ++index < length)) ***REMOVED***
      if (iteratee(iterable[index], index, iterable) === false) ***REMOVED***
        break;
    ***REMOVED***
  ***REMOVED***
    return collection;
***REMOVED***;
}

module.exports = createBaseEach;
