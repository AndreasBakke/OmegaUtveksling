var arrayPush = require('./_arrayPush'),
    isArray = require('./isArray');

/**
 * The base implementation of `getAllKeys` and `getAllKeysIn` which uses
 * `keysFunc` and `symbolsFunc` to get the enumerable property names and
 * symbols of `object`.
 *
 * @private
 * @param ***REMOVED***Object} object The object to query.
 * @param ***REMOVED***Function} keysFunc The function to get the keys of `object`.
 * @param ***REMOVED***Function} symbolsFunc The function to get the symbols of `object`.
 * @returns ***REMOVED***Array} Returns the array of property names and symbols.
 */
function baseGetAllKeys(object, keysFunc, symbolsFunc) ***REMOVED***
  var result = keysFunc(object);
  return isArray(object) ? result : arrayPush(result, symbolsFunc(object));
}

module.exports = baseGetAllKeys;
