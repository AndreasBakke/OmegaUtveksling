/**
 * Gets the value at `key`, unless `key` is "__proto__" or "constructor".
 *
 * @private
 * @param ***REMOVED***Object} object The object to query.
 * @param ***REMOVED***string} key The key of the property to get.
 * @returns ***REMOVED****} Returns the property value.
 */
function safeGet(object, key) ***REMOVED***
  if (key === 'constructor' && typeof object[key] === 'function') ***REMOVED***
    return;
***REMOVED***

  if (key == '__proto__') ***REMOVED***
    return;
***REMOVED***

  return object[key];
}

module.exports = safeGet;
