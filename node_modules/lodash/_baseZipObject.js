/**
 * This base implementation of `_.zipObject` which assigns values using `assignFunc`.
 *
 * @private
 * @param ***REMOVED***Array} props The property identifiers.
 * @param ***REMOVED***Array} values The property values.
 * @param ***REMOVED***Function} assignFunc The function to assign values.
 * @returns ***REMOVED***Object} Returns the new object.
 */
function baseZipObject(props, values, assignFunc) ***REMOVED***
  var index = -1,
      length = props.length,
      valsLength = values.length,
      result = ***REMOVED***};

  while (++index < length) ***REMOVED***
    var value = index < valsLength ? values[index] : undefined;
    assignFunc(result, props[index], value);
***REMOVED***
  return result;
}

module.exports = baseZipObject;
