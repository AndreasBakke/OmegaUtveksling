/**
 * The base implementation of `_.times` without support for iteratee shorthands
 * or max array length checks.
 *
 * @private
 * @param ***REMOVED***number} n The number of times to invoke `iteratee`.
 * @param ***REMOVED***Function} iteratee The function invoked per iteration.
 * @returns ***REMOVED***Array} Returns the array of results.
 */
function baseTimes(n, iteratee) ***REMOVED***
  var index = -1,
      result = Array(n);

  while (++index < n) ***REMOVED***
    result[index] = iteratee(index);
***REMOVED***
  return result;
}

module.exports = baseTimes;
