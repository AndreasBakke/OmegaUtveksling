var baseDelay = require('./_baseDelay'),
    baseRest = require('./_baseRest');

/**
 * Defers invoking the `func` until the current call stack has cleared. Any
 * additional arguments are provided to `func` when it's invoked.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Function
 * @param ***REMOVED***Function} func The function to defer.
 * @param ***REMOVED***...*} [args] The arguments to invoke `func` with.
 * @returns ***REMOVED***number} Returns the timer id.
 * @example
 *
 * _.defer(function(text) ***REMOVED***
 *   console.log(text);
 * }, 'deferred');
 * // => Logs 'deferred' after one millisecond.
 */
var defer = baseRest(function(func, args) ***REMOVED***
  return baseDelay(func, 1, args);
});

module.exports = defer;
