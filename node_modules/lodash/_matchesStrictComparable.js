/**
 * A specialized version of `matchesProperty` for source values suitable
 * for strict equality comparisons, i.e. `===`.
 *
 * @private
 * @param ***REMOVED***string} key The key of the property to get.
 * @param ***REMOVED****} srcValue The value to match.
 * @returns ***REMOVED***Function} Returns the new spec function.
 */
function matchesStrictComparable(key, srcValue) ***REMOVED***
  return function(object) ***REMOVED***
    if (object == null) ***REMOVED***
      return false;
  ***REMOVED***
    return object[key] === srcValue &&
      (srcValue !== undefined || (key in Object(object)));
***REMOVED***;
}

module.exports = matchesStrictComparable;
