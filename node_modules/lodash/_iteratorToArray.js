/**
 * Converts `iterator` to an array.
 *
 * @private
 * @param ***REMOVED***Object} iterator The iterator to convert.
 * @returns ***REMOVED***Array} Returns the converted array.
 */
function iteratorToArray(iterator) ***REMOVED***
  var data,
      result = [];

  while (!(data = iterator.next()).done) ***REMOVED***
    result.push(data.value);
***REMOVED***
  return result;
}

module.exports = iteratorToArray;
