var baseMerge = require('./_baseMerge'),
    createAssigner = require('./_createAssigner');

/**
 * This method is like `_.merge` except that it accepts `customizer` which
 * is invoked to produce the merged values of the destination and source
 * properties. If `customizer` returns `undefined`, merging is handled by the
 * method instead. The `customizer` is invoked with six arguments:
 * (objValue, srcValue, key, object, source, stack).
 *
 * **Note:** This method mutates `object`.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Object
 * @param ***REMOVED***Object} object The destination object.
 * @param ***REMOVED***...Object} sources The source objects.
 * @param ***REMOVED***Function} customizer The function to customize assigned values.
 * @returns ***REMOVED***Object} Returns `object`.
 * @example
 *
 * function customizer(objValue, srcValue) ***REMOVED***
 *   if (_.isArray(objValue)) ***REMOVED***
 *     return objValue.concat(srcValue);
 * ***REMOVED***
 * }
 *
 * var object = ***REMOVED*** 'a': [1], 'b': [2] };
 * var other = ***REMOVED*** 'a': [3], 'b': [4] };
 *
 * _.mergeWith(object, other, customizer);
 * // => ***REMOVED*** 'a': [1, 3], 'b': [2, 4] }
 */
var mergeWith = createAssigner(function(object, source, srcIndex, customizer) ***REMOVED***
  baseMerge(object, source, srcIndex, customizer);
});

module.exports = mergeWith;
