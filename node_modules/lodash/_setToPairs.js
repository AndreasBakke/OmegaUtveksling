/**
 * Converts `set` to its value-value pairs.
 *
 * @private
 * @param ***REMOVED***Object} set The set to convert.
 * @returns ***REMOVED***Array} Returns the value-value pairs.
 */
function setToPairs(set) ***REMOVED***
  var index = -1,
      result = Array(set.size);

  set.forEach(function(value) ***REMOVED***
    result[++index] = [value, value];
***REMOVED***);
  return result;
}

module.exports = setToPairs;
