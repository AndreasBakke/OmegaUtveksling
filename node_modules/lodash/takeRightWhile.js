var baseIteratee = require('./_baseIteratee'),
    baseWhile = require('./_baseWhile');

/**
 * Creates a slice of `array` with elements taken from the end. Elements are
 * taken until `predicate` returns falsey. The predicate is invoked with
 * three arguments: (value, index, array).
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Array
 * @param ***REMOVED***Array} array The array to query.
 * @param ***REMOVED***Function} [predicate=_.identity] The function invoked per iteration.
 * @returns ***REMOVED***Array} Returns the slice of `array`.
 * @example
 *
 * var users = [
 *   ***REMOVED*** 'user': 'barney',  'active': true },
 *   ***REMOVED*** 'user': 'fred',    'active': false },
 *   ***REMOVED*** 'user': 'pebbles', 'active': false }
 * ];
 *
 * _.takeRightWhile(users, function(o) ***REMOVED*** return !o.active; });
 * // => objects for ['fred', 'pebbles']
 *
 * // The `_.matches` iteratee shorthand.
 * _.takeRightWhile(users, ***REMOVED*** 'user': 'pebbles', 'active': false });
 * // => objects for ['pebbles']
 *
 * // The `_.matchesProperty` iteratee shorthand.
 * _.takeRightWhile(users, ['active', false]);
 * // => objects for ['fred', 'pebbles']
 *
 * // The `_.property` iteratee shorthand.
 * _.takeRightWhile(users, 'active');
 * // => []
 */
function takeRightWhile(array, predicate) ***REMOVED***
  return (array && array.length)
    ? baseWhile(array, baseIteratee(predicate, 3), false, true)
    : [];
}

module.exports = takeRightWhile;
