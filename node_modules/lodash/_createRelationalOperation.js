var toNumber = require('./toNumber');

/**
 * Creates a function that performs a relational operation on two values.
 *
 * @private
 * @param ***REMOVED***Function} operator The function to perform the operation.
 * @returns ***REMOVED***Function} Returns the new relational operation function.
 */
function createRelationalOperation(operator) ***REMOVED***
  return function(value, other) ***REMOVED***
    if (!(typeof value == 'string' && typeof other == 'string')) ***REMOVED***
      value = toNumber(value);
      other = toNumber(other);
  ***REMOVED***
    return operator(value, other);
***REMOVED***;
}

module.exports = createRelationalOperation;
