/**
 * Checks if a stack value for `key` exists.
 *
 * @private
 * @name has
 * @memberOf Stack
 * @param ***REMOVED***string} key The key of the entry to check.
 * @returns ***REMOVED***boolean} Returns `true` if an entry for `key` exists, else `false`.
 */
function stackHas(key) ***REMOVED***
  return this.__data__.has(key);
}

module.exports = stackHas;
