/**
 * Appends the elements of `values` to `array`.
 *
 * @private
 * @param ***REMOVED***Array} array The array to modify.
 * @param ***REMOVED***Array} values The values to append.
 * @returns ***REMOVED***Array} Returns `array`.
 */
function arrayPush(array, values) ***REMOVED***
  var index = -1,
      length = values.length,
      offset = array.length;

  while (++index < length) ***REMOVED***
    array[offset + index] = values[index];
***REMOVED***
  return array;
}

module.exports = arrayPush;
