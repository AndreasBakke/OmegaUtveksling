var baseForOwn = require('./_baseForOwn');

/**
 * The base implementation of `_.invert` and `_.invertBy` which inverts
 * `object` with values transformed by `iteratee` and set by `setter`.
 *
 * @private
 * @param ***REMOVED***Object} object The object to iterate over.
 * @param ***REMOVED***Function} setter The function to set `accumulator` values.
 * @param ***REMOVED***Function} iteratee The iteratee to transform values.
 * @param ***REMOVED***Object} accumulator The initial inverted object.
 * @returns ***REMOVED***Function} Returns `accumulator`.
 */
function baseInverter(object, setter, iteratee, accumulator) ***REMOVED***
  baseForOwn(object, function(value, key, object) ***REMOVED***
    setter(accumulator, iteratee(value), key, object);
***REMOVED***);
  return accumulator;
}

module.exports = baseInverter;
