/**
 * Copies the values of `source` to `array`.
 *
 * @private
 * @param ***REMOVED***Array} source The array to copy values from.
 * @param ***REMOVED***Array} [array=[]] The array to copy values to.
 * @returns ***REMOVED***Array} Returns `array`.
 */
function copyArray(source, array) ***REMOVED***
  var index = -1,
      length = source.length;

  array || (array = Array(length));
  while (++index < length) ***REMOVED***
    array[index] = source[index];
***REMOVED***
  return array;
}

module.exports = copyArray;
