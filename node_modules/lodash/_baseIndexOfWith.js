/**
 * This function is like `baseIndexOf` except that it accepts a comparator.
 *
 * @private
 * @param ***REMOVED***Array} array The array to inspect.
 * @param ***REMOVED****} value The value to search for.
 * @param ***REMOVED***number} fromIndex The index to search from.
 * @param ***REMOVED***Function} comparator The comparator invoked per element.
 * @returns ***REMOVED***number} Returns the index of the matched value, else `-1`.
 */
function baseIndexOfWith(array, value, fromIndex, comparator) ***REMOVED***
  var index = fromIndex - 1,
      length = array.length;

  while (++index < length) ***REMOVED***
    if (comparator(array[index], value)) ***REMOVED***
      return index;
  ***REMOVED***
***REMOVED***
  return -1;
}

module.exports = baseIndexOfWith;
