var baseAssignValue = require('./_baseAssignValue'),
    eq = require('./eq');

/**
 * This function is like `assignValue` except that it doesn't assign
 * `undefined` values.
 *
 * @private
 * @param ***REMOVED***Object} object The object to modify.
 * @param ***REMOVED***string} key The key of the property to assign.
 * @param ***REMOVED****} value The value to assign.
 */
function assignMergeValue(object, key, value) ***REMOVED***
  if ((value !== undefined && !eq(object[key], value)) ||
      (value === undefined && !(key in object))) ***REMOVED***
    baseAssignValue(object, key, value);
***REMOVED***
}

module.exports = assignMergeValue;
