var baseFindIndex = require('./_baseFindIndex'),
    baseIsNaN = require('./_baseIsNaN'),
    strictIndexOf = require('./_strictIndexOf');

/**
 * The base implementation of `_.indexOf` without `fromIndex` bounds checks.
 *
 * @private
 * @param ***REMOVED***Array} array The array to inspect.
 * @param ***REMOVED****} value The value to search for.
 * @param ***REMOVED***number} fromIndex The index to search from.
 * @returns ***REMOVED***number} Returns the index of the matched value, else `-1`.
 */
function baseIndexOf(array, value, fromIndex) ***REMOVED***
  return value === value
    ? strictIndexOf(array, value, fromIndex)
    : baseFindIndex(array, baseIsNaN, fromIndex);
}

module.exports = baseIndexOf;
