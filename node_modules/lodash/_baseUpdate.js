var baseGet = require('./_baseGet'),
    baseSet = require('./_baseSet');

/**
 * The base implementation of `_.update`.
 *
 * @private
 * @param ***REMOVED***Object} object The object to modify.
 * @param ***REMOVED***Array|string} path The path of the property to update.
 * @param ***REMOVED***Function} updater The function to produce the updated value.
 * @param ***REMOVED***Function} [customizer] The function to customize path creation.
 * @returns ***REMOVED***Object} Returns `object`.
 */
function baseUpdate(object, path, updater, customizer) ***REMOVED***
  return baseSet(object, path, updater(baseGet(object, path)), customizer);
}

module.exports = baseUpdate;
