/**
 * A specialized version of `_.forEachRight` for arrays without support for
 * iteratee shorthands.
 *
 * @private
 * @param ***REMOVED***Array} [array] The array to iterate over.
 * @param ***REMOVED***Function} iteratee The function invoked per iteration.
 * @returns ***REMOVED***Array} Returns `array`.
 */
function arrayEachRight(array, iteratee) ***REMOVED***
  var length = array == null ? 0 : array.length;

  while (length--) ***REMOVED***
    if (iteratee(array[length], length, array) === false) ***REMOVED***
      break;
  ***REMOVED***
***REMOVED***
  return array;
}

module.exports = arrayEachRight;
