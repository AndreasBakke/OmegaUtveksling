var baseIsMatch = require('./_baseIsMatch'),
    getMatchData = require('./_getMatchData'),
    matchesStrictComparable = require('./_matchesStrictComparable');

/**
 * The base implementation of `_.matches` which doesn't clone `source`.
 *
 * @private
 * @param ***REMOVED***Object} source The object of property values to match.
 * @returns ***REMOVED***Function} Returns the new spec function.
 */
function baseMatches(source) ***REMOVED***
  var matchData = getMatchData(source);
  if (matchData.length == 1 && matchData[0][2]) ***REMOVED***
    return matchesStrictComparable(matchData[0][0], matchData[0][1]);
***REMOVED***
  return function(object) ***REMOVED***
    return object === source || baseIsMatch(object, source, matchData);
***REMOVED***;
}

module.exports = baseMatches;
