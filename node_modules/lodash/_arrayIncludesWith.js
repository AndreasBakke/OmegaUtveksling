/**
 * This function is like `arrayIncludes` except that it accepts a comparator.
 *
 * @private
 * @param ***REMOVED***Array} [array] The array to inspect.
 * @param ***REMOVED****} target The value to search for.
 * @param ***REMOVED***Function} comparator The comparator invoked per element.
 * @returns ***REMOVED***boolean} Returns `true` if `target` is found, else `false`.
 */
function arrayIncludesWith(array, value, comparator) ***REMOVED***
  var index = -1,
      length = array == null ? 0 : array.length;

  while (++index < length) ***REMOVED***
    if (comparator(value, array[index])) ***REMOVED***
      return true;
  ***REMOVED***
***REMOVED***
  return false;
}

module.exports = arrayIncludesWith;
