var arrayMap = require('./_arrayMap');

/**
 * The base implementation of `_.toPairs` and `_.toPairsIn` which creates an array
 * of key-value pairs for `object` corresponding to the property names of `props`.
 *
 * @private
 * @param ***REMOVED***Object} object The object to query.
 * @param ***REMOVED***Array} props The property names to get values for.
 * @returns ***REMOVED***Object} Returns the key-value pairs.
 */
function baseToPairs(object, props) ***REMOVED***
  return arrayMap(props, function(key) ***REMOVED***
    return [key, object[key]];
***REMOVED***);
}

module.exports = baseToPairs;
