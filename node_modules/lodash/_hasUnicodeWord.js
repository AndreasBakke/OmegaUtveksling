/** Used to detect strings that need a more robust regexp to match words. */
var reHasUnicodeWord = /[a-z][A-Z]|[A-Z]***REMOVED***2}[a-z]|[0-9][a-zA-Z]|[a-zA-Z][0-9]|[^a-zA-Z0-9 ]/;

/**
 * Checks if `string` contains a word composed of Unicode symbols.
 *
 * @private
 * @param ***REMOVED***string} string The string to inspect.
 * @returns ***REMOVED***boolean} Returns `true` if a word is found, else `false`.
 */
function hasUnicodeWord(string) ***REMOVED***
  return reHasUnicodeWord.test(string);
}

module.exports = hasUnicodeWord;
