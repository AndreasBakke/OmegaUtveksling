var copyObject = require('./_copyObject'),
    keysIn = require('./keysIn');

/**
 * Converts `value` to a plain object flattening inherited enumerable string
 * keyed properties of `value` to own properties of the plain object.
 *
 * @static
 * @memberOf _
 * @since 3.0.0
 * @category Lang
 * @param ***REMOVED****} value The value to convert.
 * @returns ***REMOVED***Object} Returns the converted plain object.
 * @example
 *
 * function Foo() ***REMOVED***
 *   this.b = 2;
 * }
 *
 * Foo.prototype.c = 3;
 *
 * _.assign(***REMOVED*** 'a': 1 }, new Foo);
 * // => ***REMOVED*** 'a': 1, 'b': 2 }
 *
 * _.assign(***REMOVED*** 'a': 1 }, _.toPlainObject(new Foo));
 * // => ***REMOVED*** 'a': 1, 'b': 2, 'c': 3 }
 */
function toPlainObject(value) ***REMOVED***
  return copyObject(value, keysIn(value));
}

module.exports = toPlainObject;
