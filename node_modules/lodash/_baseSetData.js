var identity = require('./identity'),
    metaMap = require('./_metaMap');

/**
 * The base implementation of `setData` without support for hot loop shorting.
 *
 * @private
 * @param ***REMOVED***Function} func The function to associate metadata with.
 * @param ***REMOVED****} data The metadata.
 * @returns ***REMOVED***Function} Returns `func`.
 */
var baseSetData = !metaMap ? identity : function(func, data) ***REMOVED***
  metaMap.set(func, data);
  return func;
};

module.exports = baseSetData;
