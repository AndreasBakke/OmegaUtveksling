var baseSet = require('./_baseSet');

/**
 * This method is like `_.set` except that it accepts `customizer` which is
 * invoked to produce the objects of `path`.  If `customizer` returns `undefined`
 * path creation is handled by the method instead. The `customizer` is invoked
 * with three arguments: (nsValue, key, nsObject).
 *
 * **Note:** This method mutates `object`.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Object
 * @param ***REMOVED***Object} object The object to modify.
 * @param ***REMOVED***Array|string} path The path of the property to set.
 * @param ***REMOVED****} value The value to set.
 * @param ***REMOVED***Function} [customizer] The function to customize assigned values.
 * @returns ***REMOVED***Object} Returns `object`.
 * @example
 *
 * var object = ***REMOVED***};
 *
 * _.setWith(object, '[0][1]', 'a', Object);
 * // => ***REMOVED*** '0': ***REMOVED*** '1': 'a' } }
 */
function setWith(object, path, value, customizer) ***REMOVED***
  customizer = typeof customizer == 'function' ? customizer : undefined;
  return object == null ? object : baseSet(object, path, value, customizer);
}

module.exports = setWith;
