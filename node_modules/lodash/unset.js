var baseUnset = require('./_baseUnset');

/**
 * Removes the property at `path` of `object`.
 *
 * **Note:** This method mutates `object`.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Object
 * @param ***REMOVED***Object} object The object to modify.
 * @param ***REMOVED***Array|string} path The path of the property to unset.
 * @returns ***REMOVED***boolean} Returns `true` if the property is deleted, else `false`.
 * @example
 *
 * var object = ***REMOVED*** 'a': [***REMOVED*** 'b': ***REMOVED*** 'c': 7 } }] };
 * _.unset(object, 'a[0].b.c');
 * // => true
 *
 * console.log(object);
 * // => ***REMOVED*** 'a': [***REMOVED*** 'b': ***REMOVED***} }] };
 *
 * _.unset(object, ['a', '0', 'b', 'c']);
 * // => true
 *
 * console.log(object);
 * // => ***REMOVED*** 'a': [***REMOVED*** 'b': ***REMOVED***} }] };
 */
function unset(object, path) ***REMOVED***
  return object == null ? true : baseUnset(object, path);
}

module.exports = unset;
