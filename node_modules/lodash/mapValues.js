var baseAssignValue = require('./_baseAssignValue'),
    baseForOwn = require('./_baseForOwn'),
    baseIteratee = require('./_baseIteratee');

/**
 * Creates an object with the same keys as `object` and values generated
 * by running each own enumerable string keyed property of `object` thru
 * `iteratee`. The iteratee is invoked with three arguments:
 * (value, key, object).
 *
 * @static
 * @memberOf _
 * @since 2.4.0
 * @category Object
 * @param ***REMOVED***Object} object The object to iterate over.
 * @param ***REMOVED***Function} [iteratee=_.identity] The function invoked per iteration.
 * @returns ***REMOVED***Object} Returns the new mapped object.
 * @see _.mapKeys
 * @example
 *
 * var users = ***REMOVED***
 *   'fred':    ***REMOVED*** 'user': 'fred',    'age': 40 },
 *   'pebbles': ***REMOVED*** 'user': 'pebbles', 'age': 1 }
 * };
 *
 * _.mapValues(users, function(o) ***REMOVED*** return o.age; });
 * // => ***REMOVED*** 'fred': 40, 'pebbles': 1 } (iteration order is not guaranteed)
 *
 * // The `_.property` iteratee shorthand.
 * _.mapValues(users, 'age');
 * // => ***REMOVED*** 'fred': 40, 'pebbles': 1 } (iteration order is not guaranteed)
 */
function mapValues(object, iteratee) ***REMOVED***
  var result = ***REMOVED***};
  iteratee = baseIteratee(iteratee, 3);

  baseForOwn(object, function(value, key, object) ***REMOVED***
    baseAssignValue(result, key, iteratee(value, key, object));
***REMOVED***);
  return result;
}

module.exports = mapValues;
