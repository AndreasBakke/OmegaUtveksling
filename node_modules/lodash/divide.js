var createMathOperation = require('./_createMathOperation');

/**
 * Divide two numbers.
 *
 * @static
 * @memberOf _
 * @since 4.7.0
 * @category Math
 * @param ***REMOVED***number} dividend The first number in a division.
 * @param ***REMOVED***number} divisor The second number in a division.
 * @returns ***REMOVED***number} Returns the quotient.
 * @example
 *
 * _.divide(6, 4);
 * // => 1.5
 */
var divide = createMathOperation(function(dividend, divisor) ***REMOVED***
  return dividend / divisor;
}, 1);

module.exports = divide;
