var createWrap = require('./_createWrap');

/** Used to compose bitmasks for function metadata. */
var WRAP_FLIP_FLAG = 512;

/**
 * Creates a function that invokes `func` with arguments reversed.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Function
 * @param ***REMOVED***Function} func The function to flip arguments for.
 * @returns ***REMOVED***Function} Returns the new flipped function.
 * @example
 *
 * var flipped = _.flip(function() ***REMOVED***
 *   return _.toArray(arguments);
 * });
 *
 * flipped('a', 'b', 'c', 'd');
 * // => ['d', 'c', 'b', 'a']
 */
function flip(func) ***REMOVED***
  return createWrap(func, WRAP_FLIP_FLAG);
}

module.exports = flip;
