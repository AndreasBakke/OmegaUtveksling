var baseDelay = require('./_baseDelay'),
    baseRest = require('./_baseRest'),
    toNumber = require('./toNumber');

/**
 * Invokes `func` after `wait` milliseconds. Any additional arguments are
 * provided to `func` when it's invoked.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Function
 * @param ***REMOVED***Function} func The function to delay.
 * @param ***REMOVED***number} wait The number of milliseconds to delay invocation.
 * @param ***REMOVED***...*} [args] The arguments to invoke `func` with.
 * @returns ***REMOVED***number} Returns the timer id.
 * @example
 *
 * _.delay(function(text) ***REMOVED***
 *   console.log(text);
 * }, 1000, 'later');
 * // => Logs 'later' after one second.
 */
var delay = baseRest(function(func, wait, args) ***REMOVED***
  return baseDelay(func, toNumber(wait) || 0, args);
});

module.exports = delay;
