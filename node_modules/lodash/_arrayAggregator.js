/**
 * A specialized version of `baseAggregator` for arrays.
 *
 * @private
 * @param ***REMOVED***Array} [array] The array to iterate over.
 * @param ***REMOVED***Function} setter The function to set `accumulator` values.
 * @param ***REMOVED***Function} iteratee The iteratee to transform keys.
 * @param ***REMOVED***Object} accumulator The initial aggregated object.
 * @returns ***REMOVED***Function} Returns `accumulator`.
 */
function arrayAggregator(array, setter, iteratee, accumulator) ***REMOVED***
  var index = -1,
      length = array == null ? 0 : array.length;

  while (++index < length) ***REMOVED***
    var value = array[index];
    setter(accumulator, value, iteratee(value), array);
***REMOVED***
  return accumulator;
}

module.exports = arrayAggregator;
