var toInteger = require('./toInteger'),
    toLength = require('./toLength');

/**
 * The base implementation of `_.fill` without an iteratee call guard.
 *
 * @private
 * @param ***REMOVED***Array} array The array to fill.
 * @param ***REMOVED****} value The value to fill `array` with.
 * @param ***REMOVED***number} [start=0] The start position.
 * @param ***REMOVED***number} [end=array.length] The end position.
 * @returns ***REMOVED***Array} Returns `array`.
 */
function baseFill(array, value, start, end) ***REMOVED***
  var length = array.length;

  start = toInteger(start);
  if (start < 0) ***REMOVED***
    start = -start > length ? 0 : (length + start);
***REMOVED***
  end = (end === undefined || end > length) ? length : toInteger(end);
  if (end < 0) ***REMOVED***
    end += length;
***REMOVED***
  end = start > end ? 0 : toLength(end);
  while (start < end) ***REMOVED***
    array[start++] = value;
***REMOVED***
  return array;
}

module.exports = baseFill;
