var baseFunctions = require('./_baseFunctions'),
    keys = require('./keys');

/**
 * Creates an array of function property names from own enumerable properties
 * of `object`.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Object
 * @param ***REMOVED***Object} object The object to inspect.
 * @returns ***REMOVED***Array} Returns the function names.
 * @see _.functionsIn
 * @example
 *
 * function Foo() ***REMOVED***
 *   this.a = _.constant('a');
 *   this.b = _.constant('b');
 * }
 *
 * Foo.prototype.c = _.constant('c');
 *
 * _.functions(new Foo);
 * // => ['a', 'b']
 */
function functions(object) ***REMOVED***
  return object == null ? [] : baseFunctions(object, keys(object));
}

module.exports = functions;
