var isSymbol = require('./isSymbol');

/**
 * The base implementation of methods like `_.max` and `_.min` which accepts a
 * `comparator` to determine the extremum value.
 *
 * @private
 * @param ***REMOVED***Array} array The array to iterate over.
 * @param ***REMOVED***Function} iteratee The iteratee invoked per iteration.
 * @param ***REMOVED***Function} comparator The comparator used to compare values.
 * @returns ***REMOVED****} Returns the extremum value.
 */
function baseExtremum(array, iteratee, comparator) ***REMOVED***
  var index = -1,
      length = array.length;

  while (++index < length) ***REMOVED***
    var value = array[index],
        current = iteratee(value);

    if (current != null && (computed === undefined
          ? (current === current && !isSymbol(current))
          : comparator(current, computed)
        )) ***REMOVED***
      var computed = current,
          result = value;
  ***REMOVED***
***REMOVED***
  return result;
}

module.exports = baseExtremum;
