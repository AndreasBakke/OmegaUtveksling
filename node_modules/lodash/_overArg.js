/**
 * Creates a unary function that invokes `func` with its argument transformed.
 *
 * @private
 * @param ***REMOVED***Function} func The function to wrap.
 * @param ***REMOVED***Function} transform The argument transform.
 * @returns ***REMOVED***Function} Returns the new function.
 */
function overArg(func, transform) ***REMOVED***
  return function(arg) ***REMOVED***
    return func(transform(arg));
***REMOVED***;
}

module.exports = overArg;
