var baseIsNative = require('./_baseIsNative'),
    getValue = require('./_getValue');

/**
 * Gets the native function at `key` of `object`.
 *
 * @private
 * @param ***REMOVED***Object} object The object to query.
 * @param ***REMOVED***string} key The key of the method to get.
 * @returns ***REMOVED****} Returns the function if it's native, else `undefined`.
 */
function getNative(object, key) ***REMOVED***
  var value = getValue(object, key);
  return baseIsNative(value) ? value : undefined;
}

module.exports = getNative;
