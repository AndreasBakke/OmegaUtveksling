var shuffleSelf = require('./_shuffleSelf'),
    values = require('./values');

/**
 * The base implementation of `_.shuffle`.
 *
 * @private
 * @param ***REMOVED***Array|Object} collection The collection to shuffle.
 * @returns ***REMOVED***Array} Returns the new shuffled array.
 */
function baseShuffle(collection) ***REMOVED***
  return shuffleSelf(values(collection));
}

module.exports = baseShuffle;
