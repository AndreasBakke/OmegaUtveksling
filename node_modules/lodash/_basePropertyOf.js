/**
 * The base implementation of `_.propertyOf` without support for deep paths.
 *
 * @private
 * @param ***REMOVED***Object} object The object to query.
 * @returns ***REMOVED***Function} Returns the new accessor function.
 */
function basePropertyOf(object) ***REMOVED***
  return function(key) ***REMOVED***
    return object == null ? undefined : object[key];
***REMOVED***;
}

module.exports = basePropertyOf;
