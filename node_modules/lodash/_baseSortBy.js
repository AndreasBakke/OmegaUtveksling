/**
 * The base implementation of `_.sortBy` which uses `comparer` to define the
 * sort order of `array` and replaces criteria objects with their corresponding
 * values.
 *
 * @private
 * @param ***REMOVED***Array} array The array to sort.
 * @param ***REMOVED***Function} comparer The function to define sort order.
 * @returns ***REMOVED***Array} Returns `array`.
 */
function baseSortBy(array, comparer) ***REMOVED***
  var length = array.length;

  array.sort(comparer);
  while (length--) ***REMOVED***
    array[length] = array[length].value;
***REMOVED***
  return array;
}

module.exports = baseSortBy;
