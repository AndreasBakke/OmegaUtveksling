var baseInverter = require('./_baseInverter');

/**
 * Creates a function like `_.invertBy`.
 *
 * @private
 * @param ***REMOVED***Function} setter The function to set accumulator values.
 * @param ***REMOVED***Function} toIteratee The function to resolve iteratees.
 * @returns ***REMOVED***Function} Returns the new inverter function.
 */
function createInverter(setter, toIteratee) ***REMOVED***
  return function(object, iteratee) ***REMOVED***
    return baseInverter(object, setter, toIteratee(iteratee), ***REMOVED***});
***REMOVED***;
}

module.exports = createInverter;
