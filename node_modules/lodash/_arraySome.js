/**
 * A specialized version of `_.some` for arrays without support for iteratee
 * shorthands.
 *
 * @private
 * @param ***REMOVED***Array} [array] The array to iterate over.
 * @param ***REMOVED***Function} predicate The function invoked per iteration.
 * @returns ***REMOVED***boolean} Returns `true` if any element passes the predicate check,
 *  else `false`.
 */
function arraySome(array, predicate) ***REMOVED***
  var index = -1,
      length = array == null ? 0 : array.length;

  while (++index < length) ***REMOVED***
    if (predicate(array[index], index, array)) ***REMOVED***
      return true;
  ***REMOVED***
***REMOVED***
  return false;
}

module.exports = arraySome;
