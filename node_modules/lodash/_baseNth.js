var isIndex = require('./_isIndex');

/**
 * The base implementation of `_.nth` which doesn't coerce arguments.
 *
 * @private
 * @param ***REMOVED***Array} array The array to query.
 * @param ***REMOVED***number} n The index of the element to return.
 * @returns ***REMOVED****} Returns the nth element of `array`.
 */
function baseNth(array, n) ***REMOVED***
  var length = array.length;
  if (!length) ***REMOVED***
    return;
***REMOVED***
  n += n < 0 ? length : 0;
  return isIndex(n, length) ? array[n] : undefined;
}

module.exports = baseNth;
