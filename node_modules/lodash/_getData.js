var metaMap = require('./_metaMap'),
    noop = require('./noop');

/**
 * Gets metadata for `func`.
 *
 * @private
 * @param ***REMOVED***Function} func The function to query.
 * @returns ***REMOVED****} Returns the metadata for `func`.
 */
var getData = !metaMap ? noop : function(func) ***REMOVED***
  return metaMap.get(func);
};

module.exports = getData;
