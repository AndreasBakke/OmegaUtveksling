var baseSet = require('./_baseSet'),
    baseZipObject = require('./_baseZipObject');

/**
 * This method is like `_.zipObject` except that it supports property paths.
 *
 * @static
 * @memberOf _
 * @since 4.1.0
 * @category Array
 * @param ***REMOVED***Array} [props=[]] The property identifiers.
 * @param ***REMOVED***Array} [values=[]] The property values.
 * @returns ***REMOVED***Object} Returns the new object.
 * @example
 *
 * _.zipObjectDeep(['a.b[0].c', 'a.b[1].d'], [1, 2]);
 * // => ***REMOVED*** 'a': ***REMOVED*** 'b': [***REMOVED*** 'c': 1 }, ***REMOVED*** 'd': 2 }] } }
 */
function zipObjectDeep(props, values) ***REMOVED***
  return baseZipObject(props || [], values || [], baseSet);
}

module.exports = zipObjectDeep;
