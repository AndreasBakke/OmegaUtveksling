/**
 * The base implementation of `_.clamp` which doesn't coerce arguments.
 *
 * @private
 * @param ***REMOVED***number} number The number to clamp.
 * @param ***REMOVED***number} [lower] The lower bound.
 * @param ***REMOVED***number} upper The upper bound.
 * @returns ***REMOVED***number} Returns the clamped number.
 */
function baseClamp(number, lower, upper) ***REMOVED***
  if (number === number) ***REMOVED***
    if (upper !== undefined) ***REMOVED***
      number = number <= upper ? number : upper;
  ***REMOVED***
    if (lower !== undefined) ***REMOVED***
      number = number >= lower ? number : lower;
  ***REMOVED***
***REMOVED***
  return number;
}

module.exports = baseClamp;
