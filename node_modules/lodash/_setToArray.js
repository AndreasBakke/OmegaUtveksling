/**
 * Converts `set` to an array of its values.
 *
 * @private
 * @param ***REMOVED***Object} set The set to convert.
 * @returns ***REMOVED***Array} Returns the values.
 */
function setToArray(set) ***REMOVED***
  var index = -1,
      result = Array(set.size);

  set.forEach(function(value) ***REMOVED***
    result[++index] = value;
***REMOVED***);
  return result;
}

module.exports = setToArray;
