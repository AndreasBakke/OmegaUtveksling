var baseGet = require('./_baseGet'),
    baseSet = require('./_baseSet'),
    castPath = require('./_castPath');

/**
 * The base implementation of  `_.pickBy` without support for iteratee shorthands.
 *
 * @private
 * @param ***REMOVED***Object} object The source object.
 * @param ***REMOVED***string[]} paths The property paths to pick.
 * @param ***REMOVED***Function} predicate The function invoked per property.
 * @returns ***REMOVED***Object} Returns the new object.
 */
function basePickBy(object, paths, predicate) ***REMOVED***
  var index = -1,
      length = paths.length,
      result = ***REMOVED***};

  while (++index < length) ***REMOVED***
    var path = paths[index],
        value = baseGet(object, path);

    if (predicate(value, path)) ***REMOVED***
      baseSet(result, castPath(path, object), value);
  ***REMOVED***
***REMOVED***
  return result;
}

module.exports = basePickBy;
