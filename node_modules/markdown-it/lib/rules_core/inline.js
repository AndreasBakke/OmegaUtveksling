'use strict';

module.exports = function inline(state) ***REMOVED***
  var tokens = state.tokens, tok, i, l;

  // Parse inlines
  for (i = 0, l = tokens.length; i < l; i++) ***REMOVED***
    tok = tokens[i];
    if (tok.type === 'inline') ***REMOVED***
      state.md.inline.parse(tok.content, state.md, state.env, tok.children);
  ***REMOVED***
***REMOVED***
};
