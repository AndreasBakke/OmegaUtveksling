// Parse link label
//
// this function assumes that first character ("[") already matches;
// returns the end of the label
//
'use strict';

module.exports = function parseLinkLabel(state, start, disableNested) ***REMOVED***
  var level, found, marker, prevPos,
      labelEnd = -1,
      max = state.posMax,
      oldPos = state.pos;

  state.pos = start + 1;
  level = 1;

  while (state.pos < max) ***REMOVED***
    marker = state.src.charCodeAt(state.pos);
    if (marker === 0x5D /* ] */) ***REMOVED***
      level--;
      if (level === 0) ***REMOVED***
        found = true;
        break;
    ***REMOVED***
  ***REMOVED***

    prevPos = state.pos;
    state.md.inline.skipToken(state);
    if (marker === 0x5B /* [ */) ***REMOVED***
      if (prevPos === state.pos - 1) ***REMOVED***
        // increase level if we find text `[`, which is not a part of any token
        level++;
    ***REMOVED*** else if (disableNested) ***REMOVED***
        state.pos = oldPos;
        return -1;
    ***REMOVED***
  ***REMOVED***
***REMOVED***

  if (found) ***REMOVED***
    labelEnd = state.pos;
***REMOVED***

  // restore old state
  state.pos = oldPos;

  return labelEnd;
};
