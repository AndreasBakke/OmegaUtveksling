// HTML block

'use strict';


var block_names = require('../common/html_blocks');
var HTML_OPEN_CLOSE_TAG_RE = require('../common/html_re').HTML_OPEN_CLOSE_TAG_RE;

// An array of opening and corresponding closing sequences for html tags,
// last argument defines whether it can terminate a paragraph or not
//
var HTML_SEQUENCES = [
  [ /^<(script|pre|style|textarea)(?=(\s|>|$))/i, /<\/(script|pre|style|textarea)>/i, true ],
  [ /^<!--/,        /-->/,   true ],
  [ /^<\?/,         /\?>/,   true ],
  [ /^<![A-Z]/,     />/,     true ],
  [ /^<!\[CDATA\[/, /\]\]>/, true ],
  [ new RegExp('^</?(' + block_names.join('|') + ')(?=(\\s|/?>|$))', 'i'), /^$/, true ],
  [ new RegExp(HTML_OPEN_CLOSE_TAG_RE.source + '\\s*$'),  /^$/, false ]
];


module.exports = function html_block(state, startLine, endLine, silent) ***REMOVED***
  var i, nextLine, token, lineText,
      pos = state.bMarks[startLine] + state.tShift[startLine],
      max = state.eMarks[startLine];

  // if it's indented more than 3 spaces, it should be a code block
  if (state.sCount[startLine] - state.blkIndent >= 4) ***REMOVED*** return false; }

  if (!state.md.options.html) ***REMOVED*** return false; }

  if (state.src.charCodeAt(pos) !== 0x3C/* < */) ***REMOVED*** return false; }

  lineText = state.src.slice(pos, max);

  for (i = 0; i < HTML_SEQUENCES.length; i++) ***REMOVED***
    if (HTML_SEQUENCES[i][0].test(lineText)) ***REMOVED*** break; }
***REMOVED***

  if (i === HTML_SEQUENCES.length) ***REMOVED*** return false; }

  if (silent) ***REMOVED***
    // true if this sequence can be a terminator, false otherwise
    return HTML_SEQUENCES[i][2];
***REMOVED***

  nextLine = startLine + 1;

  // If we are here - we detected HTML block.
  // Let's roll down till block end.
  if (!HTML_SEQUENCES[i][1].test(lineText)) ***REMOVED***
    for (; nextLine < endLine; nextLine++) ***REMOVED***
      if (state.sCount[nextLine] < state.blkIndent) ***REMOVED*** break; }

      pos = state.bMarks[nextLine] + state.tShift[nextLine];
      max = state.eMarks[nextLine];
      lineText = state.src.slice(pos, max);

      if (HTML_SEQUENCES[i][1].test(lineText)) ***REMOVED***
        if (lineText.length !== 0) ***REMOVED*** nextLine++; }
        break;
    ***REMOVED***
  ***REMOVED***
***REMOVED***

  state.line = nextLine;

  token         = state.push('html_block', '', 0);
  token.map     = [ startLine, nextLine ];
  token.content = state.getLines(startLine, nextLine, state.blkIndent, true);

  return true;
};
