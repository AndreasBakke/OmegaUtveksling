// fences (``` lang, ~~~ lang)

'use strict';


module.exports = function fence(state, startLine, endLine, silent) ***REMOVED***
  var marker, len, params, nextLine, mem, token, markup,
      haveEndMarker = false,
      pos = state.bMarks[startLine] + state.tShift[startLine],
      max = state.eMarks[startLine];

  // if it's indented more than 3 spaces, it should be a code block
  if (state.sCount[startLine] - state.blkIndent >= 4) ***REMOVED*** return false; }

  if (pos + 3 > max) ***REMOVED*** return false; }

  marker = state.src.charCodeAt(pos);

  if (marker !== 0x7E/* ~ */ && marker !== 0x60 /* ` */) ***REMOVED***
    return false;
***REMOVED***

  // scan marker length
  mem = pos;
  pos = state.skipChars(pos, marker);

  len = pos - mem;

  if (len < 3) ***REMOVED*** return false; }

  markup = state.src.slice(mem, pos);
  params = state.src.slice(pos, max);

  if (marker === 0x60 /* ` */) ***REMOVED***
    if (params.indexOf(String.fromCharCode(marker)) >= 0) ***REMOVED***
      return false;
  ***REMOVED***
***REMOVED***

  // Since start is found, we can report success here in validation mode
  if (silent) ***REMOVED*** return true; }

  // search end of block
  nextLine = startLine;

  for (;;) ***REMOVED***
    nextLine++;
    if (nextLine >= endLine) ***REMOVED***
      // unclosed block should be autoclosed by end of document.
      // also block seems to be autoclosed by end of parent
      break;
  ***REMOVED***

    pos = mem = state.bMarks[nextLine] + state.tShift[nextLine];
    max = state.eMarks[nextLine];

    if (pos < max && state.sCount[nextLine] < state.blkIndent) ***REMOVED***
      // non-empty line with negative indent should stop the list:
      // - ```
      //  test
      break;
  ***REMOVED***

    if (state.src.charCodeAt(pos) !== marker) ***REMOVED*** continue; }

    if (state.sCount[nextLine] - state.blkIndent >= 4) ***REMOVED***
      // closing fence should be indented less than 4 spaces
      continue;
  ***REMOVED***

    pos = state.skipChars(pos, marker);

    // closing code fence must be at least as long as the opening one
    if (pos - mem < len) ***REMOVED*** continue; }

    // make sure tail has spaces only
    pos = state.skipSpaces(pos);

    if (pos < max) ***REMOVED*** continue; }

    haveEndMarker = true;
    // found!
    break;
***REMOVED***

  // If a fence has heading spaces, they should be removed from its inner block
  len = state.sCount[startLine];

  state.line = nextLine + (haveEndMarker ? 1 : 0);

  token         = state.push('fence', 'code', 0);
  token.info    = params;
  token.content = state.getLines(startLine + 1, nextLine, len, true);
  token.markup  = markup;
  token.map     = [ startLine, state.line ];

  return true;
};
