// Code block (4 spaces padded)

'use strict';


module.exports = function code(state, startLine, endLine/*, silent*/) ***REMOVED***
  var nextLine, last, token;

  if (state.sCount[startLine] - state.blkIndent < 4) ***REMOVED*** return false; }

  last = nextLine = startLine + 1;

  while (nextLine < endLine) ***REMOVED***
    if (state.isEmpty(nextLine)) ***REMOVED***
      nextLine++;
      continue;
  ***REMOVED***

    if (state.sCount[nextLine] - state.blkIndent >= 4) ***REMOVED***
      nextLine++;
      last = nextLine;
      continue;
  ***REMOVED***
    break;
***REMOVED***

  state.line = last;

  token         = state.push('code_block', 'code', 0);
  token.content = state.getLines(startLine, last, 4 + state.blkIndent, false) + '\n';
  token.map     = [ startLine, state.line ];

  return true;
};
