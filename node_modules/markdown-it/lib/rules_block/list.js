// Lists

'use strict';

var isSpace = require('../common/utils').isSpace;


// Search `[-+*][\n ]`, returns next pos after marker on success
// or -1 on fail.
function skipBulletListMarker(state, startLine) ***REMOVED***
  var marker, pos, max, ch;

  pos = state.bMarks[startLine] + state.tShift[startLine];
  max = state.eMarks[startLine];

  marker = state.src.charCodeAt(pos++);
  // Check bullet
  if (marker !== 0x2A/* * */ &&
      marker !== 0x2D/* - */ &&
      marker !== 0x2B/* + */) ***REMOVED***
    return -1;
***REMOVED***

  if (pos < max) ***REMOVED***
    ch = state.src.charCodeAt(pos);

    if (!isSpace(ch)) ***REMOVED***
      // " -test " - is not a list item
      return -1;
  ***REMOVED***
***REMOVED***

  return pos;
}

// Search `\d+[.)][\n ]`, returns next pos after marker on success
// or -1 on fail.
function skipOrderedListMarker(state, startLine) ***REMOVED***
  var ch,
      start = state.bMarks[startLine] + state.tShift[startLine],
      pos = start,
      max = state.eMarks[startLine];

  // List marker should have at least 2 chars (digit + dot)
  if (pos + 1 >= max) ***REMOVED*** return -1; }

  ch = state.src.charCodeAt(pos++);

  if (ch < 0x30/* 0 */ || ch > 0x39/* 9 */) ***REMOVED*** return -1; }

  for (;;) ***REMOVED***
    // EOL -> fail
    if (pos >= max) ***REMOVED*** return -1; }

    ch = state.src.charCodeAt(pos++);

    if (ch >= 0x30/* 0 */ && ch <= 0x39/* 9 */) ***REMOVED***

      // List marker should have no more than 9 digits
      // (prevents integer overflow in browsers)
      if (pos - start >= 10) ***REMOVED*** return -1; }

      continue;
  ***REMOVED***

    // found valid marker
    if (ch === 0x29/* ) */ || ch === 0x2e/* . */) ***REMOVED***
      break;
  ***REMOVED***

    return -1;
***REMOVED***


  if (pos < max) ***REMOVED***
    ch = state.src.charCodeAt(pos);

    if (!isSpace(ch)) ***REMOVED***
      // " 1.test " - is not a list item
      return -1;
  ***REMOVED***
***REMOVED***
  return pos;
}

function markTightParagraphs(state, idx) ***REMOVED***
  var i, l,
      level = state.level + 2;

  for (i = idx + 2, l = state.tokens.length - 2; i < l; i++) ***REMOVED***
    if (state.tokens[i].level === level && state.tokens[i].type === 'paragraph_open') ***REMOVED***
      state.tokens[i + 2].hidden = true;
      state.tokens[i].hidden = true;
      i += 2;
  ***REMOVED***
***REMOVED***
}


module.exports = function list(state, startLine, endLine, silent) ***REMOVED***
  var ch,
      contentStart,
      i,
      indent,
      indentAfterMarker,
      initial,
      isOrdered,
      itemLines,
      l,
      listLines,
      listTokIdx,
      markerCharCode,
      markerValue,
      max,
      nextLine,
      offset,
      oldListIndent,
      oldParentType,
      oldSCount,
      oldTShift,
      oldTight,
      pos,
      posAfterMarker,
      prevEmptyEnd,
      start,
      terminate,
      terminatorRules,
      token,
      isTerminatingParagraph = false,
      tight = true;

  // if it's indented more than 3 spaces, it should be a code block
  if (state.sCount[startLine] - state.blkIndent >= 4) ***REMOVED*** return false; }

  // Special case:
  //  - item 1
  //   - item 2
  //    - item 3
  //     - item 4
  //      - this one is a paragraph continuation
  if (state.listIndent >= 0 &&
      state.sCount[startLine] - state.listIndent >= 4 &&
      state.sCount[startLine] < state.blkIndent) ***REMOVED***
    return false;
***REMOVED***

  // limit conditions when list can interrupt
  // a paragraph (validation mode only)
  if (silent && state.parentType === 'paragraph') ***REMOVED***
    // Next list item should still terminate previous list item;
    //
    // This code can fail if plugins use blkIndent as well as lists,
    // but I hope the spec gets fixed long before that happens.
    //
    if (state.sCount[startLine] >= state.blkIndent) ***REMOVED***
      isTerminatingParagraph = true;
  ***REMOVED***
***REMOVED***

  // Detect list type and position after marker
  if ((posAfterMarker = skipOrderedListMarker(state, startLine)) >= 0) ***REMOVED***
    isOrdered = true;
    start = state.bMarks[startLine] + state.tShift[startLine];
    markerValue = Number(state.src.slice(start, posAfterMarker - 1));

    // If we're starting a new ordered list right after
    // a paragraph, it should start with 1.
    if (isTerminatingParagraph && markerValue !== 1) return false;

***REMOVED*** else if ((posAfterMarker = skipBulletListMarker(state, startLine)) >= 0) ***REMOVED***
    isOrdered = false;

***REMOVED*** else ***REMOVED***
    return false;
***REMOVED***

  // If we're starting a new unordered list right after
  // a paragraph, first line should not be empty.
  if (isTerminatingParagraph) ***REMOVED***
    if (state.skipSpaces(posAfterMarker) >= state.eMarks[startLine]) return false;
***REMOVED***

  // We should terminate list on style change. Remember first one to compare.
  markerCharCode = state.src.charCodeAt(posAfterMarker - 1);

  // For validation mode we can terminate immediately
  if (silent) ***REMOVED*** return true; }

  // Start list
  listTokIdx = state.tokens.length;

  if (isOrdered) ***REMOVED***
    token       = state.push('ordered_list_open', 'ol', 1);
    if (markerValue !== 1) ***REMOVED***
      token.attrs = [ [ 'start', markerValue ] ];
  ***REMOVED***

***REMOVED*** else ***REMOVED***
    token       = state.push('bullet_list_open', 'ul', 1);
***REMOVED***

  token.map    = listLines = [ startLine, 0 ];
  token.markup = String.fromCharCode(markerCharCode);

  //
  // Iterate list items
  //

  nextLine = startLine;
  prevEmptyEnd = false;
  terminatorRules = state.md.block.ruler.getRules('list');

  oldParentType = state.parentType;
  state.parentType = 'list';

  while (nextLine < endLine) ***REMOVED***
    pos = posAfterMarker;
    max = state.eMarks[nextLine];

    initial = offset = state.sCount[nextLine] + posAfterMarker - (state.bMarks[startLine] + state.tShift[startLine]);

    while (pos < max) ***REMOVED***
      ch = state.src.charCodeAt(pos);

      if (ch === 0x09) ***REMOVED***
        offset += 4 - (offset + state.bsCount[nextLine]) % 4;
    ***REMOVED*** else if (ch === 0x20) ***REMOVED***
        offset++;
    ***REMOVED*** else ***REMOVED***
        break;
    ***REMOVED***

      pos++;
  ***REMOVED***

    contentStart = pos;

    if (contentStart >= max) ***REMOVED***
      // trimming space in "-    \n  3" case, indent is 1 here
      indentAfterMarker = 1;
  ***REMOVED*** else ***REMOVED***
      indentAfterMarker = offset - initial;
  ***REMOVED***

    // If we have more than 4 spaces, the indent is 1
    // (the rest is just indented code block)
    if (indentAfterMarker > 4) ***REMOVED*** indentAfterMarker = 1; }

    // "  -  test"
    //  ^^^^^ - calculating total length of this thing
    indent = initial + indentAfterMarker;

    // Run subparser & write tokens
    token        = state.push('list_item_open', 'li', 1);
    token.markup = String.fromCharCode(markerCharCode);
    token.map    = itemLines = [ startLine, 0 ];
    if (isOrdered) ***REMOVED***
      token.info = state.src.slice(start, posAfterMarker - 1);
  ***REMOVED***

    // change current state, then restore it after parser subcall
    oldTight = state.tight;
    oldTShift = state.tShift[startLine];
    oldSCount = state.sCount[startLine];

    //  - example list
    // ^ listIndent position will be here
    //   ^ blkIndent position will be here
    //
    oldListIndent = state.listIndent;
    state.listIndent = state.blkIndent;
    state.blkIndent = indent;

    state.tight = true;
    state.tShift[startLine] = contentStart - state.bMarks[startLine];
    state.sCount[startLine] = offset;

    if (contentStart >= max && state.isEmpty(startLine + 1)) ***REMOVED***
      // workaround for this case
      // (list item is empty, list terminates before "foo"):
      // ~~~~~~~~
      //   -
      //
      //     foo
      // ~~~~~~~~
      state.line = Math.min(state.line + 2, endLine);
  ***REMOVED*** else ***REMOVED***
      state.md.block.tokenize(state, startLine, endLine, true);
  ***REMOVED***

    // If any of list item is tight, mark list as tight
    if (!state.tight || prevEmptyEnd) ***REMOVED***
      tight = false;
  ***REMOVED***
    // Item become loose if finish with empty line,
    // but we should filter last element, because it means list finish
    prevEmptyEnd = (state.line - startLine) > 1 && state.isEmpty(state.line - 1);

    state.blkIndent = state.listIndent;
    state.listIndent = oldListIndent;
    state.tShift[startLine] = oldTShift;
    state.sCount[startLine] = oldSCount;
    state.tight = oldTight;

    token        = state.push('list_item_close', 'li', -1);
    token.markup = String.fromCharCode(markerCharCode);

    nextLine = startLine = state.line;
    itemLines[1] = nextLine;
    contentStart = state.bMarks[startLine];

    if (nextLine >= endLine) ***REMOVED*** break; }

    //
    // Try to check if list is terminated or continued.
    //
    if (state.sCount[nextLine] < state.blkIndent) ***REMOVED*** break; }

    // if it's indented more than 3 spaces, it should be a code block
    if (state.sCount[startLine] - state.blkIndent >= 4) ***REMOVED*** break; }

    // fail if terminating block found
    terminate = false;
    for (i = 0, l = terminatorRules.length; i < l; i++) ***REMOVED***
      if (terminatorRules[i](state, nextLine, endLine, true)) ***REMOVED***
        terminate = true;
        break;
    ***REMOVED***
  ***REMOVED***
    if (terminate) ***REMOVED*** break; }

    // fail if list has another type
    if (isOrdered) ***REMOVED***
      posAfterMarker = skipOrderedListMarker(state, nextLine);
      if (posAfterMarker < 0) ***REMOVED*** break; }
      start = state.bMarks[nextLine] + state.tShift[nextLine];
  ***REMOVED*** else ***REMOVED***
      posAfterMarker = skipBulletListMarker(state, nextLine);
      if (posAfterMarker < 0) ***REMOVED*** break; }
  ***REMOVED***

    if (markerCharCode !== state.src.charCodeAt(posAfterMarker - 1)) ***REMOVED*** break; }
***REMOVED***

  // Finalize list
  if (isOrdered) ***REMOVED***
    token = state.push('ordered_list_close', 'ol', -1);
***REMOVED*** else ***REMOVED***
    token = state.push('bullet_list_close', 'ul', -1);
***REMOVED***
  token.markup = String.fromCharCode(markerCharCode);

  listLines[1] = nextLine;
  state.line = nextLine;

  state.parentType = oldParentType;

  // mark paragraphs tight if needed
  if (tight) ***REMOVED***
    markTightParagraphs(state, listTokIdx);
***REMOVED***

  return true;
};
