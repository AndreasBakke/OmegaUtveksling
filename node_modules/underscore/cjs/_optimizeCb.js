// Internal function that returns an efficient (for current engines) version
// of the passed-in callback, to be repeatedly applied in other Underscore
// functions.
function optimizeCb(func, context, argCount) ***REMOVED***
  if (context === void 0) return func;
  switch (argCount == null ? 3 : argCount) ***REMOVED***
    case 1: return function(value) ***REMOVED***
      return func.call(context, value);
  ***REMOVED***;
    // The 2-argument case is omitted because we’re not using it.
    case 3: return function(value, index, collection) ***REMOVED***
      return func.call(context, value, index, collection);
  ***REMOVED***;
    case 4: return function(accumulator, value, index, collection) ***REMOVED***
      return func.call(context, accumulator, value, index, collection);
  ***REMOVED***;
***REMOVED***
  return function() ***REMOVED***
    return func.apply(context, arguments);
***REMOVED***;
}

module.exports = optimizeCb;
