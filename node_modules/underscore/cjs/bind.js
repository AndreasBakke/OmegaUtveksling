var restArguments = require('./restArguments.js');
var isFunction = require('./isFunction.js');
var _executeBound = require('./_executeBound.js');

// Create a function bound to a given object (assigning `this`, and arguments,
// optionally).
var bind = restArguments(function(func, context, args) ***REMOVED***
  if (!isFunction(func)) throw new TypeError('Bind must be called on a function');
  var bound = restArguments(function(callArgs) ***REMOVED***
    return _executeBound(func, bound, context, this, args.concat(callArgs));
***REMOVED***);
  return bound;
});

module.exports = bind;
