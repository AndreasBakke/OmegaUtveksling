var now = require('./now.js');

// Returns a function, that, when invoked, will only be triggered at most once
// during a given window of time. Normally, the throttled function will run
// as much as it can, without ever going more than once per `wait` duration;
// but if you'd like to disable the execution on the leading edge, pass
// `***REMOVED***leading: false}`. To disable execution on the trailing edge, ditto.
function throttle(func, wait, options) ***REMOVED***
  var timeout, context, args, result;
  var previous = 0;
  if (!options) options = ***REMOVED***};

  var later = function() ***REMOVED***
    previous = options.leading === false ? 0 : now();
    timeout = null;
    result = func.apply(context, args);
    if (!timeout) context = args = null;
***REMOVED***;

  var throttled = function() ***REMOVED***
    var _now = now();
    if (!previous && options.leading === false) previous = _now;
    var remaining = wait - (_now - previous);
    context = this;
    args = arguments;
    if (remaining <= 0 || remaining > wait) ***REMOVED***
      if (timeout) ***REMOVED***
        clearTimeout(timeout);
        timeout = null;
    ***REMOVED***
      previous = _now;
      result = func.apply(context, args);
      if (!timeout) context = args = null;
  ***REMOVED*** else if (!timeout && options.trailing !== false) ***REMOVED***
      timeout = setTimeout(later, remaining);
  ***REMOVED***
    return result;
***REMOVED***;

  throttled.cancel = function() ***REMOVED***
    clearTimeout(timeout);
    previous = 0;
    timeout = context = args = null;
***REMOVED***;

  return throttled;
}

module.exports = throttle;
