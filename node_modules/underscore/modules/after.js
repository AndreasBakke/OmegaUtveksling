// Returns a function that will only be executed on and after the Nth call.
export default function after(times, func) ***REMOVED***
  return function() ***REMOVED***
    if (--times < 1) ***REMOVED***
      return func.apply(this, arguments);
  ***REMOVED***
***REMOVED***;
}
