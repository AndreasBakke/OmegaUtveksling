import ***REMOVED*** toString } from './_setup.js';

// Internal function for creating a `toString`-based type tester.
export default function tagTester(name) ***REMOVED***
  var tag = '[object ' + name + ']';
  return function(obj) ***REMOVED***
    return toString.call(obj) === tag;
***REMOVED***;
}
