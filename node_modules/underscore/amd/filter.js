define(['./_cb', './each'], function (_cb, each) ***REMOVED***

  // Return all the elements that pass a truth test.
  function filter(obj, predicate, context) ***REMOVED***
    var results = [];
    predicate = _cb(predicate, context);
    each(obj, function(value, index, list) ***REMOVED***
      if (predicate(value, index, list)) results.push(value);
  ***REMOVED***);
    return results;
***REMOVED***

  return filter;

});
