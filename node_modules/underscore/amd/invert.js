define(['./keys'], function (keys) ***REMOVED***

  // Invert the keys and values of an object. The values must be serializable.
  function invert(obj) ***REMOVED***
    var result = ***REMOVED***};
    var _keys = keys(obj);
    for (var i = 0, length = _keys.length; i < length; i++) ***REMOVED***
      result[obj[_keys[i]]] = _keys[i];
  ***REMOVED***
    return result;
***REMOVED***

  return invert;

});
