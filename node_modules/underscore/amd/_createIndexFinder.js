define(['./_getLength', './_setup', './isNaN'], function (_getLength, _setup, _isNaN) ***REMOVED***

  // Internal function to generate the `_.indexOf` and `_.lastIndexOf` functions.
  function createIndexFinder(dir, predicateFind, sortedIndex) ***REMOVED***
    return function(array, item, idx) ***REMOVED***
      var i = 0, length = _getLength(array);
      if (typeof idx == 'number') ***REMOVED***
        if (dir > 0) ***REMOVED***
          i = idx >= 0 ? idx : Math.max(idx + length, i);
      ***REMOVED*** else ***REMOVED***
          length = idx >= 0 ? Math.min(idx + 1, length) : idx + length + 1;
      ***REMOVED***
    ***REMOVED*** else if (sortedIndex && idx && length) ***REMOVED***
        idx = sortedIndex(array, item);
        return array[idx] === item ? idx : -1;
    ***REMOVED***
      if (item !== item) ***REMOVED***
        idx = predicateFind(_setup.slice.call(array, i, length), _isNaN);
        return idx >= 0 ? idx + i : -1;
    ***REMOVED***
      for (idx = dir > 0 ? i : length - 1; idx >= 0 && idx < length; idx += dir) ***REMOVED***
        if (array[idx] === item) return idx;
    ***REMOVED***
      return -1;
  ***REMOVED***;
***REMOVED***

  return createIndexFinder;

});
