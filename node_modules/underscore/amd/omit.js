define(['./restArguments', './isFunction', './negate', './map', './_flatten', './contains', './pick'], function (restArguments, isFunction, negate, map, _flatten, contains, pick) ***REMOVED***

  // Return a copy of the object without the disallowed properties.
  var omit = restArguments(function(obj, keys) ***REMOVED***
    var iteratee = keys[0], context;
    if (isFunction(iteratee)) ***REMOVED***
      iteratee = negate(iteratee);
      if (keys.length > 1) context = keys[1];
  ***REMOVED*** else ***REMOVED***
      keys = map(_flatten(keys, false, false), String);
      iteratee = function(value, key) ***REMOVED***
        return !contains(keys, key);
    ***REMOVED***;
  ***REMOVED***
    return pick(obj, iteratee, context);
***REMOVED***);

  return omit;

});
