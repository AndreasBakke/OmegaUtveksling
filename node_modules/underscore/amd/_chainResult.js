define(['./underscore'], function (underscore) ***REMOVED***

  // Helper function to continue chaining intermediate results.
  function chainResult(instance, obj) ***REMOVED***
    return instance._chain ? underscore(obj).chain() : obj;
***REMOVED***

  return chainResult;

});
