define(['./_shallowProperty'], function (_shallowProperty) ***REMOVED***

	// Internal helper to obtain the `length` property of an object.
	var getLength = _shallowProperty('length');

	return getLength;

});
