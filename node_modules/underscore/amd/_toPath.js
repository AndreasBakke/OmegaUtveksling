define(['./underscore', './toPath'], function (underscore, toPath$1) ***REMOVED***

  // Internal wrapper for `_.toPath` to enable minification.
  // Similar to `cb` for `_.iteratee`.
  function toPath(path) ***REMOVED***
    return underscore.toPath(path);
***REMOVED***

  return toPath;

});
