define(['./filter'], function (filter) ***REMOVED***

  // Trim out all falsy values from an array.
  function compact(array) ***REMOVED***
    return filter(array, Boolean);
***REMOVED***

  return compact;

});
