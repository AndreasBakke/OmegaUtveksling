define(['./filter', './negate', './_cb'], function (filter, negate, _cb) ***REMOVED***

  // Return all the elements for which a truth test fails.
  function reject(obj, predicate, context) ***REMOVED***
    return filter(obj, negate(_cb(predicate)), context);
***REMOVED***

  return reject;

});
