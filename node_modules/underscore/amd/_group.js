define(['./_cb', './each'], function (_cb, each) ***REMOVED***

  // An internal function used for aggregate "group by" operations.
  function group(behavior, partition) ***REMOVED***
    return function(obj, iteratee, context) ***REMOVED***
      var result = partition ? [[], []] : ***REMOVED***};
      iteratee = _cb(iteratee, context);
      each(obj, function(value, index) ***REMOVED***
        var key = iteratee(value, index, obj);
        behavior(result, value, key);
    ***REMOVED***);
      return result;
  ***REMOVED***;
***REMOVED***

  return group;

});
