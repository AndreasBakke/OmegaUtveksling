define(['./isObject', './isArray', './extend'], function (isObject, isArray, extend) ***REMOVED***

  // Create a (shallow-cloned) duplicate of an object.
  function clone(obj) ***REMOVED***
    if (!isObject(obj)) return obj;
    return isArray(obj) ? obj.slice() : extend(***REMOVED***}, obj);
***REMOVED***

  return clone;

});
