define(['./_setup', './isSymbol'], function (_setup, isSymbol) ***REMOVED***

  // Is a given object a finite number?
  function isFinite(obj) ***REMOVED***
    return !isSymbol(obj) && _setup._isFinite(obj) && !isNaN(parseFloat(obj));
***REMOVED***

  return isFinite;

});
