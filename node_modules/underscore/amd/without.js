define(['./restArguments', './difference'], function (restArguments, difference) ***REMOVED***

  // Return a version of the array that does not contain the specified value(s).
  var without = restArguments(function(array, otherArrays) ***REMOVED***
    return difference(array, otherArrays);
***REMOVED***);

  return without;

});
