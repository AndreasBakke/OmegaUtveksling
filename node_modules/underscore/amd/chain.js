define(['./underscore'], function (underscore) ***REMOVED***

  // Start chaining a wrapped Underscore object.
  function chain(obj) ***REMOVED***
    var instance = underscore(obj);
    instance._chain = true;
    return instance;
***REMOVED***

  return chain;

});
