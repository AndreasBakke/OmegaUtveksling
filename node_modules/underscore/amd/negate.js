define(function () ***REMOVED***

  // Returns a negated version of the passed-in predicate.
  function negate(predicate) ***REMOVED***
    return function() ***REMOVED***
      return !predicate.apply(this, arguments);
  ***REMOVED***;
***REMOVED***

  return negate;

});
