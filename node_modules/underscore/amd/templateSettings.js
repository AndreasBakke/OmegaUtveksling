define(['./underscore'], function (underscore) ***REMOVED***

  // By default, Underscore uses ERB-style template delimiters. Change the
  // following template settings to use alternative delimiters.
  var templateSettings = underscore.templateSettings = ***REMOVED***
    evaluate: /<%([\s\S]+?)%>/g,
    interpolate: /<%=([\s\S]+?)%>/g,
    escape: /<%-([\s\S]+?)%>/g
***REMOVED***;

  return templateSettings;

});
