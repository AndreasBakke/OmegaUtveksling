define(['./restArguments', './_flatten', './filter', './contains'], function (restArguments, _flatten, filter, contains) ***REMOVED***

  // Take the difference between one array and a number of other arrays.
  // Only the elements present in just the first array will remain.
  var difference = restArguments(function(array, rest) ***REMOVED***
    rest = _flatten(rest, true, true);
    return filter(array, function(value)***REMOVED***
      return !contains(rest, value);
  ***REMOVED***);
***REMOVED***);

  return difference;

});
