define(['./_isArrayLike', './keys'], function (_isArrayLike, keys) ***REMOVED***

  // Return the number of elements in a collection.
  function size(obj) ***REMOVED***
    if (obj == null) return 0;
    return _isArrayLike(obj) ? obj.length : keys(obj).length;
***REMOVED***

  return size;

});
