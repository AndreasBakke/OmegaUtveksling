define(['./map', './property'], function (map, property) ***REMOVED***

  // Convenience version of a common use case of `_.map`: fetching a property.
  function pluck(obj, key) ***REMOVED***
    return map(obj, property(key));
***REMOVED***

  return pluck;

});
