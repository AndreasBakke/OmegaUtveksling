define(['./_getLength', './contains'], function (_getLength, contains) ***REMOVED***

  // Produce an array that contains every item shared between all the
  // passed-in arrays.
  function intersection(array) ***REMOVED***
    var result = [];
    var argsLength = arguments.length;
    for (var i = 0, length = _getLength(array); i < length; i++) ***REMOVED***
      var item = array[i];
      if (contains(result, item)) continue;
      var j;
      for (j = 1; j < argsLength; j++) ***REMOVED***
        if (!contains(arguments[j], item)) break;
    ***REMOVED***
      if (j === argsLength) result.push(item);
  ***REMOVED***
    return result;
***REMOVED***

  return intersection;

});
