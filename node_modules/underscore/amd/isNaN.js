define(['./_setup', './isNumber'], function (_setup, isNumber) ***REMOVED***

  // Is the given value `NaN`?
  function isNaN(obj) ***REMOVED***
    return isNumber(obj) && _setup._isNaN(obj);
***REMOVED***

  return isNaN;

});
