define(['./_setup'], function (_setup) ***REMOVED***

  // Is a given value a boolean?
  function isBoolean(obj) ***REMOVED***
    return obj === true || obj === false || _setup.toString.call(obj) === '[object Boolean]';
***REMOVED***

  return isBoolean;

});
