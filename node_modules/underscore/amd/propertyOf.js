define(['./noop', './get'], function (noop, get) ***REMOVED***

  // Generates a function for a given object that returns a given property.
  function propertyOf(obj) ***REMOVED***
    if (obj == null) return noop;
    return function(path) ***REMOVED***
      return get(obj, path);
  ***REMOVED***;
***REMOVED***

  return propertyOf;

});
