define(['./underscore', './each', './functions', './_setup', './_chainResult'], function (underscore, each, functions, _setup, _chainResult) ***REMOVED***

  // Add your own custom functions to the Underscore object.
  function mixin(obj) ***REMOVED***
    each(functions(obj), function(name) ***REMOVED***
      var func = underscore[name] = obj[name];
      underscore.prototype[name] = function() ***REMOVED***
        var args = [this._wrapped];
        _setup.push.apply(args, arguments);
        return _chainResult(this, func.apply(underscore, args));
    ***REMOVED***;
  ***REMOVED***);
    return underscore;
***REMOVED***

  return mixin;

});
