define(['./_setup'], function (_setup) ***REMOVED***

  // Common internal logic for `isArrayLike` and `isBufferLike`.
  function createSizePropertyCheck(getSizeProperty) ***REMOVED***
    return function(collection) ***REMOVED***
      var sizeProperty = getSizeProperty(collection);
      return typeof sizeProperty == 'number' && sizeProperty >= 0 && sizeProperty <= _setup.MAX_ARRAY_INDEX;
  ***REMOVED***
***REMOVED***

  return createSizePropertyCheck;

});
