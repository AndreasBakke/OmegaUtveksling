/*!
 * statuses
 * Copyright(c) 2014 Jonathan Ong
 * Copyright(c) 2016 Douglas Christopher Wilson
 * MIT Licensed
 */

'use strict'

/**
 * Module dependencies.
 * @private
 */

var codes = require('./codes.json')

/**
 * Module exports.
 * @public
 */

module.exports = status

// status code to message map
status.message = codes

// status message (lower-case) to code map
status.code = createMessageToStatusCodeMap(codes)

// array of status codes
status.codes = createStatusCodeList(codes)

// status codes for redirects
status.redirect = ***REMOVED***
  300: true,
  301: true,
  302: true,
  303: true,
  305: true,
  307: true,
  308: true
}

// status codes for empty bodies
status.empty = ***REMOVED***
  204: true,
  205: true,
  304: true
}

// status codes for when you should retry the request
status.retry = ***REMOVED***
  502: true,
  503: true,
  504: true
}

/**
 * Create a map of message to status code.
 * @private
 */

function createMessageToStatusCodeMap (codes) ***REMOVED***
  var map = ***REMOVED***}

  Object.keys(codes).forEach(function forEachCode (code) ***REMOVED***
    var message = codes[code]
    var status = Number(code)

    // populate map
    map[message.toLowerCase()] = status
***REMOVED***)

  return map
}

/**
 * Create a list of all status codes.
 * @private
 */

function createStatusCodeList (codes) ***REMOVED***
  return Object.keys(codes).map(function mapCode (code) ***REMOVED***
    return Number(code)
***REMOVED***)
}

/**
 * Get the status code for given message.
 * @private
 */

function getStatusCode (message) ***REMOVED***
  var msg = message.toLowerCase()

  if (!Object.prototype.hasOwnProperty.call(status.code, msg)) ***REMOVED***
    throw new Error('invalid status message: "' + message + '"')
***REMOVED***

  return status.code[msg]
}

/**
 * Get the status message for given code.
 * @private
 */

function getStatusMessage (code) ***REMOVED***
  if (!Object.prototype.hasOwnProperty.call(status.message, code)) ***REMOVED***
    throw new Error('invalid status code: ' + code)
***REMOVED***

  return status.message[code]
}

/**
 * Get the status code.
 *
 * Given a number, this will throw if it is not a known status
 * code, otherwise the code will be returned. Given a string,
 * the string will be parsed for a number and return the code
 * if valid, otherwise will lookup the code assuming this is
 * the status message.
 *
 * @param ***REMOVED***string|number} code
 * @returns ***REMOVED***number}
 * @public
 */

function status (code) ***REMOVED***
  if (typeof code === 'number') ***REMOVED***
    return getStatusMessage(code)
***REMOVED***

  if (typeof code !== 'string') ***REMOVED***
    throw new TypeError('code must be a number or string')
***REMOVED***

  // '403'
  var n = parseInt(code, 10)
  if (!isNaN(n)) ***REMOVED***
    return getStatusMessage(n)
***REMOVED***

  return getStatusCode(code)
}
