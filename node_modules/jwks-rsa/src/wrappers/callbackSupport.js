const ***REMOVED*** callbackify } = require('util');

const callbackSupport = (client) => ***REMOVED***
  const getSigningKey = client.getSigningKey.bind(client);

  return (kid, cb) => ***REMOVED***
    if (cb) ***REMOVED***
      const callbackFunc = callbackify(getSigningKey);
      return callbackFunc(kid, cb);
  ***REMOVED***

    return getSigningKey(kid);
***REMOVED***;
};

module.exports.default = callbackSupport;
