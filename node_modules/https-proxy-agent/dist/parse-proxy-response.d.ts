/// <reference types="node" />
import ***REMOVED*** Readable } from 'stream';
export interface ProxyResponse ***REMOVED***
    statusCode: number;
    buffered: Buffer;
}
export default function parseProxyResponse(socket: Readable): Promise<ProxyResponse>;
