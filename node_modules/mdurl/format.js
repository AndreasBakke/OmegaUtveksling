
'use strict';


module.exports = function format(url) ***REMOVED***
  var result = '';

  result += url.protocol || '';
  result += url.slashes ? '//' : '';
  result += url.auth ? url.auth + '@' : '';

  if (url.hostname && url.hostname.indexOf(':') !== -1) ***REMOVED***
    // ipv6 address
    result += '[' + url.hostname + ']';
***REMOVED*** else ***REMOVED***
    result += url.hostname || '';
***REMOVED***

  result += url.port ? ':' + url.port : '';
  result += url.pathname || '';
  result += url.search || '';
  result += url.hash || '';

  return result;
};
