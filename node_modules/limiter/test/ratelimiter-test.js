var vows = require('vows');
var assert = require('assert');
var RateLimiter = require('../lib/rateLimiter');

vows.describe('RateLimiter').addBatch(***REMOVED***
  'interval validation': ***REMOVED***
    'invalid interval': function() ***REMOVED***
      assert.throws(function() ***REMOVED*** new RateLimiter(1, 'junk'); }, /interval/);
  ***REMOVED***,
    'valid intervals': function() ***REMOVED***
      assert.doesNotThrow(function() ***REMOVED*** new RateLimiter(1, 'sec'); });
      assert.doesNotThrow(function() ***REMOVED*** new RateLimiter(1, 'second'); });
      assert.doesNotThrow(function() ***REMOVED*** new RateLimiter(1, 'min'); });
      assert.doesNotThrow(function() ***REMOVED*** new RateLimiter(1, 'minute'); });
      assert.doesNotThrow(function() ***REMOVED*** new RateLimiter(1, 'hr'); });
      assert.doesNotThrow(function() ***REMOVED*** new RateLimiter(1, 'hour'); });
      assert.doesNotThrow(function() ***REMOVED*** new RateLimiter(1, 'day'); });
  ***REMOVED***
***REMOVED***
}).export(module);
