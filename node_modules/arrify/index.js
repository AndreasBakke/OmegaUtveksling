'use strict';

const arrify = value => ***REMOVED***
	if (value === null || value === undefined) ***REMOVED***
		return [];
	}

	if (Array.isArray(value)) ***REMOVED***
		return value;
	}

	if (typeof value === 'string') ***REMOVED***
		return [value];
	}

	if (typeof value[Symbol.iterator] === 'function') ***REMOVED***
		return [...value];
	}

	return [value];
};

module.exports = arrify;
