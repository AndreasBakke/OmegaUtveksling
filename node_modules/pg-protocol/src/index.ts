import ***REMOVED*** BackendMessage, DatabaseError } from './messages'
import ***REMOVED*** serialize } from './serializer'
import ***REMOVED*** Parser, MessageCallback } from './parser'

export function parse(stream: NodeJS.ReadableStream, callback: MessageCallback): Promise<void> ***REMOVED***
  const parser = new Parser()
  stream.on('data', (buffer: Buffer) => parser.parse(buffer, callback))
  return new Promise((resolve) => stream.on('end', () => resolve()))
}

export ***REMOVED*** serialize, DatabaseError }
