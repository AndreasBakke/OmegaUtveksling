// file for microbenchmarking

import ***REMOVED*** Writer } from './buffer-writer'
import ***REMOVED*** serialize } from './index'
import ***REMOVED*** BufferReader } from './buffer-reader'

const LOOPS = 1000
let count = 0
let start = Date.now()
const writer = new Writer()

const reader = new BufferReader()
const buffer = Buffer.from([33, 33, 33, 33, 33, 33, 33, 0])

const run = () => ***REMOVED***
  if (count > LOOPS) ***REMOVED***
    console.log(Date.now() - start)
    return
***REMOVED***
  count++
  for (let i = 0; i < LOOPS; i++) ***REMOVED***
    reader.setBuffer(0, buffer)
    reader.cstring()
***REMOVED***
  setImmediate(run)
}

run()
